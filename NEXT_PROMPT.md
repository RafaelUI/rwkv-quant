Прочитай ~/Develop/rwkv-quant/NEXT_SESSION.md целиком, потом можешь сразу
работать.

Кратко про рамки. Машина — M4 Air, 16 ГБ, безвентиляторная. Максимальное
ожидание у macos mcp — 4 минуты, поэтому долгие прогоны ставь в фон и
опрашивай лог. Память меряй ТОЛЬКО системными командами (/usr/bin/time -l
-> peak memory footprint, vm_stat, sysctl -n vm.swapusage, memory_pressure
-Q): метрики torch и MLX для unified memory врут. На замерах КАЧЕСТВА своп
до 2 ГБ приемлем, на замерах СКОРОСТИ малейший рост свопа во время самого
измерения делает его недействительным. Декод троттлит на 30-40% за минуту —
окна замеров короткие, сравнения только чередованием A/B в одном процессе.
У меня локаль с запятичным разделителем: скрипты, разбирающие вывод
системных утилит, обязаны звать их с LC_ALL=C.

Что сделано в прошлой сессии (15.08).

Память загрузки: 5511 -> 2290 МБ на 1.5B и 6383 -> 3633 на 2.9B, все
параметры бит-в-бит прежние. Оп-каунт: SymQuantLinearFused плюс хвост TMix
одним кернелем — +1.15 мс, 53.6 -> 57.1 ток/с при FUSE=True.

И пересобрана вся карта скорости, потому что четыре записанных вывода
оказались неверны. Потолок полосы 104 ГБ/с, а не 95.7 — значит наш GEMV на
90.6% достижимого, а не на 98, и «трогать нечего» снято. mx.compile не
звался на префилле: починка дала +35%, и разрыв с llama.cpp там 1.24-1.36x,
а не 2x. Штатно квантованная модель MollySophia (mlx-affine, тот же
бит/вес) быстрее нас в 1.39x на префилле и 1.18x на декоде — при этом наш
GPU-кернель на декоде вровень с mx.quantized_matmul, проигрывает обвязка, а
на префилле мы уходим с кернеля на _dequant_w плюс плотный матмул. И
CPU-to-GPU латентность из трассы — это очередь, а не накладные: забег
вперёд не даёт ничего.

Дальше по приоритету.

1. LoRA до полки. Единственная статья, идущая не у своей полосы: 46.6 ГБ/с
   против 85-93 у всего остального, потолок 2.50 мс (13.5% шага). Причина —
   форма (ранги 96/96/64/256 против 2048), а не байты и не запуски. Нужны
   оба рычага: склейка четырёх down-проекций в один матмул (точная,
   значения те же) и квантование под mx.quantized_matmul. Второе — НОВОЕ
   квантование, а не репак: в файле LoRA лежит в asym gw64, но группы там
   вдоль выходной оси матмула и формы ragged. Значит ppl-гейт обязателен, и
   сквозной A/B тоже: микрозамеры на этих формах врут в плюс, склейка
   показала 157.6 ГБ/с при потолке 104 — матрица сидела в кэше.
2. GEMM-ветка префилла в родной mx.quantized_matmul. Проигрываем 1.8-2.4x
   на слое. Декодный GEMV трогать не надо, он вровень, поэтому affine нужен
   как вторая раскладка для префилла, а не как замена формата.
3. Решение по пресету REDUCTION — цифры собраны все, открыт лишь выбор
   критерия (ppl против KL, они разошлись на emb). Это вопрос ко мне, не
   инженерная задача; спроси, когда дойдёт.
4. Потом переносим инференс в ~/Develop/rwkv-metal и делаем там
   QLoRA-бенчмарк на 1.5B: потери, скорость, память, три руки — bf16-LoRA,
   QLoRA на REDUCTION, QLoRA на int8. Данные скачаю я, датасеты кладу в
   ~/Develop/data.

Главное для меня: ускорять инференс БЕЗ потери качества. Префилл тоже
важен — он параллельный и быстрый, но прожорливый по памяти.

Отдельно, потому что прошлая сессия на этом обожглась несколько раз.
Проверяй собственный измерительный инструмент прежде, чем верить его
числам: цепочка вызовов на одну синхронизацию съела 6.4 ГБ и утащила
машину в своп; сид из hash() рандомизировался по процессу и показал
расхождение во всех выходах; аблация монкипатчем не видела лениво
закешированных копий весов; аблация «сделать компонент маленьким» мерила
не то же, что «убрать компонент». Каждый раз это выглядело как настоящий
результат.
