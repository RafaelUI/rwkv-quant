Прочитай `~/Develop/rwkv-quant/NEXT_SESSION.md` целиком, потом сразу работай.

**Рамки.** M4 Air, 16 ГБ, безвентиляторный. Долгие прогоны в фон (`nohup`)
и опрашивать лог; ожидание у macos mcp — около минуты. Память — только
системными командами (`/usr/bin/time -l`, `sysctl -n vm.swapusage`,
`memory_pressure -Q`); на замерах качества своп до 2 ГБ приемлем, на
замерах скорости важен не итог «вырос ли», а ТРАССА ПО РАУНДАМ: рост во
время загрузки модели замер не портит, рост во время раундов — портит.
Замеры скорости — чередованием в одном процессе, порядок рандомизировать;
межпрогоновый дрейф до 30%. Локаль запятая: утилиты звать с `LC_ALL=C`.
Первым делом — `rwkv-quant/tests/restore_tmp.sh`.

**Сделано 24.08 (раздел 24.08 + закон 36).** Backward WKV переписан по
схеме Бо Пэна (поток держит КОЛОНКУ состояния, пять межпотоковых редукций
на шаг времени схлопнулись в скалярные произведения; пять произведений
слиты в один проход по `j` — это дало больше, чем сам порт): ядро
**13.57 → 7.17 мс на слой (x1.89)**, сквозной шаг **2766 → 2663 мс**,
пик не изменился, умолчание ВКЛЮЧЕНО (`WKV_BWD_V2=0` возвращает старое
ядро динамически). Закрыто отрицательными результатами: fused
dequant+GEMM (+96 мс на шаге, оставлен opt-in ради −296 МБ пика), батч
(4% за +2.2 ГБ), миф «int8 быстрее нас втрое» (записанные 0.7-0.8 с — это
T=128, на равной длине int8 на 13% МЕДЛЕННЕЕ). Чекпоинтинг перемерен
начисто: 567 мс (20.6%) за пик 3272 → 9153 МБ, умолчание не меняется.

**Приоритеты:**
1. **2.9B (закон 10) — главное.** Каст входа, bf16-деквант и backward v2
   меняли шаг только на 1.5B; скорость и ПАМЯТЬ шага на 2.9B после всех
   правок не мерены ни разу, а мы с тех пор сняли около гигабайта пика.
   Корректность v2 на H=40 уже проверена (relmax 4.6e-7…1.5e-5).
2. **Красный `test_streaming_inference`** (rwkv-quant). Он красный из-за
   ПОРОГА, а не бага: сравниваются два РАЗНЫХ ядра (`wkv7_infer` против
   `wkv7_train`), расхождение 0.000244 = 2⁻¹² — один ulp fp16, то есть
   порог 1e-4 лежит ниже решётки типа. Закрывать по закону 36: построить
   ИЗМЕРЕННЫЙ пол, поставить порог по нему, поправить комментарий в тесте
   (он утверждает, что расхождение батчевого пути было нулевым — ноль там
   невозможен). Регрессии нет: до правок 23.08 те же числа.
3. **`emb` в плотном fp16** — 268 МБ против 109 на диске, лечится
   indexed-gather по sb6, качества не трогает, снимает заодно половину
   остаточного пика загрузки. Последняя статья памяти инференса.
4. **Плотный только `g_lora`** (+25 МБ вместо +47), мерить парами по сиду
   (закон 35).
5. Спека формата `.rwkvq` (шаг 5) — работа на вечер.

**Грабли.** Закон 14 живой на ВТОРОМ пакете: `rwkv_metal` стоял в венве
не-editable снимком от 26 июля, спасал только `sys.path.insert` в шапке
тестов; переставлено на `pip install -e .`, но проверять надо КАЖДЫЙ пакет.
Закон 27 мутировал дважды: «база» в A/B обязана быть замороженной копией,
список проверять grep'ом после КАЖДОЙ смены умолчания. Мутационные крышки
проверять на самих себе — крышка `zero` в `test_sym_gemm_fused` два дня
мерила ЗДОРОВЫЙ код, потому что мутация подменяла фабрику, а функция уже
лежала в кеше модуля. В mx.fast-кернелях тип называется `bfloat16_t`,
коды интерлива — ЗНАКОВЫЕ int8.

**Решения владельца, ждут:** критерий для пресета REDUCTION (ppl и KL
разошлись на emb); отдельный QLoRA-вариант `.rwkvq` с плотными ветками;
`CAST_WKV_OUTPUT` (+21% шага, ppl −0.23%, но KL от bf16 +23% против +3% у
каста в слое) — фактически отклонён.

**Отложено пользователем:** ВЕСЬ ПРЕСЕТ COMPRESSION целиком (не устраивают
размер, скорость и качество; отдельная работа, REDUCTION от неё не
зависит) — включая `FUSE` по умолчанию для COMPRESSION и кернель
распаковки. Также recall@k.

**Артефакты.** Исходники Бо Пэна склонированы локально:
`~/Develop/upstream/RWKV-LM`, `~/Develop/upstream/RWKV-CUDA` (ядро —
`RWKV-CUDA/rwkv7_fast_fused/cuda/rwkv7_clampw.cu`), смотреть с диска, не по
сети. JSON — в `~/Develop/WKV-kvant/`; адаптеры сидов —
`/tmp/qlora_arm_*_s*.safetensors`. База 1.5B — `/tmp/reduction_new.rwkvq`,
2.9B — `/tmp/reduction_sym_head8_2p9b.rwkvq`.
