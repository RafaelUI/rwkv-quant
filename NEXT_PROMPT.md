Прочитай `~/Develop/rwkv-quant/NEXT_SESSION.md` целиком, потом можешь
сразу работать.

Кратко про рамки. Машина — M4 Air, 16 ГБ, безвентиляторная.
Максимальное ожидание у macos mcp — 4 минуты, поэтому долгие прогоны
ставь в фон и опрашивай лог. Память меряй ТОЛЬКО системными командами
(`/usr/bin/time -l` -> peak memory footprint, `vm_stat`,
`sysctl -n vm.swapusage`, `memory_pressure -Q`): метрики torch и MLX для
unified memory врут. На замерах КАЧЕСТВА своп до 2 ГБ приемлем, на
замерах СКОРОСТИ малейший рост свопа во время самого измерения делает
его недействительным. Декод троттлит на 30-40% за минуту — окна замеров
короткие, сравнения только чередованием A/B в одном процессе.
У меня локаль с запятичным разделителем: скрипты, разбирающие вывод
системных утилит, обязаны звать их с LC_ALL=C.

Что сделано в прошлой сессии: упаковщик и Metal-кернель Q6_K-раскладки
(`sym`) на 6 и 8 бит, гейты равенства зелёные на обоих чекпоинтах,
композит с head@8 и полный перемер на реальном пути `real_gw=True`,
скорость, память и Metal-трасса. Бюджет REDUCTION упал с +0.772% до
+0.192% на 1.5B и с +1.370% до +0.273% на 2.9B ценой 3.5% и 2.6%
размера и 3% декода. Пресеты при этом НЕ ТРОНУТЫ сознательно.

Дальше по приоритету:

1. **Память загрузки: 6.05 ГБ пика на файл 1.3 ГБ** (на 2.9B это
   11-12 ГБ и главное ограничение). Это не префилл — он добавляет
   0.18 ГБ. Виноват `_dense()` (bf16 -> fp32 -> numpy -> mx -> fp16) и
   лишние копии в конструкторах линейных слоёв. Правки механические,
   качества не касаются.
2. **Оп-каунт: измеренный бюджет 4.97 мс на токен (27% шага).** GEMV
   уже на 98% полосы, трогать его нечего. Резерв — фьюз LoRA-блока,
   fuse groupnorm+bonus+gate, `SymQuantLinearFused`. Только с гейтом
   `tests/test_fuse_parity.py`: фьюзы меняют порядок суммирования.
3. **Решение по пресету REDUCTION** — цифры собраны все, открыт лишь
   выбор критерия (ppl против KL, они разошлись на emb). Это вопрос ко
   мне, не инженерная задача; спроси, когда дойдёт.

Главное для меня: ускорять инференс БЕЗ потери качества. Префилл тоже
важен — он параллельный и быстрый, но прожорливый по памяти.
