Прочитай ~/Develop/rwkv-quant/NEXT_SESSION.md целиком: свежий раздел 17.09,
приоритеты там, а не в шапке. Разделы 16.09 и 15.09 читай ЦЕЛИКОМ и вместе:
в 15.09 есть вывод «время тождественно трафику», уточнённый 16.09 (верен
только для поэлементной копии fp16), и раздел «КУДА ЕЩЁ ИДТИ ПО ПРЕФИЛЛУ» с
ПОПРАВКОЙ 17.09 прямо в нём. Разделы 11.09, 10.09, 09.09 и 05.09 читай ТОЛЬКО
вместе с пометками под их заголовками — у 10.09 отменены четыре утверждения
замерами 11.09, у 09.09 четыре замерами 10.09, у 05.09 четыре замерами 09.09.
Разделы 27.08-04.09 в docs/LOG-2708-0409.md, там же 28.08 — РЕКОНСТРУКЦИЯ из
коммитов. Законы 1-40 — docs/LAWS.md, до первого замера. Остальное в docs/,
адресно. Записка у проекта ОДНА, эта; этот файл — только шапка.

ПЕРЕД ЛЮБЫМ НОВЫМ ПРИБОРОМ: ls -lat tests/_sess и ls -lat ~/Develop/WKV-kvant/
*.log, и поиск по записке. В сессию 15-17.09 я трижды строил то, что уже
было: дважды дублировал приборы, один раз снимал трассу ради числа, которое
лежало в разделе 10.09.

Порядок работ: rwkv-metal -> SwiftRWKV -> Synapse. Замечания по Synapse и
Swift не трогать, пока очередь не дошла.

Рамки. M4 Air, 16 ГБ, безвентиляторный. Долгие прогоны в фон, но ТОЛЬКО через
screen -dmS: nohup умирает вместе с сервером macos mcp. ПЕРЕД тяжёлым
запуском — ps, что предыдущий умер, и ПОСЛЕ kill — тоже ps, причём screen -X
quit НЕ убивает шелл скрипта. ps aux | grep ЧЕРЕЗ wc -l врёт: собственная
командная строка попадает в совпадения, смотреть вывод, а не счёт.
Параллелить прогоны на одном GPU бессмысленно. Своп мерить дельтой, и
отдельно Swapouts из vm_stat: занятый своп может не двигаться при активном
обмене. Память процесса — только peak memory footprint из /usr/bin/time -l,
счётчики MLX врут (решение владельца 15.09), а значит память мерить ОДНИМ
конфигом на процесс, скорость — чередованием в одном. torch.load только с
mmap=True. Результаты построчно в файл под ~/Develop/WKV-kvant/.
Венв: ~/Develop/rwkv-metal/.venv/bin/python.

ТРАНСПОРТ. Паузы не длиннее 8 секунд: sleep 10 и ДВА sleep 8 подряд обрывают
вызов по таймауту MCP. Прогоны в screen обрыв переживают. Файлы писать
printf %s построчно (heredoc запрещён, закон 33) и ПРОВЕРЯТЬ: py_compile для
питона, sh -n для шелла, wc и md5 для остального. Длинные правки чужих файлов
делать питоном с assert на число вхождений якоря, а не sed; ассерты ловят
больше, чем кажется — в эту сессию они трижды остановили неверную правку до
записи. В файлах, ГЕНЕРИРУЮЩИХ Metal, обратный слэш ЗАПРЕЩЁН, перевод строки
только chr(10): литеральный слэш-n роняет компиляцию в utils.h:442, а не на
своей строке. На macOS base64 декодирует флагом -D; timeout отсутствует;
cat -A молча пуст. ПОСЛЕ КАЖДОЙ ТРАССЫ xctrace чистить TMPDIR руками
(instruments*.ktrace и xrgpu_aps_* — гигабайты, удаление .trace их не
убирает) и смотреть df; окна 3-5 с достаточно, 25 с на декоде дают 3.3 ГБ.

Состояние. rwkv-quant: c5b7d52, дерево ЧИСТОЕ. rwkv-metal: 0bdfbe7, чисто,
main на 14 коммитов впереди origin. SwiftRWKV не трогался. В РАБОЧИЙ КОД
ничего не внесено: _dequant_w как был, кернель зовётся только из гейта и
бенча.

Модели в ~/Develop/WKV-kvant/: compression_0p1b, compression_0p4b,
compression_v1_ref, compression_v2_cand (1.5B), compression_2p9b,
reduction_1p5b_0709. Там же GGUF для сравнения (rwkv7-1p5b-Q4_K_M 990 МБ,
Q6_K 1336 МБ) и rwkv7-1p5b.imatrix; llama-bench стоит из brew, строить
ничего не надо. Эталоны квантования fp32 на 38 окон — все четыре размера.

Опорные числа. Префилл 1.5B T=512: v1 785.7 мс = 651.7 т/с, С КЕРНЕЛЕМ
657.5 = 778.7, пол со снятым деквантом 610.1 = 839.2, потолок арифметикой
485 мс = 1055 т/с при измеренных 2.55 ТФЛОП/с. Прочие статьи ~125 мс = 19%.
Одна полноразмерная материализация в графе 45.2 мс = 107 ГБ/с; кернель идёт
на 68.8 ГБ/с. T=4096 B=1: 773.8 т/с без кернеля. Декод 13.049 мс = 76.6 т/с,
дно 8.89 = 112, оверхед 4.31 из 13.20. llama.cpp здесь же: Q4_K_M 750.8 /
60.9, Q6_K 729.0 / 50.4 — по префиллу паритет, по декоду +26%.

ПРИОРИТЕТ 1: включить кернель декванта в _dequant_w для K3 с откатом на V1.
Это -16.3% префилла, лежащее готовым и загейченное бит-в-бит на всех четырёх
размерах. Прогнать гейты проекта целиком; ppl и KL НЕ нужны — бит-в-бит, и
отпечатки логитов совпали во всех плечах. ВЕТКА xbits>=2 ГЕЙТОМ НЕ ПОКРЫТА:
таких тензоров нет ни в одной модели, при появлении 6-битного asym гейт
обязателен до включения.

ПРИОРИТЕТ 2: настройки кернеля взять у sym (_dq_tg, _dq_vec в
quant_linear_sym.py), у нового фиксированные 256 потоков и запись half4.
68.8 -> к 100 ГБ/с это 47.4 -> ~32 мс. Закон 23: брать оттуда, не подбирать.

РЕШЕНИЕ, КОТОРОЕ ЖДЁТ ВЛАДЕЛЬЦА: раскладывать ли ~125 мс прочих статей
префилла (WKV-скан, нормы, token-shift lerp, гейты). 16.09 решено не
раскладывать, но по ошибочному числу ~70; настоящее — 125, то есть 19%
префилла и единственная оставшаяся масса. Кернель после настройки исчерпан:
идеальный деквант дал бы +2.4%.

FAST_LN. Включён для COMPRESSION, выключен для REDUCTION (решение владельца
12.09). Флаг — свойство МОДЕЛИ: QuantRWKV7(ckpt, fast_ln=...), иначе пресет
файла, иначе умолчание False. Пресет определяется СТРОГИМ совпадением
конфига с PRESETS. Гейт — tests/test_fast_ln_parity.py, красен ровно один
пункт на 1.5B, это дефект формы теста, разбор в долге 8. ПРИ ЛЮБОМ A/B
переключать И на модели, И на блоках.

НЕ ДЕЛАТЬ без нового замера: тайловый GEMM ПОД ПРЕФИЛЛ (mx.matmul берёт 93%
потолка машины, отбирать нечего; остаётся только под батчевый декод);
раскрытие нибблов броадкастом (проигрывает 25.4 мс); хранение кодов
удвоёнными (68.9 мс, но +0.60 ГБ на 1.5B и +1.2 на 2.9B, отклонено);
вариант v3 цепочки MLX (кернель втрое лучше); батч на префилле (+4% за
вдвое срезанный поток и всю память); ядра по образцу fused_tail на статьи,
которые mx.compile уже свёл к одному диспатчу; расширение NB_CHUNK;
спекулятивный декод с черновиком 0.1B; ядро шапки слоя; поиск статьи нормы в
тренировочном пути rwkv-metal; сборка эталонов KL в /tmp. FUSE_PREWKV False.
