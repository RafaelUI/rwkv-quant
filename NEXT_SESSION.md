# Continuation prompt — rwkv-quant

Квантование RWKV-7 под Apple Silicon (M4 base, 16 ГБ, безвентиляторный).
`rwkv-quant` = квантизация + формат `.rwkvq` + референсный Metal-бэкенд.
`rwkv-metal` и `SwiftRWKV` = бэкенд + QLoRA-файнтюн, потребляют формат.

Чекпоинты, все четыре в world naming: `~/Develop/WKV-kvant/rwkv7-g1d-0.1b.pth`
(0.1B), `/Users/s/rwkv7-g1d-0.4b-ctx8192.pth` (0.4B),
`~/Develop/WKV-kvant/rwkv7-g1h-1.5b-ctx10240.pth` (1.5B),
`~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` (2.9B). Квантованные новым
COMPRESSION лежат все четыре в `~/Develop/WKV-kvant/`: `compression_0p1b`,
`compression_0p4b`, `compression_v2_cand` (это 1.5B), `compression_2p9b`
(собраны 10.09, таблица в разделе 10.09).

## Как читать этот набор (разбито 27.08)

Целиком читать только ЭТОТ файл. История и справочники вынесены в `docs/`
и открываются адресно, по таблице. Ни строки не удалено: склейка всех
частей в исходном порядке побайтово равна файлу до разбиения
(md5 `d285a8fd646aef359d15b5b20516250a`, 6609 строк, 516292 байта).

| файл | что внутри | когда открывать |
|---|---|---|
| **NEXT_SESSION.md** (этот) | шапка состояния, свежий раздел, приоритеты, закрытые темы, открытые вопросы, где что лежит | всегда, первым и целиком |
| `docs/LAWS.md` | законы методологии 1-40 | до первого замера сессии; при любом споре о методике |
| `docs/LOG-2708-0409.md` | сессии 27.08-04.09: пик загрузки QLoRA, 28.08 (реконструкция), ориентация LoRA в rwkv-metal | история правок формата и QLoRA |
| `docs/LOG-2208-2608.md` | сессии 22-26.08: пересборка 2.9B, emb-gather, эталон KL, разброс по сидам, QLoRA шестью плечами | контекст свежих правок |
| `docs/LOG-1708-1808.md` | сессии 17-18.08: WKV на префилле, sym-база в rwkv-metal, разбор шага обучения, забытый `astype` | вопросы обучения и префилла |
| `docs/LOG-1008-1608.md` | сессии 10-16.08: калибровка, Q6_K, LoRA, полоса, mstrace, потолки | кернели, полоса, раскладки |
| `docs/REFERENCE.md` | формат `.rwkvq` как самостоятельный, позиция против llama.cpp, память инференса, потребители формата | формат и внешние сравнения |
| `docs/TOOLS.md` | QLoRA поверх `.rwkvq` в rwkv-metal; тесты и замеры -- что где лежит | поиск скрипта или гейта |

ПРАВИЛО НА БУДУЩЕЕ: новая сессия дописывает СВОЙ раздел в этот файл.
Когда разделов накопится три-четыре, старшие переезжают в `docs/LOG-*`
тем же способом: копия оригинала в `/tmp`, нарезка `sed -n 'a,bp'` по
границам из `grep -n "^## "`, склейка ВСЕХ частей обратно и сверка md5 с
оригиналом -- и только после совпадения md5 что-либо менять.

**СТАТУС НА 12.09:** действующий список приоритетов -- в разделе «12.09»,
последнем перед «Закрытые темы». Разделы 11.09 и ниже -- история. Читать
их ТОЛЬКО вместе с пометками под их заголовками: у 10.09 четыре
утверждения отменены замерами 11.09, у 09.09 четыре замерами 10.09, у
05.09 четыре замерами 09.09. Разделы 27.08-04.09 переехали 12.09 в
`docs/LOG-2708-0409.md` по правилу выше; склейка сверена `cmp`.

Списки приоритетов прежних сессий (27.08, 04.09 и «ТЕКУЩИЕ ПРИОРИТЕТЫ
(24.08)» в шапке ниже) НЕ ДЕЙСТВУЮТ и оставлены как история.

**УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ 13.08, всё перемерено на реальном
пути** — см. раздел «УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ». `real_gw=True`
собирает файл, Metal-GEMV его читает, гейты равенства зелёные на обоих
чекпоинтах. Бюджет REDUCTION на РЕАЛЬНОМ пути: **+0.772% -> +0.192% на
1.5B за +44 МБ** и **+1.370% -> +0.273% на 2.9B за +63 МБ**, без единого
размена по языкам.

**СКОРОСТЬ ИЗМЕРЕНА 13.08: цена раскладки равна её байтам.** Декод
54.5 -> 52.9 ток/с (−3%) за +3.5% файла и вчетверо меньший бюджет;
кернель sym сверх размера не стоит ничего.

**ПАМЯТЬ ЗАГРУЗКИ СРЕЗАНА ВДВОЕ С ЛИШНИМ, 15.08.** A/B чередованием:
**5511 -> 2290 МБ на 1.5B (2.41x)** и **6383 -> 3633 МБ на 2.9B (1.76x)**,
все параметры БИТ-В-БИТ прежние (гейт `test_dense_load_parity`). Максимум
теперь держит ПРЕФИЛЛ, а не загрузка: 3265 МБ против 2290.

**КАРТА СКОРОСТИ ПЕРЕСОБРАНА 15.08, И ЧЕТЫРЕ ЗАПИСАННЫХ ВЫВОДА СНЯТЫ.**

- **Потолок полосы 104 ГБ/с, а не 95.7** (`bench_membw`, два инструмента,
  разброс 2-4%). 120 -- паспортная арифметика шины, не замер. Отсюда:
  наш GEMV идёт на **90.6% достижимого, а не на 98%**, то есть в нём
  осталось ~9% (1.3 мс), а не 2.
- **`mx.compile` не звался на префилле.** Починка дала **+35%**
  (344 -> 533 ток/с). Разрыв с llama.cpp по префиллу оказался **1.24x на
  COMPRESSION и 1.36x на REDUCTION**, а не 2x. Декод +14.5% и +16.6%.
- **Штатный mlx-affine быстрее нас** в 1.39x на префилле и 1.18x на
  декоде при том же бит/вес -- и обгоняет llama.cpp там, где мы ей
  проигрываем. Поштучно: наш GPU-кернель на декоде ВРОВЕНЬ с
  `mx.quantized_matmul` (1.02-1.10x), проигрывает обвязка; а на префилле
  мы уходим с кернеля на `_dequant_w` + плотный матмул и теряем 1.8-2.4x.
- **`CPU to GPU Latency` 2.8 мс -- это ОЧЕРЕДЬ, не накладные.** Забег
  вперёд не даёт ничего, ручки командных буферов не помогают, арифметика
  без неё сходится. Платим за КОЛИЧЕСТВО запусков.

**ИЗ ЧЕГО СОСТОИТ ШАГ (покомпонентные аблации, 15.08, шаг 17.72 мс):**

| компонент | мс | % | трафик | ГБ/с |
|---|---|---|---|---|
| cmix | 7.58 | 42.2% | 660.7 МБ | 87.2 |
| проекции r/k/v/o | 3.89 | 21.7% | 330.3 МБ | 84.9 |
| **LoRA-ветки** | **2.50** | **13.5%** | **100.1 МБ** | **46.6** |
| голова | 1.54 | 8.5% | 143.7 МБ | 93.3 |
| WKV-скан | 0.84 | 4.6% | ~1 МБ | -- |
| нормы, лерпы, token-shift | 0.67 | 3.8% | -- | -- |

**LoRA -- единственная статья не у своей полки** (46.6 против 85-93).
Причина -- форма: ранги 96/96/64/256 против 2048. WKV дёшев, «упрощение
математики» имеет потолок 3.8%, переписывание на C++ даёт примерно то,
что уже отдал `mx.compile` (−2.7 мс) -- всё три проверены и закрыты.

**LoRA ЗАКРЫТА КВАНТОВАНИЕМ ПОД `mx.quantized_matmul`, 16.08.** Декод
**17.97 -> 16.65 мс/ток (+1.32 мс, 7.9%), 55.6 -> 60.1 ток/с**; на
фьюзнутом пути 16.48 -> 15.88 (+0.60 мс), то есть **63.0 ток/с** обоими
рычагами вместе. ppl **+0.012% [-0.006; +0.029], НЕ значимо**, ни одного
размена по языкам. Префилл не тронут (994.9 -> 992.7 мс). При этом ОБЕ
записанные гипотезы о причинах не подтвердились: склейки четырёх
down-проекций в записанном виде НЕ СУЩЕСТВУЕТ (у них разные входы), а
выигрыш пришёл НЕ от байтов -- шесть бит и восемь дают одно и то же время
при полуторакратной разнице в трафике. См. раздел «LoRA: что оказалось
рычагом».

**ПРЕФИЛЛ 997 -> 753 мс БЕЗ ЕДИНОГО ИЗМЕНЁННОГО БИТА, 16.08.** 60%
времени GEMM-префилла оказалось не матмулом, а самим деквантом: он был
написан цепочкой MLX-операций и шёл со скоростью 4.4 ГБ/с. Одним Metal-
кернелем -- 10.68 -> 0.84 мс на слое, разрыв с нативным квантованным
матмулом схлопнулся с 2.4x до 1.08x, сквозной префилл 511 -> 680 ток/с,
декод не тронут, гейт бит-в-бит на 97 тензорах. Это 97% того, что давал
бы перевод модели в affine, но без +0.077% ppl и без +291 МБ.

**ИТОГО ЗА СЕССИЮ 16.08 (1.5B, sym-пресет, файл не менялся):** декод
**55.6 -> 60.1 ток/с** (с FUSE -- 63.0), префилл **511 -> 680 ток/с**,
качество -- +0.012% на декоде (LoRA @8, в шуме) и бит-в-бит на префилле.

**УМОЛЧАНИЯ ИЗМЕНЕНЫ 16.08 РЕШЕНИЕМ ВЛАДЕЛЬЦА: `FUSE = True` и
`LORA_Q = "sep"` @8.** Состояние на умолчаниях (1.5B, sym-пресет,
чередование в одном процессе): **декод 16.31-15.89 мс/ток (61.3-62.9
ток/с) против 17.89-18.12 у прежних умолчаний, префилл 748.4 мс (684
ток/с)**. Резидентно это ~250 МБ за фьюз и +56 МБ за квантованные ветки.

И ОДНО СЛЕДСТВИЕ, КОТОРОГО НЕ БЫЛО В РАСЧЁТЕ: **выигрыш фьюза
схлопнулся, когда LoRA стала квантованной.** Раньше он давал +1.15 мс,
из которых 0.58 приходилось на укрупнение LoRA-запусков; теперь эти
запуски укрупнены иначе, и фьюзу остаётся r/k/v с хвостом TMix:
на sym-пресете +0.57 мс (16.88 -> 16.31), а на sb6/COMPRESSION
**+0.06 мс, то есть ничто** (`test_fuse_parity` на champion_v2).
Значит для COMPRESSION 250 МБ за фьюз платить больше не за что, и это
надо перемерить прежде, чем считать умолчание удачным для обоих
пресетов.

**КВАНТОВАННЫЕ ВЕТКИ ЖИВУТ ТОЛЬКО В ДЕКОДЕ** (`LORA_Q_DECODE_ONLY`).
На префилле они проигрывают плотным (789.5 против 748.4 мс на фьюзнутом
пути), поэтому при T > 1 путь идёт плотным. Побочно это лучше по
качеству: префилл считает LoRA точным деквантом файла, а измеренные
+0.012% ppl относятся только к декоду. Гейт требует, чтобы префилл при
FUSE=False был БИТ-В-БИТ равен плотному, а декод -- отличался; без
второго условия диспетчеризация могла бы молча выключить квантование
целиком.

**ВСЁ ПРОВЕРЕНО НА 2.9B, ЗАКОН 10 ВЫПОЛНЕН (16.08).** Ни один вывод не
развернулся: деквант-кернель бит-в-бит на 129 тензорах и те же 1.33x
префилла (1908 -> 1440 мс, 267.7 -> 355.3 ток/с), квантованная LoRA
+7.1% декода (29.7 -> 32.1 ток/с вместе с фьюзом), **ppl -0.010%
[-0.029; +0.008], не значимо**, greedy 48/48, своп не двигался ни разу.
Шесть бит на 2.9B значимо хуже (+0.109% [+0.045; +0.174]) при том, что
на 1.5B были в шуме -- выбор восьми бит подтверждён вторым масштабом, а
не выведен из первого.

**ПОТОЛОК ПРЕФИЛЛА ИЗМЕРЕН: 2.80 ТФЛОП/с, мы на 68-71% от него.**
Формула декода на префилл НЕ ПЕРЕНОСИТСЯ -- см. раздел «Потолок
префилла». Крупнейшая незакрытая статья там -- **WKV-скан, 95 мс из 750
(12.7%)**, и он ни разу не смотрелся под префилл.

**ПРЕСЕТ REDUCTION ПЕРЕСОБРАН И ВНЕСЁН 16.08** (решение владельца).
`proj/cmix/emb/head` -> `sym_aw` (Q6_K-раскладка), битность `proj=8,
head=8, emb=8`, `cmix=6`. Реальным путём на обоих чекпоинтах:

| | было | стало |
|---|---|---|
| 1.5B | 1255.4 МБ (2.43x), **+0.772%** | **1435.1 МБ (2.13x), +0.108%** |
| 2.9B | 2420.9 МБ (2.44x), **+1.370%** | **2736.8 МБ (2.15x), +0.157%** |

Деградация в **7.1 и 8.7 раза меньше** за +14% и +13% файла; по языкам
на 1.5B en/ru/sr +0.179/+0.014/+0.245% против +1.043/+0.549/+0.996%.
Сборка проверена: файл 1435.1 МБ против обещанных замером 1434.7, гейты
упаковщика, контейнера, манифеста и codec зелёные, оба пресета собираются
и на 0.1B.

**ПРОТИВ llama.cpp ЭТО РАЗВОРОТ ПО ВСЕМ ТРЁМ ОСЯМ СРАЗУ** (тот же
харнесс `bench_decode_vs_gguf`, что и в прежней таблице):

| | размер | Δppl | pp512 | tg128 |
|---|---|---|---|---|
| **`reduction`** | 1435 МБ | **+0.108%** | **734.4** | **58.84** |
| Q6_K + imatrix | 1336 МБ | +0.19% | 705.5 | 48.59 |
| **`compression`** | 970 МБ | +3.63% | 612.3 | **76.05** |
| Q4_K_M + imatrix | 990 МБ | +3.44% | **745.2** | 58.16 |

Записанное «префилл проигрываем вдвое» снято окончательно: на
`reduction` мы теперь БЫСТРЕЕ на 4%, на `compression` проигрываем 1.22x.

**ОБНОВЛЕНО ВЕЧЕРОМ 24.08: 2.9B ПРОМЕРЕН, КРАСНЫЙ ТЕСТ ЗАКРЫТ.** Шаг на
2.9B -- 4955 мс и 4.68 ГБ аллокаторного пика (9487 МБ footprint), своп
неподвижен; backward v2 держит там 3.5%, каст входа 13.0% и -520 МБ. Ни
один вывод, снятый на 1.5B, на втором масштабе не развернулся (закон 10
выполнен для всех трёх правок). `test_streaming_inference` зелёный: порог
поставлен по измеренному полу, точность ядер вынесена в отдельный гейт с
fp64-эталоном. Подробности -- раздел «24.08, ВЕЧЕР». Из этого следует, что
ОСТАВШИЕСЯ приоритеты -- ниже по списку: `emb` в плотном fp16 (последняя
статья памяти инференса), плотный только `g_lora`, спека формата.

**ОБНОВЛЕНО 26.08 ВЕЧЕРОМ, И ГЛАВНОЕ КАСАЕТСЯ НЕ ПРЕСЕТА, А ИЗМЕРЕНИЯ.**
2.9B пересобран нынешним пресетом (2737.3 МБ, 54 с, своп неподвижен) и
подтвердил записанное: **ppl +0.157%**, bf16 7.1630 -- совпало до третьего
знака. 1.5B перемерен тем же прогоном: **+0.108%**, тоже совпало.
emb-gather на 2.9B включается сам и зелёный: бит-в-бит на шести формах,
**footprint -606 МБ** при **+0.2% декода** (на 1.5B было -537 и +0.5%).

А дальше выяснилось, что **ЭТАЛОН KL БЫЛ ХУЖЕ ТОГО, ЧТО ИМ МЕРИЛИ**.
Эталон снимался RWKV7Ref в bf16; MLX-путь БЕЗ КВАНТОВАНИЯ отстоял от него
на 0.000575 нат/токен, и это записывалось бы как «цена пути». Против
независимого fp32-эталона: **эталон 0.000756, MLX-путь 0.000004** (0.1B),
на 1.5B MLX-путь **0.000002 при top-1 99.878%**. То есть пола пути не
существует, а все KL проекта завышены собственной ошибкой эталона.
Числа с fp32-эталоном (1.5B, пресет): fake **0.002762** (было 0.002908),
реальный путь **0.003303** (было 0.003878). Поправка fake->real честно
равна **0.000541**, а не 0.000970. См. раздел «26.08» и закон 38.

**ТРИ РЕШЕНИЯ ВЛАДЕЛЬЦА 26.08, ВНЕСЕНЫ И ЗАКРЫТЫ ГЕЙТАМИ:**

1. **`RWKVQ_EMB_GATHER` ВКЛЮЧЁН ПО УМОЛЧАНИЮ.** Выход бит-в-бит, поэтому
   ни одно число качества не двинулось; это чистая память (-123/-537 МБ
   на 1.5B, -156/-606 на 2.9B при цене декода внутри разброса плеча).
   `RWKVQ_EMB_GATHER=0` возвращает плотный путь. COMPRESSION не затронут:
   там emb в sb6, применимость не проходит, отказ печатается и путь
   остаётся плотным (проверено на `champion_v2.rwkvq`).
2. **`_aw` СНЯТ С `emb`** (`group_scale_mode["emb"] = "sym"`). Это не
   смена схемы, а приведение надписи к тому, что код делал всегда:
   статистики для `emb.weight` не существует и не может существовать.
   НИ ОДНО записанное число не меняется. Сторожит
   `tests/test_emb_aw_is_noop.py`: он краснеет, если кто-нибудь допишет
   статистику для emb, -- то есть ровно в момент, когда пресет молча
   поменял бы смысл (закон 30).
3. **ЭТАЛОН KL ПО УМОЛЧАНИЮ ТЕПЕРЬ fp32** (`RWKVQ_REF_DTYPE`, тип едет
   В ИМЯ файла эталона). Прежние bf16-числа НЕ пересчитаны задним
   числом и не смешиваются с новыми: имя различает. Кто сравнивает со
   старыми записями -- поправка составляет -5% на fake-пути и -17% на
   реальном (1.5B, композит). `RWKVQ_REF_DTYPE=bf16` возвращает прежнее.

**ПОБОЧНО ЗАКРЫТА ДЫРА В `test_dense_load_parity`:** его умолчания
указывали на файлы, которых `restore_tmp.sh` не создаёт, поэтому гейт
пропускал ОБА случая и печатал зелёный, не проверив ничего (закон 31).
Умолчания переставлены на восстанавливаемые файлы, добавлен отказ при
нуле исполненных случаев. Теперь проверяет 194 и 258 тензоров на двух
масштабах. Новый 2.9B уложен в `artifacts/` и восстанавливается
скриптом.

**ТЕКУЩИЕ ПРИОРИТЕТЫ (пересобраны 24.08, после закрытия двух крупных):**

1. **Решение по пресету REDUCTION** -- цифры собраны все, открыт лишь выбор
   критерия (ppl против KL, они разошлись на emb). Вопрос к владельцу.
2. **GEMM-ветка префилла в родной `mx.quantized_matmul`.** Проигрываем
   1.8-2.4x на слое, 1.39x сквозным. Наш GEMV на декоде трогать не надо --
   он вровень; affine нужен как ВТОРАЯ раскладка для префилла, а не как
   замена формата. ОГОВОРКА 24.08: ожидание от этой работы надо понизить.
   Замер int8 против sym на шаге обучения показал, что родной квантованный
   матмул сам по себе не быстрее нашей цепочки «деквант + плотный GEMM»
   (см. раздел 24.08), поэтому «родной путь» -- не бесплатный выигрыш.
3. **Плотный только `g_lora`** (плюс ~25 МБ вместо +47): эффект плотных
   веток подтверждён на 3 сидах (-0.00164+-0.00035), мерить по установленной
   многосидовой парной методике (закон 35).
4. **Транзиент шага**: главная оставшаяся статья -- активации ~1.3 ГБ,
   растущие с T (градиентная надбавка всего 257 МБ).
5. Шаг 5 формата: нормативный текст спеки `.rwkvq`. Работа на вечер.

**ОТЛОЖЕНО ВЛАДЕЛЬЦЕМ 24.08: ВЕСЬ ПРЕСЕТ COMPRESSION.** Не устраивает ни
размер, ни скорость, ни качество -- это отдельная работа, и она сознательно
отложена, потому что REDUCTION от неё не зависит. Не тратить на неё сессии
и не поднимать связанные вопросы (в том числе `FUSE` по умолчанию для
COMPRESSION и кернель распаковки COMPRESSION) до отдельного указания.

**ЗАКРЫТО 23-24.08, НЕ ПЕРЕОТКРЫВАТЬ БЕЗ НОВЫХ ДАННЫХ:**
- ~~fused dequant+GEMM для sym~~ -- написан, бит-в-бит, но по скорости
  ОТРИЦАТЕЛЕН (+96 мс на шаге), оставлен opt-in как рычаг по памяти (-296 МБ).
- ~~порт кернеля Бо Пэна~~ -- СДЕЛАН 24.08, ядро backward x1.89, шаг +103 мс,
  умолчание включено.
- ~~«int8 быстрее нас втрое»~~ -- измерено, разрыва нет; записанные 0.7-0.8 с
  относятся к T=128.
- ~~микротюнинг кернеля декванта~~ -- TG/векторизация/64 колонки, все 0.99+.
- ~~красный `test_streaming_inference`~~ -- ЗАКРЫТ 24.08 вечером: тест падал
  на решётке типа, а не на баге (порог 1e-4 при разрешении инструмента
  2.6e-4 = 0.5 ulp fp16). Порог поставлен по измеренному полу, добавлены
  `probe_streaming_floor.py`, `mutate_streaming_gate.py` и гейт точности
  ядер `test_wkv_infer_vs_train.py`. Не переоткрывать: числа и границы
  применимости записаны в самих файлах.
- ~~«правки мерены только на 1.5B»~~ -- 2.9B промерен 24.08 вечером по всем
  трём правкам, доли выигрышей сохранились.

Оп-каунт закрыт как «взято, сколько было»: `SymQuantLinearFused` и хвост
TMix дали **+1.15 мс (6.2%), 53.6 -> 57.1 ток/с**, гейты зелёные. Сидевшая
в «прочем» LoRA закрыта 16.08 квантованием веток.

Формат `.rwkvq` как самостоятельный — ЗАКРЫТ 04-11.08: шаги 1-4 и 6
сделаны, приложение разблокировано, пользователь качает 1855 МБ на 2.9B
вместо 7691. Остался только шаг 5 (см. приоритет 5).

Скорость декода закрыта как «достаточно хорошо»: мы быстрее llama.cpp на
13% (замер ниже), дальше гнаться дорого. Полный лог сессий до 03.08 — в
git-истории.

**Две функции полезности, из кода они не следуют** (зафиксировано
владельцем проекта 12.08). REDUCTION — база для QLoRA (по замерам ведёт
себя лучше int8) и для векторных моделей: это точка МАКСИМУМА КАЧЕСТВА
при приемлемом размере, и условные +50 МБ ради качества там приемлемы.
COMPRESSION — максимум производительности при минимальных потерях
качества, борьба за мегабайт идёт там. Одна и та же правка может быть
обязательной для одного пресета и неуместной для другого; решать порознь.

---

**17.08. WKV НА ПРЕФИЛЛЕ УСКОРЕН БИТ-В-БИТ, sym-БАЗА ЖИВЁТ В rwkv-metal,
ШАГ ОБУЧЕНИЯ РАЗЛОЖЕН.**

- **WKV: 95.0 -> 54.6 мс** застилкой строк в threadgroup, выход бит-в-бит
  (гейт `test_wkv_infer_parity`). Префилл +6.0% на 1.5B, +3.8% на 2.9B.
- **QLoRA умеет `sym`**, и попутно найдена строка в `model/convert.py`,
  разворачивавшая весь квантованный базис в плотный bf16:
  **пик сборки 9.57 -> 3.90 ГБ системных, сборка 6.8 -> 1.3 с**, логиты
  бит-в-бит.
- **Шаг обучения (T=512, r=16, 3288 мс, пик 4261 МБ): WKV — 35% времени
  и 819 МБ, деквант — 7% и 467 МБ.** Внутри WKV forward считается дважды
  (пересчёт под чекпоинтингом) и даёт 54% его времени.
- **Четыре рычага закрыты замером:** fp16-оптимизатор (вся `opt.state`
  75.5 МБ), fp16-активации (чекпоинтинг их не хранит), чанкованный лосс
  (17 мс, −13 МБ), свой VJP (нативный путь плотную `W` и так не
  материализует).
- **`mx.compile` на обучении не выигрывает, забег вперёд не помогает**
  (`bench_train_compile_ab`), но и пик 12 ГБ compile не возвращает.
- ~~**ГЛАВНЫЙ ОТКРЫТЫЙ СЛЕД: тренировочный forward WKV 321 мс против
  инференсного 54.6 мс.**~~ ЗАКРЫТО 18.08, И РАЗРЫВА НЕ СУЩЕСТВУЕТ: см.
  раздел «РАЗРЫВ 5.9x РАСТВОРИЛСЯ».

---

**18.08. РАЗРЫВА 5.9x НЕТ. ЕСТЬ ОДИН ЗАБЫТЫЙ `astype`, И ОН СТОИТ 12-27%
ТРЕНИРОВОЧНОГО ШАГА.**

- **Ядра различаются на 9%, а не в 5.9 раза.** На ОДНИХ И ТЕХ ЖЕ входах
  (`bench_wkv_train_vs_infer_ab`, чередование, порядок рандомизирован):
  инференсное 1.78 мс на слой, тренировочное 1.95 (1.09x), из них +2% за
  `sa_out` и +7% за `h_checkpoints`.
- **Записанные «321 мс» -- это на 93% НЕ WKV.** Прежняя аблация подменяла
  ядро заглушкой, отдававшей bf16, тогда как ядро отдаёт **fp32**, и
  вместе с рекуррентностью уносила перевод всего хвоста блока в bf16.
  Чистая цена скана -- **16.5 мс на forward (0.69 мс/слой)**, то есть
  ВТРОЕ ДЕШЕВЛЕ инференсных 54.1, а не в 5.9 раза дороже.
- **Механизм найден: `RwkvqSymLinear.__call__` -- это `x @ w.T` без каста.**
  fp32-выход WKV тянет за собой fp32-копию деквантованной матрицы и
  fp32-матмул `o_proj` на каждом из 24 слоёв. В rwkv-quant того же нет:
  там линейный слой сам приводит вход (`x.astype(w.dtype)`), и потому
  инференсная аблация WKV честна -- проверено (54.1 против 53.1 мс).
- **Цена, замеренная сквозным A/B шага (`bench_wkv_fp32_tail_ab`):**

  | вариант | мс/шаг | выигрыш | пик МБ | relmax логитов |
  |---|---|---|---|---|
  | база | 3196.2 | -- | 4302 | -- |
  | каст в линейном слое | **2850.8** | **+345 (12.1%)** | 3772 (−530) | 1.06e-2 |
  | `CAST_WKV_OUTPUT=True` | **2523.0** | **+673 (26.7%)** | 3582 (−720) | 5.70e-2 |
  | оба | 2520.4 | +676 | 3582 | 5.70e-2 |

  Не аддитивны, и это ожидаемо: приведение выхода ядра делает вход
  `o_proj` bf16 и тем самым включает в себя первую правку.
- **ОБЕ правки МЕНЯЮТ ЧИСЛА, ppl не мерен.** Внедрять только с гейтом на
  мультиязычном корпусе (закон 9). Каст в слое при этом сближает
  rwkv-metal с rwkv-quant, а не разводит их.
- **Заодно снимается «WKV -- 35% шага и 819 МБ».** По тому же разбору на
  шаге с градиентом: прежняя аблация даёт 1102.6 мс, из них 679-750 --
  fp32-хвост, на скан остаётся около 350 мс (11% шага). Память сходится
  независимо: приведение выхода снимает 720 МБ пика при записанных 819.

---

## 05.09. ПУБЛИЧНЫЙ API ПАДАЛ У ВСЕХ, КРОМЕ НАС; ОРИЕНТАЦИЯ ДОВЕДЕНА ДО САЙДКАРА; ПЕРЕСЧЁТ ПРОТИВ llama.cpp ПРЕВОСХОДСТВА НЕ ПОДТВЕРДИЛ; ЗАКОН 40

> **ВНИМАНИЕ (09.09).** Четыре утверждения этого раздела отменены замерами: расхождение выбора типов у llama.cpp (его нет), размер `reduction` 1342.5 МБ (не воспроизводится, верно 1435.1), единицы в таблице против llama.cpp (их сторона взята в МиБ, наша в МБ) и «фоновые задания обрыв переживают» (не переживают). Подробности и исправленные числа -- в разделе 09.09.

**Два дефекта публичного API, оба невидимые из дерева исходников.**
`calib_corpus.txt` не попадал в собранный пакет: каталог
`rwkv_quant/data/` пакетом не является, `packages.find` его не брал,
`package-data` и `MANIFEST.in` отсутствовали. Умолчательный
`quantize(act_stats="auto")` читает этот файл -- значит API не работал ни
у кого, кроме запускающих из репозитория (`661594a`). Второй: `_encoder`
не разбирал путь к словарю, потому что у `str` метод `.encode` ЕСТЬ, а
проверка `hasattr` стояла выше `isinstance(..., str)`; чанк корпуса
уходил в `str.encode` как ИМЯ КОДИРОВКИ, наружу шёл `LookupError:
unknown encoding: <текст чанка>`. Форма вызова из quick-start не работала
никогда (`8465707`).

**Гейты на оба.** `a799cab` -- третья ветка `_encoder` (str и PathLike),
значение проверяется МУТАЦИЕЙ: в файлах новой раскладки все ключи не
транспонированы, поэтому совпадение само по себе зелено и на
захардкоженном False. `0fe58ea` + `313f296` -- файлы данных обязаны
доезжать в колесо И sdist, содержимое сверяется побайтово. Файл общий для
rwkv-quant и rwkv-metal, md5 обязан совпадать; сборка идёт из копии по
`git ls-files` (рабочее дерево rwkv-metal содержит `runs/` на 2.6 ГБ).
Упаковка rwkv-metal проверена и ИСПРАВНА: словарь 1093733 байта доезжает
в оба канала.

**Закон 40.** Первая редакция гейта упаковки мутацию НЕ ловила: собирала
в рабочем каталоге, а `build/lib` и `egg-info/SOURCES.txt` переживают
правку `pyproject.toml` и подкладывают файлы предыдущей удачной сборки
(`b1ec91d`). Записка `rwkv-metal/NEXT_SESSION.md` помечена устаревшей
(`0bdfbe7`): правилась 03.08, отстала на месяц и увела планирование.
Записка у проекта теперь ОДНА -- эта.

**Ориентация доведена до сайдкара.** `export_mlx` поле `transposed` не
писал вовсе: манифест `.rwkvq` его знает, экспортёр ронял, и потребитель
мог узнать ориентацию только из зашитой у себя таблицы имён. Теперь
пишет, неизвестная ориентация падает громко (`dacf6ed`). Гейт
самоописания сторожит запись (`feac758`). `verify_sidecar_full` сторожит
выход и показывает порту контракт через `linear_weight_from_sidecar`
(`5a6a537`): деквант по осям хранения, ориентация ПОСЛЕ и ИЗ МАНИФЕСТА,
запасной путь через `codec.is_transposed` для сайдкаров без поля.
Проверено на 1.5B прежней раскладки: 798 ключей, расхождений с
источником НОЛЬ, 192 транспонированных доехали, формы сходятся.

Остался только Swift (`RwkvqFullConvert.swift:79-93`, восемь жёстких
`.transposed()`), он ждёт очереди: порядок работ rwkv-metal -> SwiftRWKV
-> Synapse. Умолчание при отсутствии поля обязано ОСТАТЬСЯ таблицей
имён: выкаченный сайдкар в Synapse собран 8 августа, поля не имеет, лежит
в прежней раскладке, и нынешнее жёсткое поведение для него ВЕРНОЕ.

**Конвертер `.pth` -> gguf** (`b25da11`). Апстримного на машине нет: в
`build-tools/llama.cpp` лежит обрезанный `convert_hf_to_gguf.py` (296
строк, слова `rwkv` нет ни разу), а HF-репозитория для g1h может не
существовать. Схема снята с существующего `Q4_K_M`, который llama.cpp уже
читает, и сверена с `src/models/rwkv7.cpp`. Три ловушки: ориентация
(линейные веса как есть, восемь низкоранговых транспонируются), слой 0
(llama.cpp ждёт там `v1`/`v2` с рангом ICLR 96, а не value-residual 64, с
пометкой actually not used -- дополняем нулями, иначе падает загрузка),
порядок lerp строго `r, w, k, v, a, g`. Выход за диапазон F16 не
обрезается молча, а падает: это база для Δppl всей их стороны.

**ПЕРЕСЧЁТ ПРОТИВ llama.cpp: ПРЕВОСХОДСТВА НЕТ.** Обе стороны на честной
калибровке, корпус v1 (38 окон, 19456 токенов); их imatrix снят по
КАЛИБРОВОЧНОЙ половине (17 окон, 8704 токена), а не по измерительной --
именно это и было нечестным в прежней постановке. Обе половины
декодированы из токенов в текст, чтобы llama.cpp видела ровно те же
данные.

| сторона | размер | Δppl к своей базе |
|---|---|---|
| наш `reduction` | 1342.5 МБ | +0.153% |
| llama.cpp Q6_K + imatrix | 1274.2 МБ | +0.235% ± 0.096 |
| наш `compression` | 970.2 МБ | +4.032% |
| llama.cpp Q4_K_M + imatrix | 944.2 МБ | +3.147% ± 0.317 |

Их числа ПАРНЫЕ (`--kl-divergence-base`, 1211 МБ логитов F16), интервал
по отношению ppl. KL: Q6_K 0.004005 ± 0.000055, Q4_K_M 0.037340 ±
0.000758. База F16 ppl 7.2804, наша bf16 ppl 8.198 -- абсолютные
несравнимы из-за разной токенизации, поэтому только Δ% к своей базе.

Вывод. В почти-безпотерьной точке разница по ppl (0.153 против 0.235)
лежит ВНУТРИ одной их сигмы, то есть неразличима, а по размеру мы хуже
на 5.4%. В точке сжатия отставание 0.885 п.п. -- около 2.8 сигмы,
различимо, и файл у них ещё и меньше. Формулировка README про
превосходство по всем трём осям пересчёта НЕ пережила. Ось скорости не
перемерялась.

ОГОВОРКА, без которой публиковать нельзя: прежние их строки (+0.19 /
+3.44) снимались с ИЮЛЬСКОГО gguf, а эти кванты собраны из моей
F16-конверсии, и размеры вышли другими (1274 против 1336, 944 против
990) -- выбор типов по тензорам не совпал. Сдвиг от +3.44 к +3.15
смешивает честный imatrix с другой сборкой и одной калибровке не
приписывается.

**Цена приборов.** Конвертация 2935 МБ: footprint 4.72 ГБ, своп 574.31 ->
574.31 (дельта 0). ppl F16 26 с, логиты базы 1211 МБ, парные прогоны --
дельта свопа 0 везде. ВНИМАНИЕ: `llama-imatrix` (25.6 с) и
`llama-quantize` (15.1 с) мерялись, когда рядом 48 минут крутился
забытый `llama-cli` на 100% CPU -- эти два тайминга НЕДЕЙСТВИТЕЛЬНЫ.
Причина: `kill` был отдан обёртке `nohup` (76120), а не процессу
(76122), и `ps` после `kill` не проверялся. Все прогоны после -- чистые.

**Долги, вскрывшиеся сегодня.**
- `/tmp/act_stats_{0p1b,0p4b,1p5b,2p9b}_world.pt`, обещанные шапкой, НЕ
  СУЩЕСТВУЮТ -- ни в `/tmp`, ни в `artifacts/`, ни в доках. Честная
  калибровка от них не зависит: она из `act_calib_multiling.pt`.
- Размер `reduction`: в таблице README 1435 МБ, замер даёт 1342.5 МБ,
  шапка `bench_decode_vs_gguf.py` -- 1255 МБ. Три числа на одну строку.
- `bench_decode_vs_gguf.py` держит `ACT_STATS = act_stats_1p5b_multiling`
  жёстко в коде: утёкшая статистика в измерительной обвязке.
- `export()` с `out_path=...safetensors` пишет манифест как
  `...safetensors.json`, а Synapse и `verify_sidecar_full` ждут
  `<base>.json`.
- Докстринг `tests/test_act_stats_auto.py` обещает проверку 2
  (токенизатор от другой модели, id за пределами `vocab_size`), которой в
  коде НЕТ: `vocab_size` встречается только в самом докстринге.

**Приоритеты после этой сессии:**

1. РЕШЕНИЕ ВЛАДЕЛЬЦА по таблице llama.cpp в README: числа пересчитаны,
   вывод не в нашу пользу, оговорка про разную сборку gguf выше.
   Публиковать разворот, доснять ось скорости или сперва выяснить, почему
   разошёлся выбор типов по тензорам.
2. Swift `RwkvqFullConvert.swift:79-93` -- когда дойдёт очередь.
3. `act_stats_2p9b_ml.pt` -- проверить принадлежность: шапка
   `build_2p9b_preset.py` утверждает, что статистика не с корпуса ppl, но
   для 1.5B ровно это утверждение оказалось ложным.
4. Долги по гейтам: `test_lora_quant_parity` требует
   `/tmp/reduction_sym_head8.rwkvq`, которого `restore_tmp.sh` не
   восстанавливает (подтверждено 05.09); `test_rwkvq_container` и
   `test_sym_packer_parity` падают, если дать путь аргументом.
5. `CAST_WKV_OUTPUT` -- см. пункт 5 раздела 04.09.
6. Решение про `format_version`: поле `transposed` пишется, версия НЕ
   бампалась, отсутствие поля по-прежнему означает вывод по таблице имён.

**Новые артефакты** (перенесены из `/tmp`, ребут переживут), все в
`~/Develop/WKV-kvant/`: `rwkv7-g1h-1.5b-F16.gguf` (2935.2 МБ),
`rwkv7-1p5b-Q6_K.gguf` (1274.2), `rwkv7-1p5b-Q4_K_M.gguf` (944.2),
`rwkv7-1p5b.imatrix` (2.5), `ppl_eval.txt` (38 окон) и `ppl_calib.txt`
(17 окон) -- обе половины v1-корпуса, декодированные из токенов.

## 09.09. COMPRESSION ПЕРЕРАСПРЕДЕЛЁН: `proj` 5->4, `cmix.key` 4->5 -- ТОТ ЖЕ РАЗМЕР ПРИ ppl +2.850% ВМЕСТО +3.390%; ЧЕТЫРЕ УТВЕРЖДЕНИЯ 05.09 ОТМЕНЕНЫ

> **ПОМЕТКА 10.09. Четыре утверждения этого раздела уточнены или отменены
> замерами 10.09 -- читать только вместе с разделом 10.09.** Отменено:
> (1) "66.8 ГБ/с = 64% паспортных 120" -- арифметика не сходится, 64% это доля
> от измеренных 104, а не от паспорта; (2) трафик 878 МБ/ток -- честные 900.9,
> прежнее число снято сломанным счётчиком; (3) ориентир 90 ГБ/с -- заменён
> владельцем на 80 т/с, потому что 90 ГБ/с меньше времени одной GEMV-фазы;
> (4) 75.3 т/с -- на новой раскладке 74.6, и раскладка тут ни при чём.

### Отменено (перепроверено, не переоткрывать без новых замеров)

**Расхождения выбора типов у llama.cpp НЕ БЫЛО.** Июльский
`rwkv7-g1h-1.5b-Q4_K_M.gguf` лежит на диске рядом с моим от 05.09.
Потензорная сверка: 678 против 678, имён только в одном ноль, расхождений
по (тип, байты, форма) НОЛЬ, сумма байт тензоров совпадает до байта
(988643328). Файлы различаются на 128 байт метаданных, md5 разные
(`ed703a58...` против `159b9786...`), то есть плечи действительно разные
файлы. Моя F16-конверсия воспроизвела июльское квантование ровно, значит
сдвиг +3.44 -> +3.147 честно принадлежит имейтриксу. Оговорка 05.09,
без которой «публиковать нельзя», СНЯТА.

**«1274 против 1336» и «944 против 990» -- это единицы, а не размеры.**
Один и тот же файл: Q6_K = 1336082624 Б = 1336.1 МБ = 1274.1 МиБ; Q4_K_M
= 990052544 Б = 990.1 МБ = 944.1 МиБ. Июльская `GGUF_COMPARE.md` считала
обе стороны в десятичных МБ, пересчёт 05.09 взял их сторону в МиБ, а нашу
оставил в МБ. В коде `/1e6` везде, кроме `tools/convert_pth_to_gguf.py:188`,
где `/1048576`, -- и именно он писал новые gguf. В одних единицах вывод
«файл у них ещё и меньше» ЛОЖЕН: на точке сжатия меньше у нас.

**Размер `reduction` 1342.5 МБ не воспроизводится.** Свежая сборка нынешним
кодом (транспонирование LoRA по умолчанию, честная калибровка) дала
1435062360 Б = 1435.1 МБ; файл 16.08 из `artifacts` весит 1435062048,
разница 312 байт -- поле `transposed` в манифесте. md5 разные. README со
своими 1435.1 прав; 1342.5 нет ни на диске, ни в git, ни в README.
Отношение к Q6_K, в одних единицах: 1435.1 / 1336.1 = **+7.4%**, не +5.4%.

**Фоновые задания обрыв транспорта НЕ переживают.** `nohup ... &` умирает
вместе с сервером macos mcp и от таймаута MCP (10 с) -- убивается вся
группа процессов. Переживает только `screen -dmS <имя> sh -c '...'`
(`setsid` и `tmux` на машине отсутствуют, `screen` есть в `/usr/bin`).
Уточнение по самому отказу: за сессию он случился ТРИ раза, все три -- на
длинном ожидании, ни разу на длинной команде; счёт вызовов был около 70,
110 и 150, то есть и гипотеза про «примерно сто десятый» не держится.

### Сделано: помотричное разложение COMPRESSION

Инструмент `tests/_sess/ablate2.py` -- плечо задаётся JSON-спецификацией
(`bits` по группам, `overrides` по отдельным матрицам, `modes`, `act`),
одно плечо на процесс, корпус мультиязычный 38 окон, `mmap=True`.
Новых переносов кода для новых плеч не требуется. Результаты копятся в
`~/Develop/WKV-kvant/ablate_compression_fine.jsonl` (26 плеч), поэтому
обрыв канала ничего не теряет.

ОГРАНИЧЕНИЕ writer, проверено в коде: при `asym_sb6*` допустимы биты
4/5/6, иначе `NotImplementedError` -- падает ГРОМКО. `bits=16` отключает
gw-ветку целиком, поэтому leave-one-out в bf16 чист.

База (моя честная статистика с репозиторного `calib_corpus`): bf16 8.198021,
COMPRESSION 8.475966 = **+3.390%** при 970.2 МБ. Это НЕ +4.03% из README:
там статистика с world-корпуса. Плечи между собой сравнимы, с README
напрямую -- нет.

**Предельная цена бита, п.п. ppl на 100 МБ** (главный результат дня):

| ход | цена |
|---|---|
| `head` 4->5 | 3.22 |
| `emb` 4->5 | 2.63 |
| `cmix.key` 4->5 | 1.94 |
| `v`/`o` 4->5 | 1.79 |
| `head` 5->6 | 1.55 |
| `cmix.value` 4->5 | 1.31 в одиночку, 0.91 поверх `cmix.key` |
| `cmix.key` 5->6 | 0.41 |
| `r`/`k` 4->5 | ноль (снятие бита не стоит ничего) |

Чувствительность расходится ВНУТРИ групп в разы: `r`/`k` несут вчетверо-
вдевятеро меньше `v`/`o`, `cmix.key` больше `cmix.value`. Группа `proj`
одной битностью была слишком крупной единицей бюджета -- отсюда весь
выигрыш дня.

ОСТОРОЖНО с методом: leave-one-out в bf16 даёт СРЕДНЮЮ отдачу по всему
прыжку и маскирует предельную. По ней пятёрка носителей выстроилась ровно
(0.243...0.195) и я ошибочно заключил, что бюджет исчерпан. Лестница по
одному биту показала разброс в 5 раз. Мерить предельную, не среднюю.

`emb` и `head` сидят на изломе с РАЗНЫХ сторон: возврат `emb` в bf16 даёт
0.073 п.п. на 176 МБ (то есть пяти бит хватает), а снятие пятого бита
стоит 0.44 п.п. за 17 МБ. Пять бит на `emb` -- ровно нужное, ни больше ни
меньше. У `head` то же, но шестой бит ещё окупается.

### Механика формата: что покупает AW

Три конфигурации одного размера 970.2 МБ:

| | ppl | Δ | покупает |
|---|---|---|---|
| без поиска, без AW (`asym_sb6`) | 8.7165 | +6.324% | -- |
| поиск без AW (`asym_sb6_search`) | 8.6759 | +5.829% | поиск 0.495 п.п. |
| поиск + AW (`asym_sb6_aw`) | 8.4760 | +3.390% | **AW 2.439 п.п.** |

AW уносит 83% выигрыша механики, сеточный поиск масштабов 17%. Главный
рычаг формата -- статистика активаций, не подбор масштаба.

**Корпус под AW не важен, проверено.** Статистика, снятая с
`act_calib_multiling.pt` (17 окон, те же три языка, что в замере, срез не
пересекается с 38 измерительными), дала 8.4865 против 8.4760 на
репозиторном корпусе, то есть подгонка под целевой домен НЕ помогает.
При том что сам AW стоит 2.44 п.п., выбор корпуса под ним -- 0.13 п.п. и
в минус. Скрытого переобучения на корпус в AW нет. Оговорка: срез вдвое
меньше репозиторного (8704 токена против 11776), часть разницы может быть
объёмом. Файл: `~/Develop/WKV-kvant/act_stats_calibml.pt`.

### Парето-фронт (26 плеч, все в jsonl)

| точка | размер | Δ ppl |
|---|---|---|
| `rk4_emb4` | 928.3 | +3.798% |
| `rk4` | 945.1 | +3.356% |
| **`rk4_ffnk5_vo4` = новый COMPRESSION** | **970.2** | **+2.850%** |
| `rk4_vo4_ffnk5_head6` | 987.0 | +2.576% |
| `rk4_ffnk5` | 995.4 | +2.377% |
| `rk4_ffnk5_head6` | 1012.2 | +2.117% |
| `rk4_ffnk5_head6_ffnv5` | 1062.5 | +1.657% |
| *Q4_K_M + imatrix* | *990.1* | *+3.147% (+-0.317)* |
| *Q6_K + imatrix* | *1336.1* | *+0.235% (+-0.096)* |

Мы теперь выше их кривой на всём диапазоне: при 1062.5 МБ интерполяция
llama.cpp между Q4_K_M и Q6_K даёт около +2.54%, у нас +1.657%.

### Новый COMPRESSION записан и проверен

`presets.py`: `proj=4, cmix=4, emb_head=5, bits_overrides={"ffn.key.weight":
5, "cmix.key.weight": 5}`, остальное без изменений. Содержательно -- 50 МБ
перенесены из четырёх проекций внимания в матрицу ключа channel-mix.

Проверка СКВОЗНЫМ путём, через контейнер и ридер, эталон fp32 на всех 38
окнах:

| | старый | новый |
|---|---|---|
| файл на диске | 970.56 МБ | 970.55 МБ |
| ppl Δ к bf16 | +3.390% | +2.850% |
| KL, нат/токен | 0.038567 | 0.035992 |
| top-1 | 89.062% | 89.149% |

Парная разность KL **+0.002575, 95% CI [+0.001688; +0.003517]** -- целиком
выше нуля. Лучше на 32 окнах из 38; en 9/9, sr 8/9, ru 15/20 (на русском
выигрыш вдвое меньше). Файлы: `compression_v1_ref.rwkvq` и
`compression_v2_cand.rwkvq` в `~/Develop/WKV-kvant/`, оба собраны одним
кодом и одной статистикой -- различается ТОЛЬКО распределение бит.

### Приборы: два дефекта, оба класса «молчаливый неверный ответ»

**KL считался ТОЛЬКО ПО-РУССКИ.** Корпус отсортирован по языкам (первые 20
окон ru, затем 9 en, затем 9 sr), а вся KL-обвязка берёт срез `[:8]`. Все
прежние KL-числа проекта -- русскоязычные. Лечится `RWKVQ_KL_NSEQ=38`; имя
файла эталона содержит NSEQ, так что подмены не будет. Эталон на 38 окон
собирается за 166 с и весит 5.09 ГБ, лежит в `/tmp` и РЕБУТ ЕГО СНОСИТ.

**Сборка fp32-эталона была сломана и оставляла нулевой файл.**
`load_data()` кладёт токены на mps, а при `REF_DTYPE=fp32` модель
создаётся на cpu -- `torch.embedding` падает на несовпадении устройств. И
падение оставляло пятигигабайтный НУЛЕВОЙ файл под настоящим именем, а
`build_ref()` проверяет только существование: следующий прогон посчитал бы
KL против нулей и промолчал. Починено в `tests/ablate_subgroups.py`:
перенос данных на устройство модели и запись через `.part` с
переименованием в конце.

**НЕ починено, решение владельца:** контроль различности плеч в
`kl_real_path.py` снимает отпечаток только с `emb`. Два плеча, законно
совпадающие в `emb` и различающиеся в `proj`/`cmix`, он объявляет
неразличимыми и отказывается сравнивать (сработало на нашей паре, проверено
по самим файлам: `att.key`, `att.value`, `ffn.key` различаются, `emb`,
`ffn.value`, `head` совпадают). Парная разность выше посчитана в обход, из
`per_seq` в `/tmp/kl_real_path.json`. Отпечаток надо расширить на несколько
представительных тензоров.

### Долги, оставленные этой сессией

1. **README: строки `compression` для 0.1B / 0.4B / 2.9B недействительны.**
   Перемерен только 1.5B, а чувствительность по масштабам не переносится.
   Предупреждение в README вставлено, пересчёт не сделан.
2. ppl НАПРЯМУЮ С ЗАПИСАННОГО ФАЙЛА не снят: с реального пути снят KL, а
   ppl приходит из свипа, где чекпоинт квантовался в памяти.
3. Долг приоритета 4 с 05.09 не тронут: три гейта, `/tmp/reduction_sym_head8.rwkvq`.
4. Структурная ось не тронута: размер блока `gs` (у нас 32, у Q6_K 16).
   Это единственное, что осталось после исчерпания оси распределения бит.
5. Третий пресет: между COMPRESSION (970 МБ) и REDUCTION (1435 МБ) зияет
   465 МБ. `rk4_ffnk5_head6_ffnv5` (1062.5 МБ, +1.657%) ложится ровно туда.

### Состояние на конец 09.09

`rwkv-quant`: `eaebc62`, дерево чистое. В коммите -- `presets.py`,
`README.md`, `tests/ablate_subgroups.py`, новые `tests/_sess/ablate2.py` и
`tests/_sess/ablate_compression_fine.py`.
`rwkv-metal`: `0bdfbe7` (шапка 05.09 говорила `297ff2f` -- отстала на один
коммит), дерево чистое, main на 14 коммитов впереди origin.
`SwiftRWKV` не трогался.

Скорость за сессию не мерена ни разу. Уже снятое (README): `compression`
декодирует 75.3 т/с при 878 МБ/токен = 66.8 ГБ/с, 64% паспортных 120.
Ориентир владельца -- 90 ГБ/с, это около 101 т/с, рост в 1.35 раза.


## 10.09. БЮДЖЕТ ШАГА ДЕКОДА РАЗЛОЖЕН ЦЕЛИКОМ И СХОДИТСЯ В 0.3%; СЧЁТЧИК ТРАФИКА ДАВАЛ ПЛАВАЮЩИЙ ОТВЕТ; ОРИЕНТИР СМЕНЁН НА 80 т/с; ДОЛГ 3 ЗАКРЫТ

> **ПОМЕТКА 11.09. Четыре утверждения этого раздела отменены замерами
> 11.09 -- читать только вместе с разделом 11.09.** Отменено:
> (1) статья пред-WKV 0.387 мс -- в настоящем шаге 0.574, прежнее число
> было нижней границей прокси `probe_chain_cost`, как и сказано в его
> собственном докстринге; (2) цель «снять 0.91 мс двумя ядрами по
> образцу fused_tail» -- для половины пред-WKV потолок 0.292 мс, а факт
> по трём вариантам ядра 0.00 при разбросе раундов 1 мс; (3) гипотеза
> «около пяти запусков на слой» -- на компилированном пути `mx.compile`
> уже сводит цепочку примерно к одному диспатчу, и 0.282 мс из 0.574 это
> цена самого запуска; (4) зарегистрированное предсказание, что ошибка
> в `w` компаундится как (1+eps)^n по длине контекста -- НЕ
> подтвердилось: дрейф выходит на плато ~1.6e-4 и держится (32 токена
> 1.164e-4, 1024 токена 1.651e-4), проверено на всех четырёх размерах.
> Пороги приёмки 10.09 НЕ двигались и взяты все: `k`, `v` бит-в-бит,
> `w` relmax 5.96e-08, `kk` и `kk*a` РОВНО 0.

### Отменено и уточнено в разделе 09.09

**"66.8 ГБ/с = 64% паспортных 120" арифметически не сходится.** 66.8/120 =
55.7%. Записанные 64% -- это доля от 104 ГБ/с, то есть от ИЗМЕРЕННОГО потолка
(закон 12), а не от паспорта. Дальше везде считать от 104.

**Трафик 878 МБ/ток занижен, честные 900.9.** Из них 843.1 читает GEMV-фаза
(head 94.4 + proj 232.8 + cmix.key 283.1 + cmix.value 232.8), 54.8 -- активная
квантованная копия LoRA, 3.0 -- мелочь. Состояние WKV (25.2 МБ чтение+запись за
токен) сюда НЕ входит: это активации, а не веса. Прежние 878 снимались
счётчиком, который был сломан двумя способами сразу (ниже).

**Ориентир 90 ГБ/с заменён владельцем на 80 т/с как первый рубеж.** Причина
арифметическая: 900.9/90 = 10.01 мс/ток, а одна GEMV-фаза стоит 10.17 мс.
Значит 90 ГБ/с недостижимы устранением оверхеда в принципе -- нужен ещё и более
быстрый кернель. 80 т/с = 12.5 мс = снять 0.91 мс.

**"Замер делать на новом пресете" выполнено, ответ отрицательный.** v1 и v2
неразличимы по скорости: ОДИН файл в двух процессах расходится на 2.7%, а v1
ложится между двумя прогонами v2. Это предсказывалось арифметикой до замера:
Перераспределение бит трафик-нейтрально, потому что proj (4 x 2048^2 на слой) и
ffn.key (2048 x 8192) равны по числу элементов -- бит переехал между двумя
группами одинакового размера, оттого и размер файла тот же.

### Сделано: полный бюджет шага декода

Все случаи чередуются в ОДНОМ процессе (закон 1), из каждого вычтен пол eval
0.207 мс. Модель compression_v2_cand, разброс full 1.2%, своп не рос.

| статья | запусков/слой | мс | % шага | дно по памяти | оверхед |
|---|---|---|---|---|---|
| GEMV: rkv+o_proj+cmix.key+cmix.value+голова | 4 (+1) | 10.165 | 77.2 | 8.11 | 2.06 |
| LoRA sep | 8 | 1.276 | 9.7 | 0.53 | 0.75 |
| нормы + сдвиги + лерпы | ~6 | 0.726 | 5.5 | ~0.01 | 0.72 |
| WKV | 1 | 0.426 | 3.2 | 0.24 | 0.19 |
| пред-WKV блок | ~5 | 0.387 | 2.9 | ~0 | 0.39 |
| хвост (уже фьюзнут) | 1 | 0.224 | 1.7 | ~0 | 0.22 |
| **работа шага** | | **13.204** | | **8.89** | **4.31** |
| синк | | 0.207 | | | |
| **итого** | | **13.41 = 74.6 т/с** | | **= 112 т/с** | |

Сумма статей 13.204 против 13.162 мс работы в full: расхождение 0.3%, то есть
неучтённого в шаге не осталось. Две последние статьи сняты прокси-цепочками под
mx.compile, где 24 слоя идут подряд и compile сшивает их лучше, чем в настоящем
шаге, -- это НИЖНИЕ границы, ошибка в сторону "выигрыш будет не меньше".

Полоса по группам форм, независимой выдачей, пол вычтен из каждой:

| группа | форма, бит | ГБ/с | % от 104 |
|---|---|---|---|
| proj r/k/v/o | 2048->2048, 4 | 85.6 | 82 |
| cmix.key | 2048->8192, 5 | 99.6 | 96 |
| cmix.value | 8192->2048, 4 | 87.4 | 84 |
| head | 2048->65536, 5 | 86.6 | 83 |
| вместе | | 90.5 | 87 |

Та же работа ЦЕПОЧКОЙ, как в настоящем шаге, -- 80.2 ГБ/с. Разница около 1.0 мс
на 97 последовательных запусках GEMV. Кернель, стало быть, НЕ медленный: он идёт
на 87% измеренного потолка, а теряется на сериализации. Цена одного
последовательного запуска снята отдельно разгоном длины цепочки: 13.1 мкс при
N=24 (на коротких цепочках шумно: 11.6-20.1).

### Отрицательный результат: LoRA glue проиграла, решение 16.08 подтверждено

LoRA при LORA_Q="sep" -- это восемь запусков на слой (четыре down плюс четыре
up), 192 на шаг, вдвое больше, чем вся GEMV-часть, ради 6% байт. Готовая
альтернатива glue складывает четыре down в один матмул. Замер (bench_lora_quant_ab,
5 раундов чередованием): glue 14.4-14.65 мс/ток против sep 13.72-14.06. Байты
паддинга ранга дороже сэкономленных запусков. Ранги веток разные (96/96/64/256),
поэтому ЛЮБАЯ укладка в один вызов средствами MLX требует паддинга. Вывод для
приоритета: сокращать число запусков ценой байтов нельзя, только своим ядром.

### Приборы: три дефекта класса "молчаливый неверный ответ", все три починены

**1. decode_traffic_mb дедуплицировал по id() неудерживаемого объекта.** Поля
qs, d, codes -- СВОЙСТВА, отдающие свежий mx.array на каждый доступ (qblk --
настоящий атрибут). Временный массив освобождался сразу после add(), его id
доставался следующему тензору, и следующий молча объявлялся посчитанным. Из 1154
тензоров так терялось 432; ни одного НАСТОЯЩЕГО дубля в обходе нет. Ответ плавал
по воле аллокатора: 851.0 на первом вызове и 915.8 на последующих, в одном
процессе на одном файле. Починено удержанием ссылок.

**2. Тот же счётчик двоил байты.** codes -- то же хранилище, что qblk, а список
имён перечислял оба. Починено обходом vars() линейного слоя: слой хранит ровно
три буфера (qblk, qsqm, ddm), свойств среди них нет.

**3. Ленивость квантованных копий LoRA.** При LORA_Q копии строятся не при
загрузке, а на ПЕРВОМ шаге декода (quant_model.py:574). Счётчик, вызванный сразу
после загрузки, их не находит и молча считает ПЛОТНУЮ LoRA: +100.1 вместо +32
МБ. Починено громким предупреждением; считать трафик ПОСЛЕ шага декода.

Побочно: trace_decode_steady.py разбирал sys.argv[2] на уровне модуля, поэтому
падал при импорте ради decode_traffic_mb -- то есть bench_gemv_presets_ab с двумя
путями не запускался НИКОГДА, вопреки своему докстрингу. Разбор убран под
__main__.

### Приборы: дефекты найденные и НЕ починенные

- **bench_gemv_presets_ab**: с двумя полными моделями в одном процессе даёт
  разброс 18-29% при собственном пороге вердикта 4%. Чередование двух моделей на
  этой машине само портит замер. Кроме того его "полоса в GEMV-фазе" делит ПОЛНЫЙ
  трафик токена на время фазы, которая LoRA и мелочь не читает: печатает 85.7
  вместо честных 80.2.
- **bench_step_decompose**: GEMV-прокси зовёт РАЗДЕЛЬНЫЕ r_proj/k_proj/v_proj, а
  настоящий шаг идёт _rkv_fused. Разница измерена и мала (10.486 против 10.372,
  то есть 0.13 мс), но прокси не тот.
- **bench_lora_quant_ab**: роняет сводку с KeyError: None, если RWKVQ_MODES не
  включает fp16-базу. И гоняет с FUSE=False -- отношение sep/glue из него брать
  можно, абсолютные мс/ток нельзя.
- **test_fuse_parity**: якорь ppl 11.7125 привязан к champion_v2, а это СТАРАЯ
  раскладка. Для нового пресета якоря нет вообще.

### Тождество плеча: долг 3 закрыт

В kl_real_path.py контроль включения снимал отпечаток только с emb и объявлял
неразличимыми плечи, законно совпадающие в emb. Для гейта ядра это блокирующий
дефект: там оба плеча читают ОДИН файл и различаются только рантайм-флагом.
Теперь тождество двухчастное:

- отпечаток ВЕСОВ -- семь срезов из разных групп и раскладок (emb, head, r_proj
  первого слоя, o_proj среднего, cmix.key и cmix.value последнего, w_lora_A),
  каждый в fp32, с проверкой на вырождение;
- отпечаток КОНФИГУРАЦИИ -- девять флагов декода, включая заранее заведённый
  FUSE_PREWKV.

Сводка ругается, только если совпало И то и другое. "Веса совпали, конфигурации
разошлись" печатается как законная пара и считается парная разность. Старые
строки без нового отпечатка проверяются по-прежнему, по emb.

### Артефакты: три новых .rwkvq, развёртка по размерам обеспечена

Собраны НОВЫМ пресетом compression (он в репозитории с 09.09 и подтверждён
чтением манифестов):

| модель | слоёв | D | S | H | файл | МБ | бит/параметр |
|---|---|---|---|---|---|---|---|
| 0.1B | 12 | 768 | 64 | 12 | compression_0p1b.rwkvq | 126.8 | 5.31 |
| 0.4B | 24 | 1024 | 64 | 16 | compression_0p4b.rwkvq | 291.3 | 5.17 |
| 1.5B | 24 | 2048 | 64 | 32 | compression_v2_cand.rwkvq | 970.6 | 5.08 |
| 2.9B | 32 | 2560 | 64 | 40 | compression_2p9b.rwkvq | 1855.1 | 5.03 |

Бит на параметр падает с размером не из-за пресета, а потому что мелкие тензоры
лежат в 16 битах и у маленькой модели весят относительно больше. Этим закрыт
вход в долг 1: строки README по compression для 0.1B/0.4B/2.9B теперь есть на чём
перемерить.

**Память квантования: 7.7 ГБ из прежних разделов -- это про грид абляций, а не
про вызов quantize().** Обычная сборка держит пик около удвоенной плотной
модели: на 2.9B RSS 5.6 ГБ, своп за весь прогон не вырос (764.6 против 788.6 МБ
до, то есть даже упал). 2.9B проходит с запасом. Долгая часть -- act_stats: 712 с
на 2.9B, кешируется в ~/.cache/rwkv-quant.

Исходники: 0.1B и 1.5B в ~/Develop/WKV-kvant/, 0.4B в /Users/s/, 2.9B в
~/Develop/. Словарь: rwkv7-1.5B-g1g-ctx8192-mlx-6bit/rwkv_vocab_v20230424.txt.

**У 0.4B и 1.5B ОДИНАКОВАЯ глубина (24 слоя) при разнице в параметрах в 3.4
раза, а S=64 у всех четырёх.** Это делает развёртку не перебором размеров, а
проверкой механизма: см. предсказание ниже.

### Пороги приёмки ядер, зарегистрированы ДО первого замера

Владелец делегировал пороги, оговорив: декод не должен ухудшать качество, модель
и так хуже от квантования.

| что | порог |
|---|---|
| выходы k и v | БИТ-В-БИТ, без допуска |
| w, kk, kk*a | relmax <= 1e-6 |
| дрейф состояния WKV на 1024 токенах | <= 1e-3 относительных И рост не быстрее линейного |
| KL(фьюз \| нефьюз) на одной модели | <= 1e-4 нат/ток и не менее чем в 100 раз меньше KL квантования |
| greedy 256 токенов | совпадение |
| ppl | справочно, решать по ней НЕЛЬЗЯ |

Обоснование двух строк. k и v -- чистые умножения и сложения: расхождение там
означает переписанный порядок формулы, то есть баг, а не шум. ppl исключена из
решающих: квантование стоит порядка процента, требование "ядро добавляет
пренебрежимо" -- это сотая доля процента, что НИЖЕ полосы самого замера ppl
(+-0.05 п.п.); зелёная ppl тут ничего не доказывает. Решает KL между плечами.

Опасность в этом ядре одна и она не очевидна: **w -- коэффициент затухания, он
умножает состояние на КАЖДОМ шаге**, и относительная ошибка компаундится как
(1+eps)^n по длине контекста. Проверка на одном шаге её не видит, а нынешний
test_fuse_parity смотрит префилл, восемь decode-шагов и greedy на 48 токенов --
этого мало. Отсюда строка про дрейф на 1024.

### Предсказание, зарегистрировано ДО замера

S=64 у всех четырёх моделей, значит редукция всегда по 64 элементам и
погрешность НА ЭЛЕМЕНТ от размера модели не зависит вовсе. Меняются глубина
(12/24/24/32) и распределения активаций. Поэтому дрейф должен расти как глубина x
длина контекста, а не как число параметров, и **0.4B с 1.5B обязаны дать
одинаковую погрешность**. Если разойдутся -- механизм не тот, и надо
останавливаться на нём, а не двигать порог.

### План: два ядра по образцу fused_tail

Обе почти-чисто-оверхедные статьи (нормы+сдвиги+лерпы 0.726 и пред-WKV 0.387,
вместе 1.113 мс при дне около нуля) соседние по потоку данных:

    резидуал -> ln -> token-shift -> шесть лерпов -> [GEMV rkv + LoRA] ->
    пред-WKV блок -> WKV -> хвост (уже фьюзнут)

Одним ядром их не накрыть -- посередине матмулы. Двумя можно:

1. **"шапка слоя"**: резидуал + layer_norm + token-shift + шесть лерпов -> xs[6, D]
   одним запуском. Бьёт в 0.726.
2. **"пред-WKV"**: w = exp(-0.606531*sigmoid(y_w+b)); a = sigmoid(y_a+b);
   kk = l2_norm(k*k_k); k = k*(1+(a-1)*k_a); v = v+(v_first-v)*vv; выходы -kk и
   kk*a. Бьёт в 0.387. Геометрия ровно как у хвоста: одна threadgroup на пару
   (строка, голова), одна simd-редукция, l2_norm идёт по последней оси, то есть
   по голове.

Если каждое оставит от своего пула по 0.1 мс, это -0.9 мс: 12.5 мс, 80.0 т/с.
Рубеж берётся этими двумя ядрами и ничем больше; GEMV и LoRA не трогаются.

Дисциплина: **гейт пишется ПЕРВЫМ**, до ядра, иначе он подгонится под то, что
получилось. Сначала прогнать гейт с fused = ref, убедиться, что он проходит
тривиально, и только потом писать ядро.

Для гейта ядра эталон bf16 НЕ нужен: он сравнивает два плеча одной модели. Плечо
без ядра сбрасывает логиты, плечо с ядром читает и считает парную разность, файл
удаляется, следующий размер переиспользует место. Эталон bf16 нужен только под
второй вопрос -- "сколько стоит само квантование", чтобы проверить стократный
запас; он есть для 1.5B (5.09 ГБ в /tmp), и этого достаточно, чтобы установить
отношение.

### Долги, оставленные этой сессией

1. Ничего не закоммичено. В rwkv-quant грязно: правки в tests/trace_decode_steady.py
   (три штуки) и tests/_sess/kl_real_path.py, плюс семь новых проб в tests/_sess/.
2. README: строки compression для 0.1B/0.4B/2.9B по-прежнему недействительны, но
   файлы под перемер теперь есть. Для 1.5B там же занижен трафик (878 вместо
   900.9) и не сходится процент от потолка.
3. Четыре дефекта приборов из списка выше не починены.
4. Долг 4 с 05.09 (три гейта, /tmp/reduction_sym_head8.rwkvq) не тронут.
5. ppl НАПРЯМУЮ С ЗАПИСАННОГО ФАЙЛА так и не снят (долг 2 от 09.09).
6. **Записка переросла собственное правило.** Разделов семь при норме три-четыре,
   файл 1531 строка и 125 КБ; чтение его целиком в начале каждой сессии стоит
   заметной доли контекста. По правилу из шапки старшие разделы (27.08, 27.08
   вечер, 28.08) пора переселять в `docs/LOG-*` тем же способом: копия в /tmp,
   нарезка sed, сверка склейки по md5.

### Состояние на конец 10.09

rwkv-quant: HEAD 4eca257, дерево ГРЯЗНОЕ (список выше). rwkv-metal: 0bdfbe7,
чисто, main на 14 коммитов впереди origin. SwiftRWKV не трогался.

Пробы сессии в tests/_sess/: probe_traffic_stability, probe_traffic_vars,
probe_traffic_breakdown, probe_gemv_by_group, probe_chain_cost (в нём же WKV,
LoRA, пред-WKV, хвост и нормы -- он и есть инструмент бюджета), probe_wkv_cost
(отдельным процессом мерить НЕ надо, см. ниже), probe_ckpt_inventory.
Логи: ~/Develop/WKV-kvant/speed_0909_*.log, mkq_*.log, ckpt_inventory.log.

**Методическое, дорого купленное.** WKV сначала мерился отдельным процессом и дал
0.69 мс; тот же скрипт с большим числом повторов дал 1.74 при том, что пол eval
вырос с 0.221 до 0.388 -- это троттлинг (закон 25): длинный прогон греет машину и
замедляет ВСЁ. Доля от шага, снятая в другом процессе, фиктивна по построению.
Любую статью бюджета мерить только чередованием с full в одном прогоне.

**Таймаут MCP.** Паузы держать не длиннее 8 секунд: sleep 10 и два sleep 8 подряд
обрывают вызов. Прогоны в screen -dmS переживают обрыв, поэтому терять нечего,
но лишний круг стоит времени.

## 11.09. ЯДРО ПРЕД-WKV НАПИСАНО, ЗЕЛЁНОЕ И БЕСПОЛЕЗНО: СТАТЬЯ СТОИТ 0.574 МС, НО 0.282 ИЗ НИХ -- ЦЕНА САМОГО ЗАПУСКА; ТРИ ВАРИАНТА ЯДРА ДАЮТ НОЛЬ; БАТЧЕВЫЙ ДЕКОД УПИРАЕТСЯ В 2x

Коммиты: `cf33396` (пред-WKV: вынос, гейт, проба, замер), `a508e26`
(замеры осей параллелизации и префилла).

### Что сделано

**Вынос пред-WKV в одну функцию.** Формулы стояли инлайном ДВАЖДЫ -- в
`forward_stateful` и в `_forward_stateful_fused` -- и различались порядком
объявления `w` и `a`. Сведены в `fused_prewkv.prewkv_ref`, чистое
перемещение, бит-в-бит. Это стоит оставить НЕЗАВИСИМО от судьбы ядра:
правка больше не переезжает между копиями руками, и у гейта ядра есть
эталон, которым считает САМА МОДЕЛЬ, а не копия формул внутри теста
(закон 30). `l2_norm` переехал туда же, имя `qm.l2_norm` живо через импорт.

**Гейт `tests/test_prewkv_parity.py`.** Шесть пунктов приёмки 10.09, ни
один не пропускается молча: при отсутствии эталона квантования печатается
НЕ ПРОВЕРЕНО и код выхода 2. KL считается через `ablate_subgroups.kl_stats`,
тем же прибором, которым посчитаны все KL проекта -- иначе отношение к KL
квантования считалось бы двумя разными линейками (закон 39).

Одно сознательное отклонение от плана 10.09: плечи сравниваются ПООКОННО в
одном процессе, без сброса логитов на диск. Пара логитов окна -- 268 МБ,
весь корпус -- 5 ГБ; пооконное чередование даёт ту же парную разность при
постоянной памяти и без файла, который надо чистить между размерами.
Скорость гейт не меряет, поэтому закон 1 тут ни при чём.

**РАЗРЕШЕНИЕ ГЕЙТА ПОМЕРЕНО МУТАЦИЕЙ** (закон 37; ручки
`RWKVQ_GATE_MUTATE`, `RWKVQ_GATE_MUTATE_WHAT`), 0.1B, дрейф 128:

| прибор | наименьшая пойманная мутация |
|---|---|
| `k`, `v` (бит-в-бит) | 1e-7 и ниже -- упирается только в то, меняется ли хоть один бит |
| `nkk`, `kka` relmax | 1e-6 |
| `w` relmax | 1e-5 |
| KL между плечами | 1e-3 ловит с запасом 260x (2.59e-2 при пороге 1e-4) |
| дрейф <= 1e-3 | 1e-3 по `w`, 1e-2 по `nkk`/`kka` |
| greedy 256 | только 1e-2 -- САМЫЙ СЛАБЫЙ прибор набора |

Пол прибора дрейфа: при тождественных плечах дрейф РОВНО 0; мутация в один
ulp сразу даёт ~1e-4 и дальше НЕ РАСТЁТ. Ниже 3e-4 отношения соседних
отметок -- шум, и проверка роста там отключена: до починки она давала
ложный красный.

**Гейт зелёный на всех четырёх размерах** (паритет, дрейф 1024, greedy 256).
KL с настоящим ядром на 0.4B: 2.0-3.5e-7 при пороге 1e-4. Эталон
квантования лежит в `/tmp/kl_ref_rwkv7-g1h-1p5b-ctx10240_eval_corpus_multiling_38x512_fp32.npy` (5.09 ГБ, `[38, 511, 65536]` fp32, сырые логиты);
прогон с ним дал KL квантования 0.030491 на двух ru-окнах против записанных
09.09 0.035992 по всем 38 -- прибор сходится.

### ГЛАВНОЕ ЧИСЛО: ЯДРО НЕ ДАЁТ НИЧЕГО

Замер A/B шестью плечами, чередованием в ОДНОМ процессе (закон 25), 1.5B
`compression_v2_cand`, медиана по 9 раундам,
`tests/_sess/bench_prewkv_ab.py`:

| плечо | мс | что показывает |
|---|---|---|
| заглушка (арифметики пред-WKV нет) | 13.033 | потолок |
| пустое ядро (та же геометрия, без математики) | 13.315 | **цена запуска +0.282** |
| обвязка (3 сложения + 2 sigmoid) | 13.146 | +0.113 |
| без ядра (цепочка MLX) | 13.607 | **статья = 0.574** |
| с ядром | 13.637 | -0.030 |
| ядро с sigmoid внутри | 13.613 | -0.006 |
| ядро с simd-суммой вместо последовательной | 13.587 | +0.020 |

Разброс раундов 13.34-14.48, то есть все дельты по ядру лежат ГЛУБОКО в
шуме: честная формулировка -- «разницы нет», а не «-0.030».

**Причина.** Из 0.574 мс статьи 0.282 -- цена самого запуска, 11.8 мкс на
слой, что сходится с записанными 13.1 мкс за последовательный запуск.
Потолок выигрыша, если бы арифметика в ядре была бесплатной, -- 0.292 мс, а
не 0.387. `mx.compile` УЖЕ сводит цепочку примерно к одному диспатчу на
слой, и custom-ядро меняет один запуск на другой. Ни перенос sigmoid внутрь,
ни замена последовательной суммы на simd-дерево картину не двигают -- значит
дело ни в обвязке, ни в критическом пути редукции.

Гипотеза «около пяти запусков на слой, которые можно свести к одному» на
КОМПИЛИРОВАННОМ пути не подтвердилась. Для «шапки слоя» (0.726 мс) структура
та же, но это надо МЕРИТЬ тем же способом (заглушка + пустое ядро) до
написания кода, а не переносить вывод.

Ядро оставлено в дереве при `FUSE_PREWKV = False`: оно корректно, гейт на нём
зелёный, и оно документирует измеренный тупик.

### БАТЧЕВЫЙ ДЕКОД, ПРЕФИЛЛ, СПЕКУЛЯТИВНЫЙ ДЕКОД

Всё компилированным путём (`m.step`). **`forward_stateful` напрямую -- это не
префилл модели, а его некомпилированный двойник, он медленнее на треть** (1038
против 801 мс на T=512); на эту яму легко сесть в новом харнессе, я и сел.

**Префилл 1.5B сейчас:** T=256 -- 493.2 мс (519.0 ток/с), T=512 -- 800.7 мс
(639.4), T=1024 -- 1430.8 мс (715.7). Против записанных 16.08 680 ток/с на
T=512 это на 6% ниже: новый пресет, тепло или регрессия -- НЕ ПРОВЕРЕНО,
нужен перемер на `compression_v1_ref` компилированным путём.

**Батч насыщается на 2x, и почти всё берётся уже к B=4:**

| B | мс/шаг | ток/с | к B=1 | на поток |
|---|---|---|---|---|
| 1 | 13.77 | 72.6 | x1.00 | 72.6 |
| 2 | 18.23 | 109.7 | x1.51 | 54.9 |
| 4 | 30.71 | 130.3 | x1.79 | 32.6 |
| 8 | 59.82 | 133.7 | x1.84 | 16.7 |
| 16 | 111.77 | 143.2 | x1.97 | **8.9** |

Память тут ни при чём: при B=16 веса читаются один раз, пол по памяти
8.89 мс, а шаг стоит 111.8. Доминирует работа, растущая с B. Найдено где:
при `N < GEMM_MIN_BATCH_NB = 128` батч режется чанками по `NB_CHUNK = 4`
(`quant_linear_gw.py`), и веса едут из DRAM `ceil(N/4)` раз.

**Расширение чанка НЕ РАБОТАЕТ** (числа во всех вариантах бит-в-бит,
`tests/_sess/bench_nb_chunk_batch.py`):

| | chunk 4 | 8 | 16 | 32 |
|---|---|---|---|---|
| B=8 | **61.2 мс** | 67.1 | 66.8 | 66.9 |
| B=16 | **111.7** | 140.1 | 240.6 | 239.9 |

Спилл регистров, ровно как записано свипом 19.07. Теперь известно, что
оптимум 4 держится и на батчевых формах, не только на префилльных. Плотный
путь (`N >= 128`) для декода не годится: он материализует fp16-веса целиком.

**Отсюда единственный измеренный рычаг:** тайловый GEMM с декодом в
threadgroup-памяти -- декодировать плитку весов один раз и переиспользовать
по всем строкам, не материализуя fp16. Он уже стоит в открытом вопросе 2 как
хвост префилла (последние 1.08-1.10x); теперь измерено, что ОН ЖЕ снимает
потолок батчевого декода. Одна кернельная работа закрывает обе оси.

**Опорное число:** префилл амортизирует вес в 8.6 раза на токен относительно
декода (1.56 против 13.4 мс/ток). Плотный путь работает как надо, провал
именно в промежуточном режиме `N` от 2 до 127, куда попадает весь батчевый
декод.

**По токенам одного потока параллелизации НЕТ** и быть не может: состояние
RWKV -- строгая последовательная зависимость, аналога KV-кэша здесь нет.

**Спекулятивный декод померен и не окупается** (`bench_short_forward.py`).
Форвард 1.5B: T=4 стоит 1.69 шага, T=8 -- 2.99, T=16 -- 6.01. Черновик 0.1B:
шаг 3.362 мс = 0.203 шага 1.5B. При k=4 цикл стоит 2.50 шага и даёт максимум
5 токенов: acceptance 0.9 даёт 1.64x, 0.7 -- 1.11x, 0.6 -- меньше единицы.
Между 0.1B и 1.5B согласия 0.9 ждать не стоит. Строить только после замера
РЕАЛЬНОГО acceptance, и не раньше.

### Методическое, дорого купленное

**Metal по умолчанию сворачивает `a*b+c` в `fma`, и это ломает бит-в-бит.**
Цепочка `k * (1 + (a-1)*k_a)` одним выражением совпадает с MLX на 49.7% и
врёт до 410 ulp; ЯВНАЯ `fma` даёт РОВНО ТО ЖЕ (свёртка подтверждена прямо, а
не по косвенным); та же цепочка, разложенная на именованные промежуточные, --
100.00% бит-в-бит. То же на `v`: 64.3% против 100%. В кернелях этого проекта
слитные выражения -- не стилистика, а поломка.

**Свой `sigmoid` MLX-овский НЕ воспроизводит.** Прямая формула,
`precise::exp` и `precise::divide` дают между собой одно и то же и совпадают
с MLX на 93.2% при <= 2 ulp; `fast::exp` -- 57.7% и 21 ulp; форма
`exp(x)/(1+exp(x))` -- 43.8%. Устойчивая по знаку форма расходится
катастрофически, но не по точности: при больших отрицательных `x` она даёт
РОВНЫЙ НОЛЬ, а MLX возвращает крошечное ненулевое. Полезный побочный вывод:
MLX считает прямой формой и хвост не обнуляет.

**`l2_norm`: последовательное накопление точнее дерева.** Дерево -- 51.9%
бит-в-бит и до 4 ulp, последовательный проход -- 84.9% и до 2 ulp, а
написанный по шагам (`s2 = s2 + sq` отдельным оператором вместо `s2 += t*t`)
-- БИТ-В-БИТ. На 64 элементах цена -- 64 итерации в лейне, и по замеру она
ничего не стоит.

**ulp мерить В ТИПЕ ЭТАЛОНА.** Первый заход мерил в fp16 при fp32-пути и
завышал совпадение: приведение к fp16 само стирает расхождения младших
разрядов. Линейка грубее измеряемого (закон 39).

**Плечо-заглушка загрязняется мёртвыми ветками.** Пустое ядро, которому не
подали `y_w` и `y_a`, сделало их мёртвыми, `mx.compile` выкинул ветки LoRA, и
плечо вышло на 0.322 мс БЫСТРЕЕ заглушки, которая не делает вообще ничего.
Любая заглушка обязана держать входы живыми -- `reshape` или ссылка в
выходе, -- иначе она меряет чужую статью.

**Ловушка `mx.compile` в A/B по флагу.** `step` трассирует ветку на момент
компиляции, и смена флага ПОСЛЕ ничего не меняет: померилось бы одно плечо
дважды. Лечится `del m._step_compiled` перед каждым замером, прогрев -- уже
после сброса.

**Параллельные прогоны на одном GPU смысла не имеют.** Три гейта разом идут
ровно как последовательные, а память уводит в своп. Отдельно: `screen -X quit`
НЕ убивает шелл скрипта -- он переживает и запускает следующий шаг, из-за чего
два процесса писали в один лог. Убивать по дереву и сверять `ps`.

### Мои ошибки этой сессии, для протокола

Заявил, что порог relmax 1e-6 лежит под решёткой типа и потому требует
бит-в-бит, -- исходя из допущения, что путь fp16. Путь **fp32**, замер
допущение не подтвердил, заявление снято целиком. Один раз влепил heredoc
вопреки закону 33. Загрязнил пустое плечо мёртвыми ветками -- ровно та
ловушка, от которой сам предостерёг в комментарии строкой выше. Промазал
глобом по эталону KL (`ref32_*` вместо `kl_ref_*`) и поспешно объявил его
потерянным; файл лежал на месте.

### Состояние на конец 11.09

rwkv-quant: HEAD `a508e26`. Дерево грязное ровно тем, что было грязным до
сессии: `NEXT_SESSION.md`, `tests/_sess/kl_real_path.py`,
`tests/trace_decode_steady.py` и семь проб 10.09 в `tests/_sess/`
(probe_chain_cost, probe_ckpt_inventory, probe_gemv_by_group,
probe_traffic_breakdown, probe_traffic_stability, probe_traffic_vars,
probe_wkv_cost). rwkv-metal: `0bdfbe7`, чисто, main на 14 впереди origin.
SwiftRWKV не трогался.

Новое в дереве (закоммичено): `rwkv_quant/backends/metal/fused_prewkv.py`
(эталон + три варианта ядра, два из них замерочные),
`tests/test_prewkv_parity.py`, `tests/_sess/probe_mlx_bitmatch.py`,
`tests/_sess/bench_prewkv_ab.py`, `tests/_sess/bench_batch_prefill.py`,
`tests/_sess/bench_short_forward.py`, `tests/_sess/bench_nb_chunk_batch.py`.
Логи: `~/Develop/WKV-kvant/prewkv_gate_*.log`, `bench_prewkv_ab_1p5b.log`,
`bench_prefill_compiled.log`, `bench_short_forward.log`, `bench_nb_chunk.log`,
`probe_mlx_bitmatch.log`.

### ПРИОРИТЕТЫ (11.09) -- действующий список

**ПРИОРИТЕТ 1 -- тайловый GEMM с декодом в threadgroup-памяти.** Единственный
рычаг, подтверждённый замером с двух сторон: он снимает потолок батчевого
декода (сейчас 2x, причина -- `ceil(N/4)` перечитываний весов при `N < 128`) и
закрывает последние 1.08-1.10x префилла, которые уже стояли открытым
вопросом 2. Цель ставить ЧИСЛОМ и мерить чередованием: батч B=8 и B=16 на
декоде плюс T=512 на префилле. Гейт -- до кернеля, по образцу
`test_prewkv_parity`, и с померенным мутацией разрешением; `nb`-кернель
обещает бит-в-бит по паре (строка, колонка) и это уже проверено на свипе
чанка, так что порог бит-в-бит здесь реалистичен.

**ПРИОРИТЕТ 2 -- перепроверить премиссу «шапки слоя» ДО написания ядра.** Статья 0.726 мс (резидуал + `layer_norm` + token-shift + шесть лерпов),
но после 11.09 её нельзя брать на веру: ставить заглушку и пустое ядро той же
геометрии и смотреть, сколько из статьи -- цена запуска. Если картина как у
пред-WKV, половину ПРИОРИТЕТА 1 от 10.09 закрывать замером, а не кодом.
Заодно учесть, что шапка нужна ДВАЖДЫ на слой (перед TMix шесть лерпов,
перед CMix один -- `QuantCMix.forward_stateful`), и второй вызов в статью
входит.

**ПРИОРИТЕТ 3 -- закрыть вопрос по префиллу 639 против записанных 680 ток/с.** Перемер на `compression_v1_ref` компилированным путём, минута работы.
Регрессия это или смена пресета -- сейчас неизвестно, а число опорное.

**ДОЛГИ, по убыванию цены.** README (строки compression для всех четырёх
размеров, файлы под перемер есть); ppl напрямую с записанного файла; четыре
дефекта приборов из раздела 10.09; долг 4 с 05.09; WKV-скан под префилл
(95 мс из 750, 12.7%, ни разу не смотрелся); переселение старших разделов
записки в `docs/LOG-*` (разделов накопилось шесть, правило из шапки
требует переезда при трёх-четырёх).

**НЕ ДЕЛАТЬ без нового замера:** ядра по образцу `fused_tail` на статьи,
которые `mx.compile` уже свёл к одному диспатчу; расширение `NB_CHUNK`;
спекулятивный декод с черновиком 0.1B.

## 12.09. ПРИОРИТЕТ 2 ЗАКРЫТ БЕЗ ЯДРА: СТАТЬЯ ШАПКИ СЛОЯ СИДЕЛА В РЕДУКЦИЯХ РУКОПИСНОЙ НОРМЫ, ШТАТНЫЙ `mx.fast.layer_norm` БЕРЁТ 87% ЕЁ; ПРИОРИТЕТ 3 ЗАКРЫТ -- СРАВНИВАЛИСЬ РАЗНЫЕ ПРЕСЕТЫ; ПЛЕЧО В fp32 НЕ ВОСПРОИЗВОДИТСЯ МЕЖДУ ПРОЦЕССАМИ

Сделано: закрыт приоритет 3 (чтением записей плюс три прогона), закрыт
приоритет 2 (замером ДО написания ядра), внесена правка `FAST_LN` под
флагом с умолчанием False, написан и померен мутацией гейт
`tests/test_prewkv_parity.py`-образца -- `tests/test_fast_ln_parity.py`,
прогнан на четырёх размерах. Закоммичено: `a1ef881` (долг 1 раздела
10.09 -- дерево было грязным с 10.09), `b8d4051` (правка и пробы 12.09).

### ПРИОРИТЕТ 3 ЗАКРЫТ: РЕГРЕССИИ НЕТ, СРАВНИВАЛИСЬ РАЗНЫЕ ПРЕСЕТЫ

Записанные 16.08 680 ток/с (753 мс на T=512) принадлежат **REDUCTION/sym**,
а не compression: в `docs/LOG-1008-1608.md` таблица декванта, на которой
они сняты, идёт по cmix/proj @6 и head @8. У compression за тот же период
записано 908.3 мс на T=512 (`bench_lora_quant_ab`, `PREFILL_T=512`, тоже
под `mx.compile`, FUSE=0) = 563 ток/с.

Прогоны 12.09, T=512, компилированный путь, B=1, своп 1079 -> 1079 МБ:

    compression_v1_ref   807.0 мс = 634.5 т/с ; T=256 503.4 ; T=1024 1469.2 (697.0) ; декод 13.428 мс
    compression_v2_cand  800.7 мс = 639.4 т/с (11.09, тот же прибор)
    reduction_1p5b_0709  670.1 мс = 764.1 т/с ; T=256 358.8 ; T=1024 1319.6 (776.0) ; декод 17.642 мс

То есть: v1 и v2 неразличимы (0.8% при записанном разбросе одного файла в
двух процессах 2.7%) -- перераспределение бит 09.09 префилл-нейтрально,
как и предсказывала арифметика для декода. Reduction сегодня 670.1 мс
против записанных 753 -- число воспроизводится на своём пресете с запасом.
Контроль прибора: декод compression 13.428 против опорных 13.4 мс.

НОВОЕ, ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ ЧЕРЕДОВАНИЕМ В ОДНОМ ПРОЦЕССЕ: compression
проигрывает reduction на префилле **1.20x** (807 против 670 мс) при том,
что читает в 1.5 раза МЕНЬШЕ байт (970 против 1435 МБ). Значит префилл
упирается не в трафик, а в цену декванта раскладки `asym sb6` против
`sym`. Это ровно та статья, куда бьёт приоритет 1. Снято в ДВУХ
процессах, поэтому как утверждение пока не годится (закон 1).

### ПРИОРИТЕТ 2: ЯДРО ШАПКИ СЛОЯ ПИСАТЬ НЕ НАДО

Разложение статьи 0.726 мс плечами-заглушками, держащими входы живыми
(`tests/_sess/bench_head_ab.py`, 1.5B, чередование в одном процессе,
7 раундов, своп 0):

    off (как сейчас)            13.662 мс
    нормы без mean/var (x*w+b)  13.096   +0.566
    норма снята целиком         13.220   +0.442
    token-shift снят            13.575   +0.087
    нормы + token-shift         13.043   +0.619

Статья почти вся в РЕДУКЦИЯХ рукописного `_layer_norm`, а не в цене
запусков и не в лерпах: аффинный хвост бесплатен (`lnaff` и `lnid`
неразличимы), token-shift 0.087, лерпы в остатке ~0.07.

Отсюда рычаг -- не кернель, а штатный `mx.fast.layer_norm`
(`bench_head_fast_ab.py`, `bench_lnfast_scale.py`):

    1.5B  декод 13.649 -> 13.218 мс  +0.431 (87% потолка 0.497), 73.3 -> 75.7 т/с
    0.1B  декод  2.911 ->  2.708 мс  +0.203 (+7.0%,  26 вызовов нормы)
    2.9B  декод 25.065 -> 24.168 мс  +0.897 (+3.6%,  66 вызовов)
    префилл 1.5B T=512 802.6 -> 791.8 (+1.3%), T=1024 1435.1 -> 1410.1 (+1.7%)
    префилл 0.1B +2.8%, 2.9B 1626.1 -> 1608.2 (+1.1%)

Декодные 0.431 мс воспроизведены в двух процессах до третьего знака.

ДВА ПРЕДСКАЗАНИЯ, ЗАРЕГИСТРИРОВАННЫХ ДО ЗАМЕРА, НЕ СБЫЛИСЬ. Первое:
потолок оценивался в 0.16 мс исходя из того, что статья вся -- цена
запусков (48 x 11.8 мкс из 0.726); механизм другой. Второе: для 2.9B
ждал 0.54 мс по постоянной цене на вызов, вышло 0.897 -- цена на вызов
растёт с D: 7.8 мкс при D=768, 8.6 при 2048, 13.6 при 2560.

ГДЕ ИМЕННО ВЫИГРЫШ В ТОЧНОСТИ. Из 52 вызовов нормы 50 идут в fp32 и
только 2 в fp16 (`ln0` после эмбеддинга). На fp32-вызовах наша норма и
примитив равны (5e-9...1e-7, кто ближе к fp64 -- по вызовам по-разному,
по медиане примитив чуть точнее), на двух fp16-вызовах примитив точнее
НАШЕГО В 6700 РАЗ (9.8e-10 против 6.6e-6): он поднимает редукцию в
широкий тип, наша считает в типе входа.

### ГЕЙТ `tests/test_fast_ln_parity.py`: ЧТО ПОКАЗАЛ

Пороги зарегистрированы в шапке файла до прогона. Плечи: `off`, `fast` и
ПОЛ СЕМЕЙСТВА -- однопроходная формула E[x^2]-E[x]^2 в том же типе.
Прогон с `RWKVQ_FAST_LN_ALIAS=1` зелёный, то есть гейт не красен сам по
себе. Разрешение, померено мутацией: пункт 1 ловит 1e-6 и зелен на 1e-7;
дрейф ловит 1e-3; KL ловит 1e-2 (2.248e-03 при пороге 1e-4).

    размер   relmax        дрейф 1024   пол      greedy 256           вердикт
    0.1B     0 нарушений   3.294e-04    4.15e-04 256/256              ЗЕЛЁНЫЙ
    0.4B     0 нарушений   3.098e-04    2.95e-04 256/256              ЗЕЛЁНЫЙ
    2.9B     0 нарушений   8.010e-04    3.96e-04 76/256, расход с 73  ЗЕЛЁНЫЙ
    1.5B     0 нарушений   3.294e-04    1.98e-04 256/256              КРАСНЫЙ (рост)

KL на 38 окнах всего корпуса, плечи чередуются потокенно:

    0.1B   KL(fast || off) 1.714e-06 [1.47e-06; 1.97e-06], 19418 токенов
    1.5B   KL(fast || off) 4.302e-07 [3.96e-07; 4.66e-07], худшее окно 7.293e-07
    1.5B   KL квантования 0.036034, ЗАПАС 83766x (требуется >= 100x)
    1.5B   ppl СПРАВОЧНО: off 8.4315, fast 8.4316
    1.5B   кто ближе к эталону fp32: off 0.036034 против fast 0.036035 -- off
           ближе на 1e-6, то есть на 0.003% величины

ЕДИНСТВЕННЫЙ КРАСНЫЙ ПУНКТ -- 1.5B, «рост дрейфа не быстрее линейного»:
отношение соседних отметок 4.13 при пороге 3.05. Разбор: всплеск
приходится на ОДНУ отметку 256 и есть у ОБОИХ плеч (fast 1.167e-03, пол
6.961e-04), а к 512 спадает. Сам дрейф на 1024 равен 3.294e-04 при
дрейфе на 32 в 2.299e-04, то есть за 32-кратный рост длины он вырос в
1.43 раза -- компаундирования, ради которого пункт и заводился, НЕТ.
Попарное отношение как тест тренда ломается об один всплеск; это дефект
формы теста, и переписывать его в третий раз я не стал -- решение за
владельцем.

### МЕТОДИЧЕСКОЕ, ДОРОЖЕ САМОЙ ПРАВКИ

ПЛЕЧО С ПЕРЕСЧЁТОМ В fp32 НЕ ВОСПРОИЗВОДИТСЯ МЕЖДУ ПРОЦЕССАМИ. Два
запуска ОДНОГО кода на 0.1B дали разные состояния WKV в 11 слоях из 12,
до 22% относительных; `off`, `fast` и однопроходное плечо бит-в-бит
равны себе и между процессами (сверено дампами состояний в npz).
Математически тождественный подсчёт в fp32 выбирает, судя по всему,
разную стратегию редукции по выравниванию буферов. Порог, меняющийся от
прогона к прогону, это не порог -- пол семейства перестроен на
однопроходную формулу. ВЫВОД ДЛЯ ПРОЕКТА: у идеи считать активации в
fp32 ради точности есть скрытая цена -- невоспроизводимость между
процессами. Проверять это ДО того, как опереться на fp32-плечо.

ПУНКТ «fast НЕ ДАЛЬШЕ ОТ fp64» СПЕЦИФИЦИРОВАН ДВАЖДЫ И ДВАЖДЫ НЕВЕРНО.
Сперва по синтетике в fp16 (где примитив точнее во всех формах), потом
ослаблен до пола семейства -- падал так же. Правильный вывод не
ослаблять дальше, а признать: на fp32-шуме ни одна из двух реализаций не
доминирует другую покомпонентно (медиана у примитива лучше, но на 13-20%
вызовов он хуже на ~6%), значит порога по этой величине НЕ СУЩЕСТВУЕТ.
Пункт понижен до диагностики, качество судит KL на уровне модели.

ДЕФЕКТ ГЕЙТА, НАЙДЕННЫЙ ПРОГОНОМ: правило про пол прибора дрейфа
(`DRIFT_FLOOR = 3e-4`, померено мутацией 11.09) было в
`test_prewkv_parity.py` и я его в новый гейт НЕ ПЕРЕНЁС -- отсюда часть
ложной красноты. Перенесено 12.09.

ТРЕНИРОВОЧНЫЙ ПУТЬ `rwkv-metal`: СТАТЬИ НЕТ, закрыто чтением. Все нормы
там -- штатные `nn.LayerNorm`, а он внутри зовёт `mx.fast.layer_norm`;
`ln_x` -- это `nn.GroupNorm`, и её `_pytorch_compatible_group_norm` тоже
идёт через `mx.fast.layer_norm` (плюс reshape и transpose). Рукописной
нормы через mean/var в `rwkv_metal` нет вовсе.

РУКОПИСНЫЙ `_group_norm` В `rwkv-quant` НЕ ЗОВЁТСЯ НИ НА ОДНОМ ПУТИ:
захват на префилле T=512 и на шаге декода дал 0 вызовов при
FUSE_TAIL=True -- всё берёт фьюзнутый хвост. Рычага там нет.

### СОСТОЯНИЕ

`rwkv-quant`: два коммита сверху (`b8d4051`), в дереве после них --
`test_fast_ln_parity.py` и правки его критериев. `FAST_LN=False` по
умолчанию, эталонная ветка вынесена в `_layer_norm_ref` текстуально без
изменений, поведение по умолчанию не тронуто ни на бит. `rwkv-metal` не
трогался, `SwiftRWKV` не трогался.

МАШИНА РЕБУТИЛАСЬ около 11:15 12.09, УЖЕ ПОСЛЕ последнего прогона, и
снесла `/tmp` вместе с эталоном квантования fp32 для 1.5B. Числа KL
успели сняться до ребута, а эталон ПЕРЕСОБРАН в тот же день и теперь
лежит НЕ в `/tmp`, а рядом с моделями:

    ~/Develop/WKV-kvant/kl_ref_rwkv7-g1h-1p5b-ctx10240_eval_corpus_multiling_38x512_fp32.npy

5 090 312 320 байт, столько же, сколько у снесённой версии; 38 окон по
511, fp32 на CPU, около пяти минут, своп не двинулся. Сверен НЕ размером,
а числами: KL квантования на окнах 0, 1, 2 равны 0.030161, 0.030820,
0.040689 -- совпадают с записанными до ребута до шестого знака, KL плеч
тоже (3.689e-07, 3.032e-07, 4.930e-07). Передаётся гейту через
`RWKVQ_GATE_QKL`, поэтому смена места ничего не ломает; имя оставлено
прежним, чтобы старое и новое не смешались молча (закон 30). Собирать
его В `/tmp` больше не надо -- это и был долг, и он закрыт.

ЭТАЛОНЫ СОБРАНЫ ДЛЯ ВСЕХ ЧЕТЫРЁХ РАЗМЕРОВ, все лежат в
~/Develop/WKV-kvant/, все по 5 090 312 320 байт (размер не зависит от
размера модели: 38 x 511 x 65536 x 4, поэтому размером тут НЕЧЕГО
проверять):

    kl_ref_rwkv7-g1d-0p1b_..._38x512_fp32.npy            ~1 мин, своп 0
    kl_ref_rwkv7-g1d-0p4b-ctx8192_..._38x512_fp32.npy    ~2.5 мин, своп 0
    kl_ref_rwkv7-g1h-1p5b-ctx10240_..._38x512_fp32.npy   ~5 мин, своп 0
    kl_ref_rwkv7-g1h-2p9b-ctx10240_..._38x512_fp32.npy   ~12 мин, своп +1962 МБ

Чекпоинты: 0.1B ~/Develop/WKV-kvant/rwkv7-g1d-0.1b.pth, 0.4B
/Users/s/rwkv7-g1d-0.4b-ctx8192.pth (лежит в корне домашней, не рядом с
остальными), 1.5B и 2.9B ~/Develop/{WKV-kvant/,}rwkv7-g1h-*-ctx10240.pth.
Формы всех сверены с манифестами .rwkvq ДО запуска (закон 15).

2.9B В fp32 НА CPU НЕ ВЛЕЗАЕТ БЕЗ СВОПА: ~11.8 ГБ под веса при 17 ГБ ОЗУ,
окна шли 8 с в начале и 20-25 с под свопом. Ставить его одним, ничего
рядом.

СВЕРКА ЧИСЛАМИ, А НЕ РАЗМЕРОМ (tests-скрипт остался в /tmp/chk_ref.py):
нулевое окно пересчитано неквантованной моделью тем же путём и сравнено с
записанной строкой -- БИТ-В-БИТ на всех трёх новых эталонах (argmax 511 из
511). Для 1.5B сверка шла иначе и тоже сошлась: KL квантования на окнах 0,
1, 2 совпал с числами, снятыми ДО ребута, до шестого знака.

ЗАПАС ПРОТИВ KL КВАНТОВАНИЯ ТЕПЕРЬ ИЗМЕРЕН НА ВСЕХ РАЗМЕРАХ (3 окна на
размер, окна русские, цифра индикативная):

    0.1B  KL кванта 0.105277, запас 52318x, ppl 11.6955 -> 11.6952, fast БЛИЖЕ
    0.4B  KL кванта 0.054408, запас 79329x, ppl  8.7650 ->  8.7648, fast БЛИЖЕ
    1.5B  KL кванта 0.033890, запас 87261x, ppl  6.7759 ->  6.7761, off ближе
    2.9B  KL кванта 0.045072, запас 66549x, ppl  6.3151 ->  6.3150, off ближе

ТО ЕСТЬ ЗНАК РАЗНИЦЫ КАЧЕСТВА МЕНЯЕТСЯ ПО РАЗМЕРАМ и по величине равен
1.9-4.8e-05 при KL квантования 0.034-0.105 -- это 0.02-0.05% от того, что
уже платит сам пресет. На малых размерах fast ближе к эталону, на больших
дальше, и правдоподобное объяснение -- два fp16-вызова ln0, где примитив
точнее нашего в 6700 раз: у 0.1B они составляют 2 вызова из 26, у 2.9B --
2 из 66. Это ГИПОТЕЗА, замером она не подтверждена.

### РЕШЕНИЕ, КОТОРОЕ ЖДЁТ ВЛАДЕЛЬЦА

Включать ли `FAST_LN` по умолчанию. За: +3.2% декода на 1.5B, +3.6% на
2.9B, +7.0% на 0.1B, префилл +1.1...2.8%, KL между плечами 4.3e-07 при
запасе 83766x против KL квантования, ppl 8.4315 -> 8.4316, дрейф в
пороге на всех размерах. Против: числа перестают быть бит-в-бит
сравнимыми с записанными (все KL и ppl проекта сдвинутся в четвёртом
знаке), на 2.9B greedy расходится с 73-го токена, на 2.9B дрейф 8.0e-04
при пороге 1e-3 -- запас всего 1.25x, и на 1.5B красен пункт про рост
дрейфа (разбор выше: тест ломается об один всплеск, компаундирования нет).


### ПРИОРИТЕТ 1 (единственный) -- ТАЙЛОВЫЙ GEMM С ДЕКОДОМ В threadgroup-ПАМЯТИ

Переносится из 11.09 неизменным и остаётся единственным рычагом,
подтверждённым замером с двух сторон: снимает потолок батчевого декода
(причина -- ceil(N/4) перечитываний весов при N < GEMM_MIN_BATCH_NB=128,
чанки по NB_CHUNK=4) и закрывает последние 1.08-1.10x префилла из
открытого вопроса 2. Цель ставить ЧИСЛОМ. ГЕЙТ ПИШЕТСЯ ПЕРВЫМ, до
кернеля, по образцу tests/test_prewkv_parity.py, и его разрешение
МЕРИТСЯ МУТАЦИЕЙ (закон 37). Порог бит-в-бит здесь реалистичен:
nb-кернель обещает бит-в-бит по паре (строка, колонка), и на свипе чанка
это подтвердилось. Мерить только чередованием плеч в ОДНОМ процессе
(закон 25), для A/B по флагу обязателен del m._step_compiled перед
каждым замером.

> **ВЫПОЛНЕНО 13.09.** Подтверждено чередованием в ОДНОМ процессе: 1.19-1.21x, разброс 1.2-2.2%, своп 0. Разбор, цель числом и переоценка механизма -- в разделе 13.09.

ПЕРВЫМ ЗАМЕРОМ, ДО КОДА: подтвердить чередованием в одном процессе
находку 12.09 -- compression проигрывает reduction на префилле 1.20x
(807.0 против 670.1 мс на T=512) при в 1.5 раза МЕНЬШЕМ трафике (970
против 1435 МБ). Снято в двух процессах, поэтому утверждением пока не
является (закон 1). От результата зависит, куда бить: если 1.20x
подтвердится, префилл упирается в цену декванта раскладки asym sb6, а не
в перечитывания весов, и тайл надо проектировать вокруг декванта.
Риск замера: два файла в одном процессе -- это ~2.8 ГБ резидентно, 10.09
записано, что чередование двух моделей на этой машине само портит замер
(разброс 18-29% на декоде). Мерить префилл, своп дельтой, и если разброс
уедет -- считать вопрос открытым, а не подгонять.

### ДОЛГИ, по убыванию цены

1. README: строки compression для всех четырёх размеров.
2. ppl напрямую с записанного файла.
3. Четыре дефекта приборов из раздела 10.09.
4. Долг 4 с 05.09.
5. WKV-скан под префилл: 95 мс из 750, ни разу не смотрелся.
6. Полный прогон KL на 38 окнах для 0.1B, 0.4B, 2.9B. Эталоны собраны
   12.09, но запас против KL квантования на них померен по ТРЁМ окнам, а
   окна 0-2 в корпусе русские. На 1.5B три окна дали 87261x против
   83766x на всём корпусе, то есть для этой величины трёх окон
   достаточно -- долг дешёвый и низкоприоритетный.
7. Гипотеза про смену знака разницы качества по размерам (на 0.1B и 0.4B
   fast ближе к эталону, на 1.5B и 2.9B дальше): объяснение через долю
   двух fp16-вызовов ln0 замером НЕ подтверждено. Проверяется за один
   прогон -- поднять ln0 в fp32 на 2.9B и посмотреть, уйдёт ли знак.
8. Красный пункт гейта FAST_LN на 1.5B (рост дрейфа, попарное отношение
   ломается об один всплеск на отметке 256). Форму теста надо либо
   заменить на сквозное отношение дрейф(1024)/дрейф(32), либо признать
   пункт диагностикой. Я менял критерии этого гейта дважды за 12.09 и
   третий раз не стал -- решение за владельцем.

### НЕ ДЕЛАТЬ без нового замера

Ядра по образцу fused_tail на статьи, которые mx.compile уже свёл к
одному диспатчу. Расширение NB_CHUNK. Спекулятивный декод с черновиком
0.1B. FUSE_PREWKV оставить False. Ядро «шапки слоя» -- закрыто 12.09,
87% статьи взял штатный примитив. Искать статью нормы в тренировочном
пути rwkv-metal -- там всё уже на mx.fast.layer_norm. Собирать эталоны
KL в /tmp -- ребут их сносит, класть рядом с моделями.

## 13.09. ПЕРВЫЙ ЗАМЕР ПРИОРИТЕТА 1 ПОДТВЕРЖДЁН ЧЕРЕДОВАНИЕМ В ОДНОМ ПРОЦЕССЕ (1.19-1.21x); ЦЕЛЬ ПОСТАВЛЕНА ЧИСЛОМ -- СТАТЬЯ ДЕКВАНТА НА ПРЕФИЛЛЕ 166-179 МС = 21-23%; ПРЕФИЛЛ НЕ УПИРАЕТСЯ В ТРАФИК ВООБЩЕ

Коммиты: `fcaf977` (гейт tiled GEMM написан ДО кернеля плюс первый замер),
плюс коммит этой сессии с прибором разложения.

### ПЕРВЫЙ ЗАМЕР: 1.20x ПОДТВЕРЖДЁН, ТЕПЕРЬ ЭТО УТВЕРЖДЕНИЕ

`tests/_sess/bench_prefill_presets_ab.py`, оба файла в ОДНОМ процессе
(2.4 ГБ резидентно), плечи чередуются по раундам, 7 раундов, T=512,
своп 507.8 МБ до и после (дельта +0.0). Лог
`~/Develop/WKV-kvant/bench_prefill_presets_ab_1309.log`:

    ФАЗА A  compression fast 807.4 мс / reduction off 677.6 -- 1.192x, разброс обоих плеч 1.8%
    ФАЗА B  compression off  819.4 мс / reduction off 676.9 -- 1.210x, разброс 1.2-1.4%
    ФАЗА A2 compression fast 810.1 мс / reduction off 676.6 -- 1.197x, разброс 1.4-2.2%

Риск, записанный 12.09 (чередование двух моделей само портит замер,
разброс 18-29% на декоде), НЕ реализовался: на префилле разброс 1.2-2.2%.
Отпечатки логитов: у compression fast и off различаются (-207915.56
против -207463.44) -- значит переключение флага состоялось И на модели,
И на блоках; у reduction между фазами совпадают до бита -- значит второе
плечо не поехало. Контроль прибора: 807.4 против записанных 12.09 в двух
процессах 807.0, reduction 677.6 против 670.1.

ВЫВОД, теперь утверждение (закон 1 выполнен): compression проигрывает
reduction на префилле 1.19-1.21x, читая в 1.5 раза МЕНЬШЕ байт (970
против 1435 МБ). Префилл упирается не в трафик, а в цену декванта
раскладки asym sb6. Тайл проектировать вокруг декванта.

### ГДЕ ИМЕННО СИДИТ ДЕКВАНТ (закрыто чтением)

`GwQuantLinear.__call__` при N >= `GEMM_MIN_BATCH_NB`=128 идёт ПЛОТНЫМ
путём: `_dequant_w()` материализует ВЕСЬ тензор в fp16, а внутри себя
держит ещё несколько полноразмерных временных (`concatenate` кодов,
битпланы `qh`/`qh2` развёрнутые в [OUT, IN], `q*s+m`), и только потом
`mx.matmul`. `GwQuantLinearFused` -- ТОЛЬКО декодный путь (B*T=1), на
префилле не участвует, патчить надо `GwQuantLinear._dequant_w`.

### ЦЕЛЬ ЧИСЛОМ: 166-179 МС ИЗ 777

`tests/_sess/bench_prefill_dequant_ab.py`, compression 1.5B, T=512, 7
раундов на фазу, лог `~/Develop/WKV-kvant/bench_prefill_dequant_ab_1309.log`.
Плечо B подменяет `_dequant_w` возвратом ОДИН РАЗ посчитанного тензора --
та же форма, тот же dtype, та же математика (закон 34), поэтому отпечатки
логитов обязаны совпасть бит-в-бит и совпали во всех трёх фазах.

    A  как есть              776.9 мс = 659.0 т/с, разброс 0.2%
    B  деквант кеширован     610.6 мс = 838.5 т/с, разброс 0.5%
    A2 как есть (повтор)     789.6 мс = 648.4 т/с, разброс 1.1%, дрейф A->A2 +1.6%

СТАТЬЯ ДЕКВАНТА = 166.3 мс против A и 179.0 против A2, то есть 21-23%
префилла. Кеш 144 тензора, 2415.9 МБ, своп за весь прогон +0.0.

ЦЕЛЬ ПРИОРИТЕТА 1 НА ПРЕФИЛЛЕ, ЧИСЛОМ: 1.5B T=512 777 -> 611 мс
(659 -> 838 т/с). Это ПОТОЛОК, а не обещание: 611 снято при уже
деквантованных весах, то есть тайловое ядро обязано взять эту планку,
ДЕЛАЯ деквант внутри себя.

ПОБОЧНО: убрать один только деквант уже уводит compression НИЖЕ
reduction (610.6 против 670.1), то есть диагноз 12.09 подтверждён и
количественно, а не только по знаку.

### ЧТО ЭТО МЕНЯЕТ В ПРОЕКТИРОВАНИИ ТАЙЛА (риск регистрируется ДО кода)

Плечо B читает деквантованные веса в fp16 -- 2415.9 МБ за один проход
префилла. При измеренной полосе 104 ГБ/с (закон 12) это 23 мс из 611.
Значит ПРЕФИЛЛ НЕ УПИРАЕТСЯ В ПОЛОСУ ВООБЩЕ, он упирается в счёт: 144
тензора, 1.21e9 весов, 2*512*1.21e9 = 1.24 ТФЛОП за проход, то есть
2.0 ТФЛОП/с на плече B.

СЛЕДСТВИЕ, РОВНО ОБРАТНОЕ ИСХОДНОЙ МОТИВИРОВКЕ ПРИОРИТЕТА 1. Выигрыш на
префилле НЕ в том, что тайл читает 0.625 байта на вес вместо 2 (эта
экономия стоит 23 мс и не окупает ничего), а ТОЛЬКО в том, что он не
материализует деквантованный тензор. Отсюда жёсткое условие приёмки:
ядро обязано выйти на пропускную способность `mx.matmul` на тех же
формах, ДОПОЛНИТЕЛЬНО выполняя деквант в threadgroup-памяти. Если оно
проиграет штатному matmul больше 21%, приоритет 1 даёт на префилле НОЛЬ
или минус -- при том, что ядро уже написано. Померить пропускную
способность `mx.matmul` на реальных формах НАДО ДО написания ядра: это
следующий шаг, и он дешевле самого ядра на порядок.

ОГОВОРКА ОБ ОБЛАСТИ ДЕЙСТВИЯ: всё вышесказанное про ПРЕФИЛЛ. Вторая
половина приоритета 1 -- потолок батчевого декода (ceil(N/4)
перечитываний весов при N < 128, чанки по NB_CHUNK=4) -- этим замером НЕ
затронута, там трафик как раз и есть узкое место. Два обоснования
приоритета 1 держатся на РАЗНЫХ механизмах, и замер этой сессии
подтверждает только одно из них.

### ЛОВУШКА, СТОИВШАЯ ОДНОГО ПРОГОНА: mx.compile ВМОРАЖИВАЕТ ВЕТКУ

Первая редакция прибора переключала питоновский флаг ПОРАУНДОВО, как
требует закон 1. Флаг не дошёл: `m.step` -- это
`mx.compile(forward_stateful)`, ветка вморожена в трассировку первого
вызова, и оба плеча мерили ОДИН И ТОТ ЖЕ граф. Напечаталось
правдоподобное «статья декванта -0.3 мс», то есть прибор молча ответил
«эффекта нет» на вопрос, которого не задавал. Поймано не глазами, а тем,
что прибор печатает размер кеша: 0 тензоров при 144 ожидаемых. В новой
редакции стоит `assert len(_CACHE) > 0`.

ПРАКТИКА: A/B питоновским флагом на пути под `mx.compile` требует ФАЗ
(del `_step_compiled` плюс свежий прогрев на плечо), а пораундовое
чередование там физически невозможно. Заглушка обязана печатать
доказательство, что она исполнилась, -- иначе гейт закона 1 сам себе
рисует зелёный. Это закон 31 в новой форме: плечо, не прошедшее по своей
ветке, даёт не ошибку, а правдоподобный ноль.

ПОБОЧНАЯ ПОЛЬЗА: тот ложный прогон померил СОБСТВЕННЫЙ ШУМ прибора на
разности двух одинаковых плеч -- медиана -0.3 мс, разброс -3.9 до +8.7
на 786. Статья 166 мс превышает шум в 20-40 раз.

### СОСТОЯНИЕ НА КОНЕЦ 13.09

`rwkv-quant`: дерево ЧИСТОЕ. `rwkv-metal` и `SwiftRWKV` не трогались.
`FAST_LN` как решено 12.09: True у compression пресетом, False у
reduction. Гейт `tests/test_gw_tiled_parity.py` на месте и тривиально
зелён (оба плеча -- один плотный путь), `gw_linear_tiled.py` -- пока
псевдоним эталона, НАСТОЯЩЕГО ЯДРА НЕТ.

## 15.09. ТРИ ВАРИАНТА ЦЕПОЧКИ ДЕКВАНТА СНИМАЮТ 25% СТАТЬИ; ГИПОТЕЗА «mx.compile НЕ СЛИВАЕТ» ОПРОВЕРГНУТА МУТАЦИЕЙ, ОСТАТОК 131.6 МС НЕ ОБЪЯСНЁН; `mx.matmul` НА РЕАЛЬНЫХ ФОРМАХ БЕРЁТ 93% ПОТОЛКА МАШИНЫ

Приборы: `bench_dequant_cost.py`, `bench_prefill_deqvar_ab.py`,
`bench_deq_fusion_probe.py` (и probe2), плюс два дублирующих --
`bench_dequant_fusion_ab.py`, `bench_dequant_reform_ab.py`. Логи
одноимённые с суффиксом `_1509` в `~/Develop/WKV-kvant/`.

### ПОЛ И ПОТОЛОК ИЗМЕРЕНЫ

145 тензоров, 843.1 МБ квантованных байт, 2684.4 МБ в fp16, 1.374 ТФЛОП
при N=512. Опорное чтение 2684.4 МБ: 24.87 мс = 107.9 ГБ/с (закон 12
ждал около 104, сходится).

    ПОЛ ТРАФИКА ДЕКВАНТА (843.1 чтение + 2684.4 запись) / 107.9 = 32.7 мс
    деквант eager по всему стеку   418.9 мс = 12.82x пола
    matmul реальные формы          539.3 мс = 2.55 ТФЛОП/с
    matmul квадрат 4096            50.32 мс = 2.73 ТФЛОП/с -- потолок машины

РЕАЛЬНЫЕ ФОРМЫ БЕРУТ 93% ПОТОЛКА fp16-GEMM. Это закрывает вопрос,
оставленный 13.09: догонять `mx.matmul` тайловому ядру придётся на 93%
потолка, а отбирать у него нечего.

НАПРАВЛЕНИЕ ВРАНЬЯ ИНСТРУМЕНТА ОКАЗАЛОСЬ ОБРАТНЫМ ОЖИДАЕМОМУ. Докстринг
прибора ждал ЗАНИЖЕНИЯ (144 независимые порции GPU перекрывает), а
микрозамер дал 418.9 против сквозной статьи 166.3, то есть ЗАВЫШАЕТ
в 2.5 раза. Закон 29 в третий раз: направление вранья надо мерить
сверкой со сквозным, а не выводить из устройства прибора.

### ТРИ ВАРИАНТА ЦЕПОЧКИ, СКВОЗНОЙ ЗАМЕР (АРБИТР)

Диагноз ставился чтением: (1) `self.codes`/`qh`/`qh2` в режиме K3 --
ленивые view, каждый делает reshape НЕсмежного срезa интерлива `qblk`
(шаг 16+4*xbits), а такой reshape обязан копировать; (2)
`mx.concatenate` нибблов стоит в НАЧАЛЕ цепочки, поэтому полный размер
материализуется до применения битпланов и масштаба.

    v2 -- concatenate переезжает в КОНЕЦ, каждая половина считается до готового w
    v3 -- view не используются вовсе, срезы qblk читаются на месте, а вместо
          склейки половин удваиваются КОДЫ (uint8, вдвое дешевле fp16)

Гейт бит-в-бит по представлению uint16 на ВСЕХ 145 тензорах (xbits 0:
120, xbits 1: 25), расхождений 0. Пять фаз, T=512, 7 раундов, отпечатки
логитов совпали во всех пяти, своп +0.0:

    v1 (как есть)          787.7 мс = 650.0 т/с
    v2 (склейка в конце)   757.9 мс = 675.6 т/с  -- снимает 29.8 мс = 17% статьи
    v3 (без view)          742.7 мс = 689.4 т/с  -- снимает 45.0 мс = 25% статьи
    cache (деквант снят)   611.1 мс = 837.8 т/с  -- пол, воспроизводит 610.6 от 13.09
    v1 повтор (дрейф)      791.4 мс, дрейф +3.7 мс

Статья при v1 = 176.5 мс, остаток после v3 = 131.6 мс. ДВА ПРЕДСКАЗАНИЯ
СБЫЛИСЬ: v3 дешевле v2 (несёт 4.0 ГБ записи против 5.4) и ни один
вариант не достигает пола 32.7 мс.

### ГИПОТЕЗА ПРО СЛИЯНИЕ ОПРОВЕРГНУТА МУТАЦИЕЙ

Предсказание, зарегистрированное ДО замера: если `mx.compile` цепочку НЕ
сливает, каждая ХОЛОСТАЯ полноразмерная поэлементная операция, вставленная
в цепочку, обязана стоить около +50 мс (чтение+запись 2.68+2.68 ГБ).

    проба 1, q + 0.0                  -1.2 мс (0->1), +2.2 (1->2)
    проба 2, maximum(q, -65504)       +9.5 мс (0->1), -0.1 (1->2), дрейф +10.7

Проба 1 слаба сама по себе (`q+0.0` сворачивается константным
фолдингом), поэтому проба 2 взяла тождество, которое фолдингом НЕ
сворачивается: `maximum(q, -65504)` тождественно на диапазоне кодов
0..63. Отпечатки логитов во всех фазах совпали, то есть мутация
действительно тождественна.

ВЫВОД: `mx.compile` ЦЕПОЧКУ СЛИВАЕТ, добавочные полноразмерные
поэлементные проходы внутри графа стоят НОЛЬ в пределах дрейфа. Значит
остаток 131.6 мс НЕ есть трафик лишних поэлементных проходов, а счёт
«12.82x пола = 16.5 проходов» описывает EAGER-путь, а не тот, что
исполняется на префилле.

ОСТАТОК 131.6 МС НЕ ОБЪЯСНЁН. Кандидаты, ни один не проверен:
`concatenate` и reshape после broadcast (не поэлементные, слиянию не
поддаются), чтение `qblk` с шагом 16 из 20 байт (эффективная полоса при
таком шаге не мерена ни разу), собственно счёт распаковки. Разделять их
надо мутацией ВНУТРИ графа, микрозамер снаружи тут уже соврал дважды.

### ДУБЛЬ И НЕГОДНАЯ ЛИНЕЙКА (отрицательный результат, не удалять)

`bench_dequant_fusion_ab.py` и `bench_dequant_reform_ab.py` сделаны
ПОЗЖЕ приборов выше и в незнании о них -- контекст сессии был потерян, а
каталог `tests/_sess` и свежие логи перед началом не смотрелись. Что они
дали:

    единица = один полноразмерный проход fp16 (mx.abs на деквантованном
      тензоре, отдельным вызовом): 82.4-82.6 мс, воспроизведено в двух
      процессах до 0.2%
    eager 363.0 мс = 4.39 ед ; compiled 218.2 мс = 2.64 ед ; неизбежное 0.66 ед
    xbits 0 (120 тензоров) 2.23 ед против xbits 1 (24) 3.66 ед
    перепись (срезы qblk плюс склейка в конце): гейт бит-в-бит зелёный на
      144 тензорах, снимает 17% МИКРОзамера

ЛИНЕЙКА НЕГОДНА ДЛЯ ВНУТРИГРАФОВОЙ ЦЕНЫ, И ВЫВОД ПО НЕЙ СНЯТ. Единица
меряет отдельный вызов `mx.abs` со своим `eval`, а проба мутацией
показала, что ВНУТРИ слитого графа такой же проход стоит ноль. Поэтому
заключение «compiled = 2.64 прохода, значит лишних около двух, и их
снимет один кернель» НЕВЕРНО и не должно переоткрываться. Отношение
eager/compiled = 0.601 остаётся годным (оба плеча мерены одинаково), а
абсолютные единицы -- нет.

ПЕРЕПИСЬ НОВОГО НЕ ДАЛА: по устройству это v2 плюс часть v3 (срезы qblk),
и v3 её обгоняет, будучи померенным СКВОЗНЫМ замером. В код не идёт.

УРОК ПРОЦЕССА, дешёвый и обидный: перед постановкой нового прибора
смотреть `mtime` в `tests/_sess` и свежие логи в `WKV-kvant`. Дубль
стоил двух прогонов и одного неверного вывода, попавшего в записку до
сверки (поймано при подготовке коммита, в записку в таком виде не ушло).

### ЧТО ЭТО ЗНАЧИТ ДЛЯ ПРИОРИТЕТА 1

1. На префилле `mx.matmul` берёт 93% потолка машины. У тайлового GEMM на
   умножении отбирать нечего, а обязанность выйти на 2.55 ТФЛОП/с,
   ДОПОЛНИТЕЛЬНО декодируя, остаётся. Как ПРЕФИЛЛЬНЫЙ рычаг приоритет 1
   в прежней формулировке закрыт замером.
2. Потолок снятия статьи известен и воспроизведён дважды: 611 мс. v3
   берёт 45.0 мс из 176.5, то есть четверть пути, ничего не ломая и
   оставаясь бит-в-бит.
3. НЕ ДОКАЗАНО, что один кернель декванта возьмёт остаток 131.6 мс. Это
   верно, только если остаток есть доступ или трафик; если счёт -- ядро
   даст мало. Сначала разделить остаток, потом решать про ядро.
4. Вторая половина приоритета 1 -- потолок батчевого декода (ceil(N/4)
   перечитываний весов при N < 128) -- НИ ОДНИМ из замеров 13-15.09 не
   затронута. Там узкое место действительно трафик, и там тайл остаётся
   единственным известным рычагом.

### БАТЧЕВЫЙ ПРЕФИЛЛ T=4096: БАТЧ ПОКУПАЕТ 4% И СТОИТ ВСЮ ПАМЯТЬ

Раньше не мерено ни разу: 11.09 батч гонялся только на ДЕКОДЕ, префилл
только при B=1 и до T=1024. T=4096 идёт ОДНИМ вызовом, last_only=True.
Приборы: `bench_prefill_batch_mem.py` (память, ОДИН батч на процесс,
пик из `/usr/bin/time -l`) и `bench_prefill_batch_4k.py` (скорость,
чередование в одном процессе).

СЧЁТЧИКИ ПАМЯТИ MLX НЕ ИСПОЛЬЗУЮТСЯ: врут, решение владельца 15.09.
Память мерится тем, что видит система -- пик footprint (закон 22), своп
и Swapouts из vm_stat дельтой (закон 11). Накопительный Swapouts важен
отдельно: занятый своп может не двигаться при активном обмене.

    B | пик footprint | своп за процесс | swapout    | ток/с | годность
    1 |      5.11 ГиБ |        +0.0 МБ  |          0 | 689.7 | годен
    2 |      7.78 ГиБ |        +0.0 МБ  |          0 | 803.8 | годен
    4 |     11.73 ГиБ |     +2053.7 МБ  | 256908 стр | 743.8 | НЕДЕЙСТВИТЕЛЕН
    8 |     16.90 ГиБ |     +5470.4 МБ  | 919424 стр | 621.0 | НЕДЕЙСТВИТЕЛЕН

Скорость, чередованием B=1 и B=2 в одном процессе (арбитр, закон 1),
3 раунда, своп +0, swapout +0, дрейф плеча B=1 за прогон -0.9%:

    B=1   5293.1 мс, разброс 1.1% = 773.8 ток/с
    B=2  10143.1 мс, разброс 2.1% = 807.6 ток/с всего, 403.8 на поток, x1.04

ЗАКОН 1 ПРОДЕМОНСТРИРОВАН ПОПЕРЕЧНОЙ СВЕРКОЙ: B=1 по отдельному процессу
689.7 ток/с против 773.8 чередованием -- 12% расхождения МЕЖДУ процессами
при разбросе ВНУТРИ прогона 1.1%. Абсолюты из однобатчевых процессов
сняты ради памяти и для сравнения скорости негодны.

ВЫВОДЫ.

1. БАТЧ НА ПРЕФИЛЛЕ НЕ ПОКУПАЕТ НИЧЕГО: +4% пропускной способности и
   вдвое срезанный поток. Согласуется с замером 15.09: при B=1 GEMM уже
   идёт на 93% потолка fp16, свободного счёта для батча нет. На декоде
   батч давал 2x, потому что там амортизируется трафик весов; на
   префилле амортизировать нечего.
2. ДЛИНА ВЫГОДНЕЕ БАТЧА: при B=1 647 ток/с на T=512, 726 на T=1024,
   774 на T=4096.
3. ОГРАНИЧЕНИЕ -- ПАМЯТЬ, А НЕ СКОРОСТЬ. На 16 ГБ практический предел для
   T=4096 это B=2. B=4 требует 11.73 ГиБ и на живой машине свопит,
   B=8 хочет 16.90 ГиБ и вытолкнул в своп 15 ГБ.
4. Постоянная часть пика около 3.4 ГиБ: подгонка по краям даёт
   3.43 + 1.68*B ГиБ, но середина лежит выше неё на 1-1.5 ГиБ, то есть
   линейной модель НЕ является и подгонкой пользоваться нельзя. Файл
   весит 0.97 ГБ, значит порядка 2.4 ГиБ -- транзиенты и активации, от
   батча не зависящие.
   ГИПОТЕЗА, НЕ ПРОВЕРЕНА: это одновременно живущие деквантованные
   fp16-веса. Если так, кернель декванта срезал бы не только 131.6 мс,
   но и ПИК, а пик решает, влезет ли B=4. Проверяется плечом с
   кешированным деквантом: если пик с кешем не вырос, транзиенты уже
   были такого размера.

### ГДЕ ЛЕЖАТ 131.6 МС: ВСЁ ЭТО ТРАФИК, И СЧИТАЕТСЯ ОН ПО 107 ГБ/с

`bench_prefill_deqstruct_ab.py`, лог `bench_prefill_deqstruct_ab_1509.log`.
Шесть фаз, T=512, 7 раундов, своп +0.0, дрейф v1 +2.2 мс, разбросы
0.4-0.9%. ВСЕ ШЕСТЬ ФАЗ ДАЛИ ОДИН ОТПЕЧАТОК ЛОГИТОВ: плечи не просто той
же формы и типа, а тождественны ПО ЗНАЧЕНИЯМ (закон 34 в сильной форме),
потому что построены как тождества над кешированными весами.

    cache (пол, деквант снят)       608.5 мс
    cache1 = maximum(W, -65504)     653.8  -- одна материализация +45.2 мс
    cachecat = половины + склейка   708.5  -- склейка +54.8 мс сверх
    v3                              750.8  -- статья 142.3 мс
    v1                              785.8  -- статья 177.2 мс

ПРЕДСКАЗАНИЕ СБЫЛОСЬ ТОЧНО, впервые за 15.09: ждали +45.0 мс на
материализацию по трафиковой модели (2.42 ГБ записи + 2.42 чтения при
107.9 ГБ/с), вышло +45.2. Обратный счёт даёт 4.84 ГБ / 45.2 мс =
**107.1 ГБ/с** -- ровно независимо измеренная полоса.

> **УТОЧНЕНО 16.09.** Тождество времени и трафика подтверждено только для поэлементной копии fp16 (100 ГБ/с). Кернели распаковки идут на 48.8 ГБ/с, поэтому пол средствами MLX равен 74 мс, а не 30. Разбор -- лестница вычитанием в разделе 16.09.

ВЫВОД, РЕШАЮЩИЙ ДЛЯ ПРИОРИТЕТА 1: внутри графа ВРЕМЯ ТОЖДЕСТВЕННО
ТРАФИКУ. Ни счёт распаковки, ни цена запусков, ни питон в статье не
участвуют -- и это согласуется с пробой мутацией (операция, слитая в
существующий кернель, не добавляет трафика и потому бесплатна), и с тем,
что питон вообще сокращается в разности двух скомпилированных плеч.
Значит статья равна ЧИСЛУ МАТЕРИАЛИЗАЦИЙ, и ничему другому.

`concatenate` -- ПОЛНЫЙ БАРЬЕР, а не запись срезов из слитых
производителей: cachecat сверх cache1 стоит +54.8 мс при предсказанных
+45 (читает половины и пишет выход заново). Второе предсказание сбылось
качественно, по величине мимо на 22%.

НЕИЗБЕЖНОЕ ДЛЯ ЛЮБОГО ПОДХОДА, отдающего `mx.matmul` готовый fp16-тензор:
чтение квантованных 0.84 ГБ плюс запись w 2.42 ГБ = 3.26 ГБ = около 30 мс.
Отсюда ЦЕЛЬ ОДНОПРОХОДНОГО КЕРНЕЛЯ ДЕКВАНТА, ЧИСЛОМ: статья 142.3 -> 30
мс, префилл 750.8 -> около 639 мс (+17% к v3, +23% к v1). Теперь это
опирается на калиброванную модель со сбывшимся предсказанием, а не на
снятое рассуждение про единицы прохода.

ЧТО ОСТАЛОСЬ НЕИЗВЕСТНЫМ: СКОЛЬКО ИМЕННО материализаций делает v3. Ручной
счёт трафика даёт около 64 мс (удвоение кодов, чтение blk дважды, запись
w, развёртка битплана на 25 тензорах), а измерено 142.3 -- то есть
материализаций примерно вдвое больше, чем видно из текста выражения.
Самый вероятный невидимый кандидат -- `astype(float16)` и копия
несмежного срезa `blk[:, :, :16]`. Отвечает на это Metal System Trace
(закон 6): список кернелей с длительностями, и теперь он читается
однозначно, потому что время = трафик / 107 ГБ/с. Арматура для трассы в
проекте есть: `tests/trace_decode_steady.py`, `tests/profile_target.py`.

### СЛЕДУЮЩИЙ ШАГ

1. РЕШЕНИЕ ВЛАДЕЛЬЦА: вносить ли v3 в `_dequant_w`. За: 45.0 мс = 25%
   статьи, бит-в-бит на 145 тензорах, сквозной замер, своп 0. Против:
   меняется референсный плотный путь, на котором стоит весь проект, а
   выигрыш 5.7% префилла.
2. Разделить остаток 131.6 мс мутацией внутри графа: (а) подменить чтение
   `qblk` чтением непрерывного буфера той же длины -- цена шага 16 из 20
   байт; (б) подменить разворот битплана константой той же формы и типа
   (закон 34) -- цена битплана; (в) посмотреть, что остаётся при обоих.
3. Кернель декванта -- только если остаток окажется доступом или трафиком.
4. Тайловый GEMM -- переформулировать под батчевый декод, замер
   `mx.matmul` для префилла уже снят и приговор вынесен.

### СОСТОЯНИЕ НА КОНЕЦ 15.09

`rwkv-quant`: дерево чистое, пять приборов в `tests/_sess` закоммичены,
в рабочий код НИЧЕГО не внесено -- `_dequant_w` как был, v3 ждёт решения.
`rwkv-metal` и `SwiftRWKV` не трогались. Логи 15.09 переименованы из
`_1309` в `_1509`: суффикс ставился рукой и дату переврал, сами числа не
затронуты.

## 16.09. ТРАССА MST ЧЕРЕЗ CLI: ИМЁН КЕРНЕЛЕЙ НЕТ, СЧЁТЧИКИ ЕСТЬ -- ДИСПАТЧИ ПОДТВЕРЖДЕНЫ ПРЯМЫМ СЧЁТОМ (2.7 мс из 786)

Попытка снять per-kernel атрибуцию, чтобы посчитать материализации v3 по
именам. ВОЗРАЖЕНИЕ ВЛАДЕЛЬЦА ПОДТВЕРДИЛОСЬ, и оно же уже стояло в
docs/TOOLS.md про bench_step_decompose («замена per-kernel атрибуции,
которой в Metal System Trace нет»).

ЧТО CLI-ЭКСПОРТ ОТДАЁТ (xctrace 16.0, шаблон Metal System Trace,
--env MTL_CAPTURE_ENABLED=1, окно 25 с, цель tests/profile_target.py
prefill 512):
    metal-application-encoders-list      6094 строки -- есть
    metal-command-buffer-completed      16662 строки -- есть, но БЕЗ длительностей
    gpu-shader-profiler-interval            0 строк  -- ПУСТО
    metal-driver-event-intervals            0 строк  -- ПУСТО
Метки энкодеров родовые («Command Buffer 0»), имён шейдеров нет нигде:
MLX их не ставит, а shader profiler в обычном MST не наполняется (нужен
GPU capture, недоступный CLI-процессу без entitlement).

ЧТО ВСЁ-ТАКИ ДАЛО. Плато стационара 288 энкодеров в секунду, при проходе
786 мс это 226 ЭНКОДЕРОВ НА ОДИН ПРЕФИЛЛ. По записанной цене запуска
11.8 мкс это 2.7 мс из 786, то есть 0.3%. Раньше пренебрежимость
диспатчей была ОЦЕНКОЙ («даже если 3-4 запуска на тензор»), теперь это
прямой счёт. Вывод 15.09 -- время в статье тождественно трафику -- держится.

СЧИТАТЬ МАТЕРИАЛИЗАЦИИ ПРИДЁТСЯ ВЫЧИТАНИЕМ, как и предписано TOOLS.md.
Калибровка уже есть: 45.2 мс на одну полноразмерную. Отсюда статья v3
142.3 мс = 3.15 материализации в пересчёте на полный размер, тогда как
ручной счёт по тексту выражения даёт 1.4. Разницу (примерно 1.7
материализации) надо локализовать мутацией САМОГО V3 по одной операции:
снять astype, снять удвоение кодов, заменить несмежный срез qblk
непрерывным буфером. Такие плечи численно неверны, но форма и тип
сохраняются -- для времени этого достаточно (закон 34), и в гейт они не
идут.

### ДВА ДЕФЕКТА ПРИБОРОВ, ПОЙМАННЫХ ПО ХОДУ

1. `tests/profile_target.py` был СЛОМАН с ребута 12.09: путь прибит к
   `/tmp/compression_packed.rwkvq`, а /tmp вычищен. Первая трасса поймала
   пустоту -- цель падала через секунду. Починено: путь берётся из
   RWKVQ_MODEL с умолчанием в WKV-kvant, /tmp больше не участвует.
2. ЕСЛИ УБИТЬ ЦЕЛЬ, ПОКА xctrace СОХРАНЯЕТ ФАЙЛ, трасса выходит битой:
   экспорт падает с «Document Missing Template Error», причём ВСЕ
   таблицы читаются как пустые, а не как ошибка. Ждать строки
   «Output file saved as» и только потом трогать процессы. Трасса на
   25 с окна весит 289 МБ, на 40 с -- 860 МБ.

### ЛЕСТНИЦА ВЫЧИТАНИЕМ: 47% СТАТЬИ -- ЭТО concatenate, А ТРАФИКОВАЯ МОДЕЛЬ ДЕЙСТВУЕТ ТОЛЬКО ДЛЯ fp16-КОПИИ

`bench_deq_ladder_ab.py`, лог `bench_deq_ladder_ab_1609.log`. Семь фаз,
T=512, 7 раундов, своп +0.0, дрейф v3 -0.4 мс, разбросы 0.3-2.3%.
Непрерывные копии срезов готовятся ВНЕ замера (2013.3 МБ), поэтому плечо
не платит за их изготовление -- ровно это и вычитается.

ГЕЙТ ДВУСТОРОННИЙ И ЗЕЛЁНЫЙ: шесть плеч, меняющих только ОТКУДА берутся
байты, дали ОДИН отпечаток логитов; L1, у которого порядок нибблов иной,
отпечаток изменил. То есть прибор подтвердил и тождественность шести, и
неслучайность седьмого.

    плечо                     статья сверх пола    в материализациях
    cache1 (+1 матер.)              48.3 мс              1.07
    v3 (как есть)                  146.0                 3.23
    L3 (без копий срезa)           143.4                 3.17
    L2 (без склейки)                74.4                 1.65
    L1 (один проход броадкастом)    99.8                 2.21
    v3 повтор (дрейф)              145.6                 3.22

ЧТО СНИМАЕТ ЧТО: копии несмежного срезa 2.7 мс, склейка 68.9 мс, а
переход к броадкасту вместо склейки -25.4 мс, то есть УХУДШАЕТ.

ГЛАВНОЕ: `concatenate` УДВОЕНИЯ КОДОВ СТОИТ 68.9 МС ИЗ 146.0, то есть 47%
статьи. Предсказание было 22 мс -- МИМО в три раза. И он не
пропорционален байтам: на fp16-данных вдвое большего размера (плечо
cachecat 15.09) склейка стоила 54.8 мс, здесь на uint8 -- 68.9. Значит
дело не в его собственном трафике, а в том, что он РАЗРЫВАЕТ СЛИЯНИЕ:
цепочка распадается на склейку и на остаток, и промежуточный q
материализуется в fp16. Без него (L2) всё сливается в один кернель,
читающий uint8 и пишущий w.

УТОЧНЕНИЕ ВЫВОДА 15.09, ВАЖНОЕ. «Время тождественно трафику» было
откалибровано на ОДНОМ плече -- поэлементной копии fp16, которая идёт на
полной полосе (4.84 ГБ за 48.3 мс = 100 ГБ/с). Кернели РАСПАКОВКИ так не
идут: L2 читает 1.21 ГБ и пишет 2.42, то есть 3.63 ГБ за 74.4 мс =
48.8 ГБ/с, ровно половина. Обобщение на весь деквант было
преждевременным: в статье есть и составляющая эффективности кернеля, а не
только байты. Поэтому пол СРЕДСТВАМИ MLX равен примерно 74 мс, а не 30.

ПОБОЧНО И ПРОТИВ ОЖИДАНИЯ: L1 читает ВДВОЕ МЕНЬШЕ байт (0.60 ГБ против
1.21) и вовсе не делает битплан, но проигрывает L2 на 25.4 мс. Чтение
каждого байта дважды через броадкаст `[..., None]` дороже, чем честное
чтение вдвое большего непрерывного буфера. Предсказание 28-34 мс
провалено; формулировка «раскрыть ниббли броадкастом» закрыта замером.

### ЧТО ИЗ ЭТОГО СЛЕДУЕТ

1. КЕРНЕЛЬ ТЕПЕРЬ ОБОСНОВАН СИЛЬНЕЕ, а не слабее. Лучшая достижимая
   средствами MLX формулировка сидит на 48.8 ГБ/с при машинных 100+;
   кернель, читающий коды слитно и пишущий w один раз, должен выйти на
   30-40 мс против нынешних 146. Это 105-115 мс префилла, и требование
   «догнать mx.matmul» к нему по-прежнему НЕ предъявляется.
2. ВЫПЛЫЛ ДЕШЁВЫЙ ХОД БЕЗ КЕРНЕЛЯ, требующий решения владельца: хранить
   коды УЖЕ УДВОЕННЫМИ. Это ровно плечо L2 -- бит-в-бит (отпечаток
   совпал), даёт 68.9 мс, то есть v3 756.3 -> 684.7, а против нынешнего
   v1 787 -> 685 = +15% префилла. Цена: 0.60 ГБ к файлу (970 МБ ->
   примерно 1.57 ГБ) либо 1.21 ГБ резидентной памяти, если строить при
   загрузке. На 16 ГБ, где B=4 на префилле уже не влезает (раздел
   15.09), резидентный вариант дорог; файловый -- это та же развилка
   «быстрее, но толще», что уже записана про reduction против
   compression.
3. Копии несмежных срезов интерлива K3 НЕ являются проблемой: 2.7 мс.
   Подозрение (a) снято.

### ОДНОПРОХОДНЫЙ КЕРНЕЛЬ ДЕКВАНТА НАПИСАН: СТАТЬЯ 175.6 -> 47.4 МС, ПРЕФИЛЛ -16.3%, БИТ-В-БИТ

Решение владельца 16.09: писать кернель, а не хранить коды удвоёнными --
0.60 ГБ на 1.5B ещё терпимо, но на 2.9B добавка около 1.2 ГБ садится
ровно туда, где запаса нет.

`rwkv_quant/backends/metal/gw_dequant_kernel.py`: поток на (строку, блок),
читает 16-24 Б своего блока, распаковывает в регистрах, пишет 32 половины
подряд через half4. Распаковка НЕ переписана, а взята импортом из штатного
k3-кернеля (`_K3_DECODE`, `_k3_plane`). Масштаб и минимум считаются в MLX
теми же строками, что в `_dequant_w`, и передаются готовыми: 1/16 и 1/128
объёма, оптимизировать нечего, риск расхождения есть (закон 39).

ГЕЙТ ДО ЗАМЕРА: `tests/test_gw_dequant_kernel_parity.py` -- бит-в-бит по
представлению uint16 на ВСЕХ 145 тензорах реального файла (xbits 0: 120,
xbits 1: 25). РАЗРЕШАЮЩАЯ СПОСОБНОСТЬ ПОДТВЕРЖДЕНА МУТАЦИЕЙ (закон 37):
MUTATE=1 портит сдвиг битплоскости, и гейт краснеет.

СКВОЗНОЙ ЗАМЕР, `bench_prefill_deqkern_ab.py`, T=512, 7 раундов, отпечатки
логитов совпали во ВСЕХ фазах, дрейф +2.7 мс, своп +0.0:

    v1 (как есть)      785.7 мс = 651.7 т/с   статья 175.6
    v3 (лучший MLX)    751.1        681.6            141.0
    КЕРНЕЛЬ            657.5        778.7             47.4
    cache (пол)        610.1        839.2              0.0

Против v1: -128.2 мс = -16.3% времени, +19.5% пропускной способности.
Против v3: -93.6 мс. ЦЕЛЬ БЫЛА 30-40 МС СТАТЬИ, вышло 47.4 -- мимо на 18%
в высокую сторону. Обратный счёт: 0.84 ГБ чтения + 2.42 записи = 3.26 ГБ
за 47.4 мс = 68.8 ГБ/с. Это лучше цепочек MLX (48.8), но всё ещё ниже
поэлементной копии fp16 (100). Остаток 17 мс до полосы НЕ объяснён;
кандидаты -- чтение 16-24 Б на поток вместо вектора и запись half4 по 8 Б.

ДВА УРОКА, ОБА СТОИЛИ ПРОГОНОВ.
1. `q * s + m` в Metal СВЁРНУЛСЯ В FMA с одним округлением, тогда как MLX
   округляет ДВАЖДЫ. Расхождение было 1 ULP на 42% элементов, и ещё пары
   плюс-минус нуль. Лечится барьером округления: `(half)((float)v * sf)` и
   только потом `+ mn`. Это ровно то, что было записано как неизвестное ДО
   написания кернеля, и решил его гейт, а не рассуждение.
2. НАИВНАЯ МЕРА ULP ВРЁТ НА ЗНАКЕ: |uint16(a) - uint16(b)| на паре +0/-0
   даёт 32768, из-за чего первый прогон гейта показал «до 33000 ULP» там,
   где расхождение было в последнем знаке. Читать ULP только вместе с
   максимумом абсолютной разницы.

ТРЕТИЙ УРОК, ТРАНСПОРТНЫЙ: два патча подряд положили в источник кернеля
ЛИТЕРАЛЬНЫЙ слэш-n вместо перевода строки, и Metal падал не на нашей
строке, а в `utils.h:442` -- диагностика уводила в сторону. Лечение:
в файлах, генерирующих Metal, перевод строки только через `chr(10)`,
обратный слэш запрещён, и это проверяется ассертом в патче.

В РАБОЧИЙ ПЛОТНЫЙ ПУТЬ КЕРНЕЛЬ ПОКА НЕ ВКЛЮЧЁН: `_dequant_w` не тронут,
кернель вызывается только из гейта и бенча. Включение -- отдельный шаг:
нужен прогон гейтов всего проекта и проверка на остальных трёх размерах
(0.1B, 0.4B, 2.9B), где формы и xbits другие.

### ГЕЙТ КЕРНЕЛЯ НА ВСЕХ ЧЕТЫРЁХ РАЗМЕРАХ: ЗЕЛЁНЫЙ; ВЕТКА xbits=2 НЕ ПОКРЫТА НИГДЕ

`tests/_sess/run_gate_sizes.sh`, лог `gate_deqkern_sizes_1609.log`, одна
модель на процесс (закон 2), пик из /usr/bin/time -l, своп не шевельнулся
ни на одном (546.75 МБ до и после каждого):

    модель   тензоров  xbits 0 / 1   пик процесса   расхождений
    0.1B          73     60 / 13       2.06 ГиБ          0
    0.4B         145    120 / 25       2.68 ГиБ          0
    1.5B         145    120 / 25       5.66 ГиБ          0
    2.9B         193    160 / 33       7.60 ГиБ          0

ПРОГНОЗ НЕ СБЫЛСЯ, И В ВАЖНУЮ СТОРОНУ: я ожидал тензоры xbits=2 на 2.9B --
их нет НИ В ОДНОЙ из четырёх моделей. Значит ветка xbits>=2 в кернеле
написана, но гейтом НЕ ПОКРЫТА нигде и оживёт только на 6-битном asym.
Считать покрытие полным нельзя; при появлении такого пресета гейт
обязателен до включения.

README: раздел Kernels дополнен строкой про однопроходный кернель, таблицей
покрытия гейта и обеими оговорками (xbits=2 и барьер fma). Попутно
уточнено то, что раньше читалось неверно: «weights never exist
dequantized in memory» верно для GEMV-пути, но плотный путь префилла
материализует веса по необходимости -- иначе `mx.matmul` нечего подать.

### КУДА ЕЩЁ ИДТИ ПО ПРЕФИЛЛУ (бюджет 657.5 мс)

    ~539 мс  mx.matmul на реальных формах -- 93% потолка машины, НЕ НАШ
      47.4   деквант кернелем (пол по трафику ~32, то есть ~15 мс запаса)
      ~70    ВСЁ ОСТАЛЬНОЕ: WKV-скан, нормы, token-shift lerp, гейты

Единственный блок, где могут лежать десятки миллисекунд, -- последний, и он
НИ РАЗУ НЕ РАЗЛОЖЕН (долг 5 записывал его как «95 мс из 750»). Оценка ~70
получена вычитанием ИЗ РАЗНЫХ ПРОЦЕССОВ (610.1 минус 539.3), а разница
между процессами доходит до 12% (замер батчей 15.09), поэтому это ОЦЕНКА,
а не утверждение. Разложить тем же методом: плечи, где WKV-скан подменён
заглушкой той же формы и типа (закон 34, плечо времени, в гейт не идёт).

Пропускная способность растёт с длиной: при v1 647 т/с на T=512, 726 на
T=1024, 774 на T=4096. С кернелем T=4096 не мерен -- ожидание за 900 т/с,
но это ОЖИДАНИЕ, а не замер.

## Закрытые темы (не переоткрывать без новых данных)

- ANE/CoreML; GPU idle; qs/qm-упаковка в кернеле; пайплайн decode;
  4x-сжатие в нибл-контейнере; MXFP4; v3 simdgroup head; nb2; GEMM
  r/k/v-фьюз префилла.
- **Ядро «шапки слоя» (закрыто 12.09 замером ДО написания).** Статья
  0.726 мс сидела в редукциях рукописного `_layer_norm`, и 87% её взял
  штатный `mx.fast.layer_norm` без всякого кернеля. Остаток 0.066 мс
  меньше цены одного запуска (11.8 мкс на слой). Аффинный хвост
  бесплатен, token-shift 0.087, лерпы ~0.07.
- **Статья нормы в тренировочном пути `rwkv-metal` (закрыто 12.09
  чтением).** Все нормы там -- `nn.LayerNorm`, который внутри зовёт
  `mx.fast.layer_norm`; `ln_x` -- `nn.GroupNorm`, и её
  `_pytorch_compatible_group_norm` идёт через тот же примитив.
  Рукописной нормы через mean/var в `rwkv_metal` нет вовсе. Рукописный
  `_group_norm` в `rwkv-quant` при FUSE_TAIL=True не зовётся НИ НА
  ОДНОМ пути (захват дал 0 вызовов на префилле T=512 и на шаге декода).
- **Metal 4 / 4.1 TensorOps на M4 (закрыто 15.08, проверено по
  источникам).** Кооперативные тензоры и `matmul2d` существуют, но
  аппаратное ускорение -- это Neural Accelerators в шейдерных ядрах
  M5/A19; реверс-инжиниринг M4 Max (препринт Rigel, arXiv 2606.12765)
  даёт: выделенного матричного датапата у M4 НЕТ, `matmul2d` исполняется
  на обычных ядрах через старый `simdgroup_matrix`. То есть на нашей
  машине это API поверх того, что и так доступно.
  fp8 E4M3 как ТИП есть (`MTLTensorDataTypeMetalFloat8E4M3`), но на M4
  эмулируется: 0.94x от пропускной способности fp16 ПРИ ВДВОЕ МЕНЬШЕМ
  операнде -- если бы упирались в память, меньший операнд обязан был бы
  ускорить. И для нас fp8 был бы регрессом даже с ускорением: «экономия
  50% полосы» считается от fp16, а мы уже на 6.5625 бит/вес, тогда как
  fp8 с блочными масштабами E8M0 при группе 32 стоит 8.25 -- на 26%
  больше трафика.
  Epilogue fusion (+6.5-12.9%) -- единственный относящийся к нам пункт,
  и он у нас уже измерен независимо: хвост TMix дал +1.6% под
  `mx.compile` и +2.7% на сыром пути. Совпадает и квалификатор: в статье
  выигрыш только «в cache-resident режиме» и падает с ростом задачи.
  Переоткрывать при появлении M5/A19.
- ~~гибрид mlx-affine ради скорости~~ **ПЕРЕОТКРЫТО 15.08**: прямой A/B
  показал, что штатный `mx.quantized_matmul` быстрее нашего кернеля в
  1.39 раза на префилле и в 1.18 на декоде при том же бит/вес. См.
  раздел «Штатный mlx-affine быстрее нашего кернеля».
- xbsum-в-кернеле (K3_XSUM=False): decode x1.005, verify x0.94, дрейф
  логитов 0.02. Полублок (K3_HALF=False): микро +8-19%, E2E ноль.
  packs2: хуже базы. Свипы (NSG,RS) для cmix: тепловой шум.
- КЕРНЕЛЬ-4 закрыт руфлайн-пробой: декодный ALU бесплатен.
- LORA_Q8: decode x1.004, дрейф логитов 0.77 — отрицательно.
- **AW для `proj`** (04.08, `tests/ablate_aw_proj.py`): `asym_sb6`
  +2.36%, `asym_sb6_search` +2.22%, `asym_sb6_aw` +2.24%. AW не помогает
  — исходный вывод устоял. Но разница search/aw в пределах шума, а
  нынешний `asym_sb6` (без поиска И без AW) хуже обоих на 1.5B и лучше
  на 2.9B. Не трогать.
- **Драфт-спекулятивка** — дважды отрицательно, см. выше.

---

## Открытые вопросы

Упорядочено по приоритету. Закрытые замерами пункты убраны -- они теперь
разделы выше, а не вопросы.

1. ~~**LoRA ДО ПОЛКИ.**~~ ЗАКРЫТО 16.08: `LORA_Q = "sep"` @8 даёт
   +1.32 мс на умолчательном пути и +0.60 на фьюзнутом, ppl +0.012%
   (не значимо). Осталось три хвоста, все мелкие: 2.9B не мерен (закон
   10), префилл фьюзнутого пути теряет 4% и лечится диспетчеризацией по
   T, пик загрузки растёт на 183 МБ и лечится квантованием по слою в
   конструкторе. ПРЕЖНЯЯ ФОРМУЛИРОВКА ВОПРОСА БЫЛА НЕВЕРНА в обеих
   половинах -- см. раздел «LoRA: что оказалось рычагом».

   СТАРЫЙ ТЕКСТ ВОПРОСА (для истории): «Потолок 2.50 мс, 13.5% шага.» Единственная статья,
   идущая не у своей полосы: 46.6 ГБ/с против 85-93 у прочих, и причина
   -- форма (ранги 96/96/64/256 против 2048), а не байты и не запуски
   (те 0.35 мс). Нужны ОБА рычага: склейка четырёх down-проекций в один
   матмул [512, D] (по выходной оси, паддинга нет, значения те же до
   бита) и квантование под `mx.quantized_matmul`. Второе -- НОВОЕ
   квантование, а не репак: в файле LoRA лежит в `asym gw64`, но группы
   там вдоль ВЫХОДНОЙ оси матмула (writer квантует до транспозиции), и
   формы ragged (`att.w1` [2048, 96] при gs=64). Значит нужен ppl-гейт;
   ожидание хорошее (+0.006% изолированно, 12.08), но это ожидание.
   И сквозной A/B обязателен: микрозамеры тут врут в плюс, склейка
   показала 157.6 ГБ/с при потолке 104 -- матрица сидела в кэше.

2. **ПРЕФИЛЛ: 97% ВЫИГРЫША УЖЕ ВЗЯТО БЕСПЛАТНО (деквант кернелем,
   997 -> 753 мс, бит-в-бит).** Осталось два хвоста. Первый -- тайловый
   GEMM с декодом в threadgroup-памяти: он закрывает последние
   1.08-1.10x до нативного матмула, цена вопроса 0.5-0.7 мс на слое,
   работа кернельная. **ПОВЫШЕН ДО ПРИОРИТЕТА 1 ЗАМЕРОМ 11.09:**
   тот же тайловый GEMM снимает потолок БАТЧЕВОГО ДЕКОДА (сейчас
   2x, причина -- ceil(N/4) перечитываний весов при N < 128), то
   есть одна кернельная работа закрывает обе оси. Расширение
   NB_CHUNK проверено и не работает, см. раздел 11.09. Второй -- решение по affine ниже, которое теперь
   стоит СИЛЬНО меньше, чем казалось: +0.077% ppl и +291 МБ ради 4%
   префилла (753 против 722 мс).
   БЫЛО: **ПРЕФИЛЛ В affine: РЕШЕНИЕ ВЛАДЕЛЬЦА.**
   Всё измерено 16.08 (раздел «Префилл в родной affine»): префилл
   1.38x (708.8 ток/с, обгоняем llama.cpp), декод -12%, память +291 МБ,
   ppl +0.077%. Точного репака sym НЕ существует (MLX не знает gs=16),
   вторая раскладка рядом с нашей не влезает (+1.4 ГБ), affine@6
   бесплатен по памяти и стоит +0.881% ppl. Вопрос ровно один: для какой
   нагрузки собираем -- для векторной (префилл всё, декода нет: брать) или
   для чата (декод сотнями токенов: не брать).
   СТАРАЯ ФОРМУЛИРОВКА (для истории): при
   N >= GEMM_MIN_BATCH_NB мы уходим с кернеля на `_dequant_w` + плотный
   матмул и теряем 1.82-2.38x на слое при N=512 (1.39x сквозным против
   штатно квантованной модели). На декоде наш GEMV ВРОВЕНЬ с нативным
   (1.02-1.10x), трогать его не надо. Значит affine нужен как ВТОРАЯ
   раскладка для префилла, а не замена формата: +7.7% на диске платить
   не придётся, цена переезжает в переупаковку при загрузке или в две
   копии в памяти. Этот размен НЕ МЕРИЛСЯ.
   Заготовки: `codec.pack_mlx_affine` / `sb6_to_mlx_affine` (точный
   репак, но он про sb6 -- у sym блок 16, и для него точного пути нет).

3. **ПРАВКА ПРЕСЕТА REDUCTION -- решение владельца проекта.** Всё
   измерено: качество (+0.772% -> +0.192% на 1.5B, +1.370% -> +0.273% на
   2.9B, реальный путь), размер (+3.5% и +2.6%), скорость (−3% декода).
   Инженерии не осталось. Вопрос в КРИТЕРИИ: ppl и KL разошлись на emb, а
   REDUCTION -- база для QLoRA и векторных моделей, где верность
   распределения важнее вероятности одного токена.

4. **QLoRA-БЕНЧМАРК НА 1.5B** (уже тема rwkv-metal). Три руки:
   bf16-LoRA / QLoRA на REDUCTION / QLoRA на int8 -- чтобы записанное
   «REDUCTION ведёт себя лучше int8» стало числом, а не ссылкой на
   владельца. Домен брать тот, где база слаба, иначе дельты утонут
   (закон 9): сербская википедия. Данные в `Develop/data`.
   ВНИМАНИЕ: `RwkvqNativeLinear` в rwkv-metal ассертит `xbits == 2`, то
   есть знает только sb6@6 -- кандидатный пресет (cmix/proj в `sym@6`,
   head в `sym@8`) он не прочитает вовсе.

5. **FUSE ПО УМОЛЧАНИЮ НАДО ПЕРЕСМОТРЕТЬ ДЛЯ COMPRESSION.** После
   квантования LoRA фьюз почти обесценился: на COMPRESSION он даёт
   **13.37 -> 13.35 мс/ток, то есть 0.15%**, и при этом стоит ~250 МБ
   резидентно и 0.7% префилла (908.3 -> 914.8 мс). На sym-пресете он всё
   ещё даёт 0.57 мс. Прежний расчёт (+1.15 мс) снят: из них 0.58
   приходилось на укрупнение LoRA-запусков, а их теперь укрупняет
   квантование. Замер: `RWKVQ_MODES=None,sep RWKVQ_FUSE=0|1
   bench_lora_quant_ab.py /tmp/champion_v2.rwkvq`.
   ПРЕЖНЯЯ ФОРМУЛИРОВКА ВОПРОСА:
   `FUSE=True`: +1.15 мс (6.2%) на sym-пресете и +1.01 на sb6 ценой
   ~250 МБ резидентно на 1.5B (~425 на 2.9B). `LORA_Q="sep"` @8:
   +1.32 мс (7.9%) ценой +56 МБ, а если освобождать плотные копии --
   то -44 МБ, то есть ОТРИЦАТЕЛЬНОЙ ценой. Вместе они дают
   **17.97 -> 15.88 мс/ток, 55.6 -> 63.0 ток/с (+13.2%)**. Гейты
   зелёные у обоих, greedy совпадает. Вопрос не инженерный: сколько
   резидентной памяти покупать за декод.

6. **Память инференса: 1.18x от файла у sb6 и 1.16x у sym.** Осталась
   ОДНА статья: `emb` развёрнута в плотный fp16 (268 МБ против 109 на
   диске) -- закрывается indexed-gather по sb6, он же снимает половину
   остаточного пика загрузки. Качества не трогает.

7. **Обвязка GEMV на декоде.** Наш кернель вровень с нативным, но полный
   вызов стоит 1.05-1.20x: отдельная операция `xbsum` на каждый вызов
   (144 редукции за токен при шести битах), reshape'ы, цикл по колонкам.
   Чинится питоном, а не ядром.

8. **Перестановка каналов cmix**: ~10% трафика декода, точно и без
   потери качества (`probe_cmix_sparsity`, 04.08). cmix -- 42% шага и
   53% трафика, так что это самая крупная статья по байтам.

9. **Спека формата** (шаг 5). Свести докстринги `codec.py` и `schema.py`
   в нормативный текст: диск = каноническая раскладка, K3-интерлив =
   деталь загрузчика. Порядок работ: rwkv-quant -> rwkv-metal ->
   SwiftRWKV.

10. Разделить WKV-скан и перечитывание весов в верификации -- решает
    судьбу спекулятивки для RWKV в принципе.

11. Согласие драфта по языкам переснять на стратифицированной выборке --
    0.5755 намерено только на русских последовательностях.

12. **`test_streaming_inference` красный, и это НЕ регресс контейнера.**
    Проверено на `git stash`: до шага 4 те же числа до последнего знака.
    Батчевый `forward_stateful` против `wkv7_train` даёт rel 1.282e-4 при
    пороге 1e-4 -- чуть-чуть за. Комментарий в тесте утверждает, что
    «расхождение батчевого пути осталось нулевым», то есть rel1 уехал
    когда-то при переводе dense/LoRA на fp16 и этого не заметили. Порог
    НЕ трогать: сначала понять, откуда 1.2e-4 там, где ожидался ноль.

---

## Где лежат материалы

- `~/Develop/WKV-kvant/` — чекпоинты (1.5B G1H, драфт g1d-0.1b, ru60m),
  корпуса, `world_tokenizer.py`, все JSON-результаты и
  `GGUF_COMPARE.md`.
- `~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` — 2.9B (world naming).
- `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — `model.safetensors` bf16,
  `rwkv7-g1h-2.9b.rwkvq`, сайдкар `_mlx.safetensors`+`.json`,
  токенайзер.
- `~/Develop/SwiftRWKV/Sources/RWKVQuant/` — Swift-читалка сайдкара
  (`RwkvqSidecar.swift`, `RwkvqDequant.swift`), `.testdata/` с
  эталонами.
- `~/Develop/rwkv-metal/rwkv_metal/lora/` — QLoRA-стек поверх `.rwkvq`.
- `~/Develop/tests/venv/bin/python` — рабочее окружение (torch 2.13 +
  MLX, python 3.14).
- `/tmp`, СОСТОЯНИЕ НА 15.08: пересобраны `reduction_v2.rwkvq` (1255.8 МБ)
  и `champion_v2.rwkvq` (970.6 МБ) для гейтов `test_fuse_parity` и
  `test_gw_nb_parity`, плюс собран `reduction_sym_head8_2p9b.rwkvq`
  (2484.3 МБ, сходится с записанными 2483.9) и лежат
  `reduction.rwkvq` / `reduction_sym_head8.rwkvq` на 1.5B.
  **ВНИМАНИЕ, ЛОВУШКА:** `reduction_v2`/`champion_v2` собраны БЕЗ
  `/tmp/act_stats_1p5b.pt` (его нет, есть только `_ml`-варианты), то есть
  AW-режимы в них выродились в обычный поиск -- предупреждение при сборке
  было. Для гейтов равенства это безразлично (они сравнивают два пути на
  ОДНИХ весах), для любого замера КАЧЕСТВА -- нет: это ровно та тихая
  подмена, о которой закон 15. Сборка 2.9B шла со своей статистикой
  (`/tmp/act_stats_2p9b_ml.pt`) и этой оговорки не имеет.
- `/tmp` (пересобрать после ребута): `act_stats_*`, `rwkv15b-*.gguf`,
  `rwkv15b.imatrix`, `champion_v2.rwkvq`, `reduction_v2.rwkvq`.
  `test_gw_nb_parity.py` и `test_fuse_parity.py` жёстко зашиты на эти два
  пути и падают FileNotFoundError, если /tmp почищен. Пересборка:
  `rwkv_quant.quantize(ckpt_1p5b, "/tmp/reduction_v2.rwkvq",
  preset="reduction")` (~90 с, 1256.1 МБ) и то же с
  `preset="compression"` для `champion_v2` (970.8 МБ). `nb_parity`
  требует именно int6-чекпоинт: он ищет тензор с `gw_qh2` и падает
  StopIteration на COMPRESSION.
- Сайдкар в `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — ПРОВЕРЕНО 05.09
  чтением файла: `rwkv7-g1h-2.9b_mlx.json` это format_version 2, 1062
  тензора, поле `kind` есть у всех, дата 8 августа, 1899.5 МБ. Прежняя
  запись здесь («ещё СТАРЫЙ, v1: 194 тензора, 1795 МБ, без `kind`») была
  НЕВЕРНА. Поля `transposed` в нём нет: он собран до правки экспортёра
  05.09, лежит в ПРЕЖНЕЙ раскладке, и нынешнее жёсткое поведение Swift
  для него верное — сломать его переходом нельзя. Лежащий там же
  `rwkv7-g1h-2.9b.rwkvq` — тоже прежний, pickle-контейнер (читается,
  но не mmap'ится). Ни то ни другое не подменялось молча: выкатка —
  отдельное решение. Пересохранить контейнер можно без переквантования:
  `save_rwkvq(load_raw(старый), новый)`, ровно то, что делает гейт
  `test_rwkvq_container.py`, — результат бит-в-бит.
