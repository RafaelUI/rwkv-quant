# Continuation prompt — rwkv-quant

Квантование RWKV-7 под Apple Silicon (M4 base, 16 ГБ, безвентиляторный).
`rwkv-quant` = квантизация + формат `.rwkvq` + референсный Metal-бэкенд.
`rwkv-metal` и `SwiftRWKV` = бэкенд + QLoRA-файнтюн, потребляют формат.

Чекпоинты: `~/Develop/WKV-kvant/rwkv7-g1h-1.5b-ctx10240.pth` (1.5B,
world naming), `~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` (2.9B),
`~/Develop/WKV-kvant/rwkv7-g1d-0.1b.pth` (драфт).

**УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ 13.08, всё перемерено на реальном
пути** — см. раздел «УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ». `real_gw=True`
собирает файл, Metal-GEMV его читает, гейты равенства зелёные на обоих
чекпоинтах. Бюджет REDUCTION на РЕАЛЬНОМ пути: **+0.772% -> +0.192% на
1.5B за +44 МБ** и **+1.370% -> +0.273% на 2.9B за +63 МБ**, без единого
размена по языкам.

**СКОРОСТЬ ИЗМЕРЕНА 13.08: цена раскладки равна её байтам.** Декод
54.5 -> 52.9 ток/с (−3%) за +3.5% файла и вчетверо меньший бюджет;
кернель sym сверх размера не стоит ничего.

**ПАМЯТЬ ЗАГРУЗКИ СРЕЗАНА ВДВОЕ С ЛИШНИМ, 15.08.** A/B чередованием:
**5511 -> 2290 МБ на 1.5B (2.41x)** и **6383 -> 3633 МБ на 2.9B (1.76x)**,
все параметры БИТ-В-БИТ прежние (гейт `test_dense_load_parity`). Максимум
теперь держит ПРЕФИЛЛ, а не загрузка: 3265 МБ против 2290.

**КАРТА СКОРОСТИ ПЕРЕСОБРАНА 15.08, И ЧЕТЫРЕ ЗАПИСАННЫХ ВЫВОДА СНЯТЫ.**

- **Потолок полосы 104 ГБ/с, а не 95.7** (`bench_membw`, два инструмента,
  разброс 2-4%). 120 -- паспортная арифметика шины, не замер. Отсюда:
  наш GEMV идёт на **90.6% достижимого, а не на 98%**, то есть в нём
  осталось ~9% (1.3 мс), а не 2.
- **`mx.compile` не звался на префилле.** Починка дала **+35%**
  (344 -> 533 ток/с). Разрыв с llama.cpp по префиллу оказался **1.24x на
  COMPRESSION и 1.36x на REDUCTION**, а не 2x. Декод +14.5% и +16.6%.
- **Штатный mlx-affine быстрее нас** в 1.39x на префилле и 1.18x на
  декоде при том же бит/вес -- и обгоняет llama.cpp там, где мы ей
  проигрываем. Поштучно: наш GPU-кернель на декоде ВРОВЕНЬ с
  `mx.quantized_matmul` (1.02-1.10x), проигрывает обвязка; а на префилле
  мы уходим с кернеля на `_dequant_w` + плотный матмул и теряем 1.8-2.4x.
- **`CPU to GPU Latency` 2.8 мс -- это ОЧЕРЕДЬ, не накладные.** Забег
  вперёд не даёт ничего, ручки командных буферов не помогают, арифметика
  без неё сходится. Платим за КОЛИЧЕСТВО запусков.

**ИЗ ЧЕГО СОСТОИТ ШАГ (покомпонентные аблации, 15.08, шаг 17.72 мс):**

| компонент | мс | % | трафик | ГБ/с |
|---|---|---|---|---|
| cmix | 7.58 | 42.2% | 660.7 МБ | 87.2 |
| проекции r/k/v/o | 3.89 | 21.7% | 330.3 МБ | 84.9 |
| **LoRA-ветки** | **2.50** | **13.5%** | **100.1 МБ** | **46.6** |
| голова | 1.54 | 8.5% | 143.7 МБ | 93.3 |
| WKV-скан | 0.84 | 4.6% | ~1 МБ | -- |
| нормы, лерпы, token-shift | 0.67 | 3.8% | -- | -- |

**LoRA -- единственная статья не у своей полки** (46.6 против 85-93).
Причина -- форма: ранги 96/96/64/256 против 2048. WKV дёшев, «упрощение
математики» имеет потолок 3.8%, переписывание на C++ даёт примерно то,
что уже отдал `mx.compile` (−2.7 мс) -- всё три проверены и закрыты.

**LoRA ЗАКРЫТА КВАНТОВАНИЕМ ПОД `mx.quantized_matmul`, 16.08.** Декод
**17.97 -> 16.65 мс/ток (+1.32 мс, 7.9%), 55.6 -> 60.1 ток/с**; на
фьюзнутом пути 16.48 -> 15.88 (+0.60 мс), то есть **63.0 ток/с** обоими
рычагами вместе. ppl **+0.012% [-0.006; +0.029], НЕ значимо**, ни одного
размена по языкам. Префилл не тронут (994.9 -> 992.7 мс). При этом ОБЕ
записанные гипотезы о причинах не подтвердились: склейки четырёх
down-проекций в записанном виде НЕ СУЩЕСТВУЕТ (у них разные входы), а
выигрыш пришёл НЕ от байтов -- шесть бит и восемь дают одно и то же время
при полуторакратной разнице в трафике. См. раздел «LoRA: что оказалось
рычагом».

**ПРЕФИЛЛ 997 -> 753 мс БЕЗ ЕДИНОГО ИЗМЕНЁННОГО БИТА, 16.08.** 60%
времени GEMM-префилла оказалось не матмулом, а самим деквантом: он был
написан цепочкой MLX-операций и шёл со скоростью 4.4 ГБ/с. Одним Metal-
кернелем -- 10.68 -> 0.84 мс на слое, разрыв с нативным квантованным
матмулом схлопнулся с 2.4x до 1.08x, сквозной префилл 511 -> 680 ток/с,
декод не тронут, гейт бит-в-бит на 97 тензорах. Это 97% того, что давал
бы перевод модели в affine, но без +0.077% ppl и без +291 МБ.

**ИТОГО ЗА СЕССИЮ 16.08 (1.5B, sym-пресет, файл не менялся):** декод
**55.6 -> 60.1 ток/с** (с FUSE -- 63.0), префилл **511 -> 680 ток/с**,
качество -- +0.012% на декоде (LoRA @8, в шуме) и бит-в-бит на префилле.

**УМОЛЧАНИЯ ИЗМЕНЕНЫ 16.08 РЕШЕНИЕМ ВЛАДЕЛЬЦА: `FUSE = True` и
`LORA_Q = "sep"` @8.** Состояние на умолчаниях (1.5B, sym-пресет,
чередование в одном процессе): **декод 16.31-15.89 мс/ток (61.3-62.9
ток/с) против 17.89-18.12 у прежних умолчаний, префилл 748.4 мс (684
ток/с)**. Резидентно это ~250 МБ за фьюз и +56 МБ за квантованные ветки.

И ОДНО СЛЕДСТВИЕ, КОТОРОГО НЕ БЫЛО В РАСЧЁТЕ: **выигрыш фьюза
схлопнулся, когда LoRA стала квантованной.** Раньше он давал +1.15 мс,
из которых 0.58 приходилось на укрупнение LoRA-запусков; теперь эти
запуски укрупнены иначе, и фьюзу остаётся r/k/v с хвостом TMix:
на sym-пресете +0.57 мс (16.88 -> 16.31), а на sb6/COMPRESSION
**+0.06 мс, то есть ничто** (`test_fuse_parity` на champion_v2).
Значит для COMPRESSION 250 МБ за фьюз платить больше не за что, и это
надо перемерить прежде, чем считать умолчание удачным для обоих
пресетов.

**КВАНТОВАННЫЕ ВЕТКИ ЖИВУТ ТОЛЬКО В ДЕКОДЕ** (`LORA_Q_DECODE_ONLY`).
На префилле они проигрывают плотным (789.5 против 748.4 мс на фьюзнутом
пути), поэтому при T > 1 путь идёт плотным. Побочно это лучше по
качеству: префилл считает LoRA точным деквантом файла, а измеренные
+0.012% ppl относятся только к декоду. Гейт требует, чтобы префилл при
FUSE=False был БИТ-В-БИТ равен плотному, а декод -- отличался; без
второго условия диспетчеризация могла бы молча выключить квантование
целиком.

**ВСЁ ПРОВЕРЕНО НА 2.9B, ЗАКОН 10 ВЫПОЛНЕН (16.08).** Ни один вывод не
развернулся: деквант-кернель бит-в-бит на 129 тензорах и те же 1.33x
префилла (1908 -> 1440 мс, 267.7 -> 355.3 ток/с), квантованная LoRA
+7.1% декода (29.7 -> 32.1 ток/с вместе с фьюзом), **ppl -0.010%
[-0.029; +0.008], не значимо**, greedy 48/48, своп не двигался ни разу.
Шесть бит на 2.9B значимо хуже (+0.109% [+0.045; +0.174]) при том, что
на 1.5B были в шуме -- выбор восьми бит подтверждён вторым масштабом, а
не выведен из первого.

**ПОТОЛОК ПРЕФИЛЛА ИЗМЕРЕН: 2.80 ТФЛОП/с, мы на 68-71% от него.**
Формула декода на префилл НЕ ПЕРЕНОСИТСЯ -- см. раздел «Потолок
префилла». Крупнейшая незакрытая статья там -- **WKV-скан, 95 мс из 750
(12.7%)**, и он ни разу не смотрелся под префилл.

**ПРЕСЕТ REDUCTION ПЕРЕСОБРАН И ВНЕСЁН 16.08** (решение владельца).
`proj/cmix/emb/head` -> `sym_aw` (Q6_K-раскладка), битность `proj=8,
head=8, emb=8`, `cmix=6`. Реальным путём на обоих чекпоинтах:

| | было | стало |
|---|---|---|
| 1.5B | 1255.4 МБ (2.43x), **+0.772%** | **1435.1 МБ (2.13x), +0.108%** |
| 2.9B | 2420.9 МБ (2.44x), **+1.370%** | **2736.8 МБ (2.15x), +0.157%** |

Деградация в **7.1 и 8.7 раза меньше** за +14% и +13% файла; по языкам
на 1.5B en/ru/sr +0.179/+0.014/+0.245% против +1.043/+0.549/+0.996%.
Сборка проверена: файл 1435.1 МБ против обещанных замером 1434.7, гейты
упаковщика, контейнера, манифеста и codec зелёные, оба пресета собираются
и на 0.1B.

**ПРОТИВ llama.cpp ЭТО РАЗВОРОТ ПО ВСЕМ ТРЁМ ОСЯМ СРАЗУ** (тот же
харнесс `bench_decode_vs_gguf`, что и в прежней таблице):

| | размер | Δppl | pp512 | tg128 |
|---|---|---|---|---|
| **`reduction`** | 1435 МБ | **+0.108%** | **734.4** | **58.84** |
| Q6_K + imatrix | 1336 МБ | +0.19% | 705.5 | 48.59 |
| **`compression`** | 970 МБ | +3.63% | 612.3 | **76.05** |
| Q4_K_M + imatrix | 990 МБ | +3.44% | **745.2** | 58.16 |

Записанное «префилл проигрываем вдвое» снято окончательно: на
`reduction` мы теперь БЫСТРЕЕ на 4%, на `compression` проигрываем 1.22x.

**ТЕКУЩИЕ ПРИОРИТЕТЫ, по порядку:**

1. ~~**LoRA до полки.**~~ СДЕЛАНО 16.08 квантованием веток под
   `mx.quantized_matmul` (`LORA_Q = "sep"`, 8 бит): +1.32 мс на
   нефьюзнутом пути и +0.60 на фьюзнутом, ppl не шелохнулся. Статья
   ужалась с 2.37-2.50 мс примерно до 1.1-1.2. Осталось РЕШЕНИЕ
   ВЛАДЕЛЬЦА: включать ли по умолчанию (см. открытый вопрос 5 -- он
   теперь общий с FUSE) и освобождать ли плотные копии.
2. **GEMM-ветка префилла в родной `mx.quantized_matmul`.** Проигрываем
   1.8-2.4x на слое, 1.39x сквозным. Наш GEMV на декоде трогать не надо
   -- он вровень; значит affine нужен как ВТОРАЯ раскладка для
   префилла, а не как замена формата.
3. **Решение по пресету REDUCTION** -- цифры собраны все, открыт лишь
   выбор критерия (ppl против KL, они разошлись на emb). Вопрос к
   владельцу проекта.
4. **QLoRA-бенчмарк на 1.5B** -- потери, скорость, память; три руки
   (bf16-LoRA / QLoRA на REDUCTION / QLoRA на int8), чтобы записанное
   «REDUCTION ведёт себя лучше int8» стало числом. Данные: сербская
   википедия (домен, где база слаба -- иначе дельты утонут, закон 9),
   `Develop/data`. Это уже тема rwkv-metal, а не rwkv-quant.

Оп-каунт закрыт как «взято, сколько было»: `SymQuantLinearFused` и хвост
TMix дали **+1.15 мс (6.2%), 53.6 -> 57.1 ток/с**, гейты зелёные. Из
оставшегося в «прочем» сидит в основном LoRA, то есть приоритет 1.

Прежний приоритет — сделать `.rwkvq` самостоятельным форматом — ЗАКРЫТ
04-11.08: шаги 1-4 и 6 сделаны, приложение разблокировано, пользователь
качает 1855 МБ на 2.9B вместо 7691. Остался шаг 5 (нормативный текст
спеки), и это работа на вечер, а не приоритет.

Скорость декода закрыта как «достаточно хорошо»: мы быстрее llama.cpp на
13% (замер ниже), дальше гнаться дорого. Полный лог сессий до 03.08 — в
git-истории.

**Две функции полезности, из кода они не следуют** (зафиксировано
владельцем проекта 12.08). REDUCTION — база для QLoRA (по замерам ведёт
себя лучше int8) и для векторных моделей: это точка МАКСИМУМА КАЧЕСТВА
при приемлемом размере, и условные +50 МБ ради качества там приемлемы.
COMPRESSION — максимум производительности при минимальных потерях
качества, борьба за мегабайт идёт там. Одна и та же правка может быть
обязательной для одного пресета и неуместной для другого; решать порознь.

---

**17.08. WKV НА ПРЕФИЛЛЕ УСКОРЕН БИТ-В-БИТ, sym-БАЗА ЖИВЁТ В rwkv-metal,
ШАГ ОБУЧЕНИЯ РАЗЛОЖЕН.**

- **WKV: 95.0 -> 54.6 мс** застилкой строк в threadgroup, выход бит-в-бит
  (гейт `test_wkv_infer_parity`). Префилл +6.0% на 1.5B, +3.8% на 2.9B.
- **QLoRA умеет `sym`**, и попутно найдена строка в `model/convert.py`,
  разворачивавшая весь квантованный базис в плотный bf16:
  **пик сборки 9.57 -> 3.90 ГБ системных, сборка 6.8 -> 1.3 с**, логиты
  бит-в-бит.
- **Шаг обучения (T=512, r=16, 3288 мс, пик 4261 МБ): WKV — 35% времени
  и 819 МБ, деквант — 7% и 467 МБ.** Внутри WKV forward считается дважды
  (пересчёт под чекпоинтингом) и даёт 54% его времени.
- **Четыре рычага закрыты замером:** fp16-оптимизатор (вся `opt.state`
  75.5 МБ), fp16-активации (чекпоинтинг их не хранит), чанкованный лосс
  (17 мс, −13 МБ), свой VJP (нативный путь плотную `W` и так не
  материализует).
- **`mx.compile` на обучении не выигрывает, забег вперёд не помогает**
  (`bench_train_compile_ab`), но и пик 12 ГБ compile не возвращает.
- **ГЛАВНЫЙ ОТКРЫТЫЙ СЛЕД: тренировочный forward WKV 321 мс против
  инференсного 54.6 мс на тех же формах — 5.9x без объяснения.**

## Законы методологии

Нарушение каждого приводило к ложным выводам. Первые восемь — прежние,
9-13 добавлены 04.08.

1. Все замеры скорости — ТОЛЬКО A/B-чередованием в одном процессе.
   Безвентиляторный дрейф под нагрузкой до 1.8x на «том же» замере.
2. Тяжёлые ppl-прогоны — по одному конфигу на процесс.
3. ppl сравнивать только внутри одного корпуса.
4. В циклах с деквантом — eval каждую итерацию.
5. Комбинации конфигов непредсказуемы: изолированный выигрыш не
   переносится в композит без замера. Отрицательные результаты не удалять.
6. Metal System Trace: точные числа через `xcrun xctrace export` + union
   интервалов, не «Frame» в GUI.
7. mx-массивы иммутабельны: снапшот state = хранение ссылки.
8. В `mx.fast.metal_kernel` `threadgroup_position_in_grid` — это ИНДЕКС
   threadgroup. И: сравнение массивов может дать ЛОЖНЫЙ бит-экзакт — MLX
   рециклит выходные буферы, непокрытые гридом строки наследуют старое.
9. **КОРПУС РЕШАЕТ БОЛЬШЕ, ЧЕМ СХЕМА КВАНТОВАНИЯ.** `eval_corpus_world.pt[:8]`
   — 1016 предсказаний короткого английского — занижает деградацию
   ВШЕСТЕРО против того же корпуса целиком и вдесятеро против
   мультиязычного на 512 токенах. На нём REDUCTION даёт +0.12%, в
   реальности +2.36%. Замер на удобной выборке хуже отсутствия замера:
   он даёт ложную уверенность. Мерить на `eval_corpus_multiling.pt`.
10. **ВЫВОД С ОДНОГО МАСШТАБА НЕ ПЕРЕНОСИТСЯ НА ДРУГОЙ.**
    `proj=asym_sb6_search` на 1.5B даёт −0.14 п.п., на 2.9B портит
    сербский вдвое. Любое изменение пресета проверять на обоих
    чекпоинтах прежде, чем коммитить.
11. **`ps`/RSS для MLX бесполезен** — показывал 0.6 ГБ при 5 ГБ свопа
    (unified memory ему не видна). Смотреть `vm_stat`,
    `sysctl -n vm.swapusage`, `memory_pressure -Q`. Своп во время замера
    скорости делает замер недействительным — фиксировать своп до и после.
12. **ПОЛОСА ПАМЯТИ 104 ГБ/с, НЕ 120 (уточнено 15.08).** 120 -- это НЕ
    ЗАМЕР ВООБЩЕ, а паспортная арифметика шины: LPDDR5X-7500 x 128 бит =
    7500 МТ/с x 16 байт. Реально GPU вытягивает потоковым чтением
    **104 ГБ/с, то есть 87% паспортных** (`tests/bench_membw.py`, буфер
    2.15 ГБ -- на три порядка больше системного кэша, чередование,
    разброс 2-4%, своп не двигался). Число подтверждено ДВУМЯ
    инструментами: `mx.sum` даёт 104.0, лучший из семи вариантов
    собственного кернеля чистого чтения -- 101.3, и превзойти `mx.sum`
    не удалось ни числом потоков, ни развёрткой. Копия (чтение+запись) --
    92.6 ГБ/с суммарного трафика.
    Прежние «95.7 ГБ/с» -- это тот же по смыслу замер, но снятый один раз
    и без развёртки; он занижал на 9%, и на нём стояли ВСЕ выводы вида
    «мы на 98% полосы». Правильно: наш GEMV на 94.2 ГБ/с -- это **90.6%
    достижимого, а не 98%**.
    Почему не 120: refresh, активация/предзаряд строк на переходах
    страниц, гранулярность транзакций, и главное -- 120 есть пропускная
    способность КОНТРОЛЛЕРА, а GPU лишь один из его клиентов. 85-90% от
    паспорта -- нормальный потолок для LPDDR, и 87% на хорошем краю.
    И считать надо трафик, а не размер файла: `emb` — это gather одной
    строки, из 970 МБ COMPRESSION за токен едет 878.
13. `torch.load(..., mmap=True)` + один процесс на конфиг — обязательны
    для 2.9B. Загрузка 1062 тензоров за 0 с, страницы file-backed,
    вытесняются ядром, а не свопом. Без этого пик 9-12 ГБ и своп.
14. **В венве стоял НЕ-editable `rwkv_quant` 0.2.0**, и он перекрывал
    исходники для любого скрипта, запущенного не из корня репозитория
    (`sys.path[0]` = каталог скрипта, а не cwd). Скрипты в `tests/`
    спасал их `sys.path.insert(0, "..")`, всё остальное молча получало
    код полугодовой давности. Исправлено `pip install -e .`; если
    появится новый венв — ставить только editable.
15. **act_stats не проверялись на принадлежность модели.** Имена ключей
    у всех размеров RWKV-7 одинаковы, поэтому статистика 1.5B молча
    применялась к 0.1B и падала где-то в `reshape` внутри
    `_groupwise_fake_dequant`. При совпадении размерностей не упала бы
    вовсе. Добавлен `writer._get_ex2`: сверяет длину с числом входных
    каналов тензора и предупреждает вместо тихой подмены. Полноценной
    защитой была бы подпись чекпоинта в файле статистики.
16. **«Сделать вторую реализацию обёрткой» — не бесплатный рефакторинг.**
    План шага 3 предполагал, что torch-упаковщики станут обёртками над
    numpy-версиями. Замер отменил: torch параллелит поэлементные операции
    по ядрам, numpy нет. `tests/bench_codec_dequant_ab.py`, 344M
    элементов, чередование в одном процессе: torch 289 мс, torch с
    numpy-распаковкой 476 (1.65x), полностью numpy 760 (2.63x). Обе
    реализации оставлены, страховка — гейт паритета. Общее: прежде чем
    схлопывать две реализации в одну ради красоты, померить цену; «один
    источник истины» сам по себе не аргумент.
17. **СИНТЕТИКА В ГЕЙТЕ МОЖЕТ ОПИСЫВАТЬ НЕСУЩЕСТВУЮЩУЮ РАСКЛАДКУ.**
    В гейте паритета был случай «asym pad=3»: `gw_scale` на три колонки
    шире, чем нужно, — я предполагал, что так выглядит NBpad. На самом
    деле блоков ровно ceil(IN/gs), никогда больше. Год такой тест
    показывал бы зелёный и защищал не ту инвариантность. Вскрылось
    только когда в `codec` появился ассерт на число блоков — то есть
    когда формат стал утверждать факт, а не молчать. Мораль: если
    синтетический случай нельзя предъявить в реальном чекпоинте, он не
    покрытие, а фантазия; брать формы из настоящего файла
    (`blocks.N.att.w1` [2048, 96] при gs=64 — вот честный ragged-случай).

18. **ГЕЙТ, СВЕРЯЮЩИЙ ЭТАЛОН С ПЕРЕСЧЁТОМ ЦЕЛИКОМ, СТОИТ УДВОЕННОЙ
    ПАМЯТИ.** Первая версия гейта паритета складывала в .pt сами выходы
    (~30 вариантов на случай, среди случаев — `emb.weight` и
    `head.weight`, по 201 МБ в fp32). Файл вышел 10.2 ГБ, а гейт держал
    `torch.load(эталон)` И пересчёт одновременно — 22 ГБ на машине с 16.
    Своп, паника, замер убит. Для бит-в-бит проверки тензоры не нужны:
    хеш сырых байт ловит расхождение в одном бите, «где именно» —
    возвращается пересчётом ОДНОГО упавшего случая. Эталон стал 44 КБ,
    пик 94 МБ. И: своп смотреть ДО запуска, а не после падения (закон 11
    существовал, я его просто не выполнил).
19. **ЧАСТИЧНЫЙ КЕШ ХУЖЕ ОТСУТСТВИЯ КЕША.** Кеш квантованных весов на
    время замера (нужен: блочный поиск на два порядка дороже per-row)
    измерен чередованием в одном процессе на 1.5B:
    изолированно (одна группа, набор влезает) — 69.3/68.7 с без кеша
    против 33.2/33.2 с с кешем, **2.08x**, своп +0;
    композит (все группы, набор ~2.8 ГБ, НЕ влезает) — 90 с и своп +0
    без кеша против 119 с и свопа **+4424 МБ** с бюджетом 1536 МБ.
    То есть при нехватке места кеш проигрывает по ОБЕИМ осям сразу.
    Вытеснения нет: при исчерпании бюджета кеш отключается до конца
    замера. Бюджет — из `vm_stat` на старте (25% свободной, потолок
    2 ГБ), не константа: 1536 МБ выглядели скромно и всё равно утащили
    машину в своп, потому что пол замера уже занимал 76% памяти.

20. **ГРИД-ПОИСК ДЕРЖИТ ДЕСЯТОК ПОЛНОРАЗМЕРНЫХ КОПИЙ ТЕНЗОРА.**
    `groupwise_fake_dequant` с поиском создаёт q, qm_, wm_, cov, var,
    s_ls, m_ls, q2, e2, err — всё в fp32 и всё в размер тензора. Замер по
    процессу на форме emb у 2.9B [65536, 2560], вход 0.34 ГБ bf16: пик
    **5.67 ГБ**, то есть 17x от входа. Двух таких тензоров (emb и head)
    хватало, чтобы композит на 2.9B занял ~17 ГБ на машине с 16.
    Лечится полосами строк: вся математика поблочная вдоль ВХОДНОЙ оси и
    построчная вдоль выходной, поэтому разбиение по строкам бит-в-бит
    (гейт `test_groupwise_parity`, четвёртое утверждение). Пик 1.48 ГБ,
    время не изменилось. `RWKVQ_GW_CHUNK_MB`, умолчание 128.
    Общее: прежде чем оптимизировать скорость поэлементной математики,
    посчитать, сколько временных копий она держит, — на 16 ГБ это
    решает исход раньше, чем скорость.
    ДОПОЛНЕНО 13.08: полосы были только в АСИММЕТРИЧНОЙ ветке, а
    симметричная (`sym`, Q6_K) их не имела вовсе. На cmix и proj это не
    всплывало — [10240, 2560] даёт пик около 1.8 ГБ, — но на emb/head у
    2.9B те же 168M элементов дают порядка 11 ГБ поверх загруженной
    модели, то есть замер не соврал бы, а убил бы машину. Нарезка
    добавлена, гейт `tests/test_sym_chunk_parity.py` (пять реальных
    форм, включая ragged `att.w1` [2048, 96], восемь комбинаций
    режимов на каждую, требуется РАВЕНСТВО). Мораль: когда рядом живут
    две реализации одной идеи, защита, добавленная в одну, не
    распространяется на вторую сама собой — и обнаруживается это ровно
    в тот день, когда вторую впервые применяют к большому тензору.
21. **`ppl = 1.0000` — ЭТО НЕ РЕЗУЛЬТАТ, ЭТО СБОЙ.** На 2.9B MPS
    исчерпывает память и роняет command buffer
    (`kIOGPUCommandBufferCallbackErrorOutOfMemory`), но PyTorch на этом
    НЕ падает: сообщение уходит в stderr, тензоры возвращаются нулевыми,
    и наружу приходит ровно 1.0000 — число, которое в таблице выглядит
    как результат. `ablation.perplexity` теперь требует, чтобы средний
    NLL был конечен и положителен, и падает с объяснением. Смотреть в
    stderr на `Insufficient Memory` при любом подозрительно хорошем ppl.
22. **`peak memory footprint` из `/usr/bin/time -l`** — та метрика, что
    работает для unified memory. На bf16-прогоне 2.9B: RSS 2.21 ГБ
    против footprint **12.17 ГБ**. Закон 11 перечисляет, чем не мерить;
    мерить надо этим, и мерить ДО того, как ставить долгий прогон.

23. **НОВАЯ РАСКЛАДКА ОБЯЗАНА БЫТЬ ВПИСАНА ВО ВСЕ МЕСТА, КОТОРЫЕ
    ПЕРЕЧИСЛЯЮТ РАСКЛАДКИ.** Их шесть: `writer` (упаковка и `_n_blocks`),
    `codec` (нормативный деквант), `reader` (torch-деквант и разбор
    манифеста), `quant_model._linear` (выбор кернеля), `export_mlx`
    (сайдкар) и `verify_sidecar_full` (его читалка). Пять из них при
    незнакомом `gw_mode` падают или отдают не тот результат заметно, а
    ШЕСТОЙ — `export_mlx` — проваливается в ветку `rtn` и пишет тензор
    с чужим `kind` и без scale: сайдкар собирается, ошибка всплывает
    только у потребителя как «модель бредит». Найдено чтением при
    добавлении `sym`, а не падением. Это тот же закон 20 в общем виде:
    правка, добавленная в одну из параллельных реализаций, в остальные
    сама не переезжает.

24. **СВИП ПАРАМЕТРОВ -- ЭТО ТОЖЕ ЗАМЕР СКОРОСТИ, И ЗАКОН 1 НА НЕГО
    РАСПРОСТРАНЯЕТСЯ.** Свип (NSG, RS) для кернеля sym, гонявший
    конфигурации ПОДРЯД, дал: разброс между лучшей и худшей втрое
    (4.252 против 1.475 мс) и РАЗНЫХ победителей в двух прогонах подряд.
    Тот же свип с чередованием конфигураций внутри каждого раунда:
    все девять комбинаций в пределах 3%, победитель отрывается от
    второго на 0.0-2.1%, то есть выбирать нечего. Последовательный свип
    НАРИСОВАЛ трёхкратный эффект, которого не существует, -- и это
    опаснее обычного дрейфа, потому что выглядит как аккуратная таблица
    с явным оптимумом. Отсюда же практика: печатать разброс по повторам
    рядом с медианой и отрыв победителя от второго места.

25. **ДЕКОД ТРОТТЛИТ НА 30-40% ЗА МИНУТУ, И ЭТО МЕНЯЕТ СМЫСЛ ДЛИННЫХ
    ЗАМЕРОВ.** Ровная декодная нагрузка (`tests/trace_decode_steady.py`,
    один конфиг, ничего кроме шагов): первые 15 с -- 18.11-18.13 мс/ток
    (55.2 ток/с) с воспроизводимостью до второго знака, к шестидесятой
    секунде 19.86, к шестьдесят пятой 25.91, к семидесятой 22.84.
    Среднее за 75 с -- 19.49 мс/ток (51.3 ток/с), то есть на 7% хуже
    установившегося начала и на 40% хуже в худшей точке. Своп при этом
    не двигался: это чистый нагрев безвентиляторного корпуса.
    Следствия: (1) окно Metal-трассы брать в ПЕРВЫЕ 20-25 секунд, позже
    оно описывает перегретую машину, а не кернель; (2) любое «среднее за
    минуту» -- это среднее по остыванию, и сравнивать его с чужим
    средним нельзя; (3) закон 1 получает ещё одно обоснование: только
    чередование защищает ОТНОШЕНИЕ, абсолюты уплывают в пределах одного
    прогона.

26. **`peak memory footprint` ЗАВИСИТ ОТ СОСТОЯНИЯ КЕША СТРАНИЦ, И
    ПЕРВЫЙ ПРОГОН ПОСЛЕ ЗАПИСИ ФАЙЛА МЕРЯЕТ ДРУГОЕ.** Тот же процесс на
    том же `.rwkvq`: сразу после сборки файла -- 6048 МБ и 79426 page
    faults, он же на прогретом кеше -- 5511 МБ и 629 faults. Разница
    537 МБ -- это отображённые страницы файла, которые в холодном
    прогоне попадают в footprint. Величина сопоставима с эффектами,
    ради которых замер и ставится, поэтому: (1) «до» и «после» гонять
    ЧЕРЕДОВАНИЕМ на одинаково прогретом кеше -- закон 1 распространяется
    и на память, не только на скорость; (2) первый прогон после
    `quantize` выбрасывать; (3) воспроизводимость проверять повтором --
    на прогретом кеше пик воспроизводится до долей мегабайта (5511.09 и
    5510.67 МБ), то есть шум там нулевой и любое расхождение значимо.

28. **ЭТАЛОН, ПЕРЕЖИВАЮЩИЙ ПРОЦЕСС, НЕ МОЖЕТ ЗАВИСЕТЬ ОТ ТОГО, ЧТО
    ПРОЦЕСС НЕ ПЕРЕЖИВАЕТ.** Гейт кернеля sym замораживал выходы GEMV в
    файл на входе с сидом `abs(hash(ключ))`. `hash()` для строк в питоне
    рандомизирован ПО ПРОЦЕССУ, поэтому «до» и «после» считались на
    РАЗНЫХ входах, и гейт показал расхождение во всех 21 выходе с
    max|Δ| порядка самой величины -- то есть выглядел как сломанный
    кернель, а сломан был он сам. Ловится это дёшево (сверить эталон с
    самим собой в другом процессе), стоит дорого: сигнал был бы
    «правка испортила GEMV», и следующий шаг -- откатывать верную
    правку. Сид взят из `zlib.crc32`.

27. **A/B ПО ПАМЯТИ НАДО ДЕЛАТЬ ПОДМЕНОЙ ОДНОЙ ФУНКЦИИ, А НЕ ВЕРСИЕЙ
    ФАЙЛА.** В рабочем дереве живёт незакоммиченная работа прошлой
    сессии, поэтому `git stash` дал бы «до» вместе с откатом упаковщика
    sym -- то есть сравнивались бы не две редакции правки, а две разные
    программы. `bench_load_mem.py --old` подменяет РОВНО `_dense`
    замороженной копией из гейта, и обе ветки живут в одном процессе,
    одном венве и одном состоянии кеша.

29. **ДВА МИКРОЗАМЕРА ОДНОЙ ВЕЛИЧИНЫ МОГУТ ВРАТЬ В РАЗНЫЕ СТОРОНЫ, И
    ЗНАТЬ НАДО, В КАКУЮ.** Цена LoRA-веток меряна тремя инструментами.
    (1) Микрозамер на матрицах ОДНОГО слоя (2.1 МБ) -- рабочий набор
    влезает в системный кэш, отсюда 157.6 ГБ/с при потолке 104:
    ЗАВЫШАЕТ выигрыш укрупнения. (2) Микрозамер по ВСЕМУ стеку (100 МБ,
    кэш побеждён) -- но 23 слоя дают 23 порции НЕЗАВИСИМОЙ работы,
    которую GPU перекрывает, тогда как настоящий шаг есть цепочка
    зависимостей: ЗАНИЖАЕТ цену запусков. (3) Сквозной A/B -- арбитр.
    На этой задаче (1) показал склейку в 4.39x, (2) -- в 0.65x, а
    сквозной дал 0.98x: три разных ответа об одном и том же. Практика:
    сначала сказать, в какую сторону врёт инструмент, потом читать его
    число; и не принимать решение по микрозамеру, если сквозной замер
    стоит десять минут.

30. **ДЕЛЬТА-КОНФИГ, ПОСТРОЕННЫЙ НА ЖИВОМ ПРЕСЕТЕ, ПЕРЕОПРЕДЕЛЯЕТСЯ
    ВМЕСТЕ С НИМ -- И ВСЕ ЗАПИСАННЫЕ ПОД ЕГО ИМЕНЕМ ЧИСЛА ПЕРЕСТАЮТ
    ВОСПРОИЗВОДИТЬСЯ.** Конфиги `ablate_sym_composite` определены как
    дельты поверх `base_cfg()`, а тот читал `presets.REDUCTION`. После
    правки пресета 16.08 `reduction_sym_head8` стал означать РОВНО новый
    пресет (proj@8 и emb@8 уже внутри), то есть имя осталось, а
    эксперимент под ним подменился. Поймано случайно: на замере ретривала
    два РАЗНЫХ конфига выдали побайтово одинаковый результат, включая
    дрейф векторов 2.85e-04 -- совпадение до четвёртого знака и стало
    сигналом. Если бы разница была правдоподобно малой, а не нулевой,
    подмена прошла бы незамеченной и попала в таблицу.
    Лечится замораживанием базы РЯДОМ С ЭКСПЕРИМЕНТОМ
    (`LEGACY_REDUCTION` в том же файле), а нынешний пресет получает
    отдельное имя `preset`. Общее правило: имя конфига обозначает
    эксперимент, чьё число записано, и обязано означать то же самое через
    полгода -- значит его база не может жить в изменяемом модуле.

31. **ГЕЙТ, ВЫБИРАЮЩИЙ «САМОЕ МЕЛКОЕ», МОЖЕТ НИ РАЗУ НЕ ПРОЙТИ ПО ТОЙ
    ВЕТКЕ, РАДИ КОТОРОЙ НАПИСАН.** `test_rwkvq_sym_base` брал три самых
    маленьких ключа — все они оказались `@8`, где коды лежат простыми
    байтами, и битплановая распаковка 6 бит не исполнялась вовсе. Гейт
    был зелёным, ничего не проверив. Представителя надо брать от КАЖДОЙ
    разновидности (бит, раскладка, форма), а не от края сортировки.

32. **СООБЩЕНИЕ КОММИТА — УТВЕРЖДЕНИЕ О ФАКТЕ, И ПРОВЕРЯЕТСЯ ОНО НЕ
    ВЫВОДОМ ТОЙ ЖЕ ПРАВКИ.** Скрипт унификации списков раскладок умер на
    ассерте (`from .rwkv7_x070 import RWKV7X070` встречается трижды), я
    прочитал только ожидаемые строки вывода — и коммит `8b4e63e` заявил
    «список раскладок сведён в один», когда их оставалось три. Ловится
    независимым grep ПОСЛЕ правки; исправлено в `8f6d595`, где прямо
    сказано, что предыдущее сообщение было ложным.

33. **УСПЕШНЫЙ СТАТУС ОБОЛОЧКИ НЕ ДОКАЗЫВАЕТ, ЧТО ФАЙЛ ЗАПИСАН.**
    `cat > file << 'PYEOF'` через MCP отчитался нулевым кодом, «синтаксис
    ок» и «запущен», а ни файла, ни лога не существовало. Передача кода
    на машину заканчивается проверкой существования, размера и md5, и
    только потом — запуском. Длинные тексты идут gzip+base64 кусками.

---

## WKV НА ПРЕФИЛЛЕ: ЗАСТИЛКА СТРОК В threadgroup, 95.0 -> 54.6 мс (17.08)

Инференсный кернель `wkv7` читал 1.34 ГБ на вызов при 29.4 МБ полезного
трафика: каждый из 64 потоков головы тянул ЦЕЛЫЕ строки `a/w/k/b/r` из
device-памяти, то есть одно и то же читалось 64 раза. Правка —
threadgroup-застилка: строки кладутся в `a_sh/w_sh/k_sh/b_sh/r_sh` один
раз, дальше все потоки читают из неё; запуск переведён на
`threadgroup=(1, D, 1)`.

**Порядок суммирования не менялся — выход БИТ-В-БИТ** (гейт
`tests/test_wkv_infer_parity.py`: 9 форм на H=16/32/40, равенство, не
порог; сиды из `zlib.crc32`; контроль вырожденного эталона).

    WKV на префилле   95.0 -> 54.6 мс
    префилл 1.5B     699.0 -> 659.6 мс  (+6.0%)
    префилл 2.9B    1376.7 -> 1326.8 мс (+3.8%)

Два микрозамера, которые НЕ подтвердились (закон 29 в действии): форма
threadgroup сама по себе не значит ничего (`bench_wkv_tg`), и ACC поверх
застилки не даёт ничего (`bench_wkv_acc`) — цепочка FMA тут не узкое
место, трафик был. Арбитр — `tests/bench_wkv_e2e_ab.py`: замороженная
копия старого кернеля, обе версии компилируются, дальше чередование
в одном процессе.

**Ловушка импорта, стоившая часа.** `from rwkv_metal.kernel import wkv7`
и `import rwkv_metal.kernel.wkv7` оба отдают ФУНКЦИЮ `wkv7`: пакетный
`__init__` затеняет модуль одноимённым реэкспортом. Только
`importlib.import_module("rwkv_metal.kernel.wkv7")` даёт модуль.

## sym-БАЗА В rwkv-metal: ПОРТ, И ПИК СБОРКИ 9.57 -> 3.90 ГБ (17.08)

QLoRA-стек умел только `sb6`. Порт `sym`:

- `quant_linear_sym.py`: `_dq_writes(..., T="half")` — half-ветка
  байт-в-байт прежняя, добавлена float-ветка
  `((float)reg.comp - 32.0f) * (float)scale`; `T` вошёл в ключ кэша и в
  имя кернеля; `from_buffers(*, shape, bits, qs, d, ...)` + общий
  `_build`; `_dequant_w(dtype=mx.float16)`.
- `rwkvq_linear.py`: `QUANTIZED_KINDS = ("sb6", "sym")` — ЕДИНСТВЕННЫЙ
  список раскладок на оба проекта; класс `RwkvqSymLinear`, деквант в
  fp32 с приведением к bf16.
- `add_rwkvq.py`: `_backend_for(...)` — диспетчер ПОКЛЮЧЕВОЙ;
  `_check_quantized_coverage(...)` зовётся в начале замены целей, чтобы
  непокрытый ключ падал с диагнозом, а не KeyError у потребителя.

Гейты: `rwkv-quant/tests/test_sym_dequant_fp32.py` (146 sym-тензоров,
fp32-деквант РАВЕН `codec.dequant_key`, плюс контроль, что fp16 расходится
на всех 146) и `rwkv-metal/tests/test_rwkvq_sym_base.py` (факт включения,
равенство, отрицательный контроль) — см. закон 31 о том, как второй чуть
не оказался холостым.

**Настоящая причина 9.57 ГБ была не в LoRA, а в одной строке
`model/convert.py`:** `if meta["kind"] == "sb6" and key in skip_set`.
На sym-файле условие не срабатывало, и весь квантованный базис
разворачивался в плотный bf16 ради того, чтобы тут же быть выброшенным.
Замена `== "sb6"` на `in QUANTIZED_KINDS`:

    пик аллокатора  7.72 -> 2.36 ГБ
    пик системный   9.57 -> 3.90 ГБ   (/usr/bin/time -l)
    сборка           6.8 -> 1.3 с
    живое           1.76 ГБ, не изменилось; логиты бит-в-бит

Гипотеза «ленивая сборка интерливов», которую я выдвинул до замера, была
неверна: постадийный замер дал интерливы чистыми (1.38 ГБ пик при 1.38
живого).

## ИЗ ЧЕГО СОСТОИТ ШАГ ОБУЧЕНИЯ QLoRA (17.08, 1.5B, T=512, r=16)

Шаг 3288 мс, пик 4261 МБ, `grad_checkpoint=True` (ставить ЯВНО: умолчание
модели False — на этом уже сгорел замер, шаг честно мерился без
чекпоинтинга и весил 12 ГБ). Аблации — подменой глобалей модуля
(`mod.wkv7`, `rl.RwkvqSymLinear._dequant_w`), `tests/probe_train_components.py`;
на тренировочном пути НЕТ `mx.compile`, поэтому чередование в одном
процессе законно.

    WKV      1163-1186 мс (35% шага)   819 МБ
    деквант   229 мс      (7%)         467 МБ
    аблации аддитивны (сумма сходится с обеими сразу)

Раскладка самого WKV: **forward 321 мс, и он считается ДВАЖДЫ** (второй
раз — пересчёт под чекпоинтингом), backward 543 мс. То есть 643 из 1186
мс — это forward, 54%.

**ОТКРЫТОЕ ПРОТИВОРЕЧИЕ, главный след на следующую сессию: тренировочный
forward WKV — 321 мс, инференсный — 54.6 мс, при ТЕХ ЖЕ формах и той же
математике. 5.9x, необъяснённые.** Проверять микро-A/B двух кернелей на
одинаковых входах чередованием в одном процессе (как `bench_wkv_acc`), а
не чтением исходников.

Не разложено: ~2.6 ГБ транзиента шага (атрибутировано только ~470 МБ
декванта и ~0 у кросс-энтропии).

## ЧЕТЫРЕ РЫЧАГА ОБУЧЕНИЯ, ЗАКРЫТЫЕ ЗАМЕРОМ (17.08)

Все четыре звучали разумно, все четыре мертвы — и это ровно та экономия,
ради которой стоит мерить до того, как писать:

- **fp16 состояние оптимизатора** — вся `opt.state` весит 75.5 МБ.
  Экономить нечего.
- **fp16 активации** — чекпоинтинг их не хранит, а пересчитывает.
- **чанкованная кросс-энтропия** — 17 мс и −13 МБ (аблация «сумма
  логитов вместо CE»: логиты считаются в обоих, разность — ровно
  log_softmax и копии на [T, 65536]).
- **свой VJP на деквант** — sb6-нативный путь плотную `W` не
  материализует вовсе: 3195 мс / 4252 МБ против 3283 / 4261. Ни быстрее,
  ни меньше.

## mx.compile И ЗАБЕГ ВПЕРЁД НА ОБУЧЕНИИ: НИ ТО НИ ДРУГОЕ (17.08)

`tests/bench_train_compile_ab.py` (T=512, 3 раунда, лоссы совпали с
разбросом 8.5e-05 — сравнивались одни и те же вычисления):

    вариант                 мс/шаг  разброс  x к базе  пик МБ
    сырой, eval каждый        3378    31.9%     1.000    4261
    сырой, eval раз в 2       3418    28.7%     0.988    4433
    сырой, eval раз в 4       3797    28.4%     0.890    4701
    compiled, eval каждый     4010    23.3%     0.842    4462
    compiled, eval раз в 2    4068    32.3%     0.830    4596

Три вывода, и третий — про сам замер:

1. **Забег вперёд не помогает и здесь.** K=2 внутри шума, K=4 хуже на
   11% и дороже по пику на 440 МБ. Версия «CPU подаёт работу по
   требованию, а не уходит вперёд» не подтверждается — это то же, что
   было записано 15.08 на декоде, теперь проверено и на обучении.
2. **`mx.compile` собрался и НЕ вернул пик 12 ГБ** (4462 против 4261,
   то есть сквозь `nn.utils.checkpoint` он не протрассировал) — но и
   выигрыша не дал. Префилльные +35% на обучение не переносятся.
3. **Разброс 23-32% съедает часть выводов, и порядок вариантов внутри
   раунда БЫЛ ОДИН И ТОТ ЖЕ** (сырые сначала, compiled последними), а
   корпус безвентиляторный. Значит −19% у compiled частично наведены
   нагревом, и цитировать это число как «цена компиляции» нельзя. Что
   уцелело при любом прочтении: compile не выигрывает и пик не ломает.
   Переделывать — с рандомизацией порядка внутри раунда и коротким
   окном.

## MSTRACE ОБУЧЕНИЯ: 1790 ДИСПАТЧЕЙ И ПОЧЕМУ ЛАТЕНТНОСТЬ НЕ АДДИТИВНА

Снято пользователем в Instruments с `tests/trace_train_steady.py`
(сборка и прогрев ДО маркера готовности, `--attach`, окно — первые 10 с
устоявшегося режима). Показания: Compute Shader Launch Limiter ~100% с
множеством дыр, Kernel Occupancy в среднем ~40% и крайне непостоянна,
SIMD Groups Inflight 20-94%, F32 Limiter ~85% с дырами, F16 поднимается
до 68% лишь на миллисекунду, Integer ~20%, L1 Limiter 17%, Buffer L1
Miss Rate 50%, MMU 12-37%. За 10 с — 1790 compute-штук; дыры ~15.5 мс,
110 штук за 3.2 с.

**Арифметика против аддитивного прочтения «AVG latency 62 мс».**
1790 x 62 мс = 111 с в окне на 10 с. Задержка не складывается между
кадрами — она их ПЕРЕКРЫВАЕТ, и высокая CPU-to-GPU latency означает
«GPU занят предыдущими буферами», а не «CPU опоздал». Ровно тот же
разбор был сделан 15.08 на декоде (раздел «CPU->GPU: 2.8 мс не лежат на
критическом пути»), и замер `bench_train_compile_ab` его подтвердил.

Моя рабочая версия про дыры: это WKV-кернель, у которого 2048 потоков
(B x H x D) независимо от T — счётчикам нечего сэмплировать, и график
проваливается, хотя GPU занят. **Не проверено. Вопрос пользователю,
оставшийся без ответа: «дыры есть» — это прогон с `ABLATE=wkv` или
обычный?** От ответа зависит, жива ли эта версия: под аблацией дыры
обязаны исчезнуть.

Чего Metal-трасса не даёт в принципе (записано, чтобы не переоткрывать):
GPU Bandwidth — это трафик мимо L1, частично обслуженный SLC, а НЕ шина;
поимённой атрибуции по кернелям нет, есть только командные буферы.

## COMPRESSION ПРОТИВ REDUCTION НА GEMV: 88.4 против 96.2 ГБ/с (17.08)

`tests/bench_gemv_presets_ab.py` — обе модели в одном процессе, GEMV-фазы
чередуются: REDUCTION 96.2 ГБ/с против COMPRESSION 88.4 (1.087x, разброс
1.8-2.0%), при этом постоянная часть шага одинакова в МИЛЛИСЕКУНДАХ.
Значит отставание COMPRESSION — это РАСПАКОВКА, а не число операций, и
чинится оно кернелем. Оговорка, без которой число врёт (закон о сравнении
раскладок на разной битности): пресеты различаются И битностью, И
раскладкой, а голова REDUCTION лежит в `@8`, где распаковки нет вовсе.
Пользователь отложил: «это не прям срочно».

Заодно перемерено сквозное (`bench_decode_vs_gguf` научен принимать
готовые `.rwkvq`, чем убраны и несуществующий `ACT_STATS`, и 7.7 ГБ
рабочей памяти на квантование):

    COMPRESSION  pp512 633.2  tg128 75.25
    REDUCTION    pp512 771.4  tg128 58.20

README обновлён этими числами (декод на 20% быстрее llama.cpp, префилл
771.4 против 705.5, проигрыш по префиллу 1.18x, потолок 2.15 ТФЛОП/с =
77%, остаток 23% с разбивкой WKV 7.7% / деквант 5.5% / LoRA 5.3%).

## АРТЕФАКТЫ: ЧТО ПЕРЕЕХАЛО ИЗ /tmp (17.08)

Пересобраны все три `act_stats` (145/145/193 тензора), три `.rwkvq`
перенесены в `~/Develop/WKV-kvant/artifacts`, добавлен
`rwkv-quant/tests/restore_tmp.sh` — симлинки оттуда в `/tmp`, чтобы
жёстко зашитые пути гейтов продолжали работать после чистки. Найдено и
записано: `presets.py` ссылался на `/tmp/act_stats_1p5b.pt`, которого не
существовало никогда.

## Калибровка меряла НЕ ту схему (найдено и закрыто 10.08)

`calibration.fake_quant.q()` — функция, по которой `api.calibrate()`
выбирал битность, — знала только per-row RTN и не смотрела на
`group_scale` ВООБЩЕ. Побитово: `q(w,g,QuantConfig(proj=4))` и то же
самое с `group_scale={"proj":32}` возвращали одно и то же, тогда как
пресеты собраны ровно на `group_scale`. Плюс `calibrate()` возвращал
конфиг без `group_scale`, поэтому `quantize(config=...)` производил
per-row артефакт независимо от того, что показал поиск.

Цена ошибки (0.1B, bf16 ppl 14.5826):

| группа | per-row (что мерили) | groupwise sb6 (что деплоят) |
|---|---|---|
| cmix@4 | +1059.91% | **+8.51%** |
| proj@4 | +43.82% | +9.87% |

125x, и в сторону «отвергнуть»: при пороге 5% поиск выбрасывал cmix@4
как катастрофу и раздувал файл, спасаясь от деградации, которой в
деплое нет.

Сделано: блочные fake-схемы переехали в `calibration/groupwise.py`
(в `formats/` их не мог позвать слой калибровки — writer импортирует
calibration, обратное замкнуло бы цикл); `q()` диспетчеризует ровно как
`writer.quantize_tensor` и принимает ключ тензора для AW; поиск ведётся
по ПРИМЕНИМЫМ схемам (`calibration/schema_space.py`, применимость из
форм тензоров); бюджет означает композит, а не «порог x множитель».
Гейты: `test_calib_matches_writer`, `test_groupwise_parity`,
`probe_schema_cost --check`.

### Цена схем в битах на вес — ИЗМЕРЕНА, не выведена

`tests/probe_schema_cost.py`, blocks.3.ffn.key.weight [3072, 768]:

| схема | бит/вес | | схема | бит/вес |
|---|---|---|---|---|
| sb6@4 | 4.500 | | asym gw64@5 | **9.000** |
| sb6@5 | 5.500 | | asym gw64@6 | **9.000** |
| sb6@6 | 6.500 | | asym gw64@8 | **9.000** |
| rtn@4 | 4.021 | | rtn@6 | **8.021** |
| | | | rtn@8 | **8.021** |

Два факта, которых не было нигде:

1. **asym-ветка не зависит от битности по размеру.** Коды лежат uint8,
   scale/min — fp32 на блок 64. В пресетах `w_lora=a_lora=v_lora=6` —
   это ровно тот же файл, что при 8, только хуже по качеству. Битность
   там **бесплатный параметр**, и он сейчас потрачен впустую.
2. **rtn без суб-байтовой упаковки выше четырёх бит.** `_make_qt` пакует
   нибблы только при bits<=4. Тезис README про canonical int6 верен и
   для внутреннего пути.

Плюс латентная бага: `quantize_tensor(real_gw=True)` ПАДАЕТ на не-2-D
тензоре при bits<=4 (`pack_int4` ждёт две размерности). Прежний
`calibrate` с сеткой (8,6,4,2) мог выбрать `small=2` на чекпоинте, где
она это переживает (по README — 61M), после чего `quantize()` падал при
упаковке.

---

## emb против head: разные группы (замерено 10-11.08)

`emb_head` разделена на `emb` и `head`; "emb_head" остался псевдонимом
и раскрывается во всех полях QuantConfig, `repr()` схлопывает обратно,
поэтому `config_repr` пресетов в манифесте не изменился. Гейт
`test_group_split_compat`: 107 буферов обоих пресетов бит-в-бит.

**Изолированно** (1.5B, мультиязычный, 19418 предсказаний, bf16 8.2005,
режим asym_sb6, квантуется РОВНО одна таблица):

| бит | emb Δ | head Δ | head дороже |
|---|---|---|---|
| 6 | +0.01% | +0.16% | 16x |
| 5 | **-0.02%** | +0.67% | — |
| 4 | +0.27% | **+2.17%** | 8.1x |

**Композитом на REDUCTION** (1.5B, 8176 предсказаний, bf16 7.9985,
act_stats выключены явно — иначе результат зависит от содержимого /tmp):

| emb | head | ppl | Δ% | МБ |
|---|---|---|---|---|
| 6 | 6 | 8.0514 | +0.66% | 1254.9 |
| 5 | 6 | 8.0458 | **+0.59%** | 1238.1 (−16.8) |
| 4 | 6 | 8.0811 | +1.03% | 1221.4 (−33.6) |

`emb 6->5` — **меньше и не хуже** (−0.07 п.п. в пределах шума, −16.8 МБ).
`emb 6->4` стоит +0.37 п.п. за −33.6 МБ: на бюджете REDUCTION это рост
деградации больше чем в полтора раза, вряд ли оправдан.

На 0.1B картина та же по знаку, но в шуме — 2040 предсказаний мало
(ровно предупреждение закона 9).

### И на 2.9B это НЕ ПОВТОРИЛОСЬ (закон 10 сработал вживую)

Тот же скрипт, тот же срез корпуса (16x512), act_stats выключены,
отличие только batch=1 (закон 13). bf16 ppl 7.1515:

| emb | head | ppl | Δ% | МБ |
|---|---|---|---|---|
| 6 | 6 | 7.2762 | +1.74% | 2420.1 |
| 5 | 6 | 7.3199 | **+2.35%** | 2399.1 (−21.0) |

**emb 6->5 стоит +0.61 п.п. на 2.9B против −0.07 п.п. на 1.5B.** Знак
противоположный, величина на порядок. На бюджете REDUCTION это рост
деградации на треть ради 0.9% файла.

**ПРЕСЕТЫ НЕ ТРОНУТЫ, emb остаётся на шести битах.** Рекомендация,
выведенная из 1.5B, не пережила проверку на 2.9B — ровно то, от чего
закон 10 и предостерегает. Записано как отрицательный результат, а не
удалено (закон 5).

Что при этом УТОЧНЯЕТСЯ в прежнем выводе. NEXT_SESSION утверждал со
ссылкой на `ablate_qlora_lora_source`, что «квантование emb не стоит
ничего по качеству на обоих масштабах». Тот замер сравнивал emb в bf16
против emb в шести битах — и в этих границах он верен. Но обрыв на 2.9B
лежит между 6 и 5 битами, поэтому корректная формулировка: **emb
бесплатен ровно до шести бит, и ниже — уже нет.** Разница существенная:
из первой формулировки следовало, что emb можно жать дальше, из второй —
что нельзя.

Разделение групп при этом остаётся полезным независимо от битности: у
head чувствительность в 8-25 раз выше, и теперь `calibrate()` подбирает
их порознь под конкретный чекпоинт, вместо того чтобы брать худшее из
двух.

### Потолок памяти на 2.9B (замерено `/usr/bin/time -l`)

| прогон | maximum RSS | **peak memory footprint** |
|---|---|---|
| bf16 | 2.21 ГБ | **12.17 ГБ** |
| композит REDUCTION | 2.40 ГБ | **15.52 ГБ** |

`peak memory footprint` — метрика, которой не хватало закону 11. RSS
врёт (2.2 ГБ при реальных 12), своп показывает косвенно и с
запаздыванием, а footprint видит и GPU-кучу. Закон 11 говорит, чем НЕ
мерить; мерить надо этим.

Из чисел следует, что 2.9B на 16 ГБ идёт впритык: **чистый bf16-прогон
уже съедает три четверти памяти**, и композит проходит с запасом в
0.5 ГБ. Всё, что добавляет транзиентов, обязано считаться полосами
(см. закон 20).

---

## LoRA-битность бесплатна по размеру и бесполезна по ppl (12.08)

Проверялось утверждение: контейнер `asym gw64` стоит 9.000 бит/вес при
bits=5, 6 и 8 одинаково, значит битность там чистый параметр качества, а
в пресетах стоит 6.

**Размер подтверждён на полной модели, побайтово.** 1.5B, REDUCTION,
`w/a/v_lora` = 6 против 8:

```
lora=6: 1 255 797 192 байт      sha256 727a3a5ea62e005a
lora=8: 1 255 797 192 байт      sha256 56d3c897a538b21d
разница: +0 байт, содержимое различается
```

**Ошибка в весах падает вчетверо.** Относительная RMS-ошибка
деквантования по всем 144 тензорам `w/a/v_lora` на 1.5B: 1.94e-2 при
шести битах против 4.80e-3 при восьми, ровно 4.05x (два лишних бита).
Попутно 1.94e-2 воспроизводит записанное ранее «asym 1.90-2.15e-2»
независимым путём.

**А по ppl -- НИЧЕГО.** Изолированно (квантованы только эти три группы,
всё прочее bf16), мультиязычный корпус, 19418 предсказаний, bf16 8.2005:

| lora бит | ppl | Δ |
|---|---|---|
| 6 | 8.2009 | +0.006% |
| 8 | 8.2013 | +0.010% |

Восемь бит НОМИНАЛЬНО ХУЖЕ на 0.004 п.п. -- то есть обе точки лежат в
шуме и относительно bf16, и относительно друг друга. Причина простая и
её стоило увидеть до замера: **группа при шести битах стоит +0.006%,
значит отыгрывать нечего.** Вчетверо меньшая ошибка в весах не даёт
ничего, потому что вклад группы в ppl уже равен нулю -- очередное
подтверждение, что ошибка в весах не предсказывает эффект.

### У REDUCTION и COMPRESSION РАЗНЫЕ функции полезности (уточнено 12.08)

Зафиксировано словами владельца проекта, потому что из кода это не
следует, а решения от этого зависят напрямую. REDUCTION -- это база для
QLoRA (по замерам ведёт себя лучше int8) и для векторных моделей, то
есть точка «максимум качества при приемлемом размере». Борьба за
мегабайт -- задача COMPRESSION. **Для REDUCTION условные +50 МБ ради
качества приемлемы** (на 1.5B это 4% файла, на 2.9B -- 2%).

Практическое следствие: для REDUCTION можно рассматривать схемы, которые
раньше отбрасывались как «дороже по битам». В частности, симметричная
Q6_K-раскладка стоит 6.5625 бит/вес против наших 6.5 -- разница 0.0625
бита, то есть около +10 МБ на 1.5B, что в этой рамке вообще не аргумент.
Раньше такое сравнение шло «при равном бюджете»; теперь бюджет
неравный, и это расширяет пространство.

Второе следствие: **у sb6 нет 7 и 8 бит.** Упаковщик умеет 4 (нибблы),
5 (+qh) и 6 (+qh2), дальше механизм тот же -- ещё битплоскость, -- но не
написан, и кернель знает только xbits 0/1/2. Пока его нет, единственный
способ дать группе больше шести бит в блочной схеме -- уйти в asym gw64
и заплатить 9.000 бит/вес. Для REDUCTION это стоит рассмотреть отдельно.

Вывод для REDUCTION: LoRA-ветки НЕ являются рычагом качества инференса.
Переход на 8 бит остаётся защитимым (тот же размер, вчетверо мельче
сетка, хуже стать не может), но обосновывать его надо структурно, а не
измеренным выигрышем, и для инференса он ничего не стоит ни в ту, ни в
другую сторону.

**Где на самом деле лежит бюджет REDUCTION.** По leave-one-out от 04.08
(вклад в общие +2.36% до правки small): cmix +0.33 п.п., proj +0.18,
emb_head +0.18, все четыре LoRA вместе +0.16, small +1.59. После
`small=16` общий бюджет +0.77%, и его определяют cmix, proj, emb_head --
то есть ровно те группы, где битность УЖЕ выбрана и уменьшать её нельзя.
Значит улучшать качество при неизменном размере можно только сменой
РАСКЛАДКИ или СЕТКИ КОДОВ, а не битности: открытый вопрос 2
(Q6_K-подобная симметричная раскладка, целится прямо в cmix) и
неравномерный кодбук NF4/AF4. Оба меняют качество при том же бюджете
бит; больше в этой точке менять нечего.

---

## Q6_K-раскладка на cmix: +0.005% против +0.375% (12.08, 1.5B)

Открытый вопрос 2 закрыт положительно, и с большим запасом.
Изолированно (квантован только cmix @6, всё прочее bf16), мультиязычный
корпус, 19418 предсказаний, bf16 ppl 8.200484:

| режим | блок | бит/вес | ppl | Δ |
|---|---|---|---|---|
| `asym_sb6_search` (наш + поиск) | 32 | 6.5000 | 8.2337 | +0.405% |
| `asym_sb6_aw` (наш, режим REDUCTION) | 32 | 6.5000 | 8.2313 | +0.375% |
| `sym` (Q6_K + поиск) | 16 | 6.5625 | 8.2111 | +0.130% |
| **`sym_aw` (Q6_K + поиск + AW)** | 16 | 6.5625 | **8.2009** | **+0.005%** |

**Вклад cmix падает в 75 раз за +0.0625 бита/вес (+6.3 МБ на группу).**
Гипотеза из докстринга `groupwise_sym_fake_dequant` подтвердилась: на
шести битах отдельный min не окупается -- мы платим за него дважды
(6 бит на min И урезанный до 6 бит scale), тогда как Q6_K тратит тот же
бюджет на вдвое меньший блок с 8-битным scale. Ровно поэтому llama.cpp
меняет структуру на шести битах, а мы -- нет.

Внутренняя согласованность (важно, потому что первый прогон был
сломан): `asym_sb6_aw` и `asym_sb6_search` дали 8.2313 и 8.2337 в ОБОИХ
прогонах, до четвёртого знака -- асимметричную ветку правка сетки
поиска не задела, как и должна была. AW на cmix выигрывает 0.03 п.п. у
чистого поиска, что независимо подтверждает решение держать там AW.

### Что это значит для бюджета

REDUCTING на 1.5B стоит +0.77%, и по leave-one-out cmix несёт из них
около +0.33 п.п. Если перенос в композит хотя бы наполовину сохранит
изолированный выигрыш, бюджет уходит к ~0.45% при росте файла на 6 МБ
(0.5%). Для сравнения, llama.cpp Q6_K+imatrix даёт +0.19% при 1336 МБ
против наших 1255 -- то есть после этой правки мы оказываемся лучше и по
качеству, и по размеру, а не в паритете.

### 2.9B ПОДТВЕРДИЛ, и сильнее (13.08)

Изолированно, cmix @6, 8176 предсказаний, bf16 ppl 7.15151,
статистика активаций снята на 2.9B (193 тензора, 2560 каналов):

| режим | блок | бит/вес | ppl | Δ |
|---|---|---|---|---|
| `asym_sb6_aw` (режим REDUCTION) | 32 | 6.5000 | 7.1751 | +0.330% |
| **`sym_aw`** | 16 | 6.5625 | 7.1419 | **−0.134%** |

**−0.463 п.п. за +13.1 МБ.** Это ПЕРВЫЙ вывод сессии, переживший закон
10: на 1.5B выигрыш −0.371 п.п., на 2.9B −0.463, то же направление и
близкая величина, слегка больше на большей модели. Для сравнения,
`emb=5` в той же сессии развернулся на 2.9B в противоположную сторону.

Оговорка про знак. На 2.9B `sym_aw` вышел НИЖЕ bf16 (−0.134%), то есть
квантование формально улучшило ppl. Это правдоподобно (шум квантования
иногда работает как регуляризация на отложенном корпусе) и по величине
мало, но принимать это как «квантование улучшает модель» нельзя: 8176
предсказаний, и −0.134% лежит в той же области, где живут случайные
эффекты корпуса. Честная формулировка -- **на 2.9B cmix в
Q6_K-раскладке при шести битах перестаёт быть источником деградации
вообще**, а не «становится лучше оригинала».

### То же на proj: выигрыш есть, но вчетверо меньше

Тот же замер, группа `proj` @6, изолированно, 1.5B:

| режим | блок | бит/вес | ppl | Δ |
|---|---|---|---|---|
| `asym_sb6` (режим REDUCTION для proj) | 32 | 6.5000 | 8.2129 | +0.152% |
| `sym_aw` | 16 | 6.5625 | 8.2073 | **+0.083%** |

−0.068 п.п. за +3.1 МБ. Изолированный вклад proj (+0.152%) сходится с
leave-one-out от 04.08 (+0.18 п.п.) -- независимое подтверждение обеих
цифр. Но выигрыш здесь в 5.5 раза меньше, чем на cmix (−0.371 п.п.), то
есть **основной эффект раскладки сосредоточен в cmix**, а не размазан по
плотным группам. Это осмысленно: cmix -- это `relu(x@key.T)**2`, вход
второй матрицы неотрицателен и сильно скошен, и симметричная сетка без
min ловит такое распределение иначе.

Порядок работ отсюда: cmix стоит того, чтобы под него писать упаковщик,
proj -- вопрос открытый (3 МБ за 0.068 п.п.), emb_head не мерен.
(Композитом proj оказался НЕ вопросом, а «брать, раз упаковщик всё равно
будет»: на 2.9B он даёт −0.647 п.п. по ALL и улучшает все три языка --
см. раздел ниже.)

### КОМПОЗИТ ПОДТВЕРДИЛ, на обоих масштабах (13.08, закон 5 закрыт)

`tests/ablate_sym_composite.py`, полный мультиязычный корпус 38x512 =
19418 предсказаний на ОБОИХ чекпоинтах, статистика активаций своя для
каждого, один конфиг на процесс.

**1.5B** (bf16 8.20048):

| конфиг | МБ | Δ ALL | Δ ср.яз | en | ru | sr |
|---|---|---|---|---|---|---|
| REDUCTION | 1255.1 | +0.690% | +0.753% | +0.852% | +0.535% | +0.872% |
| + cmix `sym_aw` | 1261.4 | **+0.294%** | +0.379% | +0.421% | +0.086% | +0.629% |
| + proj `sym_aw` | 1264.6 | **+0.240%** | +0.307% | +0.346% | +0.076% | +0.498% |

**2.9B** (bf16 7.16598):

| конфиг | МБ | Δ ALL | Δ ср.яз | en | ru | sr |
|---|---|---|---|---|---|---|
| REDUCTION | 2420.5 | +1.299% | +1.213% | +0.976% | +1.509% | +1.156% |
| + cmix `sym_aw` | 2433.6 | **+1.015%** | +0.978% | +0.791% | +1.108% | +1.035% |
| + proj `sym_aw` | 2440.2 | **+0.369%** | +0.484% | +0.568% | +0.087% | +0.798% |

МБ здесь — оценка по формулам бит/вес, а не байты файла. Она сверена с
реальным writer'ом: 1255.1 против фактических 1255.4 на 1.5B и 2420.5
против 2420.9 на 2.9B, то есть 0.02%. Первая версия занижала на 6-10 МБ,
потому что считала `asym gw64` как `bits + 64/gs`; на самом деле коды
там лежат в uint8 (суб-байтовая упаковка только при bits<=4), отсюда
измеренные 9.000 бит/вес при 5, 6 и 8 одинаково. Пересчёт делается без
прогонов: `ablate_sym_composite.py --sizes`.

**Перенос состоялся полностью.** cmix композитом: −0.396 п.п. на 1.5B
(изолированно было −0.371) и −0.283 на 2.9B по ALL, −0.236 по среднему
языков (изолированно −0.463 на русском срезе; в композите на русском
−0.401). Закон 5 предупреждает, что изолированный выигрыш может не
пережить композит; здесь пережил, и это первый случай в проекте, когда
проверка ничего не отняла.

**Улучшаются ВСЕ ТРИ языка на ОБОИХ масштабах** — ни одного размена. Это
существеннее, чем величина: сербский был канарейкой (он ломался и от
`small=8`, и от `proj=asym_sb6_search` на 2.9B), а тут он идёт вместе со
всеми.

Бюджет REDUCTION после обеих групп: **+0.690% -> +0.240% на 1.5B за
+9.5 МБ** и **+1.299% -> +0.369% на 2.9B за +19.7 МБ** — то есть втрое
меньше деградации за 0.8% размера. Для рамки REDUCTION («+50 МБ ради
качества приемлемы») это дёшево. Для сравнения, llama.cpp Q6_K+imatrix
даёт +0.19% при 1336 МБ; мы после правки — 1258 МБ, и это уже не
паритет, а преимущество по обеим осям сразу. Оговорка: у нас fake-путь,
у них реальный, так что окончательное сравнение — после упаковщика.

### Контроль: сколько из выигрыша на proj — это раскладка, а сколько AW

В REDUCTION `proj` — единственная группа БЕЗ поиска и БЕЗ AW
(`asym_sb6` plain, решение сессии 19.07-5). Значит переход proj ->
`sym_aw` меняет три вещи разом, и приписывать весь эффект раскладке
нельзя. Контрольный конфиг даёт proj поиск и AW в СТАРОЙ раскладке:

| разложение | 1.5B ALL | 2.9B ALL | 2.9B по языкам |
|---|---|---|---|
| поиск + AW, старая раскладка | −0.021 п.п. | **−0.508 п.п.** | en −0.070, ru −0.968, **sr +0.080** |
| собственно Q6_K-раскладка | −0.033 п.п. | −0.139 п.п. | en −0.154, ru −0.053, **sr −0.317** |

По ALL на 2.9B выходит, что три четверти выигрыша даёт AW, а не
раскладка, — и это заманчиво, потому что AW БЕСПЛАТЕН по размеру и
никакой инженерии не требует. Но по языкам видно, что это размен:
**AW портит сербский на 2.9B и русский на 1.5B**, тогда как раскладка на
2.9B улучшает все три языка.

### Правка пресета proj -> `asym_sb6_aw` в чистом виде: ОТРИЦАТЕЛЬНО

Отдельным конфигом, без sym-cmix, ноль байт разницы:

| | Δ ALL | en | ru | sr |
|---|---|---|---|---|
| 1.5B REDUCTION | +0.690% | +0.852% | +0.535% | +0.872% |
| 1.5B + proj AW | +0.605% | +0.852% | **+0.568%** | +0.441% |
| 2.9B REDUCTION | +1.299% | +0.976% | +1.509% | +1.156% |
| 2.9B + proj AW | **+0.798%** | **+1.024%** | +0.499% | **+1.239%** |

По ALL на 2.9B это выглядит как крупная бесплатная победа (−0.501 п.п.
за 0 байт). По языкам — русский −1.010 п.п., английский +0.048,
сербский +0.083. На 1.5B зеркально: сербский вдвое лучше, русский чуть
хуже. **ПРЕСЕТ НЕ ТРОНУТ.** Это ровно тот же вывод, что уже стоял в
закрытых темах про AW для proj (04.08), полученный третьим независимым
способом, и ровно та причина, по которой `proj` не трогают с 19.07.
Записано как отрицательный результат (закон 5).

Общее правило, которое стоит держать: **ALL смещён составом корпуса**
(ru 20 последовательностей против en 9 и sr 9), поэтому правка, берущая
своё на русском, по ALL всегда выглядит лучше, чем есть. В отчёт
`ablate_sym_composite` добавлена колонка среднего по языкам с равным
весом; решать по одному ALL нельзя.

### Как это померено: предквантование вместо кванта на лету

`ablation.perplexity` зовёт `fake_quant.q()` ВНУТРИ forward, то есть
переквантовывает модель на каждый батч. Для изолированного замера это
терпимо, для композита — нет: 19 батчей x вся модель с грид-поиском.
Кеш эту цену не снимает и на композите делает хуже по обеим осям сразу
(закон 19).

`ablate_sym_composite` квантует веса ОДИН раз и кладёт обратно в модель,
а ppl считает с `QuantConfig()`. Эффект: конфиг на 1.5B — 131 с вместо
583-768, на 2.9B — 188 с; своп за все восемь прогонов НЕ вырос ни на
мегабайт, `peak memory footprint` 14.37-14.52 ГБ на 2.9B (bf16 12.17 ГБ
воспроизвёл прежний замер до второго знака, закон 22).

Это оптимизация, а не другая схема, и она обязана быть бит-в-бит
тождественна пути на лету — иначе получится ровно тот сорт тихой
подмены, о котором закон 15. Гейт `--gate` строит ДВЕ модели на 0.1B,
гоняет батч по обоим путям и требует max|Δ| = 0 по логитам: **зелёный,
168 тензоров**. Тонкость, которую гейт и поймал бы: `v_lora` на нулевом
слое в forward НЕ вызывается (v_first ещё не определён), поэтому
предквантование его тоже не трогает.

Независимая проверка харнесса: bf16 сошёлся с прежними числами на обоих
чекпоинтах (1.5B 8.20048 против 8.200484 у `ablate_sym_cmix`, 2.9B
7.15151 на том же 16-последовательностном срезе), а композит REDUCTION —
с таблицей пресетов (1.5B +0.690% против +0.77% реального пути, 2.9B
+1.299% против +1.37%). Расхождение 0.07-0.08 п.п. — это цена fake-пути
против `real_gw=True`, она одинаковая на обоих масштабах.

### Чего НЕ сделано и без чего это не деплоится

- ~~**Нет упаковщика и нет кернеля.**~~ СДЕЛАНО 13.08, см. раздел
  «УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ»: `real_gw=True` собирает файл,
  Metal-GEMV его читает, всё перемерено на реальном пути.
- ~~**Только 1.5B.**~~ ПОДТВЕРЖДЕНО на 2.9B, см. выше: −0.463 п.п.
- ~~**Только изолированно.**~~ КОМПОЗИТ СДЕЛАН на обоих масштабах, см.
  выше: перенос полный, все три языка улучшаются.
- ~~**emb_head не мерен.**~~ ИЗМЕРЕН, см. раздел «emb и head» ниже, и
  head@8 подтверждён композитом на обоих масштабах.
- **ЛОВУШКА СО СТАТИСТИКОЙ.** `collect_act_stats.py` без `RWKVQ_CKPT`
  берёт 1.5B по умолчанию, и файл с именем `*_2p9b_*` может содержать
  статистику 1.5B. Уже случилось. Дальше `_get_ex2` увидит 2048 каналов
  вместо 2560, предупредит и вернёт None, то есть `sym_aw` тихо
  выродится в `sym` -- замер пройдёт, но не тот, что заказан (закон 15).

### Замечание о стоимости прогона

`sym` уложился в 147 с против 583-768 с у остальных при том же объёме
работы. Разница -- попадания в кеш квантованных весов: своп после того
прогона вырос до 1365 МБ, то есть рабочий набор поместился в бюджет, а у
прочих конфигов кеш сдавался (см. закон 19). На корректность не влияет
(кеш бит-в-бит, гейт), но при сравнении ВРЕМЕНИ конфигов между собой это
надо помнить.

---

## emb и head: у ОДНОГО из них деградация значима (13.08)

`tests/ablate_emb_head.py`, изолированно (квантуется РОВНО одна группа,
всё прочее bf16), мультиязычный корпус 38x512 = 19418 предсказаний.

**ВПЕРВЫЕ В ПРОЕКТЕ У ДЕЛЬТ ЕСТЬ ДОВЕРИТЕЛЬНЫЕ ИНТЕРВАЛЫ.** До сих пор
«в пределах шума» говорилось на глаз. Теперь у каждой дельты 95% CI:
парный бутстрэп по последовательностям корпуса, 20000 итераций. Парный --
потому что оба конфига прогнаны по ОДНОМУ тексту, и разброс самого
корпуса (одни чанки просто труднее других) сокращается; непарный мерил бы
дисперсию корпуса вместо дисперсии эффекта и объявил бы шумом почти всё.
В JSON теперь сохраняются per-sequence NLL, поэтому интервалы для любых
прошлых и будущих прогонов пересчитываются без повторного счёта.

**Порог, который из этого следует: ±0.05-0.09 п.п. на 19418
предсказаниях.** Всё, что уже этой полосы, данным корпусом не различимо
в принципе -- сколько ни повторяй прогон. Задним числом это оправдывает
прежние пометки «в пределах шума» (например, emb 6->5 на 1.5B, −0.07
п.п.) и одновременно подтверждает, что композитные выигрыши раскладки
(−0.396 п.п. на cmix, −0.647 на proj у 2.9B) лежат далеко за ней.

### 1.5B (bf16 8.20048)

| конфиг | бит/вес | МБ | Δ | 95% CI | значимо |
|---|---|---|---|---|---|
| emb `asym_sb6_aw`@6 (пресет) | 6.5000 | 109.1 | +0.014% | [−0.056; +0.088] | НЕТ |
| emb `sym_aw`@6 | 6.5625 | 110.1 | −0.024% | [−0.078; +0.031] | НЕТ |
| emb `asym` gw64@6 | 9.0000 | 151.0 | +0.056% | [−0.019; +0.131] | НЕТ |
| emb `asym` gw64@8 | 9.0000 | 151.0 | −0.009% | [−0.065; +0.049] | НЕТ |
| head `asym_sb6_aw`@6 (пресет) | 6.5000 | 109.1 | **+0.122%** | [+0.048; +0.194] | **да** |
| head `sym_aw`@6 | 6.5625 | 110.1 | **+0.056%** | [+0.007; +0.103] | **да** |
| head `asym` gw64@6 | 9.0000 | 151.0 | **+0.317%** | [+0.226; +0.409] | **да** |
| head `asym` gw64@8 | 9.0000 | 151.0 | +0.026% | [−0.001; +0.053] | НЕТ |

**emb НЕ ОТЛИЧИМ ОТ bf16 НИ В ОДНОМ ИЗ ЧЕТЫРЁХ ВАРИАНТОВ.** Лечить
нечего; +42 МБ за gw64@8 там выброшены впустую (−0.022 п.п.
[−0.101; +0.057]). Прежняя формулировка «emb бесплатен ровно до шести
бит» получает недостающую половину: при шести битах он бесплатен НЕ
приблизительно, а в пределах измеримого. (НА 2.9B ЭТО НЕ ТАК — см.
ниже: там emb@6 значим. Величина всё равно втрое меньше head.)

**head -- ЕДИНСТВЕННАЯ группа, где деградация значима и не лечится
раскладкой до конца.** Что показали рычаги:

| рычаг | Δ против пресета | CI | цена |
|---|---|---|---|
| Q6_K-раскладка | −0.066 п.п. | [−0.145; +0.013] | +1.0 МБ |
| `asym` gw64 @8 | −0.096 п.п. | [−0.176; −0.017] | +41.9 МБ |

Раскладка тянет в ту же сторону, что на cmix и proj, но сама по себе на
границе значимости. Восемь бит в gw64 убирают деградацию практически
целиком (+0.026%, CI почти включает ноль) и делают это без размена по
языкам: en +0.036%, ru +0.030%, sr +0.007% -- все три обнуляются разом.

### «Дороже» не значит «лучше»: gw64@6 хуже пресета ВТРОЕ (только на 1.5B)

`asym` gw64 при ШЕСТИ битах -- **+0.317%, то есть в 2.6 раза ХУЖЕ
пресета, стоя при этом 9.000 бит/вес против 6.5.** Дороже на 38% по
размеру и втрое хуже по качеству. Напрашивалось структурное объяснение:
gw64 -- это блок 64 с fp32 scale/min и БЕЗ суперблочной иерархии, тогда
как sb6 берёт блок 32 и тратит биты на квантованные scale/min против
пары fp16 на суперблок, то есть гранулярность опять важнее разрядности
метаданных.

**НА 2.9B ЭТОГО НЕТ: там gw64@6 даёт +0.108% против +0.111% у пресета,
то есть ровно то же.** Объяснение не выдержало проверки и снято;
наблюдение остаётся как отрицательный результат (закон 5) -- на 1.5B
gw64@6 плох, на 2.9B безразличен, почему -- неизвестно. Если однажды
понадобится gw64 на шести битах, мерить заново на целевом чекпоинте.

Второй вывод из той же пары, и он-то ПЕРЕЖИЛ оба масштаба: **в
контейнере gw64 два лишних бита дают −0.291 п.п. на 1.5B и −0.094 на
2.9B ПРИ НУЛЕВОЙ цене в байтах** (контейнер стоит 9.000 при 5, 6 и 8
одинаково). Это ровно обратное тому, что вышло на LoRA-ветках 12.08, где
те же два бита не дали ничего, -- и по той же причине: там вклад группы
в ppl был уже нулевым, отыгрывать было нечего, а у head есть. То есть
«битность в asym gw64 -- бесплатный параметр» верно всегда, а вот «и
потому бесполезный» -- только там, где группа и так не вредит.

### Точка, которой в наборе не было: восемь бит В Q6_K-РАСКЛАДКЕ

Раз на head работают ОБА рычага и они не конкурируют, напрашивалась
третья точка: `sym_aw` при восьми битах. Она стоит 8 + 8/16 + 16/256 =
**8.5625 бит/вес**, то есть ДЕШЕВЛЕ gw64 (9.000) при вдвое меньшем блоке
и 8-битном scale. Упаковщику она ничего не усложняет -- наоборот: при
восьми битах коды это просто байты, без битплоскостей, ради которых у
sb6 заведены `qh` и `qh2`.

На 1.5B: **head ppl 8.2003 против bf16 8.20048, то есть −0.002%** --
деградация исчезает полностью, дешевле gw64 на 9 МБ.

### 2.9B (bf16 7.16598): подтверждено главное, опровергнут побочный вывод

| конфиг | бит/вес | МБ | Δ | 95% CI | значимо |
|---|---|---|---|---|---|
| emb `asym_sb6_aw`@6 (пресет) | 6.5000 | 136.3 | **+0.064%** | [+0.010; +0.116] | **да** |
| emb `sym_aw`@6 | 6.5625 | 137.6 | +0.070% | [+0.015; +0.124] | да |
| emb `asym` gw64@6 | 9.0000 | 188.7 | +0.012% | [−0.058; +0.082] | НЕТ |
| emb `asym` gw64@8 | 9.0000 | 188.7 | −0.010% | [−0.058; +0.035] | НЕТ |
| emb `sym_aw`@8 | 8.5625 | 179.6 | +0.017% | [−0.034; +0.070] | НЕТ |
| head `asym_sb6_aw`@6 (пресет) | 6.5000 | 136.3 | **+0.111%** | [+0.050; +0.173] | **да** |
| head `sym_aw`@6 | 6.5625 | 137.6 | +0.069% | [+0.030; +0.110] | да |
| head `asym` gw64@6 | 9.0000 | 188.7 | +0.108% | [+0.038; +0.182] | да |
| head `asym` gw64@8 | 9.0000 | 188.7 | +0.014% | [−0.002; +0.029] | НЕТ |
| head **`sym_aw`@8** | **8.5625** | **179.6** | **+0.017%** | [−0.003; +0.037] | **НЕТ** |

**`sym_aw`@8 на head ПОДТВЕРЖДЁН на обоих масштабах и ДОМИНИРУЕТ
gw64@8.** Против пресета: −0.094 п.п. [−0.160; −0.027] на 2.9B, значимо;
у gw64@8 −0.098 п.п. при том же качестве, но он стоит 188.7 МБ против
179.6, то есть **на 9.1 МБ дороже при неразличимом результате**. Выбор
между ними не вопрос вкуса: одна точка дешевле другой и не хуже.
По языкам на 2.9B ровно, без размена: en +0.028%, ru +0.024%, sr −0.008%.

**Раскладка сама по себе на head НЕ доказана.** `sym_aw`@6 против
пресета: −0.066 п.п. [−0.145; +0.013] на 1.5B и −0.043 [−0.104; +0.020]
на 2.9B -- ноль внутри интервала оба раза. Направление то же, что на
cmix и proj, но величина втрое меньше и из шума не выходит. **Для head
рычаг -- БИТНОСТЬ, а не раскладка**, и это отличает его от всех прочих
групп: там как раз битность менять было нельзя, а раскладку -- можно.

**ОПРОВЕРГНУТО: «gw64@6 хуже пресета втрое».** На 1.5B он дал +0.317%
против +0.122%, и это выглядело структурным фактом про гранулярность
(блок 64 без суперблока против блока 32 с ним). На 2.9B тот же конфиг --
+0.108% против +0.111%, то есть РОВНО как пресет. Вывод не переносится и
остаётся как отрицательный результат (закон 5): на 1.5B gw64@6 плох, на
2.9B безразличен, объяснения этому нет. Что уцелело из той связки --
второе наблюдение: два лишних бита в gw64 бесплатны по размеру и дают
−0.291 п.п. на 1.5B и −0.094 на 2.9B, то есть работают там, где группе
есть что отыгрывать (в отличие от LoRA-веток 12.08).

**emb на 2.9B СТОИТ денег, в отличие от 1.5B.** +0.064% [+0.010; +0.116]
-- ноль вне интервала, тогда как на 1.5B было +0.014% [−0.056; +0.088].
Это уточняет вывод от 04.08 «квантование emb не стоит ничего по качеству
на обоих масштабах»: на большей модели при шести битах оно стоит, просто
мало. Лечится тем же (gw64@8 даёт −0.074 п.п. значимо, `sym_aw`@8
−0.047 п.п. и уже в шуме), но платить +43-52 МБ за 0.06 п.п. -- размен
совсем другого порядка, чем на head. **Решение: emb оставить на шести
битах в нынешней схеме**, а место в бюджете отдать head.

Технически замер на 2.9B стал возможен только 13.08: до этого
симметричная ветка не резалась полосами, и head на 2.9B требовал бы
порядка 11 ГБ транзиентов поверх загруженной модели (см. дополнение к
закону 20). Два первых захода умерли молча, без Traceback, — ядро в те
же минуты отстреливало idle-процессы по памяти; прошло с
`RWKVQ_GW_CHUNK_MB=64`.

### Что из этого следует для пресета REDUCTION

Все три правки измерены на обоих масштабах, ни одна не даёт размена по
языкам:

| группа | было | стать | цена 1.5B | цена 2.9B |
|---|---|---|---|---|
| cmix | `asym_sb6_aw`@6 | `sym_aw`@6 | +6.3 МБ | +13.1 МБ |
| proj | `asym_sb6`@6 | `sym_aw`@6 | +3.2 МБ | +6.6 МБ |
| head | `asym_sb6_aw`@6 | **`sym_aw`@8** | +34.8 МБ | +43.3 МБ |
| emb | `asym_sb6_aw`@6 | не трогать | — | — |

Итого +44 МБ на 1.5B (3.5% файла) и +63 МБ на 2.9B (2.6%). Бюджет
REDUCTION при этом уходит с +0.690% к примерно +0.12% на 1.5B: композит
cmix+proj дал +0.240%, а изолированный вклад head (+0.122 п.п.) при
переходе на восемь бит обнуляется. **Это оценка, а не замер:** композит
с head@8 НЕ прогонялся, и закон 5 требует его прежде любой правки
пресетов.

Для COMPRESSION ничего из этого не следует: там борьба за мегабайт, а
head@8 -- это +35-43 МБ на файл, который весь целиком 970/1855 МБ.

---

## УПАКОВЩИК И КЕРНЕЛЬ Q6_K СДЕЛАНЫ, ЧИСЛА ПЕРЕМЕРЕНЫ НА РЕАЛЬНОМ ПУТИ (13.08)

Пункт 1 открытых вопросов закрыт целиком: `real_gw=True` больше не
поднимает NotImplementedError, файл собирается, кернель его читает,
композит с head@8 прогнан на обоих масштабах (закон 5), и всё перемерено
на реальном пути.

### Итог одной таблицей (РЕАЛЬНЫЙ путь, `tests/eval_real_sym.py`)

1.5B, bf16 ppl 8.1980, 19418 предсказаний:

| конфиг | МБ | Δ real | Δ fake | поправка | en | ru | sr |
|---|---|---|---|---|---|---|---|
| REDUCTION | 1255.4 | +0.772% | +0.690% | +0.082 | +1.043% | +0.549% | +0.996% |
| + cmix/proj `sym_aw`@6 | 1264.9 | +0.309% | +0.240% | +0.069 | +0.496% | +0.131% | +0.517% |
| + head `sym_aw`@8 | 1299.5 | **+0.192%** | +0.111% | +0.081 | +0.312% | +0.071% | +0.339% |

2.9B, bf16 ppl 7.1630, те же 19418 предсказаний:

| конфиг | МБ | Δ real | Δ fake | поправка | en | ru | sr |
|---|---|---|---|---|---|---|---|
| REDUCTION | 2420.9 | +1.370% | +1.299% | +0.071 | +0.935% | +1.571% | +1.359% |
| + cmix/proj `sym_aw`@6 | 2440.6 | +0.374% | +0.369% | +0.005 | +0.596% | +0.029% | +0.920% |
| + head `sym_aw`@8 | 2483.9 | **+0.273%** | +0.275% | −0.002 | +0.512% | −0.040% | +0.733% |

**Бюджет REDUCTION падает вчетверо на 1.5B (+0.772% -> +0.192% за
+44.1 МБ, 3.5% файла) и впятеро на 2.9B (+1.370% -> +0.273% за +63.0 МБ,
2.6%).** Ни одного размена по языкам ни на одном масштабе: все три языка
улучшаются на каждом шаге.

Харнесс проверен воспроизведением: bf16 сошёлся с прежней таблицей
пресетов до четвёртого знака на ОБОИХ чекпоинтах (8.1980 и 7.1630), а
REDUCTION -- до третьего (+0.772% против записанных +0.77% при тех же
1255.4 МБ; +1.370% против +1.37% при 2420.9 МБ). То есть новый скрипт
меряет то же, что мерил старый, и разница в строках ниже -- это правки,
а не смена методики.

### ОПРОВЕРГНУТО: «поправка fake->real равна 0.07-0.08 п.п. и постоянна»

Так было записано 13.08 по двум точкам, обе -- чистый REDUCTION. Она
воспроизвелась (+0.082 и +0.071), но на sym-конфигах РАЗВАЛИЛАСЬ:

| конфиг | поправка 1.5B | поправка 2.9B |
|---|---|---|
| REDUCTION | +0.082 | +0.071 |
| + cmix/proj sym | +0.069 | **+0.005** |
| + head@8 | +0.081 | **−0.002** |

На 2.9B у sym-конфигов её нет вовсе -- fake и real дают одно и то же в
пределах третьего знака. Объяснения этому у меня нет, и придумывать его
задним числом не стоит: правильный вывод в том, что **поправка есть
свойство КОНФИГА, а не константа пути**, и подставлять 0.07 в чужой
замер нельзя. Практическое следствие: сравнения с llama.cpp больше не
нужно оговаривать «наше число fake-путное» -- надо просто мерить
реальным путём, благо теперь есть чем.

### Что написано

- **`groupwise_sym_fake_dequant(..., return_parts=True)`** -- отдаёт
  `q`/`qs`/`d`, из которых упаковщик просто раскладывает биты. Ровно тот
  же приём, что у sb6: упаковщик НЕ СЧИТАЕТ НИЧЕГО ЗАНОВО, поэтому
  разойтись с fake-путём ему нечем. Нарезка полосами (закон 20) работает
  и в этом режиме -- полосы склеиваются по строкам.
- **`writer._make_qt_gw_sym`** -- реальная упаковка. Новое поле
  `gw_qs` (int8 на блок), `kind: "sym"` в манифесте, `n_blocks` из формы
  `gw_qs`.
- **`codec.dequant_sym`** (numpy, нормативная) и
  **`reader._dequantize_gw_sym`** (torch, быстрая).
- **`backends/metal/quant_linear_sym.py`** -- `SymQuantLinear` и два
  GEMV-кернеля (6 и 8 бит), N-батч встроен параметром NN.
- **`export_mlx`** и **`verify_sidecar_full`** научены раскладке sym.

### Раскладка: почему восемь бит оказались проще шести

Бюджет `bits + 8/gs + 16/(gs*sb)`, замерено по РАЗМЕРАМ БУФЕРОВ, а не
формулой: **8.5625 бит/вес при восьми битах и 6.5625 при шести**, на всех
проверенных формах.

При bits=8 код -- это знаковый байт, и `codes` пишется как есть: ни
битплоскостей, ни сдвигов, ни распаковки в кернеле. Поэтому восемь бит
сделаны первыми -- и именно они нужны head, где рычаг не раскладка, а
битность.

При bits=6 коды едут СО СДВИГОМ +32 в диапазон 0..63 (ровно как
`block_q6_K` в llama.cpp): младший ниббл -- блок-локальным split'ом,
биты 4 и 5 -- двумя битплоскостями. Это не выбор из многих, а
переиспользование: **пара блоков по 16 (32 колонки) занимает 16 байт
нибблов + 4 байта qh + 4 байта qh2 = 24 байта, то есть БАЙТ-В-БАЙТ тот же
гранул, что у sb6 при xbits=2**, и декодер нибблов вместе с мульт-трюком
битплоскостей взяты из кернеля-3 без единой правки. Сдвиг снимается в
кернеле, а не в памяти: sum((q-32)*x) = sum(q*x) - 32*sum(x), и вторая
сумма -- это тот же xbsum, который sb6 уже считает, только на блок 16.
При восьми битах xbsum не нужен вовсе, и кернель его не читает.

**ЛОВУШКА, РАДИ КОТОРОЙ ГЕЙТ КЕРНЕЛЯ И НУЖЕН: порядок регистров другой.**
Split блок-локальный, а блок теперь 16, поэтому колонки внутри пары идут
`l0,l1,h0,h1` (первый блок) и `l2,l3,h2,h3` (второй), тогда как при
gs=32 они шли `l0..l3,h0..h3`. Перепутанный порядок даёт ПРАВДОПОДОБНЫЙ
результат -- те же числа, переставленные внутри блока, -- и ловится
только сверкой с эталоном.

### Вырожденный суперблок: nan, которого не было видно

При amax = 0 (нулевые строки emb -- они реально есть) `d` уходит под
fp16-субнормаль и становится нулём, после чего `qs = round(ssb/d)` = nan.
На fake-выход это не влияло вовсе: `nz=False` зануляет и коды, и
результат, поэтому за все замеры это ни разу не всплыло. Но в ФАЙЛ nan
класть нельзя -- каст nan в int8 не определён. Пишется 0: деквант тогда
даёт `scale = half(0*0) = 0` и `w = 0`, то есть ровно то, что вернул
fake-путь. Найдено рассуждением при написании упаковщика, а не падением,
и закрыто гейтом равенства на настоящем emb.

### Гейты

- `tests/test_sym_packer_parity.py [ckpt ...]` -- ГЛАВНЫЙ. Четыре
  утверждения, все требуют РАВЕНСТВА: упаковщик против fake-пути;
  codec (numpy) против reader (torch); роундтрип через контейнер, включая
  `codec.dequant_key` на переоткрытом файле (это путь rwkv-metal и
  SwiftRWKV -- ни torch, ни пакета `rwkv_quant`); бит/вес по реальным
  размерам буферов. Пять форм из настоящего чекпоинта x 2 битности x
  3 режима. **Зелёный на 1.5B и на 2.9B.**
- `tests/test_sym_kernel.py [ckpt]` -- кернель против
  `x @ dequant(qt).T`, порог 3e-3 как у sb6-гейтов (кернель суммирует в
  другом порядке, чем плотный матмул, требовать равенства нельзя).
  Четыре формы x 2 битности x N=1/3/16/128, GEMV и GEMM. **Зелёный,
  relmax 1.2-2.8e-3.**
- Прежние гейты не тронуты: `test_codec_parity`, `test_sym_chunk_parity`,
  `test_groupwise_parity`, `test_gw_kernel_int6` -- зелёные.
  `ablate_sym_composite --gate` -- зелёный (max|Δ| логитов = 0).

### Чего в этой работе НЕТ

- **Скорость не мерена ВООБЩЕ.** Ни A/B с sb6, ни свипа (NSG, RS):
  конфигурация взята та, что свип protoD выбрал для тех же форм у sb6, и
  считать её оптимальной оснований нет. При блоке 16 масштабов вдвое
  больше на ту же строку, и это может стоить полосы. Мерить только
  чередованием в одном процессе (закон 1) и только без свопа (закон 11).
- **Пресеты НЕ ТРОНУТЫ.** Числа получены, решение о правке -- за
  владельцем проекта. Кандидат: cmix `sym_aw`@6, proj `sym_aw`@6,
  head `sym_aw`@8, emb не трогать.
- **Для COMPRESSION ничего из этого не следует**: head@8 -- это +35-43 МБ
  на файл, который весь целиком 970/1855 МБ.
- **Фьюз r/k/v не поддержан.** `GwQuantLinearFused` требует
  `GwQuantLinear`, поэтому при proj в sym фьюз просто не строится (не
  падает). Цена -- те −0.8 мс, что даёт FUSE.
- **rwkv-metal и SwiftRWKV не портированы.** Порядок работ прежний:
  rwkv-quant -> rwkv-metal -> SwiftRWKV.
- **`test_gw_kernel.py` (int4/5, COMPRESSION) КРАСНЫЙ**, и это НЕ
  регресс: reverse-apply патча даёт то же число до последнего знака
  (`blocks.0.ffn.value.weight`, N=3, rel 3.9617e-3 при пороге 3e-3).
  Разбираться отдельно.

### Память (только системными командами, закон 11/22)

`/usr/bin/time -l`, реальный путь на 2.9B: bf16 12.31 ГБ, REDUCTION
10.71, +cmix/proj 11.80, +head@8 10.90 ГБ. Композит fake-пути с head@8 на
2.9B -- 14.02 ГБ при `RWKVQ_GW_CHUNK_MB=64`. Своп за fake-композит на
2.9B вырос на 1.9 ГБ (412 -> 2345 МБ), за bf16-прогон реального пути --
на 2.3 ГБ; для замеров КАЧЕСТВА это приемлемо, для замеров скорости --
нет, и перед ними своп надо сбрасывать.

---

## ФЬЮЗ r/k/v ДЛЯ sym: +1.01 мс, БИТ-В-БИТ (15.08, 1.5B)

Первый кусок оп-каунта. `GwQuantLinearFused` требует `GwQuantLinear`,
поэтому целевой пресет (proj в sym) фьюза не получал ВООБЩЕ -- он не
падал, он молча не строился, и документированные −0.8 мс проходили мимо.

### Числа (`test_fuse_parity /tmp/reduction_sym_head8.rwkvq`)

| | мс/ток | ток/с |
|---|---|---|
| FUSE=False | 17.83 | 56.1 |
| **FUSE=True** | **16.81** | **59.5** |

**+1.01 мс, то есть 5.7%** -- больше, чем −0.8 мс у sb6. Пять пар
чередованием, медиана; разброс по парам виден в логе и отношение в нём
устойчиво. Паритет: relmax логитов 1.84e-4 (у sb6 1.77e-4), greedy 48
токенов совпал 48/48.

Цена -- память: дубль r/k/v ~248 МБ на 1.5B (по арифметике буферов),
пик загрузки 2290 -> 2664 МБ. То есть **+5.7% декода за +250 МБ**, и это
решение владельца, а не инженерная развилка: у REDUCTION функция
полезности «максимум качества при приемлемом размере», про резидентную
память там ничего не сказано.

### Как сделано: параметр, а не вторая копия кернеля

`OUT_PER` в ТОМ ЖЕ исходнике: строки K матриц конкатенируются, а строка
сама говорит, какой вход ей нужен (`xi = row0 / OUT_PER`). При
OUT_PER = OUT_C это даёт xi = 0 и NK = 1, то есть прежнюю индексацию
ровно. Отдельного фьюзнутого кернеля НЕТ сознательно -- закон 23.

Но перегенерация исходника означает, что «в нефьюзнутом пути ничего не
изменилось» надо ДОКАЗАТЬ. Гейт `tests/test_sym_fuse_parity.py`:

1. выходы GEMV заморожены в файл ДО правки (семь реальных форм из
   чекпоинта, обе битности, N=1/3/16) и сверяются ПОСЛЕ -- РАВЕНСТВО;
2. фьюзнутый вызов против K отдельных -- тоже РАВЕНСТВО, а не порог:
   фьюз проекций не меняет порядок суммирования внутри строки, поэтому
   право требовать бит-в-бит здесь есть (в отличие от фьюза лор и
   лерпов, где relmax законен).

Оба зелёные. Заморозка делалась на `git stash` рабочей правки -- иначе
эталон описал бы то, что проверяет.

---

## ПОКОМПОНЕНТНОЕ РАЗЛОЖЕНИЕ ШАГА: ДОМИНАНТА -- LoRA (15.08)

Вопрос владельца проекта: не сидит ли в шаге ОДИН медленный компонент,
который тормозит всё. Ответ: сидит, и это не то, что подозревали.

`tests/profile_components.py` -- аблации на КОМПИЛИРОВАННОМ пути:
компонент подменяется дешёвой заглушкой ДО `mx.compile` (иначе компилятор
оттрассирует старую ветку), полный и аблированный чередуются, берётся
медиана разностей. Своп не двигался.

### FUSE=False (умолчание), полный шаг 17.72 мс

| компонент | мс | % шага | трафик/ток | **ГБ/с** |
|---|---|---|---|---|
| cmix целиком | 7.58 | 42.2% | 660.7 МБ | 87.2 |
| проекции r/k/v/o | 3.89 | 21.7% | 330.3 МБ | 84.9 |
| **LoRA-ветки** | **2.15** | **12.0%** | **100.1 МБ** | **46.6** |
| голова | 1.54 | 8.5% | 143.7 МБ | 93.3 |
| WKV-скан | 0.84 | 4.6% | ~1 МБ | -- |
| layer_norm (ln1/ln2/out) | 0.45 | 2.5% | -- | -- |
| group_norm | 0.24 | 1.3% | -- | -- |
| l2_norm ключа | -0.00 | -- | -- | -- |
| token-shift | -0.02 | -- | -- | -- |

**LoRA идёт вдвое медленнее всего остального по полосе: 46.6 ГБ/с против
85-93.** Все прочие крупные статьи лежат на 82-90% измеренного потолка
104 ГБ/с, то есть у своей полки, а LoRA -- на 45%. (Цифра 2.15 мс позже
УТОЧНЕНА до 2.50 -- см. раздел «LoRA: полная цена».)

### WKV-СКАН ДЁШЕВ -- ГИПОТЕЗА НЕ ПОДТВЕРДИЛАСЬ

Подозрение было структурное: WKV приходит из `rwkv-metal`, мы его никогда
не мерили отдельно и сами не оптимизировали. Замер: **0.84 мс, 4.6%
шага**. Состояние [32, 64, 64] за токен -- около мегабайта трафика.
Трогать нечего.

### «УПРОЩЕНИЕ МАТЕМАТИКИ» ИМЕЕТ ПОТОЛОК 3.8%

Все чисто математические компоненты вместе -- l2_norm, token-shift,
group_norm, layer_norm -- стоят 0.67 мс, причём два из них НЕОТЛИЧИМЫ ОТ
НУЛЯ. Прямое следствие того, что `mx.compile` сливает поэлементные
цепочки сам. Упрощать формулы ради скорости смысла нет; экономить можно
только на запусках и на байтах.

### FUSE=True, полный шаг 16.66 мс

| компонент | мс | против FUSE=False |
|---|---|---|
| cmix | 7.71 | +0.13 (шум) |
| проекции r/k/v/o | 3.83 | −0.06 (шум) |
| LoRA-ветки | **1.57** | **−0.58** |
| голова | 1.60 | +0.06 |
| WKV | 0.79 | −0.05 |

Совпадение проекций (3.83 против 3.89) -- КОНТРОЛЬ: фьюз r/k/v не должен
менять их стоимость, и не меняет. Весь выигрыш фьюза сидит в LoRA, и
берёт он его НЕ трафиком, а укрупнением запусков.

**ЛОВУШКА, НА КОТОРУЮ ЗАМЕР НАСТУПИЛ.** Фьюз строится ЛЕНИВО и кеширует
КОПИИ весов (`_rkv_fused`, `wav_At`, `wav_Bt`), поэтому подмена
`tm.r_proj` или `tm.w_lora_A` меняет только нефьюзнутую ветку. Первый
прогон с FUSE=1 дал дельту проекций 1.26 мс вместо 3.83 -- реально
аблировался один `o_proj`. Числа выброшены, в `ab()` добавлен сброс
`_fused_built`. Общее: аблация монкипатчем видит только то, что читается
ПОСЛЕ неё, а не то, что закешировано раньше.

### Оговорка про аддитивность

Сумма дельт 16.66 мс при шаге 17.72 -- близко, но совпадение случайное:
аблации НЕ аддитивны (убрав компонент, меняешь и то, что с чем
перекрывается). Задача была найти доминанту, а не построить бюджет.

---

## LoRA: ПОЛНАЯ ЦЕНА 2.50 мс, И ПОЧЕМУ 46.6 ГБ/с (15.08)

`tests/probe_lora_cost.py`. Своп не двигался.

### СНАЧАЛА -- ПОПРАВКА К СОБСТВЕННОМУ ЗАМЕРУ

Записанные 2.15 мс ЗАНИЖЕНЫ. Аблация в `profile_components` подменяла
ранги на 1, но матмулы ОСТАВЛЯЛА -- все 192 запуска происходили в обеих
ветках и сократились в разности. Подмена самого `_mm` на выдачу нулей
(уходят и байты, и запуски) даёт **2.50 мс, 13.5% шага**. Разность
0.35 мс -- цена ЗАПУСКОВ, остальные 2.15 -- чтение 100.1 МБ на 46.6 ГБ/с.

Мораль общая: аблация «сделать компонент маленьким» и аблация «убрать
компонент» меряют РАЗНОЕ, и первая молча вычитает только ту часть, что
зависит от размера.

### ГИПОТЕЗЫ, КОТОРЫЕ ОТПАЛИ

**Раскладка в памяти -- почти ни при чём.** `_mm(x, w)` считает `x @ w.T`
при том, что веса получены как `_dense(...).T`, то есть транспонирований
два. Посчитать `A.T` заранее и непрерывно даёт по сумме веток 1.11-1.40x
-- заметно, но это не половина полосы.

**Запуски -- 0.35 мс из 2.50.** Тоже не главное.

Остаётся ФОРМА: ранги 96/96/64/256 против 2048. Поштучно ветки дают
17-32 ГБ/с, и только `g_lora` (ранг 256) выходит на 90. Чем крупнее
матрица, тем ближе к полке -- это и указывает, что делать.

### СКЛЕЙКА ЧЕТЫРЁХ down-ПРОЕКЦИЙ В ОДИН МАТМУЛ

Ранги 96+96+64+256 = 512, склейка по ВЫХОДНОЙ оси, поэтому паддинга нет
вовсе и каждая ветка получает свой срез -- значения те же до бита.
Микрозамер: 58.4 -> 13.3 мкс, то есть 4.39x.

**НО ЭТО ЧИСЛО БРАТЬ НЕЛЬЗЯ.** Оно даёт 157.6 ГБ/с при измеренном потолке
104 -- значит склеенная матрица (2.1 МБ) сидит в системном кэше, и
мерится не DRAM. В настоящей модели LoRA всех 24 слоёв -- 100 МБ, в кэш
не влезает. Микрозамер честен лишь в относительной цене запусков и формы;
величину выигрыша он завышает.

### ЧТО ЭТО ЗНАЧИТ ДЛЯ ЦЕЛИ «90+ ГБ/с»

Одной склейки мало: 2.15 мс из 2.50 -- это ЧТЕНИЕ БАЙТОВ, и оно
сократится, только если байтов станет меньше. Нужны ОБА рычага: склейка
(меньше запусков, крупнее операция) и квантование (меньше байтов). LoRA
сейчас едет плотным fp16 -- 100.1 МБ за токен; при 8 битах в affine
gs=64 это 53.2 МБ, при 6 -- 40.7 МБ.

### ПОДВОХ: РЕПАК НЕ БЕСПЛАТЕН ПО КАЧЕСТВУ

Соблазнительная мысль: в `.rwkvq` LoRA уже лежит в `asym gw64`, то есть
`w = q*scale + min` по группам 64 -- ровно контейнер mlx-affine, значит
репак точный и ppl мерить незачем. **Неверно, и обе причины проверены
чтением файла:**

1. **Ось не та.** Writer квантует LoRA по СЫРЫМ ключам state_dict, до
   транспозиции (задокументировано в докстринге `quant_model`), а
   `_dense(...).T` тензор переворачивает. Группы в итоге лежат вдоль
   ВЫХОДНОЙ оси матмула, а `quantized_matmul` требует их вдоль ВХОДНОЙ.
   Репак в нужной ориентации -- ДРУГОЕ квантование, а не перекладка.
2. **Формы ragged.** `blocks.0.att.w1` -- [2048, 96] при gs=64: блоков
   два, второй неполный (64+32), а `mx.quantize` требует кратности. Для
   up-веток (ранги 96/64/256) при gs=64 то же -- там годится только
   gs=32.

Значит квантование LoRA под нативный матмул -- НОВОЕ квантование, и ppl
на него нужен. Ожидание хорошее (изолированный вклад группы при шести
битах +0.006% на 19418 предсказаниях, замер 12.08), но это ожидание.

### План, если браться

1. Склейка четырёх down-проекций в одну матрицу [512, D] на слой.
   Точная, качества не касается; гейт -- равенство выходов.
2. Квантование склеенной матрицы `mx.quantize(gs=64)` и
   `mx.quantized_matmul`; для up-веток отдельно, там gs=32.
3. ppl-гейт на мультиязычном корпусе: это новое квантование, а не репак.
4. Сквозной A/B шага -- микрозамеры здесь врут в плюс.

Потолок статьи -- 2.50 мс (13.5% шага), реалистично 1.0-1.3.

---

## LoRA: ЧТО ОКАЗАЛОСЬ РЫЧАГОМ, А ЧТО ФАНТОМОМ ЗАМЕРА (16.08, 1.5B)

Статья закрыта: **декод 17.97 -> 16.65 мс/ток (+1.32 мс, 7.9%),
55.6 -> 60.1 ток/с** на умолчательном пути и **16.48 -> 15.88
(+0.60 мс, 3.7%), 60.7 -> 63.0 ток/с** на фьюзнутом. Префилл не тронут:
994.9 -> 992.7 мс (в пределах разброса). Пять раундов чередованием в
одном процессе, разброс 1.3-3.8%, своп не двигался ни в одном прогоне.

Но ни одно из двух записанных объяснений не выжило, и это важнее самих
миллисекунд.

### ОПРОВЕРГНУТО: «склейка четырёх down-проекций в один матмул [512, D]»

Такого матмула НЕ СУЩЕСТВУЕТ. У четырёх веток РАЗНЫЕ входы: `xw`, `xa`,
`xv`, `xg` -- это `x`, слитый с token-shift по СВОЕМУ коэффициенту.
Прежний микрозамер (`probe_lora_cost`) считал `x @ Acat` с ОДНИМ общим
входом, то есть мерил операцию, которой в модели нет. Отсюда и 4.39x.

Точная склейка возможна, но платная. Поскольку `x_i = x + xx*c_i`, лерп
складывается в матрицу: `[A | A*c]` по входу `z = [x, xx]` даёт ровно те
же значения одним запуском -- ценой ВДВОЕ большего числа строк весов
(у каждого ранга появляется копия под `xx`). Замерено на всём стеке
(`tests/probe_lora_shapes.py`, рабочий набор 100 МБ, а не 2.1 МБ одного
слоя):

| вариант down-проекций | мс на стек | МБ/слой | против базы |
|---|---|---|---|
| fp16 порознь (как сейчас) | 0.864 | 2.097 | база |
| fp16 батч wav+g (нынешний FUSE) | 0.971 | 2.228 | 0.89x |
| **fp16 склейка [512, 2D]** | **1.321** | **4.194** | **0.65x** |
| q6 склейка [512, 2D] | 0.746 | 1.704 | 1.16x |
| **q8 порознь** | **0.638** | **1.114** | **1.36x** |

Сквозным замером то же: склейка при восьми битах даёт 16.99 мс/ток
против 16.65 у поветочного квантования, то есть отдаёт четверть
выигрыша. **Склейка снята как рычаг** (закон 5).

### ОПРОВЕРГНУТО И ВТОРОЕ: «выигрыш придёт от байтов»

Записано было, что LoRA едет плотным fp16 (100.1 МБ за токен) и
квантование срежет трафик. Трафик оно и правда режет -- но время от
этого не зависит:

| | мс/ток | трафик LoRA | ppl Δ |
|---|---|---|---|
| fp16 | 17.97 | 100.1 МБ | база |
| **q8 порознь** | **16.65** | 55.0 МБ | +0.012% |
| q6 порознь | 16.70 | 42.6 МБ | +0.072% |

**Шесть бит на 23% легче восьми и при этом НЕ быстрее** (16.70 против
16.65 -- восемь бит даже номинально впереди). Значит выигрыш пришёл не
от трафика, а от ТОГО, ЧТО СЧИТАЕТ КЕРНЕЛЬ: `mx.quantized_matmul` на
формах [96..256, 2048] эффективнее плотного fp16-матмула, и на сколько
именно -- от битности почти не зависит.

Практическое следствие прямое: **брать восемь бит.** Они не дороже по
времени, вчетверо мельче сетка, и по ppl разница видна (+0.012% против
+0.072%) даже там, где обе точки в шуме.

Это же объясняет, почему прежняя попытка (`LORA_Q8`, 19.07) дала
x1.004: она квантовала БАТЧЕНЫЕ стеки фьюза, то есть меняла байты, не
меняя форму -- а форма батча (три матрицы с паддингом ранга v 64 -> 96)
и в fp16 хуже поветочной (0.971 против 0.864 по замеру выше). Объяснение
согласуется с двумя независимыми замерами, но отдельно не проверялось.

### ppl: +0.012%, и это НЕ репак

`tests/eval_lora_quant.py`, мультиязычный корпус 38x512 = 19418
предсказаний, парный бутстрэп по последовательностям (20000 итераций),
база -- тот же файл с плотным fp16, то есть деквант `.rwkvq` как есть.

| конфиг | ppl | Δ | 95% CI | значимо | en | ru | sr |
|---|---|---|---|---|---|---|---|
| fp16 (база) | 8.21373 | -- | -- | -- | -- | -- | -- |
| **sep@8** | 8.21469 | **+0.012%** | [-0.006; +0.029] | **НЕТ** | +0.022% | +0.011% | +0.002% |
| sep@6 | 8.21965 | +0.072% | [-0.004; +0.146] | НЕТ | +0.171% | -0.007% | +0.150% |
| glue@8 | 8.21471 | +0.012% | [-0.006; +0.029] | НЕТ | +0.024% | +0.012% | -0.000% |

Гейт был обязателен, и вот почему это не формальность. Соблазнительно
считать перевод в контейнер mlx-affine РЕПАКОМ: в `.rwkvq` LoRA уже лежит
в `asym gw64`, то есть `w = q*scale + min` по группам 64. Но writer
квантует их по СЫРЫМ ключам state_dict, ДО транспозиции, поэтому группы
идут вдоль ВЫХОДНОЙ оси матмула, а `quantized_matmul` требует их вдоль
ВХОДНОЙ. Здесь квантование сделано заново: down-проекции группами вдоль
D (gs=64, кратность есть всегда), up-проекции -- вдоль ранга (gs=32,
делит 96, 64 и 256). Ragged-случай, которым пугал прежний разбор,
исчезает сам: он был свойством дисковой оси, а не этой.

Попутно харнесс проверен воспроизведением: база 8.21373 против
записанных 8.1980 x (1 + 0.192%) = 8.2137 -- сходится до пятого знака,
то есть новый скрипт меряет то же, что `eval_real_sym`.

### Память: три РАЗНЫХ числа, и путать их нельзя

`tests/bench_lora_mem.py` + `/usr/bin/time -l`, чередование, три раунда;
живые буферы воспроизводятся до 0.1 МБ, пик -- до 1%.

| состояние | живые буферы | пик загрузки |
|---|---|---|
| fp16 (как сейчас) | 1506.8 МБ | 2183.4 МБ |
| обе копии (цена ВКЛЮЧЕНИЯ флага) | 1563.1 (+56.3) | 2365 (+182) |
| только квантованные (цена ВНЕДРЕНИЯ) | **1463.0 (-43.8)** | 2366 (+183) |

Обе копии живут одновременно СОЗНАТЕЛЬНО: пока они есть, A/B делается
подменой одного флага в одном процессе (закон 27), а не сравнением
процессов, где разброс втрое-вчетверо больше (закон 24).
`drop_lora_dense()` освобождает плотные и после этого fp16-путь ПАДАЕТ с
объяснением, а не подменяется молча (закон 15).

**Пик загрузки при этом растёт в обоих случаях, и это не парадокс:**
квантование делается ПОСЛЕ сборки всей модели, поэтому в момент пика
живы обе копии плюс транзиенты `mx.quantize`. Чинится квантованием
по слою прямо в конструкторе, с немедленным отпусканием плотной копии --
не сделано.

### Гейты

- `tests/test_lora_quant_parity.py` -- четыре утверждения. Главное из них
  второе: **квантованный путь ДЕЙСТВИТЕЛЬНО включился** (буферы построены
  И выход отличается от fp16). Без него гейт сравнивал бы fp16 с fp16 и
  был бы зелёным всегда -- ровно та ловушка, на которой погорела аблация
  фьюза. Дальше: relmax логитов (2e-2 префилл / 4e-3 декод при шести
  битах, 5e-3 / 1e-3 при восьми -- воспроизводит записанную ошибку весов
  asym@6 1.94e-2), greedy **48/48 во всех четырёх вариантах и на обоих
  путях**, и склейка против поветочного отдельно (перепутанный срез дал
  бы правдоподобные числа -- закон 8). **ЗЕЛЁНЫЙ.**
- Рефакторинг fp16-пути (все три ветки -- `__call__`, `forward_stateful`,
  фьюзнутая -- переведены на общий `_lora`) сверен с ЗАМОРОЖЕННЫМИ ДО
  правки выходами: **бит-в-бит по всем семи выходам**. Заморозка делалась
  на `git stash`, иначе эталон описывал бы то, что проверяет.
- `drop_lora_dense` против недропнутого: **max|Δ| = 0** (буферы те же).
- Побочно: стеки лор фьюза (`wav_At`/`wav_Bt`, 57.8 МБ) теперь строятся
  ОТДЕЛЬНО и лениво -- при `LORA_Q` они не читаются вовсе, и лежать
  мёртвым грузом им незачем. Это ровно то, что 15.08 нашлось у дубля
  r/k/v, только этажом ниже.

### Чего в этой работе НЕТ

- **Умолчание не тронуто** (`LORA_Q = None`). Включение -- решение
  владельца: +56 МБ резидентно (или -44, если освобождать плотные) за
  7.9% декода.
- **Префилл фьюзнутого пути с квантованными ветками ХУЖЕ на 4%**
  (989.6 -> 1029.2 мс): там при T=512 батченый fp16-матмул -- хороший
  GEMM, а восемь отдельных квантованных хуже. На нефьюзнутом пути этого
  нет (994.9 -> 992.7). Лечится диспетчеризацией по T (как уже сделано
  для `_rkv_fused`, который включается только при B*T == 1) -- НЕ
  сделано и НЕ мерено.
- **2.9B не мерен вовсе** -- ни скорость, ни ppl. Закон 10 требует
  проверки на втором масштабе прежде любого внедрения.
- **Остаток статьи не разложен.** Было 2.37-2.50 мс, сняли 1.32 --
  сколько осталось и что там теперь лежит, покомпонентным разложением не
  перемерено.

---

## ШТАТНЫЙ mlx-affine БЫСТРЕЕ НАШЕГО КЕРНЕЛЯ НА ОБЕИХ ОСЯХ (15.08)

**Это переоткрывает закрытую тему** («гибрид mlx-affine ради скорости»
лежал в списке «не переоткрывать без новых данных»). Новые данные -- прямой
A/B, и он говорит не то, что мы предполагали.

Чекпоинт MollySophia `rwkv7-1.5B-g1g-ctx8192-mlx-6bit` квантован штатным
`mx.nn.quantize` (affine, group_size=64, bits=6) и потому целиком идёт
через родной `mx.quantized_matmul`. То есть это и есть «мы после репака»,
только без нашей сетки кодов и без AW.

### Замер (`tests/bench_molly_ab.py`, обе модели в памяти, чередование)

Формы совпадают полностью (hidden 2048, 24 слоя, 32 головы по 64,
vocab 65536, ранги 96/96/64/256), бит/вес почти: affine gs=64 при шести
битах стоит 6 + 2*16/64 = **6.5** -- ровно как наш sb6. Своп 356 МБ,
не двигался. Два независимых прогона (4 и 7 раундов) дали одно и то же
до десятых.

| | pp512 | tg | файл | трафик/ток | ГБ/с на шаге |
|---|---|---|---|---|---|
| наша (sym + head@8) | 547.2 т/с | 55.3 т/с | 1299.8 МБ | 1234.7 МБ | 68.3 |
| **штатный mlx-affine** | **761.9 т/с** | **65.3 т/с** | 1272.0 МБ | 1190.6 МБ | **77.7** |

**Префилл в 1.39 раза быстрее нас, декод -- в 1.18.** Трафика она читает
на 3.6% меньше (у нас голова на восьми битах), но по ГБ/с быстрее на
13.7% -- то есть дело не в байтах, а в кернеле.

### И это переписывает сравнение с llama.cpp

| | pp512 | tg128 |
|---|---|---|
| наша REDUCTION | 518.4 | 56.67 |
| Q6_K+imatrix | 705.5 | 48.59 |
| **штатный mlx-affine 6bit** | **761.9** | **65.3** |

Штатный MLX **обгоняет llama.cpp на префилле** (761.9 против 705.5), там
где мы ей проигрываем в 1.36 раза. По декоду он быстрее и её, и нас.
Формулировка «мы быстрее llama.cpp по декоду» остаётся верной, но
перестаёт быть интересной: быстрее обоих третий.

### ГДЕ ИМЕННО МЫ ПРОИГРЫВАЕМ: НЕ КЕРНЕЛЬ, А GEMM-ПУТЬ

`tests/bench_gemv_vs_native_ab.py` -- наш линейный слой против
`mx.quantized_matmul` на ОДНИХ И ТЕХ ЖЕ значениях и формах (те же веса,
разложенные в affine той же битности), три точки: native / наш полный
вызов / наш кернель без обвязки.

**Декод (N=1): наш GPU-кернель идёт вровень**, 1.02-1.10x от native на
крупных формах, обвязка добавляет ещё 1.05-1.20x. То есть отставание на
декоде -- это питоновский слой (отдельная операция `xbsum` на каждый
вызов, reshape'ы) плюс наша голова на восьми битах, а НЕ ядро.
ОГОВОРКА: микрозамер при N=1 шумен (у [2048, 2048] между прогонами
26 и 76 ГБ/с), потому что op стоит десятки микросекунд при поле
синхронизации 0.238 мс. Доверять при N=1 надо покомпонентному
разложению (84.9-93.3 ГБ/с), а не этой таблице.

**Префилл: проигрываем ИМЕННО ЗДЕСЬ, и это другой код.** При
N >= GEMM_MIN_BATCH_NB наш путь уходит с кернеля на `_dequant_w` +
плотный матмул, то есть материализует матрицу целиком:

| форма | N=128 | **N=512** |
|---|---|---|
| att.receptance [2048, 2048] | 3.72x | **1.82x** |
| ffn.key [8192, 2048] | 6.59x | **2.38x** |
| ffn.value [2048, 8192] | 6.36x | **2.38x** |
| head [65536, 2048] | 1.80x | **1.19x** |

N=512 -- это то, что реально видит слой на pp512; при N=128 постоянная
цена `_dequant_w` амортизируется вчетверо хуже, поэтому та колонка даёт
ВЕРХНЮЮ оценку, а не рабочую. Голова почти не страдает (1.19x): её
матрица настолько велика, что деквант тонет в матмуле.

ПОЧЕМУ СУММА НЕ СХОДИТСЯ С E2E, И ЭТО НЕ ОШИБКА. Сумма поштучных времён
при N=512 даёт 1327 мс на 24 слоя при измеренном префилле 961 мс.
Причина в том, что ИЗОЛИРОВАННЫЙ замер запрещает перекрытие: каждый вызов
здесь обрамлён синхронизацией, тогда как в настоящем графе r/k/v
независимы и деквант следующей матрицы идёт поверх матмула текущей.
Поэтому поштучные отношения годятся, чтобы сказать ГДЕ разрыв, и не
годятся, чтобы сказать СКОЛЬКО он стоит; «сколько» уже измерено
сквозным A/B -- 1.39x.

### Чего этот замер НЕ говорит

**Ничего о качестве.** Чекпоинт другой (g1g против нашего g1h), поэтому
ppl между ними не сопоставим в принципе, и здесь он не мерился. Весь
смысл нашей раскладки -- сетка кодов и AW; affine при тех же 6.5 бит/вес
даёт другую сетку, и чего она стоит по ppl НА НАШЕМ чекпоинте -- открытый
вопрос, который решается одним прогоном.

Оговорка к разложению: если считать, что не-GEMV часть шага у обеих
моделей одинакова (код forward общий, 4.06 мс), то их GEMV идёт на
~106 ГБ/с против наших ~88, то есть у потолка 104. Но 106 ВЫШЕ потолка,
значит либо не-GEMV часть у них дешевле, либо оценка трафика неточна.
Разделяется это `bench_step_decompose` на их модели -- не сделано.

### Что отсюда следует по работам

Репак в родной `mx.quantized_matmul` перестаёт быть «оптимизацией
префилла» и становится главным рычагом СКОРОСТИ ВООБЩЕ. Арифметика
раскладок при этом жёсткая:

| путь | бит/вес | против нынешних 6.5-6.5625 | качество |
|---|---|---|---|
| sb6 -> affine gs=32 | **7.0** | +7.7% (+96 МБ на 1.5B) | БИТ-В-БИТ наше: блок 32 со scale и min ложится на affine точно |
| sym -> affine gs=16 | **8.0** | +23% (+290 МБ) | бит-в-бит наше, но дорого |
| квантовать сразу affine gs=64 | 6.5 | 0 | ДРУГАЯ сетка, ppl неизвестен |

Первая строка -- самая интересная: точный репак нашего sb6 без единого
изменения значений, ценой 7.7% размера, ради 1.2-1.4x скорости. Для
REDUCTION с его «+50 МБ ради качества приемлемы» это другой размен
(+96 МБ ради скорости), но порядок тот же.

Проверять надо в этом порядке: (1) `mx.quantize` gs=64 на НАШЕМ чекпоинте
-- сколько стоит их сетка по ppl; (2) точный репак sb6 -> affine gs=32 и
гейт равенства декванта; (3) замер скорости репакованного.

И отдельно, ДЕШЕВЛЕ ВСЕГО: репак нужен не везде. Разрыв на декоде даёт
обвязка, а не ядро, а на префилле -- `_dequant_w` + плотный матмул. Значит
можно оставить наш GEMV на декоде (он вровень) и уводить в
`mx.quantized_matmul` ТОЛЬКО GEMM-ветку, где мы проигрываем 1.8-2.4x.
Тогда репак нужен как ВТОРАЯ раскладка для префилла, а не как замена
формата: платить +7.7% размера на диске не придётся, платить придётся
временем на переупаковку при загрузке или памятью на две копии. Это
третий размен, и он не мерился.

---

## ЗАДАЧНАЯ МЕТРИКА: РАНЖИРОВАНИЕ КВАНТОВАНИЯ НЕ ЗАМЕЧАЕТ (16.08, 0.4B)

Всё качество в этом проекте меряно ppl и KL, то есть ПО ЛОГИТАМ. У
векторной модели логитов нет вовсе -- у неё нет и головы. Значит про
главного потребителя REDUCTION мы до сих пор не знали ничего.

**Модель**: RWKV-7 0.4B, дообученная владельцем под семантический поиск
(C=1024, 24 слоя, ранги 64/64/32/128). Имена параметров восстановлены из
тренировочного снимка (`master_params` -- плоский список без имён) сверкой
ВСЕХ форм: 794 тензора, **ноль расхождений**. Головы у неё нет, в
`.rwkvq` кладётся нулевая заглушка.

**Рецепт эмбеддинга взят из её кода**, а не придуман
(`EmbeddingRWKV/embedding/sft_curriculum/src/dataset.py`): к тексту
дописывается ОДИН EOS (65535), эмбеддинг -- скрытое состояние на его
позиции после `ln_out`, L2-нормированное. Длина фиксирована (256),
лишнее режется СЛЕВА -- как у них; хвост добивается нулями, что в
причинной модели на позицию EOS не влияет и даёт одну форму на все батчи.

**Данные**: LitRetrieval (ru/en), строки задачи `retrieval` -- запрос с
инструкцией против релевантного и нерелевантного отрывка. 1000 троек на
измерение, следующие 24 последовательности -- на статистику активаций
(пересечения нет).

### Результат

| конфиг | ppl 1.5B (real) | KL 1.5B | точность | зазор | дрейф 1−cos |
|---|---|---|---|---|---|
| bf16 | -- | -- | **80.00%** | 0.1349 | -- |
| REDUCTION прежний | +0.772% | -- | 80.00% | 0.1348 | **4.41e-4** |
| + cmix/proj sym, head@8 | +0.192% | 0.003813 | 80.00% | 0.1348 | **3.36e-4** |
| REDUCTION нынешний | +0.108% | 0.002908 | 79.70% | 0.1348 | **2.85e-4** |

**ДРЕЙФ ВЕКТОРОВ УПОРЯДОЧЕН РОВНО ПО КАЧЕСТВУ** -- 4.41 -> 3.36 -> 2.85,
монотонно и по ppl, и по KL. Векторное пространство разницу между
пресетами видит, и выбор бит по KL был не пустым.

**А ЗАДАЧА ЕЁ НЕ ВИДИТ.** Точность 80.00 / 80.00 / 79.70 (последнее --
три тройки из тысячи, 95% CI [−0.90; +0.30] включает ноль), зазор
одинаков до четвёртого знака во всех трёх конфигах при СЕМИКРАТНОЙ
разнице в ppl. Интервал на зазор при этом узкий ([−0.0003; +0.0001]) --
то есть это измеренный ноль, а не «не хватило чувствительности».

### Чего этот замер НЕ говорит

- **Задача лёгкая.** Разделение крупное (cos+ 0.76 против cos− 0.62,
  зазор 0.135), кандидатов ровно два. Recall@k по большому пулу на
  порядки чувствительнее пары, и там картина может быть другой.
- **Масштаб другой.** Это 0.4B; закон 10 в этом проекте разворачивал
  выводы дважды. На 1.5B/2.9B не проверено.
- **Одна модель и один домен** (художественные тексты ru/en).

### Что из этого следует для пресета

Для ретривала такого рода доплаченные 180 МБ купили полуторакратное
уменьшение дрейфа векторов и НИЧЕГО на уровне решения. Если целевое
применение -- поиск, у REDUCTION есть запас удешевления, которого не
видно ни по ppl, ни по KL. Для QLoRA-базы вывод НЕ переносится: там
квантованная база -- отправная точка обучения, а не конечная функция.

Прежде чем что-то менять, стоит померить на более трудной постановке:
recall@k против пула в тысячи документов вместо пары.

---

## КУДА ПОТРАТИТЬ ЗАПАС REDUCTION: proj@8 И emb@8 (16.08, оба масштаба)

**Функция полезности уточнена владельцем 16.08 и это меняет ответ.**
REDUCTION -- это QLoRA и векторные модели; борьбы за мегабайт там нет
вовсе, требование сформулировано так: **«в 2-2.5 раза меньше bf16 при
сопоставимом качестве»**. В байтах это 1222-1528 МБ на 1.5B (bf16
3055.3) и 2358-2948 МБ на 2.9B (bf16 5896.1). Кандидат прошлой сессии
(1299.2 и 2483.4 МБ) сидел у ВЕРХНЕГО края коридора, то есть оставлял
230 и 460 МБ неиспользованными -- и раньше это выглядело как
достоинство, а по новой рамке это просто недобранное качество.

### Композит на обоих масштабах (fake-путь, полный корпус 38x512)

**1.5B** (bf16 8.20048, 3055.3 МБ):

| конфиг | МБ | x от bf16 | Δ ALL | Δ ср.яз | en / ru / sr |
|---|---|---|---|---|---|
| REDUCTION | 1255.1 | 2.43x | +0.690% | +0.753% | +0.852 / +0.535 / +0.872 |
| + cmix/proj/head sym, head@8 | 1299.2 | 2.35x | +0.111% | +0.156% | +0.156 / −0.000 / +0.313 |
| + emb@8 | 1333.8 | 2.29x | +0.087% | +0.124% | +0.062 / −0.004 / +0.315 |
| + proj@8 | 1399.8 | 2.18x | **+0.035%** | +0.050% | +0.032 / −0.003 / +0.122 |
| **+ proj@8 + emb@8** | **1434.4** | **2.13x** | +0.036% | +0.053% | −0.003 / −0.005 / +0.167 |

**2.9B** (bf16 7.16598, 5896.1 МБ):

| конфиг | МБ | x от bf16 | Δ ALL | Δ ср.яз | en / ru / sr |
|---|---|---|---|---|---|
| REDUCTION | 2420.5 | 2.44x | +1.299% | +1.213% | +0.976 / +1.509 / +1.156 |
| + cmix/proj/head sym, head@8 | 2483.4 | 2.37x | +0.275% | +0.376% | +0.496 / +0.028 / +0.604 |
| + emb@8 | 2526.7 | 2.33x | +0.210% | +0.311% | +0.390 / −0.037 / +0.580 |
| + proj@8 | 2693.1 | 2.19x | +0.099% | +0.120% | +0.237 / +0.048 / +0.075 |
| **+ proj@8 + emb@8** | **2736.4** | **2.15x** | **+0.081%** | **+0.096%** | +0.231 / +0.044 / **+0.013** |

**proj@8 -- крупнейшая правка из оставшихся, и она переносится между
масштабами полностью**: +0.111% -> +0.035% на 1.5B за +100.6 МБ и
+0.275% -> +0.099% на 2.9B за +209.7 МБ. Все три языка улучшаются на
обоих масштабах. Строка `reduction_sym_proj8` существовала в CONFIGS с
13.08 и была ПОСЧИТАНА на 1.5B, но нигде не записана -- нашлась чтением
JSON.

### emb@8 -- ровно тот случай, где ppl и KL расходятся, и спор разрешён замером

По ppl на 1.5B emb@8 поверх proj8 не даёт НИЧЕГО (+0.036% против
+0.035%, то есть номинально хуже) и слегка двигает баланс языков
(сербский +0.122 -> +0.167, английский +0.032 -> −0.003) -- всё это
втрое ниже полосы различимости ±0.05-0.09 п.п. По ppl на 2.9B он же
помогает однозначно: +0.099% -> +0.081%, сербский +0.075 -> +0.013.

А по KL (1.5B, 8x512, composite против bf16):

| конфиг | КЛ | 95% CI | top-1 совпадает |
|---|---|---|---|
| head8 (контроль) | 0.003813 | [0.003578; 0.004033] | 96.477% |
| + emb@8 | 0.003495 | [0.003301; 0.003661] | 96.404% |
| + proj@8 | 0.003201 | [0.002897; 0.003496] | 96.820% |
| **+ proj@8 + emb@8** | **0.002908** | [0.002640; 0.003154] | **97.040%** |

**emb@8 снимает 9% композитного KL там, где ppl не видит ничего**, а
полный конфиг уходит с 0.003813 до 0.002908, то есть на 24%. И ТРЕТИЙ,
независимый от обоих, показатель идёт за KL: доля токенов, где
квантованная модель выбирает тот же топ-1, растёт монотонно с 96.48% до
97.04%.

**И НА 2.9B ЭТО СИЛЬНЕЕ, А НЕ СЛАБЕЕ** (8x512, composite против bf16):

| конфиг | KL | 95% CI | top-1 совпадает |
|---|---|---|---|
| head8 (контроль) | 0.004458 | [0.003880; 0.005108] | 96.526% |
| + emb@8 | 0.003862 | [0.003494; 0.004286] | 96.722% |
| + proj@8 | 0.002812 | [0.002497; 0.003159] | 97.260% |
| **+ proj@8 + emb@8** | **0.002212** | [0.002088; 0.002362] | **97.432%** |

**Интервалы кандидата и победителя НЕ ПЕРЕСЕКАЮТСЯ** ([0.003880; 0.005108]
против [0.002088; 0.002362]): композитный KL падает вдвое, top-1 растёт на
0.9 п.п. На 1.5B тот же переход дал −24% KL и +0.56 п.п. top-1, то есть
направление и порядок совпадают, а величина на большей модели больше.

**emb@8 на 2.9B полезен по ОБЕИМ метрикам сразу** (ppl −0.066 п.п., KL
−13.4%), тогда как на 1.5B ppl его не видел, а KL видел. То есть
расхождение критериев -- свойство МАСШТАБА, а не самой группы: там, где
вклад emb достаточно велик, его замечают оба инструмента.

Контроль воспроизведения: head8 дал 0.003813 [0.003578; 0.004033] и
top-1 96.477% на 1.5B и 0.004458 [0.003880; 0.005108] при 96.526% на
2.9B -- оба совпадают с записанным 13.08 до последней цифры.

**Вывод для пресета: брать `proj@8 + emb@8`.** Он лучший или равный по
ppl на обоих масштабах, лучший по KL и по top-1, обе точки лежат внутри
коридора 2-2.5x (2.13x и 2.15x), и ни одного размена по языкам на 2.9B.
Расхождение ppl и KL на emb теперь не вопрос вкуса: для векторных
моделей, ради которых REDUCTION и существует, логиты не используются
вовсе -- используется распределение, и меряет его KL.

### Оговорки, без которых это не деплоится

- **Числа fake-путные.** Поправка fake->real есть свойство КОНФИГА, а не
  константа пути (проверено 13.08: на sym-конфигах 2.9B она обнулилась),
  поэтому победителя надо перемерить `eval_real_sym` на обоих масштабах.
- **`presets.py` НЕ ТРОНУТ.** Правка пресета -- решение владельца, и
  теперь у него есть обе метрики на обоих масштабах.
- Размер вырос на 14% против прежнего пресета (1255 -> 1435 МБ). Для
  COMPRESSION из этого не следует НИЧЕГО: там функция полезности другая.
- **ВНЕСЕНО В `presets.py` 16.08**, реальным путём числа подтверждены:
  +0.108% на 1.5B и +0.157% на 2.9B (поправка fake->real +0.072 и +0.076
  п.п. -- то есть у ЭТОГО конфига она есть на обоих масштабах, в отличие
  от sym-конфигов 2.9B, где обнулялась; ещё одно подтверждение, что
  поправка есть свойство конфига, а не пути).

---

## ВНЕШНИЙ СЧЁТЧИК ПОЛОСЫ: МЫ И ШТАТНЫЙ MLX СИДИМ НА ОДНОЙ ПОЛКЕ (16.08)

Владелец проекта поставил SiliconScope -- независимый профилировщик
полосы памяти, ничего не знающий про наши формулы. Замер декода под ним:

| | трафик/ток | мс/ток | ГБ/с РАСЧЁТНЫЕ | ГБ/с ИЗМЕРЕННЫЕ |
|---|---|---|---|---|
| наша (пресет 16.08) | 1338.4 МБ | 17.31 | 77.3 | **80** (77 мин, 84 макс) |
| штатный mlx-affine | 1161.3 МБ | 14.99 | 77.5 | **79.6** (84 макс) |

**ОБЕ РЕАЛИЗАЦИИ УПИРАЮТСЯ В ОДНУ И ТУ ЖЕ ПОЛКУ.** Расчёт 77.3 против
77.5, измерено 80 против 79.6 -- в пределах процента. И арифметика
закрывается сама: отношение трафика 1338.4/1161.3 = **1.152**, отношение
скоростей 66.7/57.8 = **1.154**.

**Значит весь разрыв декода со штатным MLX -- это БАЙТЫ, и ничего
кроме.** Наш кернель вместе с питоновской обвязкой идёт ВРОВЕНЬ с
нативным путём по эффективности полосы; штатная модель быстрее ровно
настолько, насколько меньше читает (у неё шесть бит на всём, включая
голову и emb).

ЭТО ОТМЕНЯЕТ РАЗБОР, СДЕЛАННЫЙ ЧАСОМ РАНЬШЕ в разделе ниже («+3.7%
трафика, 2-6% ядра, 1-2% обвязки»). Тот разбор стоял на константе
трафика, которая считала LoRA плотным fp16 у обеих моделей и протухла в
день, когда LoRA квантовали, -- и на старом файле. Независимый счётчик
его снял. Микрозамер ядра (`probe_gemv_wrapper`, 1.02-1.06x при
совпадающей битности) при этом не опровергнут: 2-6% на отдельной форме
просто тонут в сквозном замере, где всё упирается в полосу.

### COMPRESSION идёт на 73 ГБ/с, а не на 80 -- и причина НЕ очевидна

Тот же внешний счётчик на COMPRESSION: 13.39 мс/ток (74.7 ток/с),
расчётные 68.2 ГБ/с, измеренные **не выше 73**. То есть на 9% ниже
полки, на которой сидят REDUCTION и штатный MLX.

Напрашивается объяснение «на 4-5 битах распаковка дороже в пересчёте на
байт» -- и оно согласуется с записанным Instruction Throughput Limiter
92% при целочисленной нагрузке. Но есть объяснение ПРОЩЕ, и его надо
исключить первым: **не-GEMV часть шага постоянна в миллисекундах, а шаг
короче.** При ~4 мс постоянной части у REDUCTION (17.31 мс) она занимает
23% шага, у COMPRESSION (13.39) -- уже 30%, и средняя полоса падает без
всякой арифметики распаковки; в фазе самих чтений обе тогда идут около
97-100 ГБ/с.

Версии разделяются УЖЕ НАПИСАННЫМ инструментом: `bench_step_decompose`
даёт GEMV-часть вычитанием. Если полоса в GEMV-фазе у обоих пресетов
одинакова -- виновата постоянная часть, и лечится она оп-каунтом; если у
COMPRESSION она ниже -- виновата распаковка, и лечится она кернелем.
НЕ СДЕЛАНО, и до этого замера выбирать между версиями нельзя.

### Что из этого следует по работам

1. **На декоде запаса В ЯДРЕ не осталось.** Хотите быстрее -- читайте
   меньше, а это битность, то есть качество. Цена нового пресета видна
   прямо в тактах: 57.8 ток/с против 62-63 на прежнем.
2. **Зато видно, где запас есть.** 80 ГБ/с из измеренных 104 -- это 77%,
   и застряли там ОБА. Значит недостающие 23% лежат НЕ в GEMV (иначе
   один из двух был бы эффективнее), а в структуре шага: промежутки
   между крупными чтениями, где полоса простаивает. Это ровно то, что
   показало покомпонентное разложение -- LoRA на мелких формах, WKV,
   поэлементное, диспетчеризация.
3. **Учёт трафика обходом живых буферов ПРОВЕРЕН.** Предсказание перед
   замером было 79-81 ГБ/с, измерено 80; накладные сверх весов -- 3.5%
   (у Molly 2.7%). Теперь это инструмент, а не самооценка:
   `trace_decode_steady` печатает трафик и ГБ/с рядом с мс/ток.

---

## РАЗРЫВ С mlx-affine ПЕРЕМЕРЕН: 1.39x -> 1.03x И 1.18x -> 1.10x (16.08)

`bench_molly_ab`, 9 раундов, обе модели в памяти, чередование, своп
неподвижен:

| | pp512 | декод | файл | трафик/ток | ГБ/с декода |
|---|---|---|---|---|---|
| наша (sym + head@8) | 730.4 т/с | 60.4 т/с | 1299.8 МБ | 1234.7 МБ | 74.6 |
| штатный mlx-affine | 750.8 т/с | 66.7 т/с | 1272.0 МБ | 1190.6 МБ | 79.4 |

Против записанного 15.08 (наши 547.2 / 55.3): **префилл 1.39x -> 1.03x**
(деквант одним кернелем плюс починка `mx.compile`), **декод
1.18x -> 1.10x** (квантование LoRA-веток).

### ПОЧЕМУ ИХ ДЕКОД БЫЛ БЫСТРЕЕ: ЭТО БЫЛА НАША LoRA

Прямое чтение их файла: `w_lora.lora.0.weight` -- `uint32 [96, 384]`
(2048*6/32 = 384 слова) плюс `scales`/`biases` fp16 `[96, 32]`
(2048/64 = 32 группы). То есть **их down-проекции лор квантованы при
шести битах группами по 64 вдоль входа -- буквально та раскладка,
которую мы сделали 16.08**, а у нас они ехали плотным fp16 (100.1 МБ за
токен). Записанные 1.18x были в основном этой статьёй.

Попутно: **up-проекции у них НЕ квантованы вовсе** (`lora.2` --
`float16 [2048, 96]`), то есть наша правка агрессивнее их схемы. И
конфиг у них однородный: `affine, group_size=64, bits=6` НА ВСЁ, включая
эмбеддинги и голову.

### ЧТО ОСТАЛОСЬ ОТ РАЗРЫВА И СТОИТ ЛИ «БРАТЬ ИХ ЛОГИКУ»

`tests/probe_gemv_wrapper.py` -- полный вызов против голого кернеля
против нативного, по ВСЕМ слоям за проход, при СОВПАДАЮЩЕЙ битности:

| группа | бит | полный | кернель | нативный | обвязка | кернель/нативный |
|---|---|---|---|---|---|---|
| cmix key | 6 | 3.789 мс | 3.747 | 3.684 | 1.01x | **1.02x** |
| proj r | 8 | 1.537 | 1.506 | 1.419 | 1.02x | **1.06x** |

**Брать нечего.** Ядро в пределах 2-6% от нативного, обвязка (reshape,
отдельный `xbsum`, цикл по колонкам) -- 1-2%. Оставшийся разрыв 1.10x
раскладывается на **+3.7% трафика** (наши proj/head/emb на восьми битах
против их шести), 2-6% ядра и 1-2% обвязки -- арифметика сходится.
То есть они быстрее ПОТОМУ ЧТО ЧИТАЮТ МЕНЬШЕ БАЙТ при худшем качестве:
их шестибитная голова -- ровно та группа, где деградация у нас оказалась
единственной значимой и куда бюджет доплачен сознательно.

ОГОВОРКА К ЭТОМУ ЗАМЕРУ, И ОНА ВАЖНАЯ: первый прогон дал обвязку 1.10x
на proj, повтор -- 1.02x. При N=1 операция стоит десятки микросекунд при
поле синхронизации 0.238 мс, и микрозамер здесь шумит (записано 15.08:
26 против 76 ГБ/с между прогонами на одной форме). Верить надо цепочке
по всем слоям и повтору, а не одному числу.

---

## ПОТОЛОК ПРЕФИЛЛА: 2.80 ТФЛОП/с, И СЧИТАЕТСЯ ОН НЕ ТАК, КАК У ДЕКОДА

**Формула декода на префилл не переносится вообще.** На декоде веса
читаются на КАЖДЫЙ токен, поэтому пол -- это трафик/полоса (1296 МБ /
104 ГБ/с = 12.46 мс = 80.2 ток/с). На префилле те же веса читаются ОДИН
РАЗ НА ВЫЗОВ: 1149 МБ за pp512 -- это 11 мс из измеренных 753, то есть
**полоса объясняет 1.5% времени**. Упираемся в арифметику:

    пол = 2 * P * T / пик  +  WKV-скан  +  поэлементное

P -- параметры, участвующие в матмулах; emb не считается (gather), голова
считается. По манифесту: **1.393 млрд на 1.5B, 2.779 на 2.9B**.

### Пик измерен, а не взят из паспорта (`tests/bench_gemm_peak.py`)

Разброс 0.1-2.8%, машина остывшая (закон 25: потолок -- величина
абсолютная, на горячей машине его мерить нельзя), своп не двигался.

| случай | ТФЛОП/с |
|---|---|
| квадрат 2048³ / 4096³ / 8192³ fp16 | 2.60 / **2.72** / 2.69 |
| наш head [65536, 2048] T=512 | **2.80** |
| наш cmix [8192, 2048] T=512 | 2.65 |
| наш proj [2048, 2048] T=512 | 2.28 |
| те же через `mx.quantized_matmul`@8 | 2.17 / 2.52 / 2.63 |

**Практический потолок 2.80 ТФЛОП/с.** Паспортные цифры для GPU M4 сюда
не годятся ровно по той же причине, по которой не годились 120 ГБ/с у
шины: это арифметика, а не замер (закон 12).

| | ФЛОП на pp512 | измерено | достигнуто | % от 2.80 |
|---|---|---|---|---|
| 1.5B | 1.426 ТФЛОП | 753 мс | 1.89 ТФЛОП/с | **68%** |
| 2.9B | 2.846 ТФЛОП | 1440 мс | 1.98 ТФЛОП/с | **71%** |

### ВАЖНОЕ ПОБОЧНОЕ: на префилле нативный квантованный матмул МЕДЛЕННЕЕ

`mx.quantized_matmul`@8 даёт 2.52 против 2.65 у плотного fp16 на cmix и
2.63 против 2.80 на голове -- то есть по арифметике он на 5% ХУЖЕ.
Выигрывал он ровно тем, что не требует отдельного прохода декванта; после
того как деквант стал стоить 0.84 мс вместо 10.68, от преимущества
остались те самые 4% (753 против 722 мс). **Это снимает affine с повестки
префилла почти полностью**: платить +0.077% ppl и +291 МБ за 4% больше
не за что.

### Из чего состоят оставшиеся 32% (`tests/probe_prefill_components.py`)

Аблации на компилированном пути, T=512, полный префилл 750.5 мс:

| компонент | мс | % шага |
|---|---|---|
| **WKV-скан** | **95.0** | **12.7%** |
| LoRA (8 матмулов на слой) | 37.6 | 5.0% |
| деквант | 29.1 | 3.9% |

Бюджет сходится: сумма изолированных матмулов по их СОБСТВЕННЫМ
достижимым скоростям (2.28 у proj, 2.65 у cmix, 2.80 у головы) даёт
~590 мс, плюс эти три статьи -- 752 при измеренных 750.

**WKV -- крупнейшая незакрытая статья префилла и единственная, которая от
арифметического потолка не ускоряется вовсе**: это последовательная
рекуррентность по чанкам, берётся из rwkv-metal как есть и под префилл не
смотрелась ни разу. На декоде она стоила 0.84 мс (4.6%) и была признана
дешёвой -- на префилле картина другая, и переносить тот вывод сюда
нельзя.

Второй по величине резерв виден в самой таблице пика: **proj идёт на
2.28 ТФЛОП/с против 2.80 у головы** -- мелкая форма [512, 2048] x
[2048, 2048] не насыщает GEMM. Склейка четырёх проекций в один матмул
упирается в то же, что и у LoRA (разные входы у r/k/v), и стоила бы
удвоения строк; но o_proj и cmix свободны от этого. Не мерено.

---

## ПРЕФИЛЛ: 60% ЕГО ВРЕМЕНИ БЫЛО НЕ МАТМУЛОМ, А ДЕКВАНТОМ (16.08)

**Префилл 997 -> 753 мс (1.33x, 680 ток/с) БЕЗ ЕДИНОГО ИЗМЕНЁННОГО БИТА
в весах.** Правка -- деквант одним Metal-кернелем вместо цепочки
MLX-операций; декод не тронут вовсе (контроль в замере: 17.5-18.6 мс в
обеих ветках, разницы нет).

### Как это нашлось

Вопрос владельца был «нельзя ли репак сделать самим, в обход MLX».
Прежде чем писать кернель, стоило разложить сам проигрыш
(`tests/probe_gemm_split.py`, N=512): весь вызов слоя против декванта
против матмула по УЖЕ готовой плотной матрице.

| матрица | было: вызов / деквант / матмул | стало: вызов / деквант | нативный affine |
|---|---|---|---|
| cmix key [8192, 2048] @6 | 17.22 / **10.68** / 7.46 | **7.29 / 0.84** | 6.78 |
| cmix value [2048, 8192] @6 | 17.76 / **10.84** / 6.55 | **7.55 / 0.82** | 6.86 |
| proj r [2048, 2048] @6 | 5.10 / **3.09** / 1.89 | **2.18 / 0.36** | 1.99 |
| head [65536, 2048] @8 | 62.63 / 11.23 / 49.23 | 55.40 / 4.65 | 51.89 |

**Шестьдесят процентов времени GEMM-префилла уходило на деквант, а не на
матмул.** Наш матмул по готовой матрице (7.46 мс) идёт почти вровень с
нативным квантованным (6.78) -- то есть проигрывали мы не матмулу и не
раскладке. Прежний `_dequant_w` собирал коды цепочкой MLX-операций
(concatenate нибблов, две битплоскости через сдвиги), каждая рождала
полноразмерный промежуточный тензор, и по итогу он шёл со скоростью
**4.4 ГБ/с** там, где ему положено прочитать 13.8 МБ и записать 33.6 --
то есть около 0.5 мс. Кернель дал 0.84 мс, в 12.7 раза быстрее.

### Что это значит для всей темы «репак в affine»

Разрыв с нативным матмулом схлопнулся с 2.2-2.5x до **1.08-1.10x на
cmix и proj**. Остаток -- это уже сам матмул по плотной матрице против
матмула по квантованной, то есть цена ЛИШНЕГО ТРАФИКА (33.6 МБ против
13.8), и закрывает её только тайловый GEMM с декодом в
threadgroup-памяти. Это следующий шаг, а не первый, и теперь видно,
сколько он стоит: около 0.5-0.7 мс на слое.

Сравнение вариантов префилла на сегодня, сквозным замером:

| путь | pp512 | ppl | память |
|---|---|---|---|
| было (цепочка декванта) | 997 мс (511 ток/с) | -- | -- |
| **деквант кернелем** | **753 мс (680 ток/с)** | **бит-в-бит** | **+0** |
| affine@8 в памяти | 722 мс (709 ток/с) | +0.077% | +291 МБ |
| llama.cpp Q6_K+imatrix | 705.5 ток/с | -- | -- |

То есть **97% выигрыша affine получено бесплатно** -- без потери
качества, без роста памяти и без второй раскладки. Оставшиеся 4% (753
против 722 мс) -- это и есть та самая разница в трафике, ради которой
пришлось бы платить +291 МБ и +0.077% ppl.

### Гейт

`tests/test_sym_dequant_kernel.py` -- **97 тензоров обеих битностей,
max|Δ| = 0**. Прежняя цепочка оставлена как `_dequant_w_ref` ровно ради
этой сверки, и переключается флагом `DEQUANT_REF` (A/B подменой одной
функции в одном процессе, закон 27). Право требовать РАВЕНСТВА здесь
есть: обе реализации считают одну формулу в одном порядке и ничего не
суммируют. Ловится гейтом не арифметика, а ПОРЯДОК КОЛОНОК внутри пары
блоков (l0,l1,h0,h1 / l2,l3,h2,h3) -- перепутать его значит получить те
же 32 числа в другом порядке. Декодер нибблов и мульт-трюк битплоскостей
взяты из GEMV ТЕМ ЖЕ генератором строк, а не переписаны (закон 23).
Прежние гейты sym зелёные: `test_sym_kernel`, `test_sym_fuse_parity`,
`test_sym_packer_parity`.

### Побочно: аллокатор транзиент НЕ отдаёт

Проверялась догадка, что 1.2 ГБ транзиента префилла возвращаются системе
между вызовами и выфолчиваются заново. Шесть префиллов подряд в одном
процессе: page reclaims 228 221 при трёх разных лимитах кеша аллокатора,
то есть РОВНО один проход по пику, а не шесть. Времена: 784, 750, 750,
749, 753, 752 мс (первый дороже на трассировку `mx.compile`).
Выключение кеша (`set_cache_limit(0)`) экономит 148 МБ пика и стоит 3%
скорости -- размен есть, выигрыша нет.

---

## ПРЕФИЛЛ В РОДНОЙ affine: 1.38x, НО ДЕКОД -12% (16.08, 1.5B)

Приоритет 2 доведён до цифр, на которых принимается решение. Замысел был
такой: держать affine ВТОРОЙ раскладкой для префилла, оставив нашу для
декода. От него пришлось отказаться, и вот почему -- по порядку.

### ТОЧНОГО РЕПАКА sym В affine НЕ СУЩЕСТВУЕТ

**MLX принимает `group_size` только 32, 64 и 128** (проверено перебором,
а не выведено: `probe_prefill_affine` пытается собрать и позвать
`quantized_matmul` на каждом). У sym блок -- ШЕСТНАДЦАТЬ, значит точное
переложение требует gs=16, которого нет. Записанная арифметика раскладок
(«sym -> affine gs=16, 8.0 бит/вес, бит-в-бит наше») ОПИСЫВАЕТ
НЕВОЗМОЖНЫЙ ПУТЬ. Для sb6 (блок 32) точный репак существует и уже
написан (`codec.sb6_to_mlx_affine`), но целевой пресет теперь sym.

Значит любая affine-раскладка нашей модели -- это ПЕРЕквантование поверх
нашего, то есть двойное. Отсюда ppl-гейт, а не гейт равенства.

### СКОРОСТЬ НАТИВНОГО МАТМУЛА НЕ ЗАВИСИТ ОТ БИТНОСТИ И ГРУППЫ

`probe_prefill_affine`, N=512, реальные формы, время слоя:

| матрица | наш GEMM | affine 6/32 | 6/64 | 8/32 | 8/64 |
|---|---|---|---|---|---|
| cmix key [8192, 2048] | 16.75 мс | 6.92 | 6.92 | 6.81 | **6.78** |
| cmix value [2048, 8192] | 16.55 | 6.98 | 6.92 | 6.91 | **6.86** |
| proj r [2048, 2048] | 4.45 | 2.02 | 2.00 | 1.98 | 1.99 |
| head [65536, 2048] | 62.15 | 53.05 | 52.76 | 52.16 | **51.89** |

Разброс между четырьмя вариантами -- 1-3%, то есть выбирать раскладку
можно ПО КАЧЕСТВУ И ПАМЯТИ, скорость от этого не зависит. Голова почти
не выигрывает (1.17-1.20x): её матрица так велика, что деквант тонет в
матмуле -- это воспроизводит записанное 15.08 наблюдение.

### КАК ЕДИНСТВЕННАЯ РАСКЛАДКА В ПАМЯТИ: ПРЕФИЛЛ БЕРЁТ, ДЕКОД ОТДАЁТ

Файл при этом НЕ МЕНЯЕТСЯ: на диске остаётся наша сетка (sym + AW, ради
которой всё делалось), а в RAM лежит её affine-транскрипция, собираемая
за 1 секунду на 145 слоёв.

| | декод | pp512 | живые буферы | ppl Δ |
|---|---|---|---|---|
| наша sym | **18.00 мс (55.6 ток/с)** | 994.8 мс (514.7 ток/с) | 1506.8 МБ | -- |
| affine@8 gs=64 | 20.12 (49.7) | **722.4 мс (708.8 ток/с)** | 1798.3 (+291) | **+0.077%** |
| affine@6 gs=64 | -- | -- | 1462.8 (-44) | **+0.881%** |

**Префилл 1.38x -- это 708.8 ток/с против 705.5 у llama.cpp Q6_K, то
есть разрыв закрывается целиком.** Но декод теряет 12%: affine@8 стоит
8.5 бит/вес против наших 6.5625, то есть +26% трафика там, где мы уже на
90.6% достижимой полосы. Арифметика сходится и попутно подтверждает
прежний вывод: чистый трафик дал бы +26% времени, а вышло +11.8% --
разницу съела обвязка, которой у нативного вызова нет (записано было
1.05-1.20x, здесь она проявилась независимо).

**affine@6 бесплатен по памяти и мёртв по качеству: +0.881%
[+0.677; +1.082].** Это вчетверо больше ВСЕГО бюджета пресета (+0.192%),
то есть двойное квантование съедает всё, что дала раскладка sym. Точка
закрыта: «квантовать сразу в affine gs=64» из записанной таблицы
раскладок стоит не «ppl неизвестен», а «ppl неприемлем».

**ppl affine@8 = +0.077% [+0.034; +0.120], значимо.** Мало, но бюджет
REDUCTION вырос бы с +0.192% до +0.269%, то есть на 40%. При функции
полезности «максимум качества» это плата, а не бесплатный обед.

### ВТОРАЯ РАСКЛАДКА РЯДОМ С НАШЕЙ -- НЕ ВЛЕЗАЕТ

Обе раскладки одновременно -- это +1.4 ГБ на 1.5B. Проверено дорогой
ценой: A/B с двумя моделями в ОДНОМ процессе (как того требует закон 1)
вырос до 7 ГБ и увёл машину в своп -- аллокатор MLX держит освобождённые
буферы в кеше, и `mx.clear_cache()` на каждом слое пик не убирает
(4882 -> 4850 МБ). Замер переделан на чередование ПРОЦЕССАМИ: закон 11
(своп убивает замер) здесь бьёт закон 1, а ожидаемые эффекты в разы
крупнее процессного разброса, который вышел меньше 1.5%.

### ОПРОВЕРГНУТО: «пик префилла держат транзиенты `_dequant_w`»

Записано было, что 3265 МБ пика префилла -- это развёрнутые в плотный
fp16 веса на каждую проекцию, и что закрывает эту статью квантованный
GEMM. Проверено разностью пиков с префиллом и без (`/usr/bin/time -l`,
RWKVQ_NOPREFILL):

| | без префилла | с префиллом | префилл добавляет |
|---|---|---|---|
| наша sym | 2268 МБ | 3529 МБ | **1261 МБ** |
| affine@8 | 3760 МБ | 4883 МБ | **1123 МБ** |

У affine `_dequant_w` НЕ ВЫЗЫВАЕТСЯ ВООБЩЕ, а префилл всё равно
добавляет 1123 МБ. Значит статья -- это АКТИВАЦИИ на 512 позиций, а
деквант весов в ней 138 МБ, то есть 11%. **Квантованный GEMM экономит на
префилле 0.14 ГБ, а не 1.2.** Заодно это объясняет прежний отрицательный
результат про нарезку промпта кусками (память почти не менялась): резать
надо было бы граф активаций, а не веса.

### ЧТО ИЗ ЭТОГО СЛЕДУЕТ

Размен зависит от НАГРУЗКИ, и у проекта ровно для этого записаны две
функции полезности. Для векторных моделей и эмбеддингов (префилл есть
всё, декода нет вовсе) affine@8 в памяти -- явный выигрыш: +38%
пропускной за +291 МБ и +0.077% ppl, файл не меняется. Для чата, где
декод идёт сотнями токенов на один промпт, это чистый убыток.

Инженерно осталось: собирать affine ПО СЛОЮ прямо в конструкторе (тогда
пик перевода 3760 МБ схлопнется -- сейчас он выше пика префилла у нашей
раскладки), и флаг выбора раскладки при загрузке. Ни то, ни другое не
делалось: сначала решение владельца.

---

## `mx.compile` НЕ ЗВАЛСЯ НА ПРЕФИЛЛЕ: +35% ЛЕЖАЛИ НА ПОЛУ (15.08)

Вопрос был «может ли компиляция где-то съедать миллисекунды и не стоит ли
скомпилировать заранее». Ответ оказался хуже, чем вопрос: компиляция не
съедала, её просто НЕ БЫЛО на половине горячего пути.

`QuantRWKV7.step` -- это `mx.compile(forward_stateful)`, и кеш там
ключуется формами, то есть один объект обслуживает и T=1, и T=512. Декод
через него шёл. **Префилл -- нет: `bench_decode_vs_gguf` и
`bench_prefill_mem` звали сырой `forward_stateful`**, причём в первом
прямо рядом стоял комментарий «прогрев mx.compile» -- намерение было,
вызова не было. Значит в таблице против llama.cpp половина строки снята
на компилированном пути, а половина на сыром.

### Замер (`tests/bench_compile_ab.py`, 1.5B sym, чередование)

| | сырой | compiled | выигрыш |
|---|---|---|---|
| префилл pp512 | 344.3 ток/с (1486.9 мс) | **532.5 ток/с (961.6 мс)** | **+35.3%** |
| декод | 43.8 ток/с (22.81 мс/ток) | 52.8 ток/с (18.95 мс/ток) | +17.0% |

Разброс 0.3-0.5%. Попутно компилированный префилл ещё и ЛЕГЧЕ: пик
3180 против 3265 МБ на sym-пресете (3304 против 3386 на sb6).

### Цена компиляции: есть, но окупается НЕМЕДЛЕННО

| что | цена трассировки |
|---|---|
| первая длина префилла | 406 мс |
| КАЖДАЯ следующая длина | 110 мс |
| декод T=1 | 33 мс |

Кеш ключуется формой, поэтому в чате с произвольными длинами промпта
платится почти на каждом. Но арифметика не оставляет выбора: один
префилл на 512 токенов экономит 525 мс (1487 -> 962), то есть даже первый
вызов на НЕЗНАКОМОЙ длине выходит на 415 мс быстрее сырого пути. Для
декода окупаемость -- девять токенов. Компилировать надо всегда.

### Два способа убрать трассировку -- оба не работают

- **Куски фиксированной формы** (`tests/bench_prefill_chunk_ab.py`).
  Идея: гонять префилл кусками одной длины с переносом состояния, тогда
  трассировка одна на процесс. Замер: куски по 256 стоят −26%, по 128
  −86%, по 64 −206% (контроль relmax к одному куску 5-8e-4). Даже с
  учётом 110 мс трассировки один кусок выигрывает: 1069 против 1208 мс.
  Это НЕ повторение замера 13.08 -- тот проверял гипотезу про ПАМЯТЬ, на
  сыром пути и с намеренным обрывом графа; здесь путь компилированный и
  граф не рвётся.
- **`mx.compile(..., shapeless=True)`** -- на этом графе не работает
  вовсе: `ValueError: [Primitive::output_shapes] Slice cannot infer
  output shapes`.

### Что это переписывает в сравнении с llama.cpp

Записано было «префилл проигрываем вдвое: 353.7 против 705.5 -- самый
большой разрыв из всех намеренных». На компилированном пути 532.5 против
705.5, то есть **1.33x, а не 2.0x**. Разрыв остаётся, но он вчетверо
меньше, чем считалось, и это меняет приоритет квантованного GEMM: он
теперь закрывает 33%, а не 100%.

Исправлено в `bench_decode_vs_gguf` (префилл через `model.step`) и в
`bench_prefill_mem`. Докстринг `QuantRWKV7.step` переписан: это вход и
для декода, и для префилла.

### Мораль, которая шире этого случая

Разрыв нашёлся не потому, что кто-то плохо оптимизировал, а потому что
рядом стоял КОММЕНТАРИЙ про компиляцию без соответствующего вызова.
Комментарий описывал намерение, читался как факт и пережил несколько
сессий замеров. Проверять надо вызовы, а не комментарии рядом с ними --
и это же причина, по которой сравнительная таблица обязана печатать, по
какому пути снята каждая строка.

---

## ПОТОЛОК ПОЛОСЫ: 104 ГБ/с, А НЕ 95.7 И НЕ 120 (15.08)

Вопрос владельца проекта: машина паспортно 120 ГБ/с -- как вообще
измерили, что они недостижимы? Ответ: НИКАК, 120 никто не мерил. Это
арифметика шины -- LPDDR5X-7500 x 128 бит = 7500 МТ/с x 16 байт. А 95.7
из закона 12 -- ОДИН замер потокового чтения через `mx.sum`, и на нём
стояли все выводы вида «мы на 98% полосы, оптимизировать нечего». То есть
утверждение было о `mx.sum`, а не о машине.

### Замер (`tests/bench_membw.py`, буфер 2.15 ГБ, чередование)

Буфер на три порядка больше системного кэша, значит это DRAM, а не SLC.
Своп не двигался.

| инструмент | ГБ/с | % от 120 |
|---|---|---|
| **`mx.sum`** | **104.0** | **86.7%** |
| свой кернель, 4096 потоков, развёртка 4 | 101.3 | 84.4% |
| свой кернель, 4096 потоков | 100.9 | 84.1% |
| свой кернель, 16384 потока | 98.8 | 82.3% |
| свой кернель, 65536 потоков | 97.3 | 81.1% |
| копия (чтение+запись) | 92.6 | 77.1% |

Разброс 2-4%. **Превзойти `mx.sum` не удалось ни числом потоков, ни
развёрткой** -- семь вариантов собственного кернеля чистого чтения все
легли ниже. Это и есть аргумент, что 104 -- потолок железа, а не предел
конкретного инструмента: два независимых инструмента упираются в одно.

### Почему не 120

Refresh, активация и предзаряд строк на переходах страниц, гранулярность
транзакций -- и главное, 120 есть пропускная способность КОНТРОЛЛЕРА, а
GPU лишь один из его клиентов. 85-90% паспорта -- нормальный потолок для
LPDDR; 87% на хорошем краю.

### Что это переписывает

| | было (при 95.7) | стало (при 104) |
|---|---|---|
| GEMV-часть шага, 94.2 ГБ/с | 98% достижимого | **90.6%** |
| шаг целиком, 68.6 ГБ/с | 72% | 66% |
| пол по трафику 1296 МБ | 13.54 мс, 73.8 ток/с | **12.46 мс, 80.2 ток/с** |
| цель «85% полосы» | 15.94 мс, 62.7 ток/с | 14.66 мс, 68.2 ток/с |

**Главное следствие: в самом GEMV осталось около 9%, а не 2.** По времени
это ~1.3 мс на токен -- то есть сопоставимо со всем, что дал фьюз. Прежний
вывод «GEMV у потолка, трогать нечего» был следствием заниженного
потолка, а не свойством кернеля.

Оговорка, которая не снялась: голова в sym@6 выдавала 113.5 ГБ/с, то есть
ВЫШЕ измеренного потолка. Значит там всё-таки помощь кэша или перекрытие
соседних запусков в цепочке, и как оценку полосы это число брать нельзя.

---

## CPU->GPU: 2.8 мс латентности НЕ ЛЕЖАТ НА КРИТИЧЕСКОМ ПУТИ (15.08)

Гипотеза была прямая: в трассе `CPU to GPU Latency` 2.8 мс в среднем и до
4.3 мс на отдельных буферах; если сбить до 1 мс, прирост будет крупным.
Проверено тремя независимыми способами, и все три говорят «нет».

### 1. Забег вперёд (`tests/bench_runahead_ab.py`)

MLX ленив, поэтому если не звать `mx.eval`, CPU уходит вперёд на K шагов
и постановка перестаёт быть на критическом пути. Токен остаётся ленивым
массивом и подаётся в следующий шаг как есть -- контроль в скрипте
требует, чтобы при всех K выдавалась ОДНА И ТА ЖЕ последовательность
токенов (выдаётся). Значения K чередуются внутри раунда (закон 24).

| K (шагов на синхронизацию) | мс/ток | против K=1 |
|---|---|---|
| 1 (как настоящий сэмплер) | 18.13 | -- |
| 2 | 18.41 | −0.27 |
| 4 | 18.20 | −0.06 |
| 8 | 17.73 | +0.40 |
| 16 | 17.93 | +0.20 |

Разброс по раундам 6.6-10.9%, то есть 1.2-2.0 мс. Эффект НЕ монотонен по
K (K=2 медленнее K=1) и весь внутри разброса. **Забег вперёд не даёт
ничего**, значит 2.8 мс -- это ОЧЕРЕДЬ: буфер ждёт, потому что GPU занят
предыдущими, а не потому что CPU опоздал.

### 2. Гранулярность командных буферов (`tests/bench_cmdbuf_ab.py`)

Зазор между буферами существует независимо от того, кто кого ждал, а MLX
режет граф на буферы по `MLX_MAX_OPS_PER_BUFFER` / `MLX_MAX_MB_PER_BUFFER`.
Переменные читаются при инициализации, поэтому чередование только по
процессам (ослабленный закон 1, и разброс это сразу показывает):

| вариант | мс/ток | против умолчания | разброс |
|---|---|---|---|
| умолчание | 17.08 | -- | 5.2% |
| `MLX_MAX_OPS_PER_BUFFER=100` | 17.54 | −0.46 | 18.4% |
| `MLX_MAX_OPS_PER_BUFFER=1000` | 17.24 | −0.16 | 17.6% |
| `MLX_METAL_FAST_SYNCH=1` | 18.70 | −1.62 | 12.0% |
| ops=1000 + fast_synch | 18.70 | −1.62 | 8.5% |

**Ни одна ручка не помогает, умолчание лучшее или вровень.** Попутно
наглядная иллюстрация закона 24: тот же по смыслу замер по процессам даёт
разброс 5-18% против 0.7-1.3% у внутрипроцессного чередования.

### 3. Арифметика уже не оставляет места

Шаг 18.1-18.7 мс. GEMV-часть 13.8-14.9 мс -- и это число получено ДВАЖДЫ
независимо: вычитанием в `bench_step_decompose` и как 1296 МБ / 94.2 ГБ/с
= 13.76 мс, то есть GEMV идёт ровно со скоростью своего трафика.
Остаток 4-5 мс на ~1000 запусков -- это 4-5 мкс на запуск, что для Metal
правдоподобно. Если бы 2.8 мс латентности лежали на критическом пути,
остаток обязан был бы быть 0.4 мс на тысячу запусков, то есть 0.4 мкс на
запуск -- ниже физического пола диспетчеризации. **Числа сходятся БЕЗ
латентности и не сходятся с ней.**

### Что из этого следует

Платим мы за КОЛИЧЕСТВО ЗАПУСКОВ, а не за задержку постановки, и оба
доступных рычага это подтверждают: `mx.compile` даёт −2.7 мс (20.38
против 17.68), фьюз -- ещё −1.15 мс. Оба сокращают число примитивов; ни
один не трогает латентность.

Дальше по этой оси остаётся только меньше ЧИТАТЬ, потому что 75-80% шага
-- это GEMV на 90.6% достижимой полосы (104 ГБ/с, закон 12):

1. **LoRA-ветки: 100.1 МБ за токен (8% трафика), но 12% шага -- идут на
   46.6 ГБ/с против 85-93 у прочих.** УТОЧНЕНО 15.08 покомпонентным
   разложением: упираются они НЕ в полосу, а в формы (ранги
   96/96/64/256, восемь матмулов на слой, 192 запуска за токен). Прежняя
   оценка «квантовать = минус 128 МБ трафика = 10% скорости» НЕВЕРНА:
   срезав байты вчетверо, время вчетверо не срежешь. Рычаг -- укрупнение
   запусков, и это доказано фьюзом: он снимает 0.58 мс, не трогая ни
   байта. Потолок статьи ~1.0 мс (5.6% шага).
2. **Перестановка каналов cmix** -- −79 МБ трафика (~10%), точно и без
   потери качества, замерено `probe_cmix_sparsity`.

## ХВОСТ TMix ОДНИМ КЕРНЕЛЕМ: +0.28 мс, И ПОЛОВИНУ УЖЕ ДЕЛАЛ mx.compile

Второй кусок оп-каунта. `group_norm + bonus + gate` -- это около
четырнадцати примитивов на слой (две редукции нормировки, редукция
bonus, остальное поэлементное), и на декоде каждый работает над
[H, S] = [32, 64], то есть над восемью килобайтами. На 24 слоя это
порядка 336 запусков за токен. `backends/metal/fused_tail.py` считает
всё это одним кернелем: одна threadgroup на голову, обе редукции
simd-древесные, сборка ровно в том же порядке `((norm*w + b) + dp*v)*g`.

### Замер (`tests/bench_tail_ab.py`, чередование в одном процессе)

FUSE=True в ОБЕИХ ветках, отличие только `FUSE_TAIL`; своп не двигался:

| путь | хвост off | хвост on | выигрыш | разброс off |
|---|---|---|---|---|
| `mx.compile` | 17.68 мс | 17.39 мс | **+0.28 мс (1.6%)** | 1.3% |
| сырой | 20.38 мс | 19.83 мс | **+0.55 мс (2.7%)** | 0.7% |

**`mx.compile` САМ СЛИВАЕТ ПРИМЕРНО ПОЛОВИНУ ХВОСТА** -- ручной кернель
на сыром пути стоит вдвое больше, чем под компиляцией. Это стоило
померить именно так: сравнение только скомпилированного пути занизило бы
эффект вдвое и не объяснило бы, почему. И это же говорит, где ручные
фьюзы вообще имеют смысл: там, где есть РЕДУКЦИИ (их компилятор не
сливает), а не там, где цепочка поэлементная.

Выигрыш 0.28 мс сопоставим с разбросом абсолютов по раундам (0.23 мс),
но замер ПАРНЫЙ: во всех пяти парах on быстрее off, на 0.18-0.34 мс.
Именно ради этого пары и чередуются.

Отдельный флаг `FUSE_TAIL` (умолчание True), но действует он только
внутри фьюзнутого пути, то есть при FUSE=False инертен.

### Итог оп-каунта на сегодня

Полный A/B на sym-пресете, скомпилированный путь, пять пар:
**18.67 -> 17.52 мс/ток, +1.15 мс (6.2%), 53.6 -> 57.1 ток/с.**
Паритет: relmax логитов 1.53e-4, greedy 48/48, ppl фьюзнутого против
нефьюзнутого расходится на 0.85e-4.

Бюджет «остального» был 4.06 мс на этом пресете; забрали 1.15, осталось
около 2.9. Что там лежит: пред-WKV блок (`l2_norm(k*k_k)`, модуляция k
через a и k_a, `exp(-0.606531*sigmoid(w))`, сигмоиды a и vv, подмешивание
v_first) -- ещё около восемнадцати примитивов на [H, S], и это самая
крупная оставшаяся гроздь; сам WKV-скан; две `_layer_norm` на блок;
`relu^2` в CMix.

### ЛОВУШКА, КОТОРУЮ ЭТО ВСКРЫЛО: якорь ppl описывал другой артефакт

`test_fuse_parity` сверял абсолютный ppl 11.7125, снятый на конкретном
`/tmp/champion_v2.rwkvq`. Файл в /tmp не переживает ребут и
пересобирается; пересобранный БЕЗ `/tmp/act_stats_1p5b.pt` даёт 12.356,
то есть якорь расходится на 5.5% -- и выглядит это как «фьюз испортил
качество». Прямой прогон: нефьюзнутый путь на том же файле даёт те же
12.356.

Утверждение переписано на ОТНОСИТЕЛЬНОЕ: фьюзнутый ppl против
нефьюзнутого на ОДНОЙ модели, порог 1e-3 (фактически 0.5-0.9e-4). Оно
проверяет ровно тот инвариант, ради которого гейт существует, и от
содержимого /tmp не зависит. Абсолютный якорь остался справочной
строкой, которая громко предупреждает при расхождении.


---

## ПАМЯТЬ ЗАГРУЗКИ: 5511 -> 2290 МБ, БИТ-В-БИТ (15.08)

Открытый вопрос 1 закрыт. Правки чисто механические, качества не
касаются: каждый параметр модели после них РАВЕН прежнему побитово, и это
не рассуждение, а гейт.

### Числа (A/B чередованием, два раунда, прогретый кеш -- закон 26)

`bench_load_mem.py`, только постройка модели из готового `.rwkvq`,
префилла нет; `peak memory footprint` из `/usr/bin/time -l` (закон 22);
своп на границе замера не двигался ни в одном прогоне.

| конфиг | было | стало | |
|---|---|---|---|
| 1.5B REDUCTION (sb6) | 5414 МБ | **2390 МБ** | 2.27x |
| 1.5B + sym + head@8 | 5430 МБ | **2290 МБ** | 2.37x |
| 2.9B + sym + head@8 | 6383 МБ | **3633 МБ** | 1.76x |

Столбец «было» -- прежний `_dense` при уже ленивом фьюзе; против
ИСХОДНОГО состояния (эагерный фьюз + прежний `_dense`) на 1.5B это
5511 -> 2290, то есть **2.41x**. Повторы сходятся до долей мегабайта
(5413.6/5414.4 и 2389.7/2390.3), так что расхождения тут значимы все.

Время постройки НЕ изменилось: 0.8 против 0.7 с. Первый прогон показал
3.5 против 0.7 и выглядел как пятикратное ускорение -- это был холодный
кеш страниц, а не правка (закон 26). Записываю отдельно, потому что
соблазн приписать себе лишнее тут был прямой.

### Что сделано, статья первая: деквант полосами и каст до выхода в MLX

`_dense()` шёл bf16 -> `.float()` -> `.numpy()` -> `mx.array` ->
`.astype(fp16)`. Это ЧЕТЫРЕ представления одного тензора, и последнее --
ЛЕНИВАЯ нода: fp32-копия оставалась входом графа и жила до
`_materialize()`, то есть до конца сборки ВСЕЙ модели. На `emb` 1.5B
[65536, 2048] это 268 МБ результата при 1.3 ГБ живых копий, и ещё
порядка 2.5 ГБ транзиента внутри самого декванта.

- Каст переехал в torch: `bf16 -> fp16` даёт ТОТ ЖЕ БИТ, что
  `bf16 -> fp32 -> fp16` (первый переход точен, округление ровно одно), и
  снимает и лишние копии, и ленивую ноду.
- `reader.dequantize_banded` собирает результат ПОЛОСАМИ СТРОК сразу в
  целевом dtype (`RWKVQ_DEQUANT_CHUNK_MB`, умолчание 64). Строка
  независима -- вся математика декванта построчная вдоль выходной оси и
  поблочная вдоль входной, -- поэтому полоса бит-в-бит равна своей части
  целого. Это тот же приём и тот же аргумент, что закон 20 применил к
  КВАНТОВАТЕЛЮ; на обратном пути его просто не было.
- `repeat_interleave` развёрнутых scale/min заменён на broadcast с
  умножением in-place: прежний код материализовал ДВЕ полноразмерные
  fp32-копии ради того, что выражается формой. Ragged-случай
  (`att.w1` [2048, 96] при gs=64) broadcast'ом не выражается и ходит
  прежним путём -- он там и остался, а не «упростился».

### Что сделано, статья вторая: фьюз строится лениво

`_build_fused()` звался в конструкторе БЕЗУСЛОВНО, а `FUSE` по умолчанию
False. То есть дубль r/k/v (`GwQuantLinearFused`, 250 МБ на 1.5B) и
стеки лор (~58 МБ) лежали мёртвым грузом у КАЖДОГО замера по умолчанию.
Теперь фьюз строится по первому входу в фьюзнутый путь; семантика флагов
сохранена целиком -- `FUSE` и `LORA_Q8` остаются переключаемыми в
рантайме, просто платит за них тот, кто включил. Гейт
`test_fuse_parity` зелёный (relmax 1.77e-4, выигрыш +0.77 мс -- прежние
числа воспроизведены).

### ОПРОВЕРГНУТО: «пресет с sym занимает в памяти на 227 МБ МЕНЬШЕ»

Прямой обход живых буферов (`report_sym_memory`) ПОСЛЕ ленивого фьюза:

| | REDUCTION | + sym + head@8 |
|---|---|---|
| файл | 1255.4 МБ | 1299.5 МБ |
| живые буферы, БЫЛО | 1791.6 (1.43x) | 1564.6 (1.20x) |
| живые буферы, СТАЛО | **1483.7 (1.18x)** | **1506.8 (1.16x)** |

Разница REDUCTION -307.9 МБ раскладывается точно: 250.1 дубль фьюза плюс
57.8 стеки лор. У sym дубля не было вовсе (фьюз требует `GwQuantLinear`),
поэтому он потерял только 57.8.

**Знак перевернулся: sym теперь на 23 МБ ТЯЖЕЛЕЕ, а не на 227 легче.**
23 МБ -- это ровно разница интерливов (1134.6 против 1111.5), то есть то,
что и должно следовать из раскладки. Прежние 227 МБ были не свойством
sym, а мёртвым грузом в sb6-ветке, который sym не строил по случайности
реализации. Записано как отрицательный результат (закон 5); общий урок
тот же, что у закона 19: сравнение двух конфигураций меряет и то, чего
одна из них НЕ делает.

### ОПРОВЕРГНУТО: «на 2.9B тот же множитель даёт 11-12 ГБ на старте»

Оценка была получена умножением 1.5B-пика на отношение файлов. Замер:
**6383 МБ прежним кодом**, то есть вдвое меньше оценки. Множитель не
переносится, потому что доминирующая статья -- транзиент декванта `emb`,
а `emb` растёт не как модель: [65536, 2048] против [65536, 2560], то есть
в 1.25 раза при модели вдвое большей. Записанные в таблицах 10.7-12.3 ГБ
относятся к ЗАМЕРАМ КАЧЕСТВА (сборка из `.pth`, воркспейс поиска), а не к
загрузке готового `.rwkvq`, и путать их не следует.

### УТОЧНЕНО: «префилл добавляет 0.18 ГБ»

Верно было только относительно тогдашнего пика загрузки, который его
перекрывал. Теперь загрузка ниже, и видно настоящее:

| | только постройка | + префилл 512 |
|---|---|---|
| 1.5B REDUCTION | 2390 МБ | **3386 МБ** |
| 1.5B + sym + head@8 | 2290 МБ | **3265 МБ** |

**Максимум держит префилл, а не загрузка.** Это транзиенты `_dequant_w`
(при N >= 128 веса разворачиваются в плотный fp16 на каждую проекцию:
только голова -- 268 МБ), и они же -- та статья, которую закрывает
квантованный GEMM (открытый вопрос 4). Разбиение промпта на куски тут не
помогает, это проверено и снято 13.08.

### Гейты

- `tests/test_dense_load_parity.py [ckpt ...]` -- ГЛАВНЫЙ, требует
  РАВЕНСТВА по четырём утверждениям: новый деквант против ЗАМОРОЖЕННОЙ
  копии прежнего кода; полосы против целого при принудительно мелкой
  полосе; новый `_dense` против замороженного прежнего, ВКЛЮЧАЯ dtype
  (правка решает fp16/fp32 до декванта по `qt.shape`, прежняя -- после,
  по форме результата; расхождение означало бы, что часть параметров
  молча сменила точность); и отдельное утверждение, что полоса не
  вырождена -- иначе второе проверяет тождество (закон 17).
  **Зелёный на обоих чекпоинтах: 798 тензоров, все четыре раскладки,
  194 тензора реально резались, до 512 полос на тензор.**
- Прежние гейты зелёные: `test_codec_parity` (798 тензоров, 1527.7M
  элементов, расхождений 0), `test_sym_packer_parity`,
  `test_groupwise_parity`, `test_fuse_parity`, `test_gw_nb_parity`,
  `test_gw_kernel_int6`, `test_sym_kernel`, `test_rwkvq_container`,
  `test_v2_format`, `test_manifest_selfdesc`, `test_quant_model_smoke`,
  `test_torch_free_import`.
- Скорость декода не трогалась и мериться не должна: буферы бит-в-бит
  прежние, путь GEMV не изменён ни строкой.

### Чего в этой работе НЕТ

- **Пик всё ещё вдвое больше модели** (2290 МБ при 1507 живых). Остаток --
  это `emb`: `dequantize_banded` собирает результат в torch-буфер, а
  `mx.array` его КОПИРУЕТ, и обойти это нечем -- MLX чужой буфер не
  принимает. Настоящее закрытие статьи -- indexed-gather по sb6, тогда
  плотной таблицы не будет вовсе (открытый вопрос 3).
- **Конструкторы линейных слоёв не трогались.** `np.ascontiguousarray` на
  уже непрерывном буфере -- не копия (numpy возвращает тот же объект), а
  `np.concatenate` интерлива плюс копия в `mx.array` дают 2x ОДНОГО
  тензора и освобождаются сразу. Это не статья: максимум 218 МБ на голове
  1.5B и не накапливается. Записано, чтобы не искать здесь второй раз.
- **2.9B перемерен только по памяти.** Файл `/tmp/reduction_sym_head8_2p9b.rwkvq`
  собран заново (2484.3 МБ -- сходится с записанными 2483.9), ppl на нём
  не пересчитывался: незачем, параметры бит-в-бит прежние.

---

## СКОРОСТЬ РАСКЛАДКИ sym: ЦЕНА РАВНА БАЙТАМ (13.08, 1.5B)

Открытый вопрос 1 (скорость) закрыт. Короткий ответ: **кернель sym не
стоит ничего сверх размера.** Раскладка, которая срезает бюджет
REDUCTION вчетверо, обходится в те же проценты по времени, что и по
файлу, и ни в один процент сверх.

### E2E-декод, чередование в одном процессе (`tests/bench_sym_e2e_ab.py`)

Обе модели живут ОДНОВРЕМЕННО и бёрсты чередуются -- иначе дрейф
безвентиляторной машины (до 1.8x) съел бы разницу в 3%. Три независимых
прогона:

| прогон | REDUCTION | +sym+head@8 | Δ времени | Δ размера |
|---|---|---|---|---|
| 1 | 18.17 мс | 18.53 мс | +2.0% | +3.5% |
| 2 | 20.50 мс | 20.85 мс | +1.7% | +3.5% |
| 3 | **18.33 мс (54.54 ток/с)** | **18.90 мс (52.91 ток/с)** | +3.1% | +3.5% |

Прогон 3 воспроизвёл прежнее число REDUCTION до второго знака (54.54
против записанных 54.86 ток/с) -- то есть харнесс меряет то же, что
`bench_decode_vs_gguf`. Прогон 2 шёл на прогретой машине (разброс по
раундам 15.6%), и абсолюты там занижены на 12% -- но ОТНОШЕНИЕ устояло,
ровно ради чего чередование и делается.

**Итог: декод 54.5 -> 52.9 ток/с, то есть −3%, за падение бюджета с
+0.772% до +0.192%.** Против llama.cpp это по-прежнему выигрыш по всем
трём осям сразу: у них Q6_K+imatrix 48.59 ток/с при 1336 МБ и +0.19%.

### Микро-A/B по формам (`tests/bench_sym_vs_sb6_ab.py`)

N=1 (декод), отношение к sb6@6 на той же матрице; >1 = sym быстрее:

| форма | sym@6 | sym@8 | МБ sb6@6 / sym@6 / sym@8 |
|---|---|---|---|
| cmix key [8192, 2048] | 0.996x | 0.783x | 116.4 / 110.1 / 143.7 |
| cmix value [2048, 8192] | 1.009x | 0.821x | 116.4 / 110.1 / 143.7 |
| proj [2048, 2048] | **1.219x** | **2.146x** | 29.1 / 27.5 / 35.9 |
| head [65536, 2048] | 0.986x | 0.760x | 931 / 881 / 1149 |

Читается это так:

1. **sym@6 -- паритет по времени при меньшем файле.** ±1.4% на cmix и
   head. Пропускная способность у него чуть ниже (88-106 против 93-113
   ГБ/с), но и читает он меньше, и в итоге время то же. Раскладка
   бесплатна.
2. **sym@8 медленнее ровно на свои байты**: 143.7 против 116.4 МБ --
   это +23%, и время +22-24%. ГБ/с при этом те же. То есть восемь бит на
   head -- это честная покупка трафика, а не плохой кернель.
3. **proj -- исключение, и оно объяснимо.** На маленькой матрице
   доминируют не байты, а диспетчеризация, и sym@8 не считает `xbsum`
   вовсе (сдвига +32 при восьми битах нет), тогда как sb6 обязан считать
   его всегда ради min-поправки. Отсюда 2.1x.

N=4 (верификация/батч) sym быстрее ВЕЗДЕ: 1.02-1.26x при шести битах и
1.14-1.56x при восьми.

Побочно: head в sym@6 выдал 99-106 ГБ/с, а голова отдельным замером --
113.5, то есть на уровне и выше измеренного потолка 104 ГБ/с (закон 12). Замер идёт цепочкой из восьми вызовов подряд, так что
часть строк, вероятно, попадает в кэш; принимать 106 ГБ/с как новую
оценку полосы нельзя, это артефакт микробенча.

### Свип (NSG, RS): выбирать нечего, и это тоже результат

Все девять комбинаций уложились в 3% на каждой форме. Дефолтная (4, 4)
не хуже любой другой, менять нечего.

**НО ПЕРВАЯ ВЕРСИЯ СВИПА СКАЗАЛА ПРОТИВОПОЛОЖНОЕ** -- см. закон 24: она
гоняла конфигурации подряд, и «победители» двух прогонов подряд не
совпали ((8,2) 1.475 мс против (8,4) 1.433), а худшая конфигурация
выглядела втрое хуже лучшей (4.252 против 1.475 мс). После перехода на
чередование разница схлопнулась в 1%. То есть свип НАРИСОВАЛ
трёхкратный эффект, которого нет.

### Уточнение к закону 11: своп меряется на ГРАНИЦЕ замера

Первый прогон E2E объявил себя недействительным: своп вырос с 316 до
1005 МБ. Разбор показал, что весь рост пришёлся на СБОРКУ двух моделей
(две по 2.3 ГБ резидентно плюс воркспейс грид-поиска на 16-гигабайтной
машине), а во время самих бёрстов своп стоял намертво. Замер портит
пейджинг ВО ВРЕМЯ измерения, а не факт, что система когда-то свопила,
поэтому критерий теперь -- дельта между входом в замер и концом, и своп
печатается по раундам, чтобы это было видно, а не постулировалось.

---

## ПРЕФИЛЛ: что из замера 13.08 уцелело

**ЧИСЛА СКОРОСТИ ИЗ ЭТОГО РАЗДЕЛА СНЯТЫ** -- они мерились БЕЗ
`mx.compile` (348.7 и 350.9 ток/с). На компилированном пути 532-533 ток/с,
см. раздел про компиляцию. Уцелели два вывода, оба качественные:

1. **Раскладка sym префилл не замедлила.** При N >= GEMM_MIN_BATCH_NB он
   идёт через `_dequant_w` + плотный матмул и кернеля sym не касается
   вовсе.
2. **Разбиение промпта на куски памяти НЕ экономит** -- гипотеза «MLX
   ленив, граф на 512 токенов держит промежуточные тензоры всех слоёв
   разом» проверена и снята: chunk 256/128/64 дал пик 6.06/6.05/6.05 ГБ
   против 6.22 одним куском, то есть −3%, при падении скорости с 345 до
   140-236 ток/с. (Отдельно от этого куски проверены КАК СПОСОБ УБРАТЬ
   ТРАССИРОВКУ на компилированном пути -- тоже не годятся, см. раздел
   про компиляцию.)

Актуальная память префилла: 3265 МБ на sym-пресете и 3386 на sb6, и это
теперь МАКСИМУМ процесса -- больше, чем загрузка (2290/2390). Статья --
транзиенты `_dequant_w`: при N >= 128 веса разворачиваются в плотный
fp16 на каждую проекцию, только голова -- 268 МБ за вызов. Закрывает её
квантованный GEMM (открытый вопрос 2).

## MSTRACE: показания счётчиков (13.08, 1.5B)

ВНИМАНИЕ, ЗАГОЛОВОК ЭТОГО РАЗДЕЛА БЫЛ «КЕРНЕЛЬ У ПОТОЛКА» И ЭТО СНЯТО
15.08: потолок полосы оказался 104 ГБ/с, а не 95.7, поэтому наш GEMV идёт
на 90.6% достижимого, а не на 98. Показания счётчиков ниже -- факты и
остаются; выводы из них перепроверены в разделах про полосу и про
CPU->GPU.

Metal System Trace, Xcode 27, конфиг `reduction_sym_head8`, окно 15 с в
начале устойчивой фазы (закон 25). Снято владельцем проекта; max/min по
окну, средние там, где инструмент их даёт.

| счётчик | показание |
|---|---|
| Top Performance limiter | **100% везде** |
| GPU Read Bandwidth | **97 ГБ/с** в работе, много дыр, местами провал до 30 |
| LLC Limiter / LLC Bandwidth | 27% / max 165, min 9, **среднее 143 ГБ/с** |
| Buffer L1 Miss Rate | среднее **50%** (до 100% и вниз до 19%) |
| L1 Cache Limiter | 20% (внутри шага падает до 0.21%) |
| Compute Shader Launch Limiter | от 1% до 100%, дыры ТОЛЬКО внутри шага |
| Kernel Occupancy / Compute SIMD Inflight | **27% / 26%** |
| F32 / F16 Utilization | 2-12% / 17% на 22 мкс в середине шага, дальше 0 |

### СНАЧАЛА -- ЧЕГО ЭТИ ЧИСЛА НЕ ЗНАЧАТ

**`GPU Bandwidth` -- НЕ пропускная способность шины памяти.** Это трафик
промахов L1 наружу, и он частично обслуживается системным кэшем, поэтому
физически МОЖЕТ превышать DRAM: на Apple Silicon SLC отдаёт порядка
15.4-19.8 байт/такт против 7.7-9.9 у главной памяти
(github.com/philipturner/metal-benchmarks). Владелец проекта наблюдал на
этой же машине 235 ГБ/с -- при паспортных 120 у шины.

Первая редакция этого раздела прочитала «97 ГБ/с при достижимых 95.7» как
«кернель у потолка памяти, оптимизировать нечего». **Это неверно:**
счётчик и «достижимая полоса» из закона 12 меряют РАЗНЫЕ вещи (второе --
потоковое чтение fp16 из DRAM), и совпадение чисел случайно. Вывод снят.

### Что из трассы стоит твёрдо

1. **Мы НЕ упираемся в ALU.** F32/F16 Utilization 2-17%, и то на 22 мкс в
   середине шага. Руфлайн-проба кернеля-4 («декодный ALU бесплатен»)
   подтверждена независимо.
2. **Мы НЕ упираемся в кэши.** L1 Cache Limiter 20%, LLC Limiter 27%.
3. **Дыры реальны и они ВНУТРИ шага**, а не между шагами -- видны в
   КАЖДОМ счётчике, наблюдение прямое.
4. **Occupancy 27%** при 26% SIMD Groups Inflight.
5. **Buffer L1 Miss Rate 50%, а не ~100%**, и LLC Bandwidth (143 ГБ/с)
   выше GPU Read (97) -- кэш реально переиспользуется. Это не веса (1.5 ГБ
   через LLC не проходят), а активации, scale и d: их читают все
   threadgroup'ы.

### Пол по DRAM: считается из АЛГОРИТМА, а не из счётчика

Веса читаются ровно по разу за токен, и рабочий набор 1.5 ГБ через
несколько мегабайт LLC не проходит -- значит 1296 МБ за токен ОБЯЗАНЫ
приехать из DRAM. При 18.1 мс это **71.6 ГБ/с устойчивого DRAM-трафика**,
то есть 69% от замеренных нами же 104 ГБ/с (закон 12, потоковое чтение).

Отсюда пол: 1296 МБ / 104 ГБ/с = **12.46 мс = 80.2 ток/с** против
нынешних 55.2. Разрыв -- 4.6 мс на токен, около 25%. Это число НЕ
зависит от того, как читать счётчики: оно из объёма весов, времени шага
и нашего собственного замера полосы.

### ВИНОВНИК НАЗВАН: Instruction Throughput Limiter 92%

Досняты остальные лимитеры (средние по шагу):

| лимитер | Limiter | Utilization |
|---|---|---|
| **Instruction Throughput** | **92%** | -- |
| Integer and Complex | 62% | 43% |
| MMU | 26% | -- |
| Integer and Conditional | 16% | 15% |
| Control Flow | 1.3% | 0.3% |
| F32 / F16 | -- | 2-12% / 17% на 22 мкс |

**Шаг упирается в ВЫДАЧУ ИНСТРУКЦИЙ, и инструкции эти ЦЕЛОЧИСЛЕННЫЕ.**
Плавающая арифметика не при чём вовсе -- ровно наоборот тому, что
подсказывала интуиция «GEMV = матмул = флопы».

### Но это НЕ большие GEMV, и это доказывается уже снятыми числами

Соблазн прочитать «92% instruction throughput» как «кернель захлёбывается
на распаковке нибблов» велик: у sym@6 на 32 колонки приходится порядка
полусотни целочисленных операций (маски, сдвиги, мульт-трюк битплоскостей
-- по целочисленному умножению на ниббл, конверсии uchar->float).
Проверяется это БЕЗ новой трассы, микро-A/B от 13.08:

| форма | sym@6 | sym@8 |
|---|---|---|
| cmix key | 88.4 ГБ/с | 90.7 ГБ/с |
| head | 105.9 ГБ/с | **106.5 ГБ/с** |

У восьми бит битплоскостей НЕТ ВООБСЕ (код -- это байт), нет и xbsum, то
есть целочисленной работы в разы меньше. **Пропускная способность при
этом одинаковая до процента.** Если бы большие GEMV упирались в выдачу
целочисленных инструкций, восьмибитный вариант обязан был бы уехать
вперёд по ГБ/с. Он не уехал -- значит большие GEMV упираются в ПАМЯТЬ,
а не в инструкции, и мульт-трюк битплоскостей платить не заставляет.

**Следовательно, 92% приходят от ОСТАЛЬНЫХ кернелей шага** -- лерпов,
восьми LoRA-матмулов на слой, поканальных модуляторов, нормировки ключа,
WKV-скана, group_norm, bonus, gate. Их около сорока на слой против шести
GEMV, они мелкие, целочисленно-индексные и с низкой занятостью (Kernel
Occupancy 27%). Именно они дают и дыры внутри шага, и целочисленную
выдачу, и низкий occupancy -- три наблюдения одной причины.

### Это уточняет и потолок

Голова в sym@6 выдала **113.5 ГБ/с** (931 МБ за 8.203 мс) -- 95% от
паспортных 120 ГБ/с шины. Подозрение, что «95.7 достижимых» занижены,
ПОДТВЕРДИЛОСЬ 15.08 прямым замером: потолок 104 ГБ/с (закон 12). Но 113.5
выше и его, значит голова в микробенче всё-таки получает помощь от
кэша/перекрытия соседних запусков, и брать её за оценку полосы нельзя.
Пол по трафику при 104 ГБ/с -- 1296 МБ / 104 = **12.46 мс = 80.2 ток/с**
против нынешних 55.2.

### РАЗЛОЖЕНИЕ ШАГА ВЫЧИТАНИЕМ: 73% GEMV, 27% всё остальное

Per-kernel атрибуции в Metal System Trace НЕТ: трек Compute показывает
команды буфера («Command Buffer 0:Compute Command 0», 294 мкс), а имена
ядер даёт только отдельный GPU frame capture. Поэтому разложение сделано
вычитанием в Python -- `tests/bench_step_decompose.py`, чередование в
одном процессе, своп на границе замера не рос:

| что | мс | доля |
|---|---|---|
| полный шаг | **18.732** | 100% |
| только GEMV (24 слоя x 6 + голова) | **13.760** | **73%** |
| всё остальное | **4.972** | **27%** |
| пол синхронизации (одна операция) | 0.257 | -- |

**GEMV-часть прокачивает 1296 МБ за 13.76 мс = 94.2 ГБ/с, то есть
90.6% от замеренных достижимых 104** (уточнено 15.08; при прежней оценке
потолка 95.7 это читалось как 98%, и отсюда шёл вывод «трогать нечего» --
он снят, в GEMV осталось ~9%, то есть 1.3 мс). Число получено прямым
вычитанием, а не чтением счётчика (тот меряет промахи L1, а не шину).
А 4.97 мс «остального» почти не создают трафика: это
LoRA-ветки (по восемь матмулов на слой, 192 запуска на шаг), WKV-скан,
нормы, лерпы, bonus, gate. Они же дают Instruction Throughput Limiter
92% с целочисленной, а не плавающей нагрузкой, и они же -- дыры внутри
шага и Kernel Occupancy 27%. Три наблюдения, одна причина.

### CPU-сторона: постановка задач стоит денег

Из скриншотов трассы: бэктрейс главного потока идёт через
`mlx::core::eval` -> `mlx::core::eval_impl`, а под ним хеш-таблицы и
`__xzm_xzone_free_to_freelist` (libsystem_malloc). То есть на каждый из
~1000 примитивов шага CPU платит поиском в хеше и аллокацией; заняты
несколько performance-ядер сразу. `CPU to GPU Latency` в снятых
командных буферах -- **4.50 и 4.10 мс**.

СНЯТО 15.08 В ЧАСТИ ЛАТЕНТНОСТИ: `CPU to GPU Latency` есть ОЧЕРЕДЬ, а не
накладные постановки -- забег вперёд на 16 шагов не даёт ничего, ручки
командных буферов не помогают (см. раздел «CPU->GPU»). Уцелело другое:
CPU-работа по построению графа реальна и стоит 2.7 мс, но её снимает
`mx.compile`, а не фьюз. Фьюз выигрывает один раз, на GPU, и это
измерено: +1.15 мс.

### Бюджет, который из этого следует

| правка | мс/ток | ток/с |
|---|---|---|
| сейчас | 18.73 | 53.4 |
| срезать «остальное» вдвое | 16.25 | **61.6** |
| срезать «остальное» целиком | 13.76 | **72.7** |

ЧТО ИЗ ЭТОГО СДЕЛАНО 15.08: `SymQuantLinearFused` и хвост TMix дали
**+1.15 мс (6.2%)**, 53.6 -> 57.1 ток/с. Покомпонентное разложение
показало, что из оставшегося «прочего» доминирует LoRA (2.50 мс), а
WKV дёшев (0.84) и нормы почти бесплатны (0.67 на все) -- поэтому
кандидат остался один, и он в открытом вопросе 1.

ВАЖНО ПРО КАЧЕСТВО: фьюзы меняют ПОРЯДОК суммирования, то есть не
обязаны быть бит-в-бит. Гейт `tests/test_fuse_parity.py` для этого и
существует; любую фьюз-правку принимать только с ним, иначе «ускорение
без потери качества» окажется ускорением с непроверенной потерей.

### Сколько это в операциях

В шаге порядка 40 примитивов на слой (6 лерпов, 4 GEMV проекций, по 2
матмула на каждую из четырёх LoRA-веток, поканальные модуляторы,
нормировка ключа, WKV, group_norm, bonus, gate) на 24 слоя плюс голова --
около тысячи запусков на токен. 4.6 мс на тысячу -- это ~4.6 мкс дыры на
запуск, что для Metal правдоподобно.

**Следствие, которое стоит держать перед глазами: если дыры и правда
диспетчерские, сокращение оп-каунта вдвое даёт +2.3 мс, то есть
15.8 мс/ток и ~63 ток/с.** Ровно цель «85% полосы». Причём инструменты для этого уже написаны и лежат
неиспользованными:

1. **FUSE=True** -- r/k/v одним запуском. Документированные −0.8 мс,
   гейты зелёные (открытый вопрос 8). Для sym нужен
   `SymQuantLinearFused` -- фьюз требует `GwQuantLinear`.
2. **Фьюз LoRA-блока** -- сейчас 6-8 опов на слой (открытый вопрос 7).
3. **Фьюз groupnorm + bonus + gate** вокруг WKV -- там же.

Прежняя оценка по этим двум пунктам была «−0.5-1 мс»; трасса говорит,
что бюджет вчетверо больше, потому что дыр 4.7 мс, а не 2.7.

### Occupancy 27% -- не проблема, но и не запас

Kernel Occupancy 27% при 26% SIMD Groups Inflight. Занятость низкая, но
пропускную это не ограничивает: мы уже на полке памяти в рабочих
участках. Полезна она в другом -- запас по occupancy означает, что
СЛИЯНИЕ запусков (больше работы на диспатч) не упрётся в железо.

### Оговорка

Счётчики стоят своих процентов, и во время захвата шаг медленнее, чем
18.1 мс. Все выводы выше сделаны по ОТНОШЕНИЯМ счётчиков между собой и
по 97 ГБ/с (величина, которая от оверхеда инструментария не зависит), а
не по абсолютной скорости во время трассы.

---

## Counter Set в Xcode 27 (прочитано с машины 13.08) и что из него нам нужно

ЗАПИСАНО С ЭКРАНА, а не из документации: набор счётчиков менялся между
поколениями (для M3/A17 Apple переписала профилировщик с нуля), и
описания из старых Tech Talk'ов уже не совпадают с именами в меню.
Проверять по этому списку, а не по памяти.

Полный набор (M4, Xcode 27): Main Thread, Vertex, Fragment, Compute,
**Top Performance limiter**, RT Unit Active, VS/FS/Kernel/Total Occupancy,
L1 Eviction Rate, Instruction Throughput Limiter/Utilization,
Vertex/Fragment/Compute Shader Launch Limiter, ALU Utilization,
F32 Limiter/Utilization, F16 Limiter/Utilization, Integer and Conditional
Limiter/Utilization, Control Flow Limiter/Utilization, Integer and Complex
Limiter/Utilization, L1 Cache Limiter/Utilization, L1 Total Residency и
по классам (Buffer / Imageblock / RT Scratch / Register / Stack /
Threadgroup / Other), L1 Read/Write Accesses по тем же классам,
**Buffer L1 Miss Rate**, Vertex/Fragment/Compute Shader Launch Utilization,
Total/Vertex/Fragment/Compute SIMD Groups Inflight, L1 Read/Write Bandwidth
и по классам, **GPU Bandwidth / GPU Read Bandwidth / GPU Write Bandwidth**,
Texture Read/Write Limiter/Utilization, Texture Read Cache
Limiter/Utilization, **MMU Limiter/Utilization**, **LLC Limiter/Utilization,
LLC Bandwidth**.

### Что из этого относится к нашей задаче

Декод -- это чистый compute: ни вершин, ни фрагментов, ни текстур, ни
трассировки лучей. Значит Vertex/Fragment/Texture/RT-ветки набора нам не
нужны вовсе, и это половина списка.

Ключевая арифметика для чтения (из Tech Talk по family 9, формулы не
менялись):

    Utilization = работа / (пиковая скорость * окно)
    Limiter     = (работа + сталлы) / (пиковая скорость * окно)

Высокий Limiter при низкой Utilization = стоим, а не считаем. Именно по
этой паре различаются гипотезы, а не по одному числу.

**Минимальный набор для нашего GEMV-декода (восемь треков):**

| счётчик | что различает |
|---|---|
| Top Performance limiter | называет узкое место сам |
| GPU Read Bandwidth | сверяется с расчётным трафиком: 1296 МБ/ток при 18.1 мс = **71.6 ГБ/с** (69% от замеренных достижимых 104). Заметно больше -- перечитываем; заметно меньше -- часть гасится кэшем |
| LLC Limiter + LLC Bandwidth | упираемся ли в последний уровень кэша / обратное давление памяти |
| Buffer L1 Miss Rate | у потокового чтения весов должен быть под 100%; ниже -- есть переиспользование, которого мы не эксплуатируем |
| L1 Cache Limiter | не упираемся ли в L1 на распаковке нибблов |
| Compute Shader Launch Limiter | гипотеза «упираемся в диспетчеризацию, а не в полосу» |
| Kernel Occupancy + Compute SIMD Groups Inflight | хватает ли параллелизма при NSG=4, RS=4 |
| F32 + F16 Utilization | ALU (декод кодов, умножения на scale) -- ожидается НИЗКОЙ |

**Гипотезы, которые этот набор разделяет:**

- H1, упираемся в DRAM: GPU Read Bandwidth ~70 ГБ/с, LLC Limiter высокий,
  Buffer L1 Miss Rate под 100%, F32/F16 Utilization низкая.
- H2, упираемся в диспетчеризацию: дыры в треке Compute, Compute Shader
  Launch Limiter высокий, Kernel Occupancy низкая, SIMD Groups Inflight
  мало. Тогда 19% до 85% полосы лежат НЕ в трафике, и оптимизировать
  надо запуски, а не байты.
- H3, упираемся в ALU: F32/F16 Limiter высокий ПРИ высокой Utilization.
  Маловероятно (кернель-4 закрыт руфлайн-пробой: декодный ALU бесплатен),
  но проверяется даром.
- H4, упираемся в MMU: MMU Limiter высокий -- TLB не держит рабочий набор
  в 1.5 ГБ буферов.

Предсказание ДО замера (чтобы трасса была проверкой, а не рыбалкой): по
микро-A/B на токен приходится ~15.4 мс GEMV (24 слоя x 0.6 мс плюс
1.04 мс голова) при измеренных 18.1 мс, значит ожидается GPU занятым на
~85% и около 2.7 мс дыр на токен.

ОГОВОРКА ПРО САМ ЗАМЕР: включённые счётчики стоят своих процентов, и
мс/ток во время захвата будет хуже 18.1. Сравнивать надо счётчики между
собой, а не абсолютную скорость с бенчем. И окно брать в первые 20-25
секунд -- дальше троттлинг (закон 25).

---

## ПАМЯТЬ И ТРАФИК ПРЕСЕТА С sym (13.08, 1.5B, `tests/report_sym_memory.py`)

Три числа, которые постоянно путают: размер ФАЙЛА, РЕЗИДЕНТНАЯ память и
ТРАФИК за токен. Они не равны и меняются в разные стороны.

| | REDUCTION | + sym + head@8 |
|---|---|---|
| файл | 1255.4 МБ | 1299.5 МБ (**+44**) |
| живые буферы в памяти | 1483.7 МБ (1.18x) | 1506.8 МБ (1.16x) |
| трафик за токен | 1273.1 МБ | 1296.2 МБ (**+1.8%**) |
| замерено | 18.33 мс, 54.5 ток/с | 18.90 мс, 52.9 ток/с (**+3.1%**) |

ОТРИЦАТЕЛЬНЫЙ РЕЗУЛЬТАТ (закон 5): первая редакция утверждала, что
пресет с sym на 227 МБ ЛЕГЧЕ. Это было свойством не раскладки, а того,
что sb6-ветка строила дубль фьюза (250 МБ), лежавший мёртвым грузом при
FUSE=False, а sym его не строил вовсе. После ленивой постройки фьюза
(15.08) sym на 23 МБ ТЯЖЕЛЕЕ -- ровно разница интерливов.

Состав памяти: REDUCTION -- sb6-интерлив 1111.5, emb плотная 268.4,
LoRA в fp16 100.1, мелочь 3.7. У sym то же с интерливом 1134.6.

Прежняя запись «1.85x от файла» получена через аллокатор MLX
(`mx.get_active_memory`); здесь -- прямой подсчёт живых буферов обходом
модели, и он даёт 1.18x. Разница -- накладные аллокатора, и полагаться в
этом вопросе на метрики фреймворка нельзя (закон 11).

### Цена по скорости = рост трафика, и ничего сверх

Трафик вырос на 1.8%, время -- на 1.7-3.1% по трём прогонам. То есть
**прибавка объясняется байтами целиком**, и это независимое
подтверждение микро-A/B, где sym@6 дал паритет, а sym@8 -- ровно свои
+23% трафика.

Почему +44 МБ файла превратились всего в +23 МБ трафика: K3-интерлив
sb6 хранит `qsqm` как uchar2 (2 байта на блок вместо канонических 1.5,
+0.125 бит/вес), а у sym масштаб блока -- уже целый байт, и такой
надбавки нет. Часть роста файла съедается тем, что раскладка дешевле
разворачивается в память.

### Где на самом деле лежит потолок скорости

Считать «файл / полоса» неверно: `emb` -- это gather одной строки, и за
токен едет 1296 МБ из 1299 МБ файла... но в ПАМЯТИ таблица развёрнута в
268 МБ, а на диске занимает 109. Правильная арифметика -- по трафику:

| | мс/ток | ток/с | доля полосы |
|---|---|---|---|
| замерено | 18.90 | 52.9 | 68.6 ГБ/с = **66%** |
| 85% достижимой (88.4 ГБ/с) | 14.66 | 68.2 | цель |
| 104 ГБ/с (достижимая, замерена 15.08) | 12.46 | 80.2 | потолок |
| 120 ГБ/с (паспортная арифметика шины) | 10.80 | 92.6 | недостижим |

**Мы на 66% полосы** (пересчитано 15.08 на измеренный потолок 104 вместо
прежней оценки 95.7). Из разрыва до 85% часть теперь лежит И В GEMV:
94.2 ГБ/с -- это 90.6% достижимого, а не 98%, то есть там осталось около
9%, или 1.3 мс на токен. Остальное -- по-прежнему трафик:

1. **LoRA-ветки: 100.1 МБ за токен (8% трафика), но 12% шага -- идут на
   46.6 ГБ/с против 85-93 у прочих.** УТОЧНЕНО 15.08 покомпонентным
   разложением: упираются они НЕ в полосу, а в формы (ранги
   96/96/64/256, восемь матмулов на слой, 192 запуска за токен). Прежняя
   оценка «квантовать = минус 128 МБ трафика = 10% скорости» НЕВЕРНА:
   срезав байты вчетверо, время вчетверо не срежешь. Рычаг -- укрупнение
   запусков, и это доказано фьюзом: он снимает 0.58 мс, не трогая ни
   байта. Потолок статьи ~1.0 мс (5.6% шага).
2. **FUSE=False** -- документированные −0.8 мс (4%), гейты зелёные
   (открытый вопрос 8). Для sym нужен `SymQuantLinearFused`, которого
   нет: фьюз требует `GwQuantLinear`.
3. Остальное -- WKV-скан, нормы, диспетчеризация.

Отдельно по ПАМЯТИ (не по скорости): `emb` развёрнута в плотный fp16 --
268.4 МБ против 109.1 на диске. Indexed-gather для sb6 снял бы 159 МБ
резидентно. Это та же незакрытая задача, что и в разделе про 1.85x.

### Сколько занимает ПРОЦЕСС (а не буферы), замерено vmmap

Обход модели даёт 1564.6 МБ живых буферов -- но это буферы, а не
процесс. `vmmap --summary` на устоявшемся декоде (конфиг с sym, 1.5B):

| | |
|---|---|
| Physical footprint, устоявшийся декод | **1.7-1.8 ГБ** |
| из них `IOAccelerator (graphics)` | **1.5 ГБ** -- ровно веса |
| Physical footprint (peak) | **7.7 ГБ** -- воркспейс КВАНТОВАНИЯ |
| ReadOnly portion of Libraries | 2.3 ГБ адресов, резидентно 225 МБ |

То есть модель -- 1.5 ГБ, процесс на декоде -- 1.7-1.8 ГБ (сверху
интерпретатор, torch, рантайм MLX), а 7.7 ГБ пика существуют только на
время сборки из `.pth`. Если наблюдаемый процесс занимает 2.7-3.5 ГБ,
это либо фаза сборки, либо загрузка иным путём, либо счётчик, который
приплюсовывает разделяемые библиотеки (их 2.3 ГБ адресного
пространства при 225 МБ резидентно).

ВНИМАНИЕ: всё это про ЛАБОРАТОРНЫЙ бэкенд `backends/metal/quant_model.py`.
У приложения расклад другой (см. раздел про потребителей формата).

---

## РАЗЛОЖЕНИЕ ГРУПП ПО KL: где именно сидит остаток (13.08, оба масштаба)

`tests/ablate_subgroups.py`. Инструмент новый, и он важнее конкретных
чисел ниже.

### Почему ppl тут не работает и чем заменена

Бюджет REDUCTION после sym -- +0.111% на fake-пути, полоса различимости
на корпусе ±0.05-0.09 п.п. Разложить это на восемь частей по ppl нельзя
в принципе: каждая часть около 0.01 п.п. Leave-one-out, которым нашли
`small`, работал при бюджете +2.36%.

**KL(bf16 ‖ квантованная) на токен** сравнивает распределения целиком --
65536 логитов на каждый токен вместо вероятности одного правильного.
На том же прогоне: композит даёт **KL = 0.003813 нат/токен, 95% CI
[0.003578; 0.004033]**, то есть ±6% от эффекта, тогда как ppl тот же
эффект (+0.111%) не отличает от нуля вовсе. Разница в разрешении --
порядок с лишним.

Эталон (логиты bf16) считается один раз и кладётся на диск во FLOAT32:
fp16 при логитах порядка 10 имеет шаг, сравнимый с измеряемым эффектом,
то есть инструмент вышел бы грубее ppl, ради ухода от которой он и
делался. 8x512 -> 1.07 ГБ.

Контроль: bf16 против самого себя даёт **KL = 0.000000 и top-1 100%**.
Заодно это проверка, что MPS воспроизводим между процессами.

### Разложение (8x512 = 4088 токенов, конфиг reduction_sym_head8)

| подмножество | KL изолированно | % композита | МБ | KL на МБ |
|---|---|---|---|---|
| cmix key | 0.001393 | 36.5% | ~330 | 4.2e-6 |
| cmix value | 0.001309 | 34.3% | ~330 | 4.0e-6 |
| **emb** | **0.001008** | **26.4%** | **109** | **9.2e-6** |
| proj v | 0.000978 | 25.7% | ~83 | **11.8e-6** |
| proj o | 0.000872 | 22.9% | ~83 | **10.6e-6** |
| proj r | 0.000644 | 16.9% | ~83 | 7.8e-6 |
| proj k | 0.000606 | 15.9% | ~83 | 7.3e-6 |
| head @8 | 0.000124 | 3.3% | 144 | 0.9e-6 |

**Сумма изолированных 0.006933 против композита 0.003813 -- 55%.**
Ошибки групп ЧАСТИЧНО ГАСЯТ ДРУГ ДРУГА, складывать вклады нельзя. Это
не мелочь методики: любой вывод вида «уберём эти три группы и получим
минус их сумму» неверен вдвое.

### Что из этого следует для битов

Столбец «KL на МБ» -- это цена уже занятого места, и он расслаивается
втрое: **proj v/o дороже cmix втрое при том, что cmix занимает
половину файла.** То есть биты дешевле всего забирать у cmix и дороже
всего экономить на proj -- ровно то, что предполагала гипотеза
перекладывания (cmix@5 + proj@8), теперь с разложением по ролям.

**emb -- главная неожиданность.** 26.4% композитного KL при 8.4% файла,
третье место по абсолюту и третье по цене за мегабайт. По ppl тот же
emb@6 на 1.5B давал +0.014% [−0.056; +0.088], то есть «не отличим от
bf16», и на этом основании было решено его не трогать. **Два
инструмента дают разный ответ, и это не противоречие:** ppl смотрит
вероятность правильного токена, KL -- всё распределение. Для ppl-таблиц
emb действительно бесплатен; для базы QLoRA и для генерации (мы из этого
распределения сэмплируем) -- нет. Какой из двух критериев целевой,
решает владелец проекта; но «emb бесплатен» больше не безусловное
утверждение.

### ОТРИЦАТЕЛЬНО: `v_first` не оказался вторым `small`

Гипотеза была структурная и правдоподобная: на нулевом слое
`v_first = v`, и КАЖДЫЙ последующий слой подмешивает этот v_first
(`v = v + (v_first - v) * resid_gate`). То есть `blocks.0.att.value.weight` --
одна матрица из 96 в группе proj, 1% группы, с влиянием на весь стек.
Профиль ровно как у `small` (147K параметров, 0.01% модели, две трети
деградации).

Замер: **1.8% композитного KL при 1% группы по размеру.** Множитель 1.8,
а не 60, как у `small`. Гипотеза не подтвердилась.

`layer0` целиком -- **6.6% при 4.2% слоёв**, множитель 1.6. Нулевой слой
чувствительнее среднего, но тоже без драмы.

Вывод, который стоит держать: **второго `small` в этих группах нет.**
`small` был исключением не потому, что мы плохо искали, а потому что
поканальные модуляторы внутри рекуррентности -- принципиально другой
объект, чем матрица проекции.

### ОПРОВЕРГНУТО: «k и v дороже r и o, потому что пишут в состояние»

Предсказание было такое: k и v входят в состояние, их ошибка копится по
длине; r и o читают текущий шаг, их ошибка живёт один токен. Значит k,v
>> r,o.

Измерено: **v 25.7% > o 22.9% > r 16.9% > k 15.9%.** v действительно
самый дорогой, но **k -- САМЫЙ ДЕШЁВЫЙ**, то есть по паре (k, o)
предсказание перевёрнуто.

Механизм у этого есть, и он специфичен для RWKV-7: **ключ
L2-НОРМАЛИЗУЕТСЯ** -- `kk = F.normalize((k * k_k).view(B,T,H,N), dim=-1)`.
Нормировка делит на норму головы, поэтому масштабная составляющая ошибки
k_proj из состояния уходит. У v такой защиты нет: v пишется в состояние
как есть И входит в bonus-член. То есть чувствительность определяется не
«пишет/читает», а тем, есть ли ПОСЛЕ матрицы нормировка.

Практическое следствие: у proj есть внутренний разброс 1.6x (v против
k), и он направлен в сторону, которую нельзя было угадать. Пресет с
разной битностью внутри proj (v/o дороже, k/r дешевле) -- следующий
кандидат после cmix@5.

### Профиль по позиции: НАКОПЛЕНИЯ НЕТ

| позиция | 0-64 | 64-128 | 128-256 | 256-512 |
|---|---|---|---|---|
| KL | 0.004804 | 0.004140 | 0.003706 | 0.003537 |

KL на токен ПАДАЕТ с позицией на 26%, а не растёт. Это не отменяет
прежнего замера «деградация растёт с контекстом» (+1.52% на 128 токенах
против +2.57% на 512): там сравнивались Δppl% на разных длинах, а ppl
сама падает с позицией, так что относительная доля могла расти при
падающем абсолюте. Но простой картины «ошибка входит в состояние и
копится» здесь НЕ видно, и строить на ней калибровку (взвешивать AW по
горизонту памяти канала) нельзя, пока не появится замер, который её
подтверждает.

### Профиль по слоям (`--trace`)

‖Δx‖/‖x‖ residual stream, bf16 против квантованной, слой за слоем:
2.1e-2 на нулевом, рост до 3.9e-2 к девятому, дальше ПЛАТО, и скачок до
4.5e-2 на последнем (23-м). То есть ошибка набирается в первой трети
стека и дальше не растёт -- норма residual stream растёт вместе с ней.
Скачок на последнем слое стоит проверить отдельно.

### 2.9B: ЧТО ПЕРЕЖИЛО ЗАКОН 10, А ЧТО НЕТ

Тот же скрипт, тот же срез 8x512, статистика активаций своя.
Композит: **KL = 0.004458, CI [0.003880; 0.005108]**, top-1 совпадает
96.5% (на 1.5B -- 96.5%, то есть распределение расходится одинаково
сильно, хотя по ppl 2.9B выглядит вдвое хуже).

| подмножество | KL 2.9B | % | МБ | KL/МБ | % на 1.5B | KL/МБ на 1.5B |
|---|---|---|---|---|---|---|
| **proj o** | **0.002048** | **45.9%** | 172 | **11.9e-6** | 22.9% | 10.6e-6 |
| cmix key | 0.001055 | 23.7% | 688 | 1.5e-6 | 36.5% | 4.2e-6 |
| **emb** | 0.001062 | **23.8%** | 136 | **7.8e-6** | **26.4%** | **9.2e-6** |
| cmix value | 0.000951 | 21.3% | 688 | 1.4e-6 | 34.3% | 4.0e-6 |
| proj v | 0.000768 | 17.2% | 172 | 4.5e-6 | 25.7% | 11.8e-6 |
| proj r | 0.000479 | 10.7% | 172 | 2.8e-6 | 16.9% | 7.8e-6 |
| proj k | 0.000472 | 10.6% | 172 | 2.7e-6 | 15.9% | 7.3e-6 |
| head @8 | 0.000118 | 2.7% | 180 | 0.7e-6 | 3.3% | 0.9e-6 |

Сумма изолированных 0.006954 против композита 0.004458 -- **64%**
(на 1.5B 55%). Субаддитивность подтвердилась на обоих масштабах:
ошибки групп частично гасят друг друга, и складывать вклады нельзя.

**Что переносится между масштабами:**

1. **cmix -- самое дешёвое место за мегабайт** (1.4-1.5e-6 против
   11.9e-6 у proj o), и при этом он занимает 55% файла. Забирать биты
   надо оттуда, и это верно на обоих масштабах.
2. **emb дорог: 24-26% композитного KL при 5.5-8.4% файла**, второе-
   третье место по цене за мегабайт на обоих чекпоинтах. Это самый
   устойчивый вывод разложения -- и он прямо противоречит решению «emb
   не трогать», принятому по ppl.
3. **Внутри proj дёшевы k и r, дороги o и v.** Порядок o/v между
   масштабами меняется (на 1.5B v первый, на 2.9B o вчетверо больше
   всех), но пара (o, v) выше пары (k, r) на обоих.
4. **head@8 переобеспечен** (0.7-0.9e-6/МБ): два лишних бита сделали
   своё дело, и трогать его больше незачем.
5. **Накопления по длине НЕТ на обоих масштабах**: KL падает с позицией
   на 26% (1.5B) и 28% (2.9B).
6. **`v_first` не второй `small`** ни там, ни там: 1.8% и 2.5%
   композитного KL при 1% и 0.8% группы по размеру.

**Что НЕ переносится -- и это главный результат замера:**

**`blocks.0` на 2.9B уносит 31.3% композитного KL, занимая 3.1% слоёв.
Множитель ДЕСЯТЬ. На 1.5B тот же нулевой блок -- 6.6% при 4.2%, то есть
1.6.** Ровно то, чего искали в vfirst, нашлось этажом выше и только на
большей модели. Это, наконец, второй случай профиля `small`: маленькая
доля параметров, непропорциональная доля деградации.

Согласуется и профиль residual stream: на 2.9B ‖Δx‖/‖x‖ МАКСИМАЛЬНО на
нулевом слое (6.9e-2) и РАЗБАВЛЯЕТСЯ к девятому (4.1e-2), дальше плато;
на 1.5B наоборот -- растёт с 2.1e-2 до 3.9e-2 к девятому. Формы
противоположные, и обе сходятся с тем, что говорит KL.

### Куда это ведёт по битам (гипотезы, НЕ замеры)

Столбцы KL/МБ расслаиваются на порядок, значит нынешняя раскладка бит
далека от оптимума ПО ЭТОМУ КРИТЕРИЮ. Кандидаты, от самого выгодного:

| правка | 2.9B, МБ | что должно уйти |
|---|---|---|
| cmix 6 -> 5 бит (sym@5) | **−210** | ~4% KL (самое дешёвое место) |
| `blocks.0` целиком @8 | +25 | до 31% KL |
| proj o @8 | +52 | до 46% KL |
| emb @8 (sym) | +43 | до 24% KL |

То есть на 2.9B видна конфигурация **меньше на ~90 МБ и заметно лучше
по KL одновременно**. Три оговорки, все существенные. Первая: вклады
субаддитивны (64%), суммировать выигрыши нельзя. Вторая: `blocks.0` и
`proj o` ПЕРЕСЕКАЮТСЯ. Третья: KL и ppl -- разные критерии, и правка,
очевидная по KL, может ничего не дать по ppl (как это уже вышло с emb,
только наоборот).

### Оговорки

Срез 8x512 на обоих чекпоинтах. Закон 10 требует проверки на 2.9B
прежде любой правки пресета; закон 5 -- композитного замера того
варианта, который решат внедрять. Ранжирование снято при ТЕКУЩЕЙ
раскладке битов (head@8, всё прочее @6) и на другую не переносится
автоматически.

---

## Состояние пресетов (перемерено 04.08, мультиязычный корпус, 512 ток)

Все числа — реальный путь `QuantRWKV7` (`real_gw=True`), не fake-quant.
Корпус `eval_corpus_multiling.pt`: 38x512 = 19456 предсказаний,
ru 20 / en 9 / sr 9 последовательностей. act_stats мультиязычные, сняты
на ОТЛОЖЕННЫХ чанках.

### 1.5B (bf16 ppl 8.1980, 3055.3 МБ)

| конфиг | МБ | en | ru | sr | ALL |
|---|---|---|---|---|---|
| REDUCTION | 1255.3 | +1.64% | +1.29% | +5.52% | +2.36% |
| REDUCTION `small=16` | 1255.4 | +1.04% | +0.55% | +1.00% | **+0.77%** |
| REDUCTION `small=16`+proj search | 1255.4 | +0.97% | +0.72% | +0.65% | +0.76% |
| COMPRESSION | 970.1 | +4.60% | +4.21% | +8.96% | +5.41% |
| COMPRESSION `small=16` | 970.2 | +4.01% | +3.40% | +3.78% | **+3.63%** |

### 2.9B (bf16 ppl 7.1630, 5896.1 МБ)

| конфиг | МБ | en | ru | sr | ALL |
|---|---|---|---|---|---|
| REDUCTION | 2420.7 | +1.00% | +1.76% | +1.49% | +1.52% |
| REDUCTION `small=16` | 2420.9 | +0.93% | +1.57% | +1.36% | **+1.37%** |
| REDUCTION `small=16`+proj search | 2420.9 | +1.17% | +1.09% | **+2.20%** | +1.37% |
| COMPRESSION | 1854.5 | +5.64% | +3.26% | +5.62% | +4.38% |
| COMPRESSION `small=16` | 1854.7 | +5.69% | +3.05% | +5.46% | **+4.24%** |

### Правки пресетов: что внесено и что решено не вносить

`small=8` -> `small=16` — ВНЕСЕНО, стоит в обоих пресетах в
`presets.py`. Цена +0.14 МБ (0.011% файла), улучшает все три языка на
обоих масштабах. Все таблицы выше, где написано «REDUCTION», уже с ним.

`proj` — НЕ ТРОГАТЬ, и это подтверждено трижды разными путями:
`asym_sb6_search` выигрывает 0.14 п.п. на 1.5B и портит сербский вдвое
на 2.9B (04.08); AW не помогает изолированно (04.08,
`tests/ablate_aw_proj.py`); композитом 13.08 `asym_sb6_aw` даёт крупный
выигрыш по ALL на 2.9B (−0.501 п.п.), но берёт его целиком на русском и
отдаёт на английском и сербском. Каждый раз это оказывается разменом
между языками, а не улучшением, — закон 10 в чистом виде.

---

## Почему `small`: диагностика 04.08

Leave-one-out по группам на REDUCTION/1.5B
(`tests/ablate_group_contrib.py`, `STAGE=1` и `STAGE=2`), вклад в общие
+2.36%:

| группа -> bf16 | вклад |
|---|---|
| proj | +0.18 п.п. |
| cmix | +0.33 п.п. |
| emb_head | +0.18 п.п. |
| w_lora / a_lora / v_lora / g_lora | −0.02 / +0.09 / +0.02 / +0.07 п.п. |
| **small (k_k, k_a, r_k)** | **+1.59 п.п.** |

147K параметров (0.01% модели) уносят две трети деградации. Три большие
матричные группы вместе — 0.69 п.п.

Механизм: `k_k`/`k_a`/`r_k` — поканальные модуляторы ВНУТРИ
рекуррентности (нормировка ключа, темп in-context обучения,
receptance-бонус). Их ошибка не портит одно предсказание, а искажает
обновление состояния и копится по длине. Подтверждение независимым
замером: деградация растёт с контекстом (+1.52% на 128 токенах против
+2.57% на 512).

На 2.9B эффект в десять раз слабее (−0.15 п.п.), и сербской катастрофы
там нет вовсе (sr +1.49% между en +1.00% и ru +1.76%). Похоже, на 1.5B
модели просто не хватало ёмкости на сербский, и квантование добивало то,
что держалось на пределе.

---

## Позиция против llama.cpp (1.5B, 04.08)

Данные разделены одинаково для обеих систем: калибровка (act_stats /
imatrix) на чанках `test.txt` № 0,4,5,6,12,16,18,20, измерение на
№ 1,2,7,8,10,13,14,17,19. Абсолютные ppl несопоставимы (llama.cpp
токенизирует сплошняком), сравниваются Δ% к своему FP-базлайну.

### Качество (llama.cpp baseline F16 = 7.2504)

| формат | МБ | без imatrix | с imatrix |
|---|---|---|---|
| Q6_K | 1336 | +0.41% | **+0.19%** |
| Q5_K_M | 1158 | +1.86% | **+1.02%** |
| Q4_K_M | 990 | +7.65% | **+3.44%** |

Против нас после `small=16`: ~1255 МБ +0.77% против интерполированных
~+0.57%; ~970 МБ +3.63% против Q4_K_M 990 МБ +3.44%. **Паритет в
пределах методической погрешности на обеих точках.** Без imatrix мы
выглядели победителями — весь отрыв объяснялся тем, что у нас AW было,
а у них нет.

ОБНОВЛЕНО 13.08, ПОСЛЕ УПАКОВЩИКА. Обе оговорки прежней редакции сняты:
точка существует в виде файла, и её число снято реальным путём, как и у
llama.cpp. На 1.5B: **мы +0.192% при 1299.5 МБ против их Q6_K+imatrix
+0.19% при 1336 МБ.** Качество совпадает (разница 0.002 п.п. — далеко
внутри полосы различимости ±0.05-0.09 п.п. на этом корпусе), файл меньше
на 36.5 МБ (2.7%). Честная формулировка: **паритет по качеству при
выигрыше по размеру**, и теперь это сравнение like-for-like, а не с
поправкой на fake-путь.

### Скорость (`tests/bench_decode_vs_gguf.py` + `llama-bench`)

ПЕРЕМЕРЕНО 15.08. Прежде наш ПРЕФИЛЛ здесь шёл СЫРЫМ путём, без
`mx.compile`, тогда как декод рядом уже был скомпилирован -- то есть
половина строки сравнивалась с llama.cpp на одном пути, а половина на
другом (см. раздел «`mx.compile` не звался на префилле»). Обе строки
теперь через `model.step`.

| | МБ | pp512 | tg128 |
|---|---|---|---|
| COMPRESSION | 970 | **603.1** t/s (было 455.7) | **66.58 t/s** |
| Q4_K_M+imat | 990 | **745.2** | 58.16 |
| REDUCTION | 1255 | **518.4** t/s (было 353.7) | **56.67 t/s** |
| Q6_K+imat | 1336 | **705.5** | 48.59 |
| F16 GGUF | 3055 | 797.3 | 30.23 |

**Декод наш на +14.5% и +16.6%.** **Префилл проигрываем в 1.24 и 1.36
раза, а не вдвое**: 603.1 против 745.2 и 518.4 против 705.5, то есть
прибавка +32% и +47% против прежних записей. llama.cpp считает GEMM через
BLAS+Metal, у нас деквант в fp16 + матмул MLX, и это по-прежнему самый
большой намеренный разрыв -- но он втрое-вчетверо меньше, чем было
записано, и квантованный GEMM закрывает теперь 25-35%, а не 100%.

ПЕРВЫЙ ПРОГОН ЭТОГО ПЕРЕМЕРА ОТБРОШЕН: во время замера вырос своп
(закон 11). Он давал 608.8/67.09 и 549.9/56.09 -- COMPRESSION сошёлся с
чистым прогоном до 0.9%, а вот префилл REDUCTION разошёлся на 6%, то есть
своп ударил именно туда. Скрипт теперь печатает своп на границе замера и
свой вердикт САМ; прежде он его не смотрел вовсе, из-за чего негодность
обнаруживалась постфактум и по внешним признакам.

Две оговорки к самому скрипту, обе прежние. `ACT_STATS` указывает на
`/tmp/act_stats_1p5b_multiling.pt`, которого нет (есть `_ml`), поэтому
AW-режимы отрабатывают без взвешивания: на СКОРОСТЬ это не влияет вовсе
(раскладка и размеры те же), на качество влияет -- числа ppl с такой
сборки брать нельзя. И префилл меряется ОДНИМ прогоном без медианы;
по `bench_compile_ab` разброс там 0.3-0.5%, так что это терпимо, но
сравнивать надо однотипные прогоны.

### Эффективность по полосе (104 ГБ/с достижимых, пересчитано 15.08)

| | трафик/ток | достигнуто | доля |
|---|---|---|---|
| COMPRESSION | 878 МБ | 57.6 ГБ/с | 55% |
| REDUCTION | 1146 МБ | 62.9 ГБ/с | 60% |
| Q4_K_M | ~905 МБ | 52.6 ГБ/с | 51% |

Мы уже эффективнее llama.cpp по полосе. До разумных 85% остаётся ~40%,
и это тяжёлый микротюнинг — сознательно отложен.

---

## ГЛАВНОЕ: `.rwkvq` как самостоятельный формат

### Проблема в одной строке (закрыта шагом 4, оставлено как мотивация)

`writer.save()` заканчивался `torch.save(QuantizedCheckpoint)`, а
`reader.load_raw()` — `torch.load(..., weights_only=False)`. Это был не
формат, а pickle питоновского объекта: нечитаем без torch И без
установленного пакета `rwkv_quant` (pickle пишет FQN классов), исполнял
произвольный код при загрузке, не имел `format_version`, mmap и
потензорного чтения.

Из-за этого появился `export_mlx.py` — мост в
`*.rwkvq_mlx.safetensors`. Но он фильтрует по `gw_mode == "sb6"` и
выбрасывает всё остальное, поэтому `SwiftRWKV`/`rwkv-metal` вынуждены
держать рядом bf16-чекпоинт.

### Цифры (2.9B, COMPRESSION), проверены чтением реальных файлов

| | МБ |
|---|---|
| `model.safetensors` bf16 | 5895 |
| `.rwkvq` (все 1062 тензора внутри) | 1855 |
| сайдкар `_mlx.safetensors` (194 sb6-тензора) | 1796 |
| **качает пользователь сейчас** | **7691** |
| **самодостаточный сайдкар** | **~1898** |

**+102 МБ, чтобы 5.9 ГБ исчезли.** Загрузка падает вчетверо. Недостающие
102 МБ: w/a/v_lora в asym-gw64 (47 МБ), g_lora @8 per-row (52 МБ),
одномерные dense bf16 (3 МБ). Плюс токенайзер 1.1 МБ.

K3-интерлив хранит `qsqm` как `uchar2` (2 байта на блок вместо 1.5) —
это +0.125 бит/вес = **+45 МБ** против канонической раскладки. Либо
платим и грузим mmap как есть, либо репакуем при загрузке.

### План

**Шаги 1-4 СДЕЛАНЫ 04.08**, 5-6 остаются.

Что это значит для приложения: `.rwkvq` теперь самодостаточен и читается
без torch. Сайдкар `_mlx.safetensors` из обязательного моста стал
необязательным кешем K3-раскладки — он всё ещё быстрее на загрузке
(раскладка уже интерливнута), но стоит +45 МБ и больше ничего не
разблокирует. Пользователь качает 1855 МБ на 2.9B вместо 7691.

1. ~~**Полный экспорт.**~~ СДЕЛАНО. `export_mlx.py` больше не фильтрует
   по `gw_mode`; манифест получил `format_version: 2` и поле `kind` на
   тензор (`sb6` / `asym` / `rtn` / `dense`), плюс `config_repr` и путь
   к токенайзеру. На 2.9B: 1062 тензора (194 sb6, 192 asym, 160 rtn,
   516 dense) -> **1899.2 МБ**, ровно расчётные 1898. Гейт
   `tests/verify_sidecar_full.py` восстанавливает из сайдкара БЕЗ torch
   и сверяет с `reader._dequantize_one`: **все четыре раскладки
   бит-в-бит, max|Δ| = 0**. Функция `dequant_from_sidecar` в этом гейте
   — референсная читалка для rwkv-metal и SwiftRWKV, порт с неё.
2. ~~**Стриминговый writer.**~~ СДЕЛАНО. `formats.writer.quantize_file`:
   `torch.load(mmap=True)`, потензорное квантование с немедленным
   отпусканием ссылки, метаданные из форм (`detect_meta`) вместо
   инстанцирования `RWKV7Ref`. `api.quantize` переведён на него. A/B на
   1.5B: минимум свободной памяти **34% у старого пути против 77% у
   нового**, время то же (43 с против 42), файл побайтово того же
   размера, 42 сверенных тензора деквантуются идентично.
   ВНИМАНИЕ: `maximum resident set size` тут вводит в заблуждение —
   у нового пути он ВЫШЕ (9.2 ГБ против 5.6), потому что mmap считает
   страницы файла резидентными. Они чистые и вытесняются без записи в
   своп. Мерить `memory_pressure -Q` и `vm.swapusage`, не RSS (закон 11).
3. ~~**torch-free распаковщики.**~~ СДЕЛАНО, но не так, как
   планировалось. Появился `formats/codec.py` — чистый numpy, ноль
   импортов torch: все четыре упаковщика И деквант всех трёх
   квантованных раскладок (`dequant_sb6` / `dequant_asym` /
   `dequant_rtn`). Это нормативная реализация канонической ДИСКОВОЙ
   раскладки, с неё портируются rwkv-metal и SwiftRWKV (у
   `verify_sidecar_full.dequant_from_sidecar` другая задача — она про
   K3-раскладку сайдкара).
   План говорил «torch-версии становятся обёртками» — замер это отменил,
   см. закон 16: обёртки стоили 1.65x на деквант. `schema.py` и
   `reader.py` остались torch, дублирование осознанное и задокументировано
   в докстрингах обоих файлов.
   Гейт `tests/test_codec_parity.py`: роундтрипы, упаковщики против
   замороженной копии прежнего torch-кода, три пути декванта (codec /
   reader / прежний torch) на синтетике всех раскладок и на реальных
   чекпоинтах. 0.1B, 1.5B COMPRESSION, 1.5B REDUCTION (int6, обе
   битплоскости), 2.9B: **2.9 G элементов, расхождений 0**.
   Что это НЕ разблокировало: rwkv-metal всё ещё не читает `.rwkvq`
   напрямую, потому что контейнер по-прежнему pickle. Нужен шаг 4;
   `codec.py` — его половина, которая теперь готова.
4. ~~**Контейнер -> safetensors.**~~ СДЕЛАНО. `writer.save_rwkvq`:
   плоские имена `key::поле`, манифест одним JSON в
   `__metadata__["rwkvq"]` (`format: "rwkvq"`, `format_version: 1`, пять
   чисел про модель, `config_repr`, на тензор — `kind` / `shape` /
   `bits` / `group` / `gw_gs` / `gw_sb` / список полей). `save()` и
   `quantize_file()` переведены, публичный API не изменился.
   `load_raw` различает контейнеры по первым байтам и читает оба.
   **Загрузка теперь ленивая, и это главный эффект** (замер
   `tests/bench_rwkvq_load.py`, по процессу на конфиг):

   | 2.9B | загрузка | maxrss |
   |---|---|---|
   | pickle | 0.24 с | 2.08 ГБ |
   | safetensors | **0.03 с** | **0.25 ГБ** |

   1.5B: 0.16 с / 1.19 ГБ против 0.05 с / 0.24 ГБ. `safe_open` отдаёт
   тензоры видами на отображённый файл, поэтому 1.85 ГБ анонимной памяти
   при загрузке больше не выделяется вовсе; страницы подтягиваются по
   мере обращения и вытесняются как чистые.
   Размер файла не изменился (−0.02%: пропал zip-оверхед torch.save).
   Гейт `tests/test_rwkvq_container.py`: роундтрип всех полей бит-в-бит
   (0.1B / 1.5B COMPRESSION / 1.5B REDUCTION / 2.9B — до 2318 буферов),
   чтение прежних pickle-файлов, torch-free путь против reader на всех
   раскладках, негативные контроли (safetensors без манифеста,
   `format_version` из будущего). Бэкенды прогнаны на новом контейнере:
   `test_v2_format`, `test_gw_kernel`, `test_gw_kernel_int6`,
   `test_gw_nb_parity`, `test_quant_model_smoke` — зелёные.
   **`codec.py` стал полной читалкой формата**: `open_rwkvq` разбирает
   safetensors на numpy+mmap, `dequant_key` отдаёт float32. Порт в
   rwkv-metal и SwiftRWKV делается с одного файла и не требует ни torch,
   ни пакета `rwkv_quant`, ни сайдкара.
5. **Спека.** `format_version` и опознание формата сделаны шагом 4
   (`manifest["format"] == "rwkvq"` плюс различение контейнеров по
   первым байтам; отдельного magic у safetensors нет и не нужно).
   Осталось собственно НОРМАТИВНЫЙ текст: докстринги `codec.py` и
   `schema.py` — уже почти он, надо свести в один документ и записать
   как обязательство: диск = каноническая раскладка, K3-интерлив =
   деталь загрузчика.
6. ~~**Самодостаточность по содержимому.**~~ СДЕЛАНО, `format_version: 2`.
   В per-tensor метаданные добавлены `n_blocks` и `transposed`, на
   верхний уровень — структурированный `config` и `tokenizer`. Читатель
   v2 понимает v1: недостающее выводится как раньше.
   Что вскрылось при этом, и ради чего шаг стоил времени:
   - **`n_blocks` — это ceil(IN/gs), а не IN//gs.** У `blocks.N.att.w1`
     формы [2048, 96] при gs=64 блоков ДВА, а деление нацело даёт один.
     Таких тензоров 48 на 1.5B. Читатель, посчитавший число блоков
     делением, взял бы на последних 32 колонках чужой масштаб — и это
     ровно тот сорт ошибки, который не падает, а тихо портит веса.
     Теперь в `codec` стоит ассерт, и он сразу поймал синтетический
     случай в прежнем гейте, который описывал несуществующую раскладку.
   - **`config_repr` был лосси.** `QuantConfig.__repr__` печатает
     битность и overrides, но молча теряет `group_scale`,
     `group_scale_mode`, `clip_percentiles`, `outlier_fracs` и
     `act_stats_path` — то есть ровно то, чем REDUCTION отличается от
     COMPRESSION при одинаковой битности. Воспроизвести квантование по
     файлу было нельзя; теперь конфиг едет структурой и собирается
     обратно в `QuantConfig`.
   Гейт `tests/test_manifest_selfdesc.py` сверяет `transposed` не с
   таблицей имён в `codec`, а с ИСХОДНИКОМ `models/rwkv7_ref.py`
   (регуляркой достаётся множество ключей, к которым ref реально
   применяет `.T`), и требует, чтобы в чекпоинте нашёлся хотя бы один
   тензор с `n_blocks != IN // gs` — иначе проверка вырождена.

Публичный API (`load_raw` -> `QuantizedCheckpoint`) при этом не
меняется, бэкенды не трогаются.

---

## Скорость декода: закрыто как «достаточно»

### Драфт-спекулятивка — ОТРИЦАТЕЛЬНО, подтверждено дважды

03.08 закрыта по согласию (`dev_draft_acceptance.py`, p = 0.5820,
потолок 1.392 принятых за раунд). 04.08 перепроверено сквозным циклом с
настоящей драфт-моделью (`tests/spec_decode_draft_model.py`): согласие
0.5755, принято 64%, 3.55 токена за раунд — и всё равно

```
plain:      16.16 мс/ток (61.9 ток/с)
spec k=4:   30.72 мс/ток (32.6 ток/с)  ->  0.53x
```

Своп за замер −48 МБ, замер валиден. Память НЕ проблема: квантованный
черновик 126.6 МБ (92 МБ трафика за токен, отношение к цели 9.5x).
Черновик обязан быть квантованным — в bf16 он 380 МБ, отношение 3.1x, и
арифметика не сходится в принципе.

### Разложение, объясняющее почему (`tests/bench_spec_breakdown.py`)

| | трафик | потолок | T=1 | T=5 | T=9 |
|---|---|---|---|---|---|
| черновик 0.1B | 92 МБ | 0.96 мс | 3.42 мс (29%) | 7.01 | 8.24 |
| цель 1.5B | 878 МБ | 9.17 мс | 15.08 мс (61%) | 39.45 (23%) | 63.18 (15%) |

**Верификация не амортизирует чтение весов.** T=5 стоит 2.6x от T=1,
предельная цена лишнего токена 6 мс против 15 одиночных — амортизация
2.5x там, где нужна 5-10x. Эффективность ПАДАЕТ с ростом T (61% -> 23%
-> 15%), хотя должна расти. N-батчевый кернель (`_get_kernel_gw_nb`)
написан, но на этом пути явно не задействован.

Синхронизация GPU->CPU ни при чём: 0.02-0.15 мс.

Если бы верификация амортизировала как положено (~18 мс вместо 39.5),
вышло бы 1.8x; с починенным черновиком 2.4x. Потенциал реален, но это
кернельная работа — отложена.

**Не проверено и решает, стоит ли возвращаться:** у RWKV верификация K
токенов содержит ПОСЛЕДОВАТЕЛЬНЫЙ прогон WKV-рекуррентности, которого у
трансформеров нет. Если значимая часть тех 6 мс — WKV-скан, а не
перечитывание весов, потолок спекулятивки для RWKV принципиально ниже.
Разделяется одним замером: T=5 с отключённой WKV-частью против полной.

### Разреженность cmix — измерена, не реализована

`tests/probe_cmix_sparsity.py`, 36792 пары токен×слой. RWKV-7 считает
`k = relu(x @ key.T)**2` — ТОЧНЫЕ нули, и маска известна ДО того, как
понадобится `value` (в отличие от трансформеров, где нужен
предсказатель).

```
нулей в k:                        86.68%
блоков по 32 целиком нулевых:     11.44%
  после перестановки каналов:     34.76%
  будь каналы независимы:          1.03%
```

11.44% против 1.03% при независимости = каналы гаснут группами, просто
группы не совпадают с границами блоков. Перестановка колонок
`cmix.value` и строк `cmix.key` (офлайн, математически тождественна)
поднимает долю целиком нулевых блоков втрое. `cmix.value` — 226 МБ из
878; минус 34.76% = **−79 МБ трафика = ~10% скорости, точно и без потери
качества**.

Оговорки: перестановка выведена на тех же токенах (нужна проверка на
отложенных); сортировка по частоте — самая тупая из возможных,
кластеризация по ко-активации должна дать больше; работает только на
декоде (в префилле маски токенов объединяются).

Приближённый вариант поверх: top-50% блоков несут 97.25% энергии
`||k||^2`, top-25% — 89.00%, top-12.5% — 77.64%.

---

## Память при инференсе (замер 04.08, ПЕРЕСЧИТАН 15.08)

**ЭТО ПРО ЛАБОРАТОРНЫЙ БЭКЕНД `backends/metal/quant_model.py`, НЕ ПРО
ПРИЛОЖЕНИЕ.** Проверено чтением кода потребителей: `_rkv_fused` есть
только здесь, в `SwiftRWKV/X070Backbone.swift` и в `rwkv-metal` фьюза
r/k/v нет вовсе.

Актуальные числа -- в разделе «Память и трафик пресета с sym»: 1.18x от
файла у sb6 и 1.16x у sym прямым обходом живых буферов. Прежние «1.85x»
получены через аллокатор MLX и включали его накладные.

Из разложения накладных осталась ОДНА живая статья:

**`emb` деквантуется в плотный fp16 при загрузке** -- 268.4 МБ на 1.5B и
335.5 на 2.9B против ~109 и ~137 в sb6 на диске. `quant_model` зовёт для
неё `_dense`, потому что emb -- gather, а не matmul, а sb6 не умеет
выбирать строки. Цена измерена: +159 МБ на 1.5B, +199 на 2.9B. Это
ОБЩАЯ с приложением статья, и закрывает её indexed-gather по sb6.

Снятые статьи: дубль `_rkv_fused` (~245/425 МБ) -- фьюз теперь строится
лениво и при FUSE=False его нет вовсе (15.08); дубль стеков LoRA во
фьюзе -- там же. K3-интерлив (`qsqm` как `uchar2` вместо 12 бит,
+0.125 бит/вес, ~23 МБ на 1.5B) остаётся и был известен заранее.

## Потребители формата: что найдено 04.08, до правок

Порядок работ: **rwkv-quant -> rwkv-metal -> SwiftRWKV**. Swift следует
питоновскому эталону, поэтому ошибки в нём чинятся после, а не
параллельно.

### rwkv-metal

- `RwkvqNativeLinear` ассертит `xbits == 2`, то есть bits=6 — это ровно
  REDUCTION. Не поддержан как раз COMPRESSION (proj/cmix на 4/5 бит,
  раскладка `mx.quantize` для не-шестибитных случаев не проверена).
  То есть QLoRA на REDUCTION — поддержанный путь, а не новый.
- `load_lora_rwkvq_model(pth_path, sidecar_path, ...)` до сих пор
  требует `.pth` ради ~650 неквантованных тензоров. Шаг 4 это снимает:
  dense/asym/rtn лежат в `.rwkvq`.
- `RwkvqLinear.from_sidecar` читает K3-буферы `qblk/qsqm/ddm`, а в
  `.rwkvq` каноническая раскладка. Либо интерливить при загрузке, либо
  оставить сайдкар кешем K3.
- **Развилка с LoRA-ветками ИЗМЕРЕНА 04.08** (`tests/ablate_qlora_lora_source.py`,
  мультиязычный корпус @512, реальный путь `QuantRWKV7`). Составы
  различаются РОВНО ветками, всё прочее идентично; `qlora_pth` — база
  как сейчас (proj/cmix/head @6 sb6, LoRA/emb/small из `.pth` в bf16).

  | конфиг | 1.5B ALL | к bf16 | 2.9B ALL | к bf16 |
  |---|---|---|---|---|
  | bf16 | 8.1980 | — | 7.1630 | — |
  | qlora_pth | 8.2495 | +0.63% | 7.2504 | +1.22% |
  | + w/a/v_lora из `.rwkvq` | 8.2515 | +0.65% | 7.2542 | +1.27% |
  | + g_lora (то, что получится) | 8.2592 | +0.75% | 7.2609 | +1.37% |
  | REDUCTION целиком | 8.2610 | +0.77% | — | +1.37% |

  **Цена отказа от `.pth`: +0.118% на 1.5B и +0.145% на 2.9B.**
  Разложение согласовано между масштабами: `w/a/v_lora` +0.024% / +0.052%,
  **`g_lora` +0.094% / +0.092%** — две трети сдвига несёт одна группа,
  и несёт одинаково на обоих масштабах.
  Хуже всего сербский (+0.344% на 2.9B) — он и раньше был канарейкой.

  Два побочных вывода, оба перепроверены цифрами:
  - Харнесс сошёлся с прежней таблицей ДО ЧЕТВЁРТОГО ЗНАКА (REDUCTION
    1.5B: en +1.04 / ru +0.55 / sr +1.00 / ALL +0.77) — независимое
    воспроизведение, а не совпадение методики.
  - `qlora_all` (emb в bf16) и REDUCTION (emb квантован) дают +0.75/+0.77
    на 1.5B и +1.367/+1.37 на 2.9B, то есть **квантование emb не стоит
    ничего по качеству** на обоих масштабах. Аргумент держать таблицу
    плотной — только механический (sb6 не умеет выбирать строки), не
    качественный.
  - **Ошибка в весах не предсказывает эффект.** Относительная ошибка
    деквантования почти одинакова у всех четырёх веток (asym
    1.90-2.15e-2, g_lora rtn 1.78e-2), а вклад в ppl различается вчетверо.
    Смотреть на нормы вместо ppl тут бесполезно.

  Решение по составу базы принимать с этими числами на руках: если
  +0.15% не устраивает, дешёвый выход — собирать QLoRA-вариант `.rwkvq`
  с `g_lora=16` (+25 МБ на 1.5B), это возвращает две трети.

### SwiftRWKV

- **Расклад по памяти у приложения ДРУГОЙ, чем у лабораторного бэкенда.**
  Фьюза r/k/v нет, дублей нет; `attachRwkvq` с `dropDenseWeights`
  освобождает bf16-копии, и база живёт сжатой. Зато `rwkvqWeight`
  НАМЕРЕННО не кеширует: каждая проекция разворачивает матрицу целиком
  в транзиент (`X070Backbone.baseProj` -> `sc.dequantize`). Для QLoRA
  это правильный размен (иначе квантованная база превращается в LoRA с
  лишними шагами), но **для инференса это другой размен, чем в
  rwkv-quant**, где кернель декодит внутри GEMV и плотная матрица не
  материализуется никогда. Отсюда следует, что цифры decode из этого
  файла (65.6 ток/с) к Swift-пути НЕ относятся — там своя механика и её
  никто не мерил. **ИЗМЕРЕНО 04.08** (`SwiftRWKV/Sources/DecodeBench`,
  release, чередование, два прохода):

  | | плотная bf16 | `.rwkvq` | отношение |
  |---|---|---|---|
  | 0.1B | 5.63 мс/ток (177.6 ток/с) | 9.62 (104.0) | 1.71x |
  | 2.9B | 68.2 мс/ток (14.7 ток/с) | 151.0 (6.6) | 2.21x |
  | 2.9B, память | 6.00 ГБ | 3.11 ГБ | −2.9 ГБ |

  Арифметика трафика сходится с точностью до 6%: плотный путь на 2.9B
  читает 5896 МБ/ток (пол 61.6 мс, измерено 68.2 — **90% полосы**), а
  `.rwkvq` читает 1855 сжатых, ПИШЕТ 5896 плотного транзиента и читает
  их обратно (13.6 ГБ, пол 142.6 мс, измерено 151). То есть узкое место
  ровно одно — материализованный bf16 на каждую проекцию. Пол самого
  `.rwkvq` — 19.4 мс = 51 ток/с, сейчас он в 7.7 раза от него.
  Практический вывод для нас: спрос будет не на «портируйте GEMV», а на
  переупаковку sb6 в родной `quantizedMatmul` (в rwkv-metal это дало
  4.4x). Проба механизма на формах 2.9B (`decode-bench --micro`):
  родное ядро быстрее нынешнего пути в 2.1-4.9x И быстрее плотного
  bf16 в 1.3-2.1x, ожидание по шагу — 22-28 ток/с против 6.6.
  **Раскладка `mx.quantize` ОДНА для 4, 5, 6 и 8 бит**
  (`tests/probe_mlx_native_packing.py`, 04.08): группа из 32 кодов —
  LSB-first поток, поле p на глобальном бите p*bits, через границу
  32-битного слова без выравнивания. Прежнее ограничение «проверено
  только для bits=6» (докстринг `rwkv_metal/lora/rwkvq_native.py`)
  снято: годятся ОБА пресета, отдельный REDUCTION-сайдкар для 2.9B не
  нужен.
  Как проверялось — важнее результата: реверс-инжиниринг не нужен,
  `mx.dequantize` есть обратная функция к упаковке, и свой упаковщик
  сверяется прямо против неё. Сверять при этом надо КОДЫ, а не
  значения: первая версия сверяла `q*scale + bias` в fp16 и дала
  75-81% совпадений на ВСЕХ битностях, включая заведомо рабочую
  шестую, — то есть показала бы «не годится» там, где годится.
  Расхождение было в округлении ядра, а не в раскладке.
  ОСТОРОЖНО: первый прогон шёл без `castWKVOutputToComputeDType`
  (умолчание модели — `false`, цена ×2.8) и дал 3.4/3.6 ток/с на 2.9B,
  то есть выводы, ПРОТИВОПОЛОЖНЫЕ верным, при полной воспроизводимости.
  Сверять новый замер с уже известным числом на том же железе.
- **emb — общая с лабораторией статья.** По умолчанию
  `quantizeEmbedding: false`, таблица живёт bf16 (268 МБ на 1.5B,
  335 на 2.9B). Если её отдать в сайдкар, станет хуже, а не лучше:
  комментарий в `embed()` честно предупреждает, что sb6-путь
  разворачивает таблицу ЦЕЛИКОМ на каждый проход. Строчной выборки
  формат не поддерживает — коды упакованы блоками вдоль входной оси.
  Для стокового `mlx.nn.quantize` строчная выборка работает (там
  per-row scale), и `embed()` её уже умеет. То есть indexed-gather для
  sb6 — единственный способ закрыть эту статью, и он один и тот же для
  обоих бэкендов.
- **`RwkvqSidecar.init` сломается на любом новом манифесте.** Он
  требует у КАЖДОЙ записи `shape.count == 2`, `bits`, `xbits`, `gw_gs`,
  `gw_sb`, а полный экспорт и манифест `.rwkvq` содержат dense-записи:
  одномерные нормы, `(1,1,C)` token-shift миксы. Парсер надо делать
  kind-aware (`sb6` / `asym` / `rtn` / `dense`).
- `loadArraysAndMetadata` уже используется в `RWKVRerank`, так что
  манифест из `__metadata__["rwkvq"]` читается сразу — отдельный `.json`
  не нужен.
- **Выигрыш mmap в Swift сам собой не воспроизведётся.** По собственному
  комментарию в `RWKVRerank/StateCache.swift`, загрузчик safetensors в
  mlx-swift материализует массив целиком. На 2.9B это 1.85 ГБ
  резидентно на старте против 0.25 ГБ в Python. Если это важно —
  писать свой mmap-ридер safetensors (u64, JSON, оффсеты);
  `codec.read_safetensors` — прямой образец для порта.

## Закрытые темы (не переоткрывать без новых данных)

- ANE/CoreML; GPU idle; qs/qm-упаковка в кернеле; пайплайн decode;
  4x-сжатие в нибл-контейнере; MXFP4; v3 simdgroup head; nb2; GEMM
  r/k/v-фьюз префилла.
- **Metal 4 / 4.1 TensorOps на M4 (закрыто 15.08, проверено по
  источникам).** Кооперативные тензоры и `matmul2d` существуют, но
  аппаратное ускорение -- это Neural Accelerators в шейдерных ядрах
  M5/A19; реверс-инжиниринг M4 Max (препринт Rigel, arXiv 2606.12765)
  даёт: выделенного матричного датапата у M4 НЕТ, `matmul2d` исполняется
  на обычных ядрах через старый `simdgroup_matrix`. То есть на нашей
  машине это API поверх того, что и так доступно.
  fp8 E4M3 как ТИП есть (`MTLTensorDataTypeMetalFloat8E4M3`), но на M4
  эмулируется: 0.94x от пропускной способности fp16 ПРИ ВДВОЕ МЕНЬШЕМ
  операнде -- если бы упирались в память, меньший операнд обязан был бы
  ускорить. И для нас fp8 был бы регрессом даже с ускорением: «экономия
  50% полосы» считается от fp16, а мы уже на 6.5625 бит/вес, тогда как
  fp8 с блочными масштабами E8M0 при группе 32 стоит 8.25 -- на 26%
  больше трафика.
  Epilogue fusion (+6.5-12.9%) -- единственный относящийся к нам пункт,
  и он у нас уже измерен независимо: хвост TMix дал +1.6% под
  `mx.compile` и +2.7% на сыром пути. Совпадает и квалификатор: в статье
  выигрыш только «в cache-resident режиме» и падает с ростом задачи.
  Переоткрывать при появлении M5/A19.
- ~~гибрид mlx-affine ради скорости~~ **ПЕРЕОТКРЫТО 15.08**: прямой A/B
  показал, что штатный `mx.quantized_matmul` быстрее нашего кернеля в
  1.39 раза на префилле и в 1.18 на декоде при том же бит/вес. См.
  раздел «Штатный mlx-affine быстрее нашего кернеля».
- xbsum-в-кернеле (K3_XSUM=False): decode x1.005, verify x0.94, дрейф
  логитов 0.02. Полублок (K3_HALF=False): микро +8-19%, E2E ноль.
  packs2: хуже базы. Свипы (NSG,RS) для cmix: тепловой шум.
- КЕРНЕЛЬ-4 закрыт руфлайн-пробой: декодный ALU бесплатен.
- LORA_Q8: decode x1.004, дрейф логитов 0.77 — отрицательно.
- **AW для `proj`** (04.08, `tests/ablate_aw_proj.py`): `asym_sb6`
  +2.36%, `asym_sb6_search` +2.22%, `asym_sb6_aw` +2.24%. AW не помогает
  — исходный вывод устоял. Но разница search/aw в пределах шума, а
  нынешний `asym_sb6` (без поиска И без AW) хуже обоих на 1.5B и лучше
  на 2.9B. Не трогать.
- **Драфт-спекулятивка** — дважды отрицательно, см. выше.

---

## Открытые вопросы

Упорядочено по приоритету. Закрытые замерами пункты убраны -- они теперь
разделы выше, а не вопросы.

1. ~~**LoRA ДО ПОЛКИ.**~~ ЗАКРЫТО 16.08: `LORA_Q = "sep"` @8 даёт
   +1.32 мс на умолчательном пути и +0.60 на фьюзнутом, ppl +0.012%
   (не значимо). Осталось три хвоста, все мелкие: 2.9B не мерен (закон
   10), префилл фьюзнутого пути теряет 4% и лечится диспетчеризацией по
   T, пик загрузки растёт на 183 МБ и лечится квантованием по слою в
   конструкторе. ПРЕЖНЯЯ ФОРМУЛИРОВКА ВОПРОСА БЫЛА НЕВЕРНА в обеих
   половинах -- см. раздел «LoRA: что оказалось рычагом».

   СТАРЫЙ ТЕКСТ ВОПРОСА (для истории): «Потолок 2.50 мс, 13.5% шага.» Единственная статья,
   идущая не у своей полосы: 46.6 ГБ/с против 85-93 у прочих, и причина
   -- форма (ранги 96/96/64/256 против 2048), а не байты и не запуски
   (те 0.35 мс). Нужны ОБА рычага: склейка четырёх down-проекций в один
   матмул [512, D] (по выходной оси, паддинга нет, значения те же до
   бита) и квантование под `mx.quantized_matmul`. Второе -- НОВОЕ
   квантование, а не репак: в файле LoRA лежит в `asym gw64`, но группы
   там вдоль ВЫХОДНОЙ оси матмула (writer квантует до транспозиции), и
   формы ragged (`att.w1` [2048, 96] при gs=64). Значит нужен ppl-гейт;
   ожидание хорошее (+0.006% изолированно, 12.08), но это ожидание.
   И сквозной A/B обязателен: микрозамеры тут врут в плюс, склейка
   показала 157.6 ГБ/с при потолке 104 -- матрица сидела в кэше.

2. **ПРЕФИЛЛ: 97% ВЫИГРЫША УЖЕ ВЗЯТО БЕСПЛАТНО (деквант кернелем,
   997 -> 753 мс, бит-в-бит).** Осталось два хвоста. Первый -- тайловый
   GEMM с декодом в threadgroup-памяти: он закрывает последние
   1.08-1.10x до нативного матмула, цена вопроса 0.5-0.7 мс на слое,
   работа кернельная. Второй -- решение по affine ниже, которое теперь
   стоит СИЛЬНО меньше, чем казалось: +0.077% ppl и +291 МБ ради 4%
   префилла (753 против 722 мс).
   БЫЛО: **ПРЕФИЛЛ В affine: РЕШЕНИЕ ВЛАДЕЛЬЦА.**
   Всё измерено 16.08 (раздел «Префилл в родной affine»): префилл
   1.38x (708.8 ток/с, обгоняем llama.cpp), декод -12%, память +291 МБ,
   ppl +0.077%. Точного репака sym НЕ существует (MLX не знает gs=16),
   вторая раскладка рядом с нашей не влезает (+1.4 ГБ), affine@6
   бесплатен по памяти и стоит +0.881% ppl. Вопрос ровно один: для какой
   нагрузки собираем -- для векторной (префилл всё, декода нет: брать) или
   для чата (декод сотнями токенов: не брать).
   СТАРАЯ ФОРМУЛИРОВКА (для истории): при
   N >= GEMM_MIN_BATCH_NB мы уходим с кернеля на `_dequant_w` + плотный
   матмул и теряем 1.82-2.38x на слое при N=512 (1.39x сквозным против
   штатно квантованной модели). На декоде наш GEMV ВРОВЕНЬ с нативным
   (1.02-1.10x), трогать его не надо. Значит affine нужен как ВТОРАЯ
   раскладка для префилла, а не замена формата: +7.7% на диске платить
   не придётся, цена переезжает в переупаковку при загрузке или в две
   копии в памяти. Этот размен НЕ МЕРИЛСЯ.
   Заготовки: `codec.pack_mlx_affine` / `sb6_to_mlx_affine` (точный
   репак, но он про sb6 -- у sym блок 16, и для него точного пути нет).

3. **ПРАВКА ПРЕСЕТА REDUCTION -- решение владельца проекта.** Всё
   измерено: качество (+0.772% -> +0.192% на 1.5B, +1.370% -> +0.273% на
   2.9B, реальный путь), размер (+3.5% и +2.6%), скорость (−3% декода).
   Инженерии не осталось. Вопрос в КРИТЕРИИ: ppl и KL разошлись на emb, а
   REDUCTION -- база для QLoRA и векторных моделей, где верность
   распределения важнее вероятности одного токена.

4. **QLoRA-БЕНЧМАРК НА 1.5B** (уже тема rwkv-metal). Три руки:
   bf16-LoRA / QLoRA на REDUCTION / QLoRA на int8 -- чтобы записанное
   «REDUCTION ведёт себя лучше int8» стало числом, а не ссылкой на
   владельца. Домен брать тот, где база слаба, иначе дельты утонут
   (закон 9): сербская википедия. Данные в `Develop/data`.
   ВНИМАНИЕ: `RwkvqNativeLinear` в rwkv-metal ассертит `xbits == 2`, то
   есть знает только sb6@6 -- кандидатный пресет (cmix/proj в `sym@6`,
   head в `sym@8`) он не прочитает вовсе.

5. **FUSE ПО УМОЛЧАНИЮ НАДО ПЕРЕСМОТРЕТЬ ДЛЯ COMPRESSION.** После
   квантования LoRA фьюз почти обесценился: на COMPRESSION он даёт
   **13.37 -> 13.35 мс/ток, то есть 0.15%**, и при этом стоит ~250 МБ
   резидентно и 0.7% префилла (908.3 -> 914.8 мс). На sym-пресете он всё
   ещё даёт 0.57 мс. Прежний расчёт (+1.15 мс) снят: из них 0.58
   приходилось на укрупнение LoRA-запусков, а их теперь укрупняет
   квантование. Замер: `RWKVQ_MODES=None,sep RWKVQ_FUSE=0|1
   bench_lora_quant_ab.py /tmp/champion_v2.rwkvq`.
   ПРЕЖНЯЯ ФОРМУЛИРОВКА ВОПРОСА:
   `FUSE=True`: +1.15 мс (6.2%) на sym-пресете и +1.01 на sb6 ценой
   ~250 МБ резидентно на 1.5B (~425 на 2.9B). `LORA_Q="sep"` @8:
   +1.32 мс (7.9%) ценой +56 МБ, а если освобождать плотные копии --
   то -44 МБ, то есть ОТРИЦАТЕЛЬНОЙ ценой. Вместе они дают
   **17.97 -> 15.88 мс/ток, 55.6 -> 63.0 ток/с (+13.2%)**. Гейты
   зелёные у обоих, greedy совпадает. Вопрос не инженерный: сколько
   резидентной памяти покупать за декод.

6. **Память инференса: 1.18x от файла у sb6 и 1.16x у sym.** Осталась
   ОДНА статья: `emb` развёрнута в плотный fp16 (268 МБ против 109 на
   диске) -- закрывается indexed-gather по sb6, он же снимает половину
   остаточного пика загрузки. Качества не трогает.

7. **Обвязка GEMV на декоде.** Наш кернель вровень с нативным, но полный
   вызов стоит 1.05-1.20x: отдельная операция `xbsum` на каждый вызов
   (144 редукции за токен при шести битах), reshape'ы, цикл по колонкам.
   Чинится питоном, а не ядром.

8. **Перестановка каналов cmix**: ~10% трафика декода, точно и без
   потери качества (`probe_cmix_sparsity`, 04.08). cmix -- 42% шага и
   53% трафика, так что это самая крупная статья по байтам.

9. **Спека формата** (шаг 5). Свести докстринги `codec.py` и `schema.py`
   в нормативный текст: диск = каноническая раскладка, K3-интерлив =
   деталь загрузчика. Порядок работ: rwkv-quant -> rwkv-metal ->
   SwiftRWKV.

10. Разделить WKV-скан и перечитывание весов в верификации -- решает
    судьбу спекулятивки для RWKV в принципе.

11. Согласие драфта по языкам переснять на стратифицированной выборке --
    0.5755 намерено только на русских последовательностях.

12. **`test_streaming_inference` красный, и это НЕ регресс контейнера.**
    Проверено на `git stash`: до шага 4 те же числа до последнего знака.
    Батчевый `forward_stateful` против `wkv7_train` даёт rel 1.282e-4 при
    пороге 1e-4 -- чуть-чуть за. Комментарий в тесте утверждает, что
    «расхождение батчевого пути осталось нулевым», то есть rel1 уехал
    когда-то при переводе dense/LoRA на fp16 и этого не заметили. Порог
    НЕ трогать: сначала понять, откуда 1.2e-4 там, где ожидался ноль.

---

## QLoRA поверх `.rwkvq` в rwkv-metal (19-20.07, актуально)

Всё бит-в-бит сверено с `rwkv_quant.formats.reader`. MLX-порт sb6/asym
dequant (`rwkv_metal/lora/rwkvq_linear.py`, `rwkvq_kernel.py`) — 0
расхождений на реальных тензорах. Финальный combine ОБЯЗАТЕЛЬНО в
float32: `GwQuantLinear._dequant_w()` держит математику в half и даёт
~18% расхождений на 1 бит bf16-мантиссы.

| путь | шаг (steady) | mx_peak | maxrss |
|---|---|---|---|
| stock QLoRA (nn.quantize 6bit) | 0.80-0.89 с | 3.02 ГБ | ~6.9 ГБ |
| rwkvq, наивный MLX-порт | 3.4-3.6 с | 4.39 ГБ | 6.6-7.0 ГБ |
| rwkvq, fused-кернель | 1.2-1.3 с | 3.65 ГБ | 3.64 ГБ |
| rwkvq, native quantized_matmul | 0.7-0.8 с | 4.59 ГБ | 4.36 ГБ |
| rwkvq, гибрид | 0.8-0.9 с | 4.19 ГБ | 4.62 ГБ |

Незакрытое: `emb.weight` не квантуется ни в одном пути (нужен
indexed-gather); native/hybrid проверены только для bits=6; качество
после сотен шагов не мерено; COMPRESSION как QLoRA-база не пробован.

---

## Тесты и замеры (что где лежит)

### Корпус и калибровка

- `tests/build_eval_multiling.py` — `test.txt` ->
  `eval_corpus_multiling.pt` (38x512, ru/en/sr, id чанка и превью в
  блобе) + `act_calib_multiling.pt`. Разбиение calib/eval ПО ЧАНКАМ:
  AW-статистика не должна сниматься с текста, на котором меряется ppl.
  Хвост >= половины окна добирается перекрытием (иначе en/sr теряют
  ~30% материала).
- `tests/collect_act_stats.py` — принимает `RWKVQ_CKPT` из окружения
  (2.9B) и argv `corpus out slice`. Артефакты: `/tmp/act_stats_1p5b.pt`
  (англ., рецепт по умолчанию), `/tmp/act_stats_1p5b_multiling.pt`,
  `/tmp/act_stats_2p9b_multiling.pt` (193 тензора). НЕ переживают ребут.

### Качество

- `tests/eval_multiling.py` — ppl по языкам и ПО ЧАНКАМ на 1.5B. NLL
  через logsumexp, а не материализацию log_softmax: логиты
  [B,511,65536], полный fp32-logp плюс копии softmax/log = ~0.8 ГБ
  транзиента на батч.
- `tests/eval_corpora_compare.py` — разложение расхождения с README на
  три множителя. Модель строится один раз на конфиг, корпусов четыре:
  A `world[:8]@128` (1016 предсказаний, воспроизводит README до
  четвёртого знака: bf16 11.4303, REDUCTION +0.12%), B `world[:24]@128`
  (+0.74%), C `ml@128` (+1.52%), D `ml@512` (+2.57%). Размер выборки,
  язык и длина контекста — по отдельности.
- `tests/ablate_group_contrib.py` — leave-one-out по группам, `STAGE=1`
  крупные группы, `STAGE=2` дробит lora+small.
- `tests/ablate_aw_proj.py` — три режима proj (нет поиска / поиск /
  поиск+AW).
- `tests/confirm_small_fix.py` — подтверждение `small=16` на обоих
  пресетах, 1.5B.
- `tests/ablate_sym_cmix.py <режим>` — ИЗОЛИРОВАННЫЙ замер раскладки на
  одной группе (`RWKVQ_GROUP`), шесть режимов. Драйверы для 2.9B:
  `run_sym_2p9b.sh` (проверяет принадлежность статистики чекпоинту и
  падает, если она от другой модели).
- `tests/ablate_emb_head.py <конфиг>` — emb и head порознь, изолированно,
  три рычага (раскладка / битность / контейнер) и ДОВЕРИТЕЛЬНЫЕ
  ИНТЕРВАЛЫ: в JSON сохраняются per-sequence NLL, `--report` считает
  парный бутстрэп по последовательностям. Это единственное место, где
  «в пределах шума» -- проверяемое утверждение, а не оценка на глаз;
  интервалы для любого прогона пересчитываются без повторного счёта.
  JSON: `/tmp/emb_head_1p5b.json`, `/tmp/emb_head_2p9b.json`.
- `tests/bench_load_mem.py <model.rwkvq> [--prefill T] [--old]` — ПАМЯТЬ
  ЗАГРУЗКИ: только постройка модели из готового `.rwkvq`, без префилла,
  поэтому `peak memory footprint` описывает ровно её. `--old` подменяет
  `_dense` замороженной копией прежнего кода из
  `test_dense_load_parity` -- A/B делается подменой ОДНОЙ функции, а не
  версией файла (закон 27). Своп печатается на границе замера. Гонять
  чередованием и на прогретом кеше (закон 26).
- `tests/eval_retrieval_emb.py <файл.rwkvq|конфиг|bf16> [N]` -- ЗАДАЧНАЯ
  метрика для векторных моделей: точность на тройках, зазор и дрейф
  векторов против bf16, парный бутстрэп по тройкам. Рецепт эмбеддинга
  взят из кода самой модели, а не придуман. `RWKVQ_EMB_ACT` -- статистика
  активаций ЭТОГО чекпоинта (чужая молча выродит AW, закон 15).
- `tests_local/build_world_ckpt_v2.py <src.pt> <out.pth>` -- восстановление
  имён из тренировочного снимка сверкой ВСЕХ форм; размерности читаются
  ПО ПОЗИЦИЯМ в порядке named_parameters(), а не угадываются по формам
  (первая редакция угадывала и взяла словарь за ffn).
- `tests/probe_gemv_wrapper.py [model.rwkvq] [раундов]` -- СКОЛЬКО СТОИТ
  ОБВЯЗКА вокруг GEMV на декоде: полный вызов / голый кернель (xbsum
  вынесен) / нативный `quantized_matmul` при ТОЙ ЖЕ битности, по всем
  слоям за проход. Битность обязана совпадать -- иначе меряется разница
  в байтах, а называется разницей ядер (на этом я споткнулся в первом
  заходе: сравнил наши 6 бит с нативными 8 и получил «ядро быстрее в
  1.25 раза»).
- `tests/bench_gemm_peak.py [раундов]` -- ПОТОЛОК АРИФМЕТИКИ, аналог
  `bench_membw` для префилла: квадратные матмулы, наши формы при T=512 и
  они же через `quantized_matmul`, чередованием. Гонять на ОСТЫВШЕЙ
  машине -- это абсолютная величина. Именно на него опираются все доли
  «% от потолка» для префилла.
- `tests/probe_prefill_components.py [model.rwkvq] [T] [раундов]` --
  из чего состоит ПРЕФИЛЛ: аблации WKV, LoRA и декванта на
  компилированном пути со сбросом фьюза. Не путать с
  `profile_components`, который мерит ДЕКОД: там пол от полосы, здесь от
  арифметики.
- `tests/probe_gemm_split.py [model.rwkvq] [N]` -- ГДЕ УХОДИТ ВРЕМЯ
  GEMM-префилла: деквант против матмула против полного вызова, плюс
  нарезка по строкам выхода. Это тот замер, который показал, что 60%
  времени -- деквант, и отменил тайловый GEMM как ПЕРВЫЙ шаг.
- `tests/test_sym_dequant_kernel.py [model.rwkvq ...]` -- ГЕЙТ декванта
  одним кернелем против прежней цепочки, требуется РАВЕНСТВО. Флаг
  `RWKVQ_DQ_REF=1` возвращает цепочку -- для A/B в одном процессе.
- `tests/probe_prefill_affine.py [model.rwkvq] [N] [раундов]` -- РАЗВЕДКА
  префилла: какие group_size вообще поддержаны (перебором), сколько стоит
  affine в битах на вес, насколько расходятся ЗНАЧЕНИЯ при
  переквантовании и сколько остаётся выигрыша на реальных формах при
  N=512.
- `tests/eval_affine_inmem.py base|a6|a8 [--report]` -- ppl affine как
  раскладки В ПАМЯТИ при неизменном файле, конфиг на процесс, парный
  бутстрэп в отчёте. `to_affine()` оттуда же переиспользуется бенчем.
- `tests/bench_affine_inmem_ab.py base|a6|a8` -- декод и pp512, ОДНА
  МОДЕЛЬ НА ПРОЦЕСС (две в одном процессе дают 7 ГБ и своп),
  `RWKVQ_NOPREFILL=1` -- прогон без префилла, разность пиков и есть цена
  префилла по памяти.
- `tests/probe_lora_shapes.py [model.rwkvq] [раундов]` -- ЧЕСТНЫЙ
  микрозамер форм LoRA: каждый вариант раскладки за проход обходит ВСЕ
  слои, то есть рабочий набор 100 МБ, а не 2.1 МБ одного слоя (прежний
  `probe_lora_cost` мерил кэш). Варианты down/up: fp16 порознь, fp16
  батч, склейка [512,2D], квантованные при 6 и 8 битах. ВНИМАНИЕ, закон
  29: этот инструмент ЗАНИЖАЕТ цену запусков (23 слоя независимой работы
  перекрываются), решать по нему нельзя.
- `tests/bench_lora_quant_ab.py [model.rwkvq] [бит] [раундов]` -- СКВОЗНОЙ
  A/B квантования LoRA: fp16 / sep / glue чередованием в одном процессе,
  декод и pp512, своп на границе. Битность на процесс одна (буферы
  строятся под неё), `RWKVQ_FUSE=1` -- то же на фьюзнутом пути.
- `tests/eval_lora_quant.py [model.rwkvq] [nseq]` -- ppl-ГЕЙТ квантования
  LoRA: мультиязычный корпус, парный бутстрэп по последовательностям,
  разбивка по языкам, per-sequence NLL в JSON (интервалы пересчитываются
  без повторного прогона). База -- тот же файл с плотным fp16.
- `tests/test_lora_quant_parity.py [model.rwkvq]` -- ГЕЙТ раскладки:
  fp16 сам с собой (детерминизм), ФАКТ ВКЛЮЧЕНИЯ квантованного пути,
  relmax логитов и greedy против fp16, склейка против поветочного.
- `tests/bench_lora_mem.py <fp16|both|qonly> [model.rwkvq]` -- память
  трёх состояний, пик снимается снаружи `/usr/bin/time -l`, режим на
  процесс.
- `tests/profile_components.py [model.rwkvq] [раундов]` — ПОКОМПОНЕНТНОЕ
  разложение шага аблациями на компилированном пути (WKV, l2_norm,
  group_norm, layer_norm, token-shift, LoRA, проекции, cmix, голова).
  `RWKVQ_FUSE=1` -- то же на фьюзнутом пути; там ОБЯЗАТЕЛЕН сброс
  `_fused_built`, иначе аблация не видна фьюзу (он кеширует копии весов).
  Аблации НЕ аддитивны -- инструмент ищет доминанту, а не строит бюджет.
- `tests/bench_gemv_vs_native_ab.py [model.rwkvq] [раундов]` — наш GEMV
  против `mx.quantized_matmul` на ОДНИХ весах и формах, три точки:
  native / наш полный вызов / наш кернель без обвязки. Длина цепочки
  вызовов адаптивная -- фиксированная убивала машину на больших N
  (шестнадцать неотевалуированных `_dequant_w` = 6.4 ГБ).
- `tests/bench_molly_ab.py [our.rwkvq] [раундов]` — НАША РАСКЛАДКА
  ПРОТИВ ШТАТНОГО mlx-affine: pp512 и tg чередованием, обе модели в
  памяти одновременно. Путь к чужому чекпоинту -- `RWKVQ_MOLLY_DIR`.
  Загрузчик берётся из `eval_molly_real` (там же гейты на полноту
  маппинга ключей). ppl не меряет СОЗНАТЕЛЬНО: чекпоинты разные.
- `tests/bench_compile_ab.py [model.rwkvq] [T] [раундов]` — ЧТО ДАЁТ И
  ЧТО СТОИТ `mx.compile`: установившаяся скорость префилла и декода
  чередованием, цена первого вызова, и возвращается ли она на ДРУГОЙ
  форме (возвращается -- кеш ключуется формой).
- `tests/bench_prefill_chunk_ab.py [model.rwkvq] [T] [раундов]` — префилл
  кусками фиксированной формы на компилированном пути (попытка получить
  одну трассировку на процесс). Контроль: куски обязаны давать те же
  логиты, что один кусок.
- `tests/bench_membw.py [ГБ] [раундов]` — ПОТОЛОК ПОЛОСЫ ПАМЯТИ:
  `mx.sum` против собственного кернеля чистого чтения (развёртка по числу
  потоков и по развёртке цикла) и копии, всё чередованием на одном
  буфере. Буфер задавать заведомо больше системного кэша, иначе замерится
  SLC. Это тот замер, на который опираются ВСЕ доли полосы в этом файле.
- `tests/bench_runahead_ab.py [model.rwkvq] [раундов]` — ЛЕЖИТ ЛИ
  ПОСТАНОВКА ЗАДАЧ НА КРИТИЧЕСКОМ ПУТИ: декод с синхронизацией раз в K
  шагов, K чередуются. Контроль: при всех K обязана выдаваться одна и та
  же последовательность токенов, иначе замер сравнивает разное.
- `tests/bench_cmdbuf_ab.py [model.rwkvq] [раундов]` — свип
  `MLX_MAX_OPS_PER_BUFFER` / `MLX_METAL_FAST_SYNCH`. Переменные читаются
  при инициализации Metal-бэкенда, поэтому чередование по ПРОЦЕССАМ, и
  разброс печатается рядом -- он там втрое-вчетверо больше
  внутрипроцессного.
- `tests/bench_tail_ab.py [model.rwkvq] [раундов]` — A/B ХВОСТА TMix:
  `FUSE_TAIL` off/on при FUSE=True в обеих ветках, чередованием в одном
  процессе, И под `mx.compile`, И на сыром пути (разница между этими
  двумя ответами показывает, сколько компилятор сливает сам). Своп на
  границе замера.
- `tests/test_sym_fuse_parity.py [--freeze] <model.rwkvq>` — ГЕЙТ ФЬЮЗА
  ПРОЕКЦИЙ sym: нефьюзнутый кернель против ЗАМОРОЖЕННЫХ В ФАЙЛ выходов
  (`/tmp/sym_kernel_ref.npz`) и фьюз против K отдельных вызовов, оба на
  РАВЕНСТВО. `--freeze` гонять ДО правки кернеля, на откаченном дереве.
- `tests/test_dense_load_parity.py [ckpt ...]` — ГЕЙТ ПРАВОК ПАМЯТИ
  ЗАГРУЗКИ: деквант против замороженного прежнего, полосы против целого,
  `_dense` против замороженного прежнего (включая dtype), непустота
  нарезки. Требуется РАВЕНСТВО. Гонять после любой правки
  `reader._dequantize_*` или `quant_model._dense`.
- `tests/bench_prefill_mem.py <model.rwkvq> [T] [кусок]` — префилл:
  скорость и ПИК ПАМЯТИ. Модель грузится из готового файла, иначе пик
  описывал бы сборку из .pth. Третий аргумент — разбиение промпта на
  куски с переносом состояния (проверка гипотезы про ленивый граф).
- `tests/make_sym_rwkvq.py <конфиг> <out.rwkvq>` — собрать файл для
  конфига из ablate_sym_composite.
- `tests/bench_step_decompose.py [конфиг]` — из чего состоит шаг: полный
  шаг против «только GEMV» вычитанием, плюс пол синхронизации.
  Чередование в одном процессе, своп на границе замера. Это замена
  per-kernel атрибуции, которой в Metal System Trace нет.
- `tests/trace_decode_steady.py <конфиг> [секунды]` — РОВНАЯ декодная
  нагрузка под Metal System Trace: один конфиг, одинаковый шаг, печать
  мс/ток и дрейфа раз в 5 с (SILENT=1 убирает и её). Окно захвата брать
  в первые 20-25 с — дальше троттлинг (закон 25).
- `tests/report_sym_memory.py <конфиг>` — память и ТРАФИК ЗА ТОКЕН для
  конфига: состав живых буферов прямым обходом модели (раскладка
  опознаётся по составу полей, а не по имени `qblk` — оно есть и у sb6,
  и у sym), трафик без таблицы emb и без дубля фьюза, потолки по полосе.
  Один конфиг на процесс; пик — `/usr/bin/time -l`, не метрики MLX.
- `tests/bench_sym_vs_sb6_ab.py [ckpt] [--sweep]` — микро-A/B кернелей
  sym против sb6 на одних и тех же весах и формах, N=1 и N=4, плюс свип
  (NSG, RS) С ЧЕРЕДОВАНИЕМ конфигураций (закон 24). Своп до и после.
- `tests/bench_sym_e2e_ab.py [ckpt]` — E2E-декод пресета с sym против
  нынешнего, ОБЕ модели в памяти одновременно, бёрсты чередуются. Своп
  фиксируется на границе замера и по раундам (уточнение к закону 11).
- `tests/ablate_subgroups.py` — РАЗЛОЖЕНИЕ ГРУПП по KL(bf16 ‖ квант) с
  доверительными интервалами, профилем по позиции и расхождением
  residual stream по слоям (`--trace`). Подмножества задаются регуляркой
  (r/k/v/o, cmix key/value, layer0, vfirst, emb, head, lora), режимы
  `--only` (изолированно) и `--except` (leave-one-out). Эталон строится
  `--ref` один раз, float32 на диск. Контроль: bf16 против самого себя
  обязан дать KL = 0. JSON: `/tmp/kl_subgroups_<ckpt>.json`.
- `tests/test_sym_packer_parity.py [ckpt ...]` — ГЕЙТ РЕАЛЬНОЙ УПАКОВКИ
  sym: упаковщик против fake-пути, codec против reader, роундтрип через
  контейнер (включая torch-free `codec.dequant_key`), бит/вес по
  размерам буферов. Требуется РАВЕНСТВО. Гонять после любой правки
  `groupwise_sym_fake_dequant` или `_make_qt_gw_sym`.
- `tests/test_sym_kernel.py [ckpt]` — Metal-GEMV sym (6 и 8 бит) против
  `x @ dequant(qt).T`, N=1/3/16/128, порог 3e-3.
- `tests/eval_real_sym.py <конфиг>` — перемер композитных конфигов на
  РЕАЛЬНОМ пути (`real_gw=True`) с МБ по байтам буферов. Конфиги
  импортируются из `ablate_sym_composite`, а не переписываются: два
  списка разъехались бы, и «поправка fake->real» оказалась бы разницей
  между разными схемами. JSON: `/tmp/sym_real_1p5b.json`,
  `/tmp/sym_real_2p9b.json`.
- `tests/test_sym_chunk_parity.py [ckpt] [device]` — гейт нарезки
  симметричной ветки полосами (пять реальных форм, включая ragged
  `att.w1`, восемь комбинаций режимов на каждую, требуется РАВЕНСТВО).
  Гонять после любой правки `groupwise_sym_fake_dequant`.
- `tests/ablate_sym_composite.py <конфиг>` — КОМПОЗИТ: полный REDUCTION
  с cmix/proj в Q6_K, разбивка по языкам, среднее с равным весом рядом
  с ALL. Веса квантуются ОДИН раз и кладутся в модель (см. раздел про
  предквантование), поэтому конфиг стоит 131 с на 1.5B вместо 583-768 и
  своп не растёт. `--gate <ckpt>` — обязательная проверка, что
  предквантование бит-в-бит тождественно кванту на лету; гонять её после
  ЛЮБОЙ правки forward в `rwkv7_ref.py`, иначе замер тихо разъедется с
  тем, что реально считает модель.
  JSON: `/tmp/sym_composite_1p5b.json`, `/tmp/sym_composite_2p9b_full.json`.
- `tests/eval_2p9b_one.py <config>` + `tests/eval_2p9b_report.py` —
  2.9B, ПО КОНФИГУ НА ПРОЦЕСС (законы 11 и 13). Конфиги: `bf16`,
  `reduction`, `reduction_small`, `reduction_fix`, `compression`,
  `compression_fix`. bf16-ветка не идёт через `quantize_tensor` (там
  `.clone()` = 5.9 ГБ анонимной памяти поверх уже отображённого файла).

### Скорость

- `tests/bench_decode_vs_gguf.py` — pp512/tg128 нашим кернелем, метрики
  как у `llama-bench`.
- `tests/bench_spec_breakdown.py` — разложение шага на трафик и
  задержку, T=1/5/9, с синхронизацией и без.
- `tests/spec_decode_draft_model.py [K] [N_GEN]` — сквозной
  спекулятивный цикл с драфт-моделью, механика pending из
  `spec_decode_champ.py`. Печатает согласие, ток/раунд, мс/ток и дельту
  свопа.
- `tests/probe_cmix_sparsity.py [n_seq]` — разреженность `relu^2`,
  блочность, выигрыш от перестановки, концентрация энергии.

### Формат

- `rwkv_quant/formats/codec.py` — НОРМАТИВНАЯ реализация формата на
  чистом numpy: контейнер (`open_rwkvq`), раскладка, деквант
  (`dequant_key`). Порт в Swift/MLX делать отсюда, файл самодостаточен.
- `tests/test_rwkvq_container.py <in.rwkvq> [out.rwkvq]` — гейт шага 4:
  роундтрип полей, чтение прежнего pickle, torch-free путь, негативные
  контроли.
- `tests/test_manifest_selfdesc.py <model.rwkvq>` — гейт шага 6:
  `transposed` против исходника `rwkv7_ref.py`, `n_blocks` против форм
  буферов, роундтрип конфига, совместимость с манифестом v1 (файл v1
  делается из v2 правкой одного заголовка, буферы те же).
- `tests/bench_rwkvq_load.py <model.rwkvq>` — время и память загрузки,
  по конфигу на процесс (законы 11 и 13). Чередование делает вызывающий
  скрипт.
- `tests/test_codec_parity.py [model.rwkvq ...]` — гейт шага 3. Без
  аргументов гоняет синтетику (секунды), с путями добавляет полный
  деквант реальных чекпоинтов. Прогнано на 0.1B / 1.5B COMPRESSION /
  1.5B REDUCTION / 2.9B — ноль расхождений.
- `tests/bench_codec_dequant_ab.py <model.rwkvq> [R] [N]` — цена
  torch-free декванта, чередование в одном процессе. Это замер из закона
  16, повторять при попытке снова схлопнуть реализации.
- `tests/verify_sidecar_full.py <сайдкар_без_расширения> <model.rwkvq>` —
  ГЕЙТ полного сайдкара. Восстанавливает все четыре раскладки без torch и
  сверяет с эталоном. `dequant_from_sidecar` в нём — референсная читалка
  K3-раскладки (в отличие от `codec.py`, который про дисковую).
  Перепроверено 04.08 на 1.5B REDUCTION: 798 тензоров, 1426 буферов,
  1278.6 МБ, все четыре раскладки 100% точных, max|Δ| = 0.
- `python -m rwkv_quant.formats.export_mlx <in.rwkvq> <out> [токенайзер]`
  — полный экспорт.
- `rwkv_quant.quantize(pth, out, preset=...)` — теперь потоковый
  (`formats.writer.quantize_file`).

### Сравнение с llama.cpp

Бинарники из homebrew (`llama-perplexity`, `llama-quantize`,
`llama-imatrix`, `llama-bench`), конвертер
`~/Develop/rwkv-gguf-compare/convert_rwkv_pth_to_gguf.py` (принимает
путь к `.pth`, нужен `pip install gguf`). Сводка — в
`~/Develop/WKV-kvant/GGUF_COMPARE.md`.

### QLoRA-база

- `tests/ablate_qlora_lora_source.py [конфиг ...]` — цена отказа от
  `.pth` в QLoRA-базе. Конфиги `bf16` / `qlora_pth` / `qlora_wav` /
  `qlora_all` / `REDUCTION`. На 2.9B гонять ПО ОДНОМУ на процесс
  (`RWKVQ_CKPT`, `RWKVQ_ACT_STATS`, `RWKVQ_OUT`, якорь
  `RWKVQ_BF16_ALL=7.1630` — bf16 на 2.9B через `quantize_tensor` не
  строится, там `.clone()` = 5.9 ГБ поверх отображённого файла).
  ВНИМАНИЕ: диагностика по весам гоняется только на `qlora_all` и стоит
  ещё одного полного прохода квантования — на 2.9B это +10 минут.

### Результаты в JSON

`~/Develop/WKV-kvant/`: `eval_multiling_1p5b.json`,
`qlora_lora_source.json`, `qlora_lora_source_2p9b.json`,
`eval_corpora_compare.json`, `ablate_group_contrib.json`,
`ablate_aw_proj.json`, `confirm_small_fix.json`, `eval_2p9b.json`,
`GGUF_COMPARE.md`.

### Прежние гейты и бенчи

`tests/`: `diagnose_one.py`, гейты (`test_gw_nb_parity`,
`test_gw_kernel[_int6]`, `test_fuse_parity`, `test_v2_*`), бенчи
(`bench_verify_cost*`, `bench_gw_nb_*`, `bench_mlx_vs_gw_*`, `4way`),
КЕРНЕЛЬ-3 (`bench_kernel3_proto/C/D/E/F/F2/F3`, `bench_k3_e2e_ab.py`),
`profile_decode_v2_compiled.py`, спекулятивка
(`spec_decode_poc/champ/draft/draft2.py`), `eval_molly_real.py`,
`eval_canonical_int4/int6.py`.

---

## Где лежат материалы

- `~/Develop/WKV-kvant/` — чекпоинты (1.5B G1H, драфт g1d-0.1b, ru60m),
  корпуса, `world_tokenizer.py`, все JSON-результаты и
  `GGUF_COMPARE.md`.
- `~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` — 2.9B (world naming).
- `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — `model.safetensors` bf16,
  `rwkv7-g1h-2.9b.rwkvq`, сайдкар `_mlx.safetensors`+`.json`,
  токенайзер.
- `~/Develop/SwiftRWKV/Sources/RWKVQuant/` — Swift-читалка сайдкара
  (`RwkvqSidecar.swift`, `RwkvqDequant.swift`), `.testdata/` с
  эталонами.
- `~/Develop/rwkv-metal/rwkv_metal/lora/` — QLoRA-стек поверх `.rwkvq`.
- `~/Develop/tests/venv/bin/python` — рабочее окружение (torch 2.13 +
  MLX, python 3.14).
- `/tmp`, СОСТОЯНИЕ НА 15.08: пересобраны `reduction_v2.rwkvq` (1255.8 МБ)
  и `champion_v2.rwkvq` (970.6 МБ) для гейтов `test_fuse_parity` и
  `test_gw_nb_parity`, плюс собран `reduction_sym_head8_2p9b.rwkvq`
  (2484.3 МБ, сходится с записанными 2483.9) и лежат
  `reduction.rwkvq` / `reduction_sym_head8.rwkvq` на 1.5B.
  **ВНИМАНИЕ, ЛОВУШКА:** `reduction_v2`/`champion_v2` собраны БЕЗ
  `/tmp/act_stats_1p5b.pt` (его нет, есть только `_ml`-варианты), то есть
  AW-режимы в них выродились в обычный поиск -- предупреждение при сборке
  было. Для гейтов равенства это безразлично (они сравнивают два пути на
  ОДНИХ весах), для любого замера КАЧЕСТВА -- нет: это ровно та тихая
  подмена, о которой закон 15. Сборка 2.9B шла со своей статистикой
  (`/tmp/act_stats_2p9b_ml.pt`) и этой оговорки не имеет.
- `/tmp` (пересобрать после ребута): `act_stats_*`, `rwkv15b-*.gguf`,
  `rwkv15b.imatrix`, `champion_v2.rwkvq`, `reduction_v2.rwkvq`.
  `test_gw_nb_parity.py` и `test_fuse_parity.py` жёстко зашиты на эти два
  пути и падают FileNotFoundError, если /tmp почищен. Пересборка:
  `rwkv_quant.quantize(ckpt_1p5b, "/tmp/reduction_v2.rwkvq",
  preset="reduction")` (~90 с, 1256.1 МБ) и то же с
  `preset="compression"` для `champion_v2` (970.8 МБ). `nb_parity`
  требует именно int6-чекпоинт: он ищет тензор с `gw_qh2` и падает
  StopIteration на COMPRESSION.
- Сайдкар в `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — ещё СТАРЫЙ,
  v1: 194 тензора, 1795 МБ, без поля `kind`. Лежащий там же
  `rwkv7-g1h-2.9b.rwkvq` — тоже прежний, pickle-контейнер (читается,
  но не mmap'ится). Ни то ни другое не подменялось молча: выкатка —
  отдельное решение. Пересохранить контейнер можно без переквантования:
  `save_rwkvq(load_raw(старый), новый)`, ровно то, что делает гейт
  `test_rwkvq_container.py`, — результат бит-в-бит.
