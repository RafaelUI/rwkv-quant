# Continuation prompt — rwkv-quant

Квантование RWKV-7 под Apple Silicon (M4 base, 16 ГБ, безвентиляторный).
`rwkv-quant` = квантизация + формат `.rwkvq` + референсный Metal-бэкенд.
`rwkv-metal` и `SwiftRWKV` = бэкенд + QLoRA-файнтюн, потребляют формат.

Чекпоинты: `~/Develop/WKV-kvant/rwkv7-g1h-1.5b-ctx10240.pth` (1.5B,
world naming), `~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` (2.9B),
`~/Develop/WKV-kvant/rwkv7-g1d-0.1b.pth` (драфт).

**ТЕКУЩИЙ ПРИОРИТЕТ — сделать `.rwkvq` самостоятельным форматом.**
Скорость декода закрыта как «достаточно хорошо»: мы быстрее llama.cpp на
13% (замер ниже), дальше гнаться дорого. Пользователь приложения сейчас
качает bf16 + сайдкар = 7.7 ГБ там, где нужно 1.9. Это и есть узкое место
продукта. Полный лог сессий до 03.08 — в git-истории.

---

## Законы методологии

Нарушение каждого приводило к ложным выводам. Первые восемь — прежние,
9-13 добавлены 04.08.

1. Все замеры скорости — ТОЛЬКО A/B-чередованием в одном процессе.
   Безвентиляторный дрейф под нагрузкой до 1.8x на «том же» замере.
2. Тяжёлые ppl-прогоны — по одному конфигу на процесс.
3. ppl сравнивать только внутри одного корпуса.
4. В циклах с деквантом — eval каждую итерацию.
5. Комбинации конфигов непредсказуемы: изолированный выигрыш не
   переносится в композит без замера. Отрицательные результаты не удалять.
6. Metal System Trace: точные числа через `xcrun xctrace export` + union
   интервалов, не «Frame» в GUI.
7. mx-массивы иммутабельны: снапшот state = хранение ссылки.
8. В `mx.fast.metal_kernel` `threadgroup_position_in_grid` — это ИНДЕКС
   threadgroup. И: сравнение массивов может дать ЛОЖНЫЙ бит-экзакт — MLX
   рециклит выходные буферы, непокрытые гридом строки наследуют старое.
9. **КОРПУС РЕШАЕТ БОЛЬШЕ, ЧЕМ СХЕМА КВАНТОВАНИЯ.** `eval_corpus_world.pt[:8]`
   — 1016 предсказаний короткого английского — занижает деградацию
   ВШЕСТЕРО против того же корпуса целиком и вдесятеро против
   мультиязычного на 512 токенах. На нём REDUCTION даёт +0.12%, в
   реальности +2.36%. Замер на удобной выборке хуже отсутствия замера:
   он даёт ложную уверенность. Мерить на `eval_corpus_multiling.pt`.
10. **ВЫВОД С ОДНОГО МАСШТАБА НЕ ПЕРЕНОСИТСЯ НА ДРУГОЙ.**
    `proj=asym_sb6_search` на 1.5B даёт −0.14 п.п., на 2.9B портит
    сербский вдвое. Любое изменение пресета проверять на обоих
    чекпоинтах прежде, чем коммитить.
11. **`ps`/RSS для MLX бесполезен** — показывал 0.6 ГБ при 5 ГБ свопа
    (unified memory ему не видна). Смотреть `vm_stat`,
    `sysctl -n vm.swapusage`, `memory_pressure -Q`. Своп во время замера
    скорости делает замер недействительным — фиксировать своп до и после.
12. **ПОЛОСА ПАМЯТИ 95.7 ГБ/с, НЕ 120.** Прежний закон про «120 GB/s =
    пол decode 7 мс/ток» неверен: 120 — паспортный пик, потоковое чтение
    fp16 в MLX даёт 87-96 ГБ/с (2 ГБ за 20.9 мс). И считать надо трафик,
    а не размер файла: `emb` — это gather одной строки, из 970 МБ
    COMPRESSION за токен едет 878.
13. `torch.load(..., mmap=True)` + один процесс на конфиг — обязательны
    для 2.9B. Загрузка 1062 тензоров за 0 с, страницы file-backed,
    вытесняются ядром, а не свопом. Без этого пик 9-12 ГБ и своп.
14. **В венве стоял НЕ-editable `rwkv_quant` 0.2.0**, и он перекрывал
    исходники для любого скрипта, запущенного не из корня репозитория
    (`sys.path[0]` = каталог скрипта, а не cwd). Скрипты в `tests/`
    спасал их `sys.path.insert(0, "..")`, всё остальное молча получало
    код полугодовой давности. Исправлено `pip install -e .`; если
    появится новый венв — ставить только editable.
15. **act_stats не проверялись на принадлежность модели.** Имена ключей
    у всех размеров RWKV-7 одинаковы, поэтому статистика 1.5B молча
    применялась к 0.1B и падала где-то в `reshape` внутри
    `_groupwise_fake_dequant`. При совпадении размерностей не упала бы
    вовсе. Добавлен `writer._get_ex2`: сверяет длину с числом входных
    каналов тензора и предупреждает вместо тихой подмены. Полноценной
    защитой была бы подпись чекпоинта в файле статистики.
16. **«Сделать вторую реализацию обёрткой» — не бесплатный рефакторинг.**
    План шага 3 предполагал, что torch-упаковщики станут обёртками над
    numpy-версиями. Замер отменил: torch параллелит поэлементные операции
    по ядрам, numpy нет. `tests/bench_codec_dequant_ab.py`, 344M
    элементов, чередование в одном процессе: torch 289 мс, torch с
    numpy-распаковкой 476 (1.65x), полностью numpy 760 (2.63x). Обе
    реализации оставлены, страховка — гейт паритета. Общее: прежде чем
    схлопывать две реализации в одну ради красоты, померить цену; «один
    источник истины» сам по себе не аргумент.
17. **СИНТЕТИКА В ГЕЙТЕ МОЖЕТ ОПИСЫВАТЬ НЕСУЩЕСТВУЮЩУЮ РАСКЛАДКУ.**
    В гейте паритета был случай «asym pad=3»: `gw_scale` на три колонки
    шире, чем нужно, — я предполагал, что так выглядит NBpad. На самом
    деле блоков ровно ceil(IN/gs), никогда больше. Год такой тест
    показывал бы зелёный и защищал не ту инвариантность. Вскрылось
    только когда в `codec` появился ассерт на число блоков — то есть
    когда формат стал утверждать факт, а не молчать. Мораль: если
    синтетический случай нельзя предъявить в реальном чекпоинте, он не
    покрытие, а фантазия; брать формы из настоящего файла
    (`blocks.N.att.w1` [2048, 96] при gs=64 — вот честный ragged-случай).

---

## Состояние пресетов (перемерено 04.08, мультиязычный корпус, 512 ток)

Все числа — реальный путь `QuantRWKV7` (`real_gw=True`), не fake-quant.
Корпус `eval_corpus_multiling.pt`: 38x512 = 19456 предсказаний,
ru 20 / en 9 / sr 9 последовательностей. act_stats мультиязычные, сняты
на ОТЛОЖЕННЫХ чанках.

### 1.5B (bf16 ppl 8.1980, 3055.3 МБ)

| конфиг | МБ | en | ru | sr | ALL |
|---|---|---|---|---|---|
| REDUCTION | 1255.3 | +1.64% | +1.29% | +5.52% | +2.36% |
| REDUCTION `small=16` | 1255.4 | +1.04% | +0.55% | +1.00% | **+0.77%** |
| REDUCTION `small=16`+proj search | 1255.4 | +0.97% | +0.72% | +0.65% | +0.76% |
| COMPRESSION | 970.1 | +4.60% | +4.21% | +8.96% | +5.41% |
| COMPRESSION `small=16` | 970.2 | +4.01% | +3.40% | +3.78% | **+3.63%** |

### 2.9B (bf16 ppl 7.1630, 5896.1 МБ)

| конфиг | МБ | en | ru | sr | ALL |
|---|---|---|---|---|---|
| REDUCTION | 2420.7 | +1.00% | +1.76% | +1.49% | +1.52% |
| REDUCTION `small=16` | 2420.9 | +0.93% | +1.57% | +1.36% | **+1.37%** |
| REDUCTION `small=16`+proj search | 2420.9 | +1.17% | +1.09% | **+2.20%** | +1.37% |
| COMPRESSION | 1854.5 | +5.64% | +3.26% | +5.62% | +4.38% |
| COMPRESSION `small=16` | 1854.7 | +5.69% | +3.05% | +5.46% | **+4.24%** |

### Единственная правка пресетов, которую стоит внести

`small=8` -> `small=16` в ОБОИХ пресетах. Стоит +0.14 МБ (0.011% файла),
улучшает все три языка на обоих масштабах. `proj` НЕ трогать: на 1.5B
`asym_sb6_search` выигрывает 0.14 п.п., на 2.9B портит сербский — закон 10.

---

## Почему `small`: диагностика 04.08

Leave-one-out по группам на REDUCTION/1.5B
(`tests/ablate_group_contrib.py`, `STAGE=1` и `STAGE=2`), вклад в общие
+2.36%:

| группа -> bf16 | вклад |
|---|---|
| proj | +0.18 п.п. |
| cmix | +0.33 п.п. |
| emb_head | +0.18 п.п. |
| w_lora / a_lora / v_lora / g_lora | −0.02 / +0.09 / +0.02 / +0.07 п.п. |
| **small (k_k, k_a, r_k)** | **+1.59 п.п.** |

147K параметров (0.01% модели) уносят две трети деградации. Три большие
матричные группы вместе — 0.69 п.п.

Механизм: `k_k`/`k_a`/`r_k` — поканальные модуляторы ВНУТРИ
рекуррентности (нормировка ключа, темп in-context обучения,
receptance-бонус). Их ошибка не портит одно предсказание, а искажает
обновление состояния и копится по длине. Подтверждение независимым
замером: деградация растёт с контекстом (+1.52% на 128 токенах против
+2.57% на 512).

На 2.9B эффект в десять раз слабее (−0.15 п.п.), и сербской катастрофы
там нет вовсе (sr +1.49% между en +1.00% и ru +1.76%). Похоже, на 1.5B
модели просто не хватало ёмкости на сербский, и квантование добивало то,
что держалось на пределе.

---

## Позиция против llama.cpp (1.5B, 04.08)

Данные разделены одинаково для обеих систем: калибровка (act_stats /
imatrix) на чанках `test.txt` № 0,4,5,6,12,16,18,20, измерение на
№ 1,2,7,8,10,13,14,17,19. Абсолютные ppl несопоставимы (llama.cpp
токенизирует сплошняком), сравниваются Δ% к своему FP-базлайну.

### Качество (llama.cpp baseline F16 = 7.2504)

| формат | МБ | без imatrix | с imatrix |
|---|---|---|---|
| Q6_K | 1336 | +0.41% | **+0.19%** |
| Q5_K_M | 1158 | +1.86% | **+1.02%** |
| Q4_K_M | 990 | +7.65% | **+3.44%** |

Против нас после `small=16`: ~1255 МБ +0.77% против интерполированных
~+0.57%; ~970 МБ +3.63% против Q4_K_M 990 МБ +3.44%. **Паритет в
пределах методической погрешности на обеих точках.** Без imatrix мы
выглядели победителями — весь отрыв объяснялся тем, что у нас AW было,
а у них нет.

### Скорость (`tests/bench_decode_vs_gguf.py` + `llama-bench`)

| | МБ | pp512 | tg128 |
|---|---|---|---|
| COMPRESSION | 970 | 455.7 t/s | **65.63 t/s** |
| Q4_K_M+imat | 990 | **745.2** | 58.16 |
| REDUCTION | 1255 | 353.7 | **54.86** |
| Q6_K+imat | 1336 | **705.5** | 48.59 |
| F16 GGUF | 3055 | 797.3 | 30.23 |

**Декод наш на +12.8% и +12.9%** (одинаковый отрыв на обеих точках — не
шум). **Префилл проигрываем вдвое**: llama.cpp считает GEMM через
BLAS+Metal, у нас деквант в fp16 + матмул MLX. Это самый большой разрыв
из всех намеренных, но для чат-приложения он менее критичен, чем декод.

### Эффективность по полосе (95.7 ГБ/с достижимых)

| | трафик/ток | достигнуто | доля |
|---|---|---|---|
| COMPRESSION | 878 МБ | 57.6 ГБ/с | 60% |
| REDUCTION | 1146 МБ | 62.9 ГБ/с | 66% |
| Q4_K_M | ~905 МБ | 52.6 ГБ/с | 55% |

Мы уже эффективнее llama.cpp по полосе. До разумных 85% остаётся ~40%,
и это тяжёлый микротюнинг — сознательно отложен.

---

## ГЛАВНОЕ: `.rwkvq` как самостоятельный формат

### Проблема в одной строке (закрыта шагом 4, оставлено как мотивация)

`writer.save()` заканчивался `torch.save(QuantizedCheckpoint)`, а
`reader.load_raw()` — `torch.load(..., weights_only=False)`. Это был не
формат, а pickle питоновского объекта: нечитаем без torch И без
установленного пакета `rwkv_quant` (pickle пишет FQN классов), исполнял
произвольный код при загрузке, не имел `format_version`, mmap и
потензорного чтения.

Из-за этого появился `export_mlx.py` — мост в
`*.rwkvq_mlx.safetensors`. Но он фильтрует по `gw_mode == "sb6"` и
выбрасывает всё остальное, поэтому `SwiftRWKV`/`rwkv-metal` вынуждены
держать рядом bf16-чекпоинт.

### Цифры (2.9B, COMPRESSION), проверены чтением реальных файлов

| | МБ |
|---|---|
| `model.safetensors` bf16 | 5895 |
| `.rwkvq` (все 1062 тензора внутри) | 1855 |
| сайдкар `_mlx.safetensors` (194 sb6-тензора) | 1796 |
| **качает пользователь сейчас** | **7691** |
| **самодостаточный сайдкар** | **~1898** |

**+102 МБ, чтобы 5.9 ГБ исчезли.** Загрузка падает вчетверо. Недостающие
102 МБ: w/a/v_lora в asym-gw64 (47 МБ), g_lora @8 per-row (52 МБ),
одномерные dense bf16 (3 МБ). Плюс токенайзер 1.1 МБ.

K3-интерлив хранит `qsqm` как `uchar2` (2 байта на блок вместо 1.5) —
это +0.125 бит/вес = **+45 МБ** против канонической раскладки. Либо
платим и грузим mmap как есть, либо репакуем при загрузке.

### План

**Шаги 1-4 СДЕЛАНЫ 04.08**, 5-6 остаются.

Что это значит для приложения: `.rwkvq` теперь самодостаточен и читается
без torch. Сайдкар `_mlx.safetensors` из обязательного моста стал
необязательным кешем K3-раскладки — он всё ещё быстрее на загрузке
(раскладка уже интерливнута), но стоит +45 МБ и больше ничего не
разблокирует. Пользователь качает 1855 МБ на 2.9B вместо 7691.

1. ~~**Полный экспорт.**~~ СДЕЛАНО. `export_mlx.py` больше не фильтрует
   по `gw_mode`; манифест получил `format_version: 2` и поле `kind` на
   тензор (`sb6` / `asym` / `rtn` / `dense`), плюс `config_repr` и путь
   к токенайзеру. На 2.9B: 1062 тензора (194 sb6, 192 asym, 160 rtn,
   516 dense) -> **1899.2 МБ**, ровно расчётные 1898. Гейт
   `tests/verify_sidecar_full.py` восстанавливает из сайдкара БЕЗ torch
   и сверяет с `reader._dequantize_one`: **все четыре раскладки
   бит-в-бит, max|Δ| = 0**. Функция `dequant_from_sidecar` в этом гейте
   — референсная читалка для rwkv-metal и SwiftRWKV, порт с неё.
2. ~~**Стриминговый writer.**~~ СДЕЛАНО. `formats.writer.quantize_file`:
   `torch.load(mmap=True)`, потензорное квантование с немедленным
   отпусканием ссылки, метаданные из форм (`detect_meta`) вместо
   инстанцирования `RWKV7Ref`. `api.quantize` переведён на него. A/B на
   1.5B: минимум свободной памяти **34% у старого пути против 77% у
   нового**, время то же (43 с против 42), файл побайтово того же
   размера, 42 сверенных тензора деквантуются идентично.
   ВНИМАНИЕ: `maximum resident set size` тут вводит в заблуждение —
   у нового пути он ВЫШЕ (9.2 ГБ против 5.6), потому что mmap считает
   страницы файла резидентными. Они чистые и вытесняются без записи в
   своп. Мерить `memory_pressure -Q` и `vm.swapusage`, не RSS (закон 11).
3. ~~**torch-free распаковщики.**~~ СДЕЛАНО, но не так, как
   планировалось. Появился `formats/codec.py` — чистый numpy, ноль
   импортов torch: все четыре упаковщика И деквант всех трёх
   квантованных раскладок (`dequant_sb6` / `dequant_asym` /
   `dequant_rtn`). Это нормативная реализация канонической ДИСКОВОЙ
   раскладки, с неё портируются rwkv-metal и SwiftRWKV (у
   `verify_sidecar_full.dequant_from_sidecar` другая задача — она про
   K3-раскладку сайдкара).
   План говорил «torch-версии становятся обёртками» — замер это отменил,
   см. закон 16: обёртки стоили 1.65x на деквант. `schema.py` и
   `reader.py` остались torch, дублирование осознанное и задокументировано
   в докстрингах обоих файлов.
   Гейт `tests/test_codec_parity.py`: роундтрипы, упаковщики против
   замороженной копии прежнего torch-кода, три пути декванта (codec /
   reader / прежний torch) на синтетике всех раскладок и на реальных
   чекпоинтах. 0.1B, 1.5B COMPRESSION, 1.5B REDUCTION (int6, обе
   битплоскости), 2.9B: **2.9 G элементов, расхождений 0**.
   Что это НЕ разблокировало: rwkv-metal всё ещё не читает `.rwkvq`
   напрямую, потому что контейнер по-прежнему pickle. Нужен шаг 4;
   `codec.py` — его половина, которая теперь готова.
4. ~~**Контейнер -> safetensors.**~~ СДЕЛАНО. `writer.save_rwkvq`:
   плоские имена `key::поле`, манифест одним JSON в
   `__metadata__["rwkvq"]` (`format: "rwkvq"`, `format_version: 1`, пять
   чисел про модель, `config_repr`, на тензор — `kind` / `shape` /
   `bits` / `group` / `gw_gs` / `gw_sb` / список полей). `save()` и
   `quantize_file()` переведены, публичный API не изменился.
   `load_raw` различает контейнеры по первым байтам и читает оба.
   **Загрузка теперь ленивая, и это главный эффект** (замер
   `tests/bench_rwkvq_load.py`, по процессу на конфиг):

   | 2.9B | загрузка | maxrss |
   |---|---|---|
   | pickle | 0.24 с | 2.08 ГБ |
   | safetensors | **0.03 с** | **0.25 ГБ** |

   1.5B: 0.16 с / 1.19 ГБ против 0.05 с / 0.24 ГБ. `safe_open` отдаёт
   тензоры видами на отображённый файл, поэтому 1.85 ГБ анонимной памяти
   при загрузке больше не выделяется вовсе; страницы подтягиваются по
   мере обращения и вытесняются как чистые.
   Размер файла не изменился (−0.02%: пропал zip-оверхед torch.save).
   Гейт `tests/test_rwkvq_container.py`: роундтрип всех полей бит-в-бит
   (0.1B / 1.5B COMPRESSION / 1.5B REDUCTION / 2.9B — до 2318 буферов),
   чтение прежних pickle-файлов, torch-free путь против reader на всех
   раскладках, негативные контроли (safetensors без манифеста,
   `format_version` из будущего). Бэкенды прогнаны на новом контейнере:
   `test_v2_format`, `test_gw_kernel`, `test_gw_kernel_int6`,
   `test_gw_nb_parity`, `test_quant_model_smoke` — зелёные.
   **`codec.py` стал полной читалкой формата**: `open_rwkvq` разбирает
   safetensors на numpy+mmap, `dequant_key` отдаёт float32. Порт в
   rwkv-metal и SwiftRWKV делается с одного файла и не требует ни torch,
   ни пакета `rwkv_quant`, ни сайдкара.
5. **Спека.** `format_version` и опознание формата сделаны шагом 4
   (`manifest["format"] == "rwkvq"` плюс различение контейнеров по
   первым байтам; отдельного magic у safetensors нет и не нужно).
   Осталось собственно НОРМАТИВНЫЙ текст: докстринги `codec.py` и
   `schema.py` — уже почти он, надо свести в один документ и записать
   как обязательство: диск = каноническая раскладка, K3-интерлив =
   деталь загрузчика.
6. ~~**Самодостаточность по содержимому.**~~ СДЕЛАНО, `format_version: 2`.
   В per-tensor метаданные добавлены `n_blocks` и `transposed`, на
   верхний уровень — структурированный `config` и `tokenizer`. Читатель
   v2 понимает v1: недостающее выводится как раньше.
   Что вскрылось при этом, и ради чего шаг стоил времени:
   - **`n_blocks` — это ceil(IN/gs), а не IN//gs.** У `blocks.N.att.w1`
     формы [2048, 96] при gs=64 блоков ДВА, а деление нацело даёт один.
     Таких тензоров 48 на 1.5B. Читатель, посчитавший число блоков
     делением, взял бы на последних 32 колонках чужой масштаб — и это
     ровно тот сорт ошибки, который не падает, а тихо портит веса.
     Теперь в `codec` стоит ассерт, и он сразу поймал синтетический
     случай в прежнем гейте, который описывал несуществующую раскладку.
   - **`config_repr` был лосси.** `QuantConfig.__repr__` печатает
     битность и overrides, но молча теряет `group_scale`,
     `group_scale_mode`, `clip_percentiles`, `outlier_fracs` и
     `act_stats_path` — то есть ровно то, чем REDUCTION отличается от
     COMPRESSION при одинаковой битности. Воспроизвести квантование по
     файлу было нельзя; теперь конфиг едет структурой и собирается
     обратно в `QuantConfig`.
   Гейт `tests/test_manifest_selfdesc.py` сверяет `transposed` не с
   таблицей имён в `codec`, а с ИСХОДНИКОМ `models/rwkv7_ref.py`
   (регуляркой достаётся множество ключей, к которым ref реально
   применяет `.T`), и требует, чтобы в чекпоинте нашёлся хотя бы один
   тензор с `n_blocks != IN // gs` — иначе проверка вырождена.

Публичный API (`load_raw` -> `QuantizedCheckpoint`) при этом не
меняется, бэкенды не трогаются.

---

## Скорость декода: закрыто как «достаточно»

### Драфт-спекулятивка — ОТРИЦАТЕЛЬНО, подтверждено дважды

03.08 закрыта по согласию (`dev_draft_acceptance.py`, p = 0.5820,
потолок 1.392 принятых за раунд). 04.08 перепроверено сквозным циклом с
настоящей драфт-моделью (`tests/spec_decode_draft_model.py`): согласие
0.5755, принято 64%, 3.55 токена за раунд — и всё равно

```
plain:      16.16 мс/ток (61.9 ток/с)
spec k=4:   30.72 мс/ток (32.6 ток/с)  ->  0.53x
```

Своп за замер −48 МБ, замер валиден. Память НЕ проблема: квантованный
черновик 126.6 МБ (92 МБ трафика за токен, отношение к цели 9.5x).
Черновик обязан быть квантованным — в bf16 он 380 МБ, отношение 3.1x, и
арифметика не сходится в принципе.

### Разложение, объясняющее почему (`tests/bench_spec_breakdown.py`)

| | трафик | потолок | T=1 | T=5 | T=9 |
|---|---|---|---|---|---|
| черновик 0.1B | 92 МБ | 0.96 мс | 3.42 мс (29%) | 7.01 | 8.24 |
| цель 1.5B | 878 МБ | 9.17 мс | 15.08 мс (61%) | 39.45 (23%) | 63.18 (15%) |

**Верификация не амортизирует чтение весов.** T=5 стоит 2.6x от T=1,
предельная цена лишнего токена 6 мс против 15 одиночных — амортизация
2.5x там, где нужна 5-10x. Эффективность ПАДАЕТ с ростом T (61% -> 23%
-> 15%), хотя должна расти. N-батчевый кернель (`_get_kernel_gw_nb`)
написан, но на этом пути явно не задействован.

Синхронизация GPU->CPU ни при чём: 0.02-0.15 мс.

Если бы верификация амортизировала как положено (~18 мс вместо 39.5),
вышло бы 1.8x; с починенным черновиком 2.4x. Потенциал реален, но это
кернельная работа — отложена.

**Не проверено и решает, стоит ли возвращаться:** у RWKV верификация K
токенов содержит ПОСЛЕДОВАТЕЛЬНЫЙ прогон WKV-рекуррентности, которого у
трансформеров нет. Если значимая часть тех 6 мс — WKV-скан, а не
перечитывание весов, потолок спекулятивки для RWKV принципиально ниже.
Разделяется одним замером: T=5 с отключённой WKV-частью против полной.

### Разреженность cmix — измерена, не реализована

`tests/probe_cmix_sparsity.py`, 36792 пары токен×слой. RWKV-7 считает
`k = relu(x @ key.T)**2` — ТОЧНЫЕ нули, и маска известна ДО того, как
понадобится `value` (в отличие от трансформеров, где нужен
предсказатель).

```
нулей в k:                        86.68%
блоков по 32 целиком нулевых:     11.44%
  после перестановки каналов:     34.76%
  будь каналы независимы:          1.03%
```

11.44% против 1.03% при независимости = каналы гаснут группами, просто
группы не совпадают с границами блоков. Перестановка колонок
`cmix.value` и строк `cmix.key` (офлайн, математически тождественна)
поднимает долю целиком нулевых блоков втрое. `cmix.value` — 226 МБ из
878; минус 34.76% = **−79 МБ трафика = ~10% скорости, точно и без потери
качества**.

Оговорки: перестановка выведена на тех же токенах (нужна проверка на
отложенных); сортировка по частоте — самая тупая из возможных,
кластеризация по ко-активации должна дать больше; работает только на
декоде (в префилле маски токенов объединяются).

Приближённый вариант поверх: top-50% блоков несут 97.25% энергии
`||k||^2`, top-25% — 89.00%, top-12.5% — 77.64%.

---

## Потребители формата: что найдено 04.08, до правок

Порядок работ: **rwkv-quant -> rwkv-metal -> SwiftRWKV**. Swift следует
питоновскому эталону, поэтому ошибки в нём чинятся после, а не
параллельно.

### rwkv-metal

- `RwkvqNativeLinear` ассертит `xbits == 2`, то есть bits=6 — это ровно
  REDUCTION. Не поддержан как раз COMPRESSION (proj/cmix на 4/5 бит,
  раскладка `mx.quantize` для не-шестибитных случаев не проверена).
  То есть QLoRA на REDUCTION — поддержанный путь, а не новый.
- `load_lora_rwkvq_model(pth_path, sidecar_path, ...)` до сих пор
  требует `.pth` ради ~650 неквантованных тензоров. Шаг 4 это снимает:
  dense/asym/rtn лежат в `.rwkvq`.
- `RwkvqLinear.from_sidecar` читает K3-буферы `qblk/qsqm/ddm`, а в
  `.rwkvq` каноническая раскладка. Либо интерливить при загрузке, либо
  оставить сайдкар кешем K3.
- **Развилка с LoRA-ветками ИЗМЕРЕНА 04.08** (`tests/ablate_qlora_lora_source.py`,
  мультиязычный корпус @512, реальный путь `QuantRWKV7`). Составы
  различаются РОВНО ветками, всё прочее идентично; `qlora_pth` — база
  как сейчас (proj/cmix/head @6 sb6, LoRA/emb/small из `.pth` в bf16).

  | конфиг | 1.5B ALL | к bf16 | 2.9B ALL | к bf16 |
  |---|---|---|---|---|
  | bf16 | 8.1980 | — | 7.1630 | — |
  | qlora_pth | 8.2495 | +0.63% | 7.2504 | +1.22% |
  | + w/a/v_lora из `.rwkvq` | 8.2515 | +0.65% | 7.2542 | +1.27% |
  | + g_lora (то, что получится) | 8.2592 | +0.75% | 7.2609 | +1.37% |
  | REDUCTION целиком | 8.2610 | +0.77% | — | +1.37% |

  **Цена отказа от `.pth`: +0.118% на 1.5B и +0.145% на 2.9B.**
  Разложение согласовано между масштабами: `w/a/v_lora` +0.024% / +0.052%,
  **`g_lora` +0.094% / +0.092%** — две трети сдвига несёт одна группа,
  и несёт одинаково на обоих масштабах.
  Хуже всего сербский (+0.344% на 2.9B) — он и раньше был канарейкой.

  Два побочных вывода, оба перепроверены цифрами:
  - Харнесс сошёлся с прежней таблицей ДО ЧЕТВЁРТОГО ЗНАКА (REDUCTION
    1.5B: en +1.04 / ru +0.55 / sr +1.00 / ALL +0.77) — независимое
    воспроизведение, а не совпадение методики.
  - `qlora_all` (emb в bf16) и REDUCTION (emb квантован) дают +0.75/+0.77
    на 1.5B и +1.367/+1.37 на 2.9B, то есть **квантование emb не стоит
    ничего по качеству** на обоих масштабах. Аргумент держать таблицу
    плотной — только механический (sb6 не умеет выбирать строки), не
    качественный.
  - **Ошибка в весах не предсказывает эффект.** Относительная ошибка
    деквантования почти одинакова у всех четырёх веток (asym
    1.90-2.15e-2, g_lora rtn 1.78e-2), а вклад в ppl различается вчетверо.
    Смотреть на нормы вместо ppl тут бесполезно.

  Решение по составу базы принимать с этими числами на руках: если
  +0.15% не устраивает, дешёвый выход — собирать QLoRA-вариант `.rwkvq`
  с `g_lora=16` (+25 МБ на 1.5B), это возвращает две трети.

### SwiftRWKV

- **`RwkvqSidecar.init` сломается на любом новом манифесте.** Он
  требует у КАЖДОЙ записи `shape.count == 2`, `bits`, `xbits`, `gw_gs`,
  `gw_sb`, а полный экспорт и манифест `.rwkvq` содержат dense-записи:
  одномерные нормы, `(1,1,C)` token-shift миксы. Парсер надо делать
  kind-aware (`sb6` / `asym` / `rtn` / `dense`).
- `loadArraysAndMetadata` уже используется в `RWKVRerank`, так что
  манифест из `__metadata__["rwkvq"]` читается сразу — отдельный `.json`
  не нужен.
- **Выигрыш mmap в Swift сам собой не воспроизведётся.** По собственному
  комментарию в `RWKVRerank/StateCache.swift`, загрузчик safetensors в
  mlx-swift материализует массив целиком. На 2.9B это 1.85 ГБ
  резидентно на старте против 0.25 ГБ в Python. Если это важно —
  писать свой mmap-ридер safetensors (u64, JSON, оффсеты);
  `codec.read_safetensors` — прямой образец для порта.

## Закрытые темы (не переоткрывать без новых данных)

- ANE/CoreML; GPU idle; qs/qm-упаковка в кернеле; пайплайн decode;
  4x-сжатие в нибл-контейнере; MXFP4; v3 simdgroup head; nb2; GEMM
  r/k/v-фьюз префилла; гибрид mlx-affine ради скорости.
- xbsum-в-кернеле (K3_XSUM=False): decode x1.005, verify x0.94, дрейф
  логитов 0.02. Полублок (K3_HALF=False): микро +8-19%, E2E ноль.
  packs2: хуже базы. Свипы (NSG,RS) для cmix: тепловой шум.
- КЕРНЕЛЬ-4 закрыт руфлайн-пробой: декодный ALU бесплатен.
- LORA_Q8: decode x1.004, дрейф логитов 0.77 — отрицательно.
- **AW для `proj`** (04.08, `tests/ablate_aw_proj.py`): `asym_sb6`
  +2.36%, `asym_sb6_search` +2.22%, `asym_sb6_aw` +2.24%. AW не помогает
  — исходный вывод устоял. Но разница search/aw в пределах шума, а
  нынешний `asym_sb6` (без поиска И без AW) хуже обоих на 1.5B и лучше
  на 2.9B. Не трогать.
- **Драфт-спекулятивка** — дважды отрицательно, см. выше.

---

## Открытые вопросы

1. **Формат** — шаги 1-4 и 6 сделаны, приложение разблокировано.
   Остался только 5: свести докстринги в нормативный текст спеки.
   Порядок дальнейших работ (согласован): rwkv-quant -> rwkv-metal ->
   SwiftRWKV, потому что Swift следует питоновскому эталону и правит
   его ошибки следом, а не параллельно.
2. Симметричная Q6_K-раскладка. Реализована в фейк-пути:
   `writer._groupwise_sym_fake_dequant` (блок 16 без min, 8-битный scale
   против fp16 d на суперблок; режимы `sym` / `sym_plain` / `sym_aw`;
   плюс параметр `outlier_frac` — SpQR поверх groupwise, чего раньше не
   было). На гауссовых весах при равном бюджете: bits=4 asym лучше
   (5.465e-3 против 5.617e-3), bits=5 sym16 лучше на 12%, bits=6 sym16
   лучше на 21%. Переход между 4 и 5 битами — ровно там, где llama.cpp
   меняет Q4_K на Q6_K. Трафик тот же (26.25 байта на 32 веса против
   26). НЕ мерено на реальной модели; мерить сразу на 1.5B И 2.9B.
3. Префилл: разрыв 2x с llama.cpp — самый большой из намеренных.
4. Перестановка каналов cmix: ~10% декода, точно и без потери качества.
5. Разделить WKV-скан и перечитывание весов в верификации — решает
   судьбу спекулятивки для RWKV в принципе.
6. ОП-КАУНТ: фьюзия лора-блока (~6-8 опов/слой сейчас), fuse
   groupnorm+bonus+gate вокруг WKV. Оценка −0.5-1 мс.
7. FUSE=True по умолчанию? Стабильно −0.8 мс, гейты зелёные.
8. Согласие драфта по языкам переснять на стратифицированной выборке —
   0.5755 намерено только на русских последовательностях.
9. **`test_streaming_inference` красный, и это НЕ регресс контейнера.**
   Проверено прямым сравнением на `git stash`: до шага 4 те же числа до
   последнего знака. Батчевый `forward_stateful` против `wkv7_train`
   даёт rel 1.282e-4 при пороге 1e-4 — чуть-чуть за. Комментарий в тесте
   утверждает, что «расхождение батчевого пути осталось нулевым», то
   есть rel1 уехал когда-то при переводе dense/LoRA на fp16, и этого не
   заметили. Порог НЕ трогал: сначала понять, откуда 1.2e-4 на пути, где
   ожидался ноль. Token-by-token (rel2 5.13e-4 при пороге 1e-3) и top-5
   в порядке, так что на качество это, скорее всего, не влияет.

---

## QLoRA поверх `.rwkvq` в rwkv-metal (19-20.07, актуально)

Всё бит-в-бит сверено с `rwkv_quant.formats.reader`. MLX-порт sb6/asym
dequant (`rwkv_metal/lora/rwkvq_linear.py`, `rwkvq_kernel.py`) — 0
расхождений на реальных тензорах. Финальный combine ОБЯЗАТЕЛЬНО в
float32: `GwQuantLinear._dequant_w()` держит математику в half и даёт
~18% расхождений на 1 бит bf16-мантиссы.

| путь | шаг (steady) | mx_peak | maxrss |
|---|---|---|---|
| stock QLoRA (nn.quantize 6bit) | 0.80-0.89 с | 3.02 ГБ | ~6.9 ГБ |
| rwkvq, наивный MLX-порт | 3.4-3.6 с | 4.39 ГБ | 6.6-7.0 ГБ |
| rwkvq, fused-кернель | 1.2-1.3 с | 3.65 ГБ | 3.64 ГБ |
| rwkvq, native quantized_matmul | 0.7-0.8 с | 4.59 ГБ | 4.36 ГБ |
| rwkvq, гибрид | 0.8-0.9 с | 4.19 ГБ | 4.62 ГБ |

Незакрытое: `emb.weight` не квантуется ни в одном пути (нужен
indexed-gather); native/hybrid проверены только для bits=6; качество
после сотен шагов не мерено; COMPRESSION как QLoRA-база не пробован.

---

## Тесты и замеры (что где лежит)

### Корпус и калибровка

- `tests/build_eval_multiling.py` — `test.txt` ->
  `eval_corpus_multiling.pt` (38x512, ru/en/sr, id чанка и превью в
  блобе) + `act_calib_multiling.pt`. Разбиение calib/eval ПО ЧАНКАМ:
  AW-статистика не должна сниматься с текста, на котором меряется ppl.
  Хвост >= половины окна добирается перекрытием (иначе en/sr теряют
  ~30% материала).
- `tests/collect_act_stats.py` — принимает `RWKVQ_CKPT` из окружения
  (2.9B) и argv `corpus out slice`. Артефакты: `/tmp/act_stats_1p5b.pt`
  (англ., рецепт по умолчанию), `/tmp/act_stats_1p5b_multiling.pt`,
  `/tmp/act_stats_2p9b_multiling.pt` (193 тензора). НЕ переживают ребут.

### Качество

- `tests/eval_multiling.py` — ppl по языкам и ПО ЧАНКАМ на 1.5B. NLL
  через logsumexp, а не материализацию log_softmax: логиты
  [B,511,65536], полный fp32-logp плюс копии softmax/log = ~0.8 ГБ
  транзиента на батч.
- `tests/eval_corpora_compare.py` — разложение расхождения с README на
  три множителя. Модель строится один раз на конфиг, корпусов четыре:
  A `world[:8]@128` (1016 предсказаний, воспроизводит README до
  четвёртого знака: bf16 11.4303, REDUCTION +0.12%), B `world[:24]@128`
  (+0.74%), C `ml@128` (+1.52%), D `ml@512` (+2.57%). Размер выборки,
  язык и длина контекста — по отдельности.
- `tests/ablate_group_contrib.py` — leave-one-out по группам, `STAGE=1`
  крупные группы, `STAGE=2` дробит lora+small.
- `tests/ablate_aw_proj.py` — три режима proj (нет поиска / поиск /
  поиск+AW).
- `tests/confirm_small_fix.py` — подтверждение `small=16` на обоих
  пресетах, 1.5B.
- `tests/eval_2p9b_one.py <config>` + `tests/eval_2p9b_report.py` —
  2.9B, ПО КОНФИГУ НА ПРОЦЕСС (законы 11 и 13). Конфиги: `bf16`,
  `reduction`, `reduction_small`, `reduction_fix`, `compression`,
  `compression_fix`. bf16-ветка не идёт через `quantize_tensor` (там
  `.clone()` = 5.9 ГБ анонимной памяти поверх уже отображённого файла).

### Скорость

- `tests/bench_decode_vs_gguf.py` — pp512/tg128 нашим кернелем, метрики
  как у `llama-bench`.
- `tests/bench_spec_breakdown.py` — разложение шага на трафик и
  задержку, T=1/5/9, с синхронизацией и без.
- `tests/spec_decode_draft_model.py [K] [N_GEN]` — сквозной
  спекулятивный цикл с драфт-моделью, механика pending из
  `spec_decode_champ.py`. Печатает согласие, ток/раунд, мс/ток и дельту
  свопа.
- `tests/probe_cmix_sparsity.py [n_seq]` — разреженность `relu^2`,
  блочность, выигрыш от перестановки, концентрация энергии.

### Формат

- `rwkv_quant/formats/codec.py` — НОРМАТИВНАЯ реализация формата на
  чистом numpy: контейнер (`open_rwkvq`), раскладка, деквант
  (`dequant_key`). Порт в Swift/MLX делать отсюда, файл самодостаточен.
- `tests/test_rwkvq_container.py <in.rwkvq> [out.rwkvq]` — гейт шага 4:
  роундтрип полей, чтение прежнего pickle, torch-free путь, негативные
  контроли.
- `tests/test_manifest_selfdesc.py <model.rwkvq>` — гейт шага 6:
  `transposed` против исходника `rwkv7_ref.py`, `n_blocks` против форм
  буферов, роундтрип конфига, совместимость с манифестом v1 (файл v1
  делается из v2 правкой одного заголовка, буферы те же).
- `tests/bench_rwkvq_load.py <model.rwkvq>` — время и память загрузки,
  по конфигу на процесс (законы 11 и 13). Чередование делает вызывающий
  скрипт.
- `tests/test_codec_parity.py [model.rwkvq ...]` — гейт шага 3. Без
  аргументов гоняет синтетику (секунды), с путями добавляет полный
  деквант реальных чекпоинтов. Прогнано на 0.1B / 1.5B COMPRESSION /
  1.5B REDUCTION / 2.9B — ноль расхождений.
- `tests/bench_codec_dequant_ab.py <model.rwkvq> [R] [N]` — цена
  torch-free декванта, чередование в одном процессе. Это замер из закона
  16, повторять при попытке снова схлопнуть реализации.
- `tests/verify_sidecar_full.py <сайдкар_без_расширения> <model.rwkvq>` —
  ГЕЙТ полного сайдкара. Восстанавливает все четыре раскладки без torch и
  сверяет с эталоном. `dequant_from_sidecar` в нём — референсная читалка
  K3-раскладки (в отличие от `codec.py`, который про дисковую).
  Перепроверено 04.08 на 1.5B REDUCTION: 798 тензоров, 1426 буферов,
  1278.6 МБ, все четыре раскладки 100% точных, max|Δ| = 0.
- `python -m rwkv_quant.formats.export_mlx <in.rwkvq> <out> [токенайзер]`
  — полный экспорт.
- `rwkv_quant.quantize(pth, out, preset=...)` — теперь потоковый
  (`formats.writer.quantize_file`).

### Сравнение с llama.cpp

Бинарники из homebrew (`llama-perplexity`, `llama-quantize`,
`llama-imatrix`, `llama-bench`), конвертер
`~/Develop/rwkv-gguf-compare/convert_rwkv_pth_to_gguf.py` (принимает
путь к `.pth`, нужен `pip install gguf`). Сводка — в
`~/Develop/WKV-kvant/GGUF_COMPARE.md`.

### QLoRA-база

- `tests/ablate_qlora_lora_source.py [конфиг ...]` — цена отказа от
  `.pth` в QLoRA-базе. Конфиги `bf16` / `qlora_pth` / `qlora_wav` /
  `qlora_all` / `REDUCTION`. На 2.9B гонять ПО ОДНОМУ на процесс
  (`RWKVQ_CKPT`, `RWKVQ_ACT_STATS`, `RWKVQ_OUT`, якорь
  `RWKVQ_BF16_ALL=7.1630` — bf16 на 2.9B через `quantize_tensor` не
  строится, там `.clone()` = 5.9 ГБ поверх отображённого файла).
  ВНИМАНИЕ: диагностика по весам гоняется только на `qlora_all` и стоит
  ещё одного полного прохода квантования — на 2.9B это +10 минут.

### Результаты в JSON

`~/Develop/WKV-kvant/`: `eval_multiling_1p5b.json`,
`qlora_lora_source.json`, `qlora_lora_source_2p9b.json`,
`eval_corpora_compare.json`, `ablate_group_contrib.json`,
`ablate_aw_proj.json`, `confirm_small_fix.json`, `eval_2p9b.json`,
`GGUF_COMPARE.md`.

### Прежние гейты и бенчи

`tests/`: `diagnose_one.py`, гейты (`test_gw_nb_parity`,
`test_gw_kernel[_int6]`, `test_fuse_parity`, `test_v2_*`), бенчи
(`bench_verify_cost*`, `bench_gw_nb_*`, `bench_mlx_vs_gw_*`, `4way`),
КЕРНЕЛЬ-3 (`bench_kernel3_proto/C/D/E/F/F2/F3`, `bench_k3_e2e_ab.py`),
`profile_decode_v2_compiled.py`, спекулятивка
(`spec_decode_poc/champ/draft/draft2.py`), `eval_molly_real.py`,
`eval_canonical_int4/int6.py`.

---

## Где лежат материалы

- `~/Develop/WKV-kvant/` — чекпоинты (1.5B G1H, драфт g1d-0.1b, ru60m),
  корпуса, `world_tokenizer.py`, все JSON-результаты и
  `GGUF_COMPARE.md`.
- `~/Develop/rwkv7-g1h-2.9b-ctx10240.pth` — 2.9B (world naming).
- `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — `model.safetensors` bf16,
  `rwkv7-g1h-2.9b.rwkvq`, сайдкар `_mlx.safetensors`+`.json`,
  токенайзер.
- `~/Develop/SwiftRWKV/Sources/RWKVQuant/` — Swift-читалка сайдкара
  (`RwkvqSidecar.swift`, `RwkvqDequant.swift`), `.testdata/` с
  эталонами.
- `~/Develop/rwkv-metal/rwkv_metal/lora/` — QLoRA-стек поверх `.rwkvq`.
- `~/Develop/tests/venv/bin/python` — рабочее окружение (torch 2.13 +
  MLX, python 3.14).
- `/tmp` (пересобрать после ребута): `act_stats_*`, `rwkv15b-*.gguf`,
  `rwkv15b.imatrix`, `champion_v2.rwkvq`, `reduction_v2.rwkvq`.
  `test_gw_nb_parity.py` и `test_fuse_parity.py` жёстко зашиты на эти два
  пути и падают FileNotFoundError, если /tmp почищен. Пересборка:
  `rwkv_quant.quantize(ckpt_1p5b, "/tmp/reduction_v2.rwkvq",
  preset="reduction")` (~90 с, 1256.1 МБ) и то же с
  `preset="compression"` для `champion_v2` (970.8 МБ). `nb_parity`
  требует именно int6-чекпоинт: он ищет тензор с `gw_qh2` и падает
  StopIteration на COMPRESSION.
- Сайдкар в `~/Develop/Synapse/Models/rwkv7-2.9b-g1h/` — ещё СТАРЫЙ,
  v1: 194 тензора, 1795 МБ, без поля `kind`. Лежащий там же
  `rwkv7-g1h-2.9b.rwkvq` — тоже прежний, pickle-контейнер (читается,
  но не mmap'ится). Ни то ни другое не подменялось молча: выкатка —
  отдельное решение. Пересохранить контейнер можно без переквантования:
  `save_rwkvq(load_raw(старый), новый)`, ровно то, что делает гейт
  `test_rwkvq_container.py`, — результат бит-в-бит.
