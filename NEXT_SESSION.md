# Continuation prompt — rwkv-quant

Квантование RWKV-7 под Apple Silicon (M4 base, 16GB, БЕЗВЕНТИЛЯТОРНЫЙ Air).
Цель: максимальная производительность decode при минимальной потере качества.
Формат `.rwkvq`, бэкенд Metal (MLX), референс-модель
`~/Develop/WKV-kvant/rwkv7-g1h-1.5b-ctx10240.pth` (1.5B, world naming, ~2.8GB).

## Законы методологии (нарушались, приводило к ложным выводам)

1. Все замеры скорости — ТОЛЬКО A/B-чередованием в одном процессе
   (безвентиляторный дрейф 15.7→26 мс под нагрузкой неустраним).
2. Тяжёлые ppl-прогоны 1.5B — по одному конфигу на процесс
   (`tests/diagnose_one.py <case>`; dense-фолбэк держит fp32, "почти всё
   dense" конфиги прожорливы; однажды 13GB RSS).
3. ppl сравнивать только внутри текущего корпуса
   (eval_corpus_world.pt[:8], построен из ~/Develop/test.txt —
   литература+техника+википедия). Числа других эпох — перемерять.
4. В циклах с деквантом — eval каждую итерацию.
5. Комбинации конфигов непредсказуемы: изолированный выигрыш группы
   не переносится в композит без замера (многократно подтверждено).

## Статус кода (провалидировано)

- Формат `.rwkvq` + INT4 bit-packing: COMPRESSION 1181MB.
- Decode: 15.2 мс/ток (65.8 tok/s) — паритет Q4_K_M llama.cpp (~68).
  Пол по руфлайну: ~9.3 мс/ток (веса/ток / 120 GB/s DRAM M4);
  разрыв = ~6 мс не-GEMV (WKV, 6 lerp token-shift, LoRA fp16, launch).
- GEMV v2-кернель: 90–96 GB/s = 75–80% пика DRAM, почти оптимален;
  v3 (simdgroup) паритет — охота за полосой GEMV малоперспективна.
- GEMM-префилл: 3.6x на T=1024 (1.8 мс/ток; чистые матмулы ~0.7 —
  добирать фьюзом и WKV).
- AW (imatrix-стиль, E[x^2]): собран `/tmp/act_stats_1p5b.pt` (145 ключей,
  сборка ~28с через collect_act_stats.py, устойчив к выбору корпуса).
- writer.py: `_groupwise_fake_dequant(w, bits, gs, sb, sb_bits, ex2)` —
  блочный асимметричный RTN; sb>0 = Q4_K-суперблок (6-бит scale/min против
  fp16-пары на суперблок); sb_bits<0 = грид(0.7..1.05)+LS-поиск scale/min;
  ex2 = AW-взвешенный критерий. `_mxfp4_fake_dequant` (E8M0+E2M1, MSE-грид
  экспоненты, опц. SpQR) — оставлен для истории, в формат не идёт.
  Режимы через `group_scale_mode`: asym / asym_sb6 / asym_sb6_search /
  asym_sb6_aw / mxfp4. ВАЖНО: gw-ветка в quantize_tensor стоит РАНЬШЕ
  act_stats-ветки (иначе per-row-AW перехватывает).

## Верифицированные измерения — ТЕКУЩИЙ корпус, eval_corpus_world.pt[:8]

Полные конфиги 1.5B (все int4-группы, если не сказано иное):

| конфиг | ppl | vs bf16 | ~размер |
|---|---|---|---|
| bf16 baseline | 11.430 | 0% | 2953MB |
| MLX INT6 эмуляция (asym gw64, всё 6-бит, small=16) | 11.439 | +0.07% | ~1190MB |
| int8 per-row (всё 8, small=8) | 11.527 | +0.8% | ~1530MB |
| aw_v2_uniform (см. формат v2) | 13.525 | +18% | ~1251MB |
| aw_v2_uniform_s16 (то же, small=16) | 13.543 | +18% | ~1255MB |
| aw_v2_composite (gw только cmix, per-row-AW прочее) | 13.757 | +18-20% | ~1216MB |
| aw_small8 (чемпион per-row-AW эры) | 14.017 | +23% | 1181MB |
| compression_fixed (per-row+SpQR) | 16.947 (перемерен 18.07) | +48% | 1181MB |

Изоляция cmix (остальное как compression_fixed):

| cmix-схема | ppl | бит/эл |
|---|---|---|
| gw32sb6 + AW-поиск | 15.361 | 4.5 |
| gw32 fp16-scale | 15.876 | 5.0 |
| gw32sb6 + MSE-поиск | 16.038 | 4.5 |
| gw32sb6 naive round | 16.075 | 4.5 |
| mxfp4 + SpQR 2% | 16.700 | ~4.6 |
| baseline per-row+SpQR | 16.947 | ~4.3 |
| mxfp4 без SpQR | 17.216 | 4.25 |

Ключевые выводы 18.07:
- ГРАНУЛЯРНОСТЬ ВАЖНЕЕ БИТНОСТИ: asym gw64@6бит lossless (+0.07%);
  per-row int8 теряет 0.8%; per-row+SpQR int4 +48%. Структура выбросов
  RWKV лечится группами, не строчными scale (canonical RTN: ppl 113).
- MXFP4-семантика ЗАКРЫТА по качеству (E8M0 без min-offset + 8 уровней
  E2M1 — двойной проигрыш; SpQR помогает, но не догоняет).
- AW-взвешивание критерия поиска в блоках: −0.68 ppl против MSE-поиска
  (паттерн per-row-AW №4f воспроизведён на блочном уровне).
- Урок №4e «gw на proj/emb_head не помогает» СНЯТ — артефакт
  невзвешенного критерия; с AW блочный путь лучше per-row-AW+SpQR.
- small=8 vs 16 — в шуме; small-тензоры НЕ источник деградации.
- Загадка MollySophia разгадана: «10% деградации» — task-метрики;
  groupwise ≥6 бит беспотерен. Плюс их 4-битные модели — те же группы.

## РЕШЕНИЕ: формат v2 (зафиксировано 18.07, с А.)

Однородный для всех int4-групп: асимметричный блок 32, суперблок 256
(8 блоков), scale/min по 6 бит против fp16-пары на суперблок = 4.5
бит/элемент. Значения — AW-взвешенный грид+LS-поиск. SpQR ПОЛНОСТЬЮ
уходит из decode-рантайма (один кернель, одна раскладка, без sparse-ветки).
Грид-поиск — свойство writer'а, формат не меняет.

## Открытые вопросы / следующие шаги

1. Разрыв 13.53 (4.5bpw) ↔ 11.44 (6.5bpw): построить лестницу битности
   на текущей машинерии — int5 gw32sb6+AW; int4 тела + 6-бит чувствительные
   группы; gw64 против gw32 при 5-6 бит. Гипотеза: есть точка ~5-5.5bpw
   около +2-5% — она может стать основным COMPRESSION-пресетом.
2. Аудит REDUCTION-пресета: его историческое 13.15 при честном
   int8 per-row = 11.53 — в конфиге легаси-вред (найти и убить).
3. Реальный writer/reader формата v2 (packing sb6-раскладки) + адаптация
   GEMV v2-кернеля (деквант qs/qm внутри, fp16-пара на суперблок).
4. AW в writer-пайплайн: collect_act_stats → calibrate() end-to-end.
5. Приоритеты скорости (№4h, в силе): mx.compile префилл-графа;
   decode-фьюз token-shift+r/k/v (-1.5-2.5 мс); WKV v2 layout;
   статичный рантайм SwiftRWKV поверх (стратегия: переиспользовать
   MLX-логику/кернели, свой тонкий decode-цикл; MLX MIT).
6. Bandwidth: системных счётчиков в Instruments НЕТ — только математика
   руфлайна + изолированный GEMV-тайминг (методика №4g).
7. M5 (аппаратные tensor-пути): не реализуем — железа для теста нет.

## Референс: rwkv app (MLX-бэкенд, INT6 1.19GB, тот же M4, 18.07)

- batch-1 decode 55.46 t/s (мы: 65.8 → наш рантайм +19%).
  Достигнутая полоса MLX ≈ 66 GB/s против наших 90-96 в GEMV.
- Батч-скейлинг до 192 t/s суммарно (batch 16, насыщение ~10) —
  у нас батча нет; для сервинга интересно.
- «228 GB/s» в их UI — фикция (192 t/s × 1.19GB; веса в батче читаются
  раз на шаг: реальный трафик ~14 GB/s, упор в latency/compute).
- LAMBADA: acc 67.7%, ppl 4.61 (task-метрика, с корпусной ppl несравнима).

## Где лежат материалы

- `~/Develop/WKV-kvant/` — чекпоинты (ru60m/, 1.5B pth), ablation-скрипты.
- `~/Develop/rwkv-metal/` — WKV7 Metal-кернели (импорт, не трогаем).
- `~/Develop/rwkv7-1.5B-g1g-mlx-6bit/` — MLX INT6 референс (safetensors).
- `~/Develop/test.txt` — корпус деградации; eval_corpus_world.pt из него.
- `tests/venv/` — torch+mlx окружение; `tests/diagnose_one.py` — кейсы
  (все конфиги этой сессии там: mxfp4_*, gw32sb6*, aw_v2_*, mlx_int6_emu,
  bf16_baseline, int8_perrow), per-process запуск.
- `/tmp/act_stats_1p5b.pt`, `/tmp/act_stats_ffn.pt` — AW-статистика
  (/tmp! после ребута пересобрать, 28с).
